"""Hiện thực năng lực. Mỗi mô-đun = một namespace của Danh mục (docs/spec/capabilities/<ns>.yaml).

Quy ước: hàm nhận (params: dict, ctx: Context) và trả dict khớp output_schema; docstring bắt đầu bằng `Spec:`.
"""
