# Plugin GEditor (Swift) — theo EIDE-GPI-23

Chưa bắt đầu ở Sprint 1. Khi bắt đầu: đọc GPI-23 (GP-01…GP-12, các mục [GE?] cần đối chiếu tài liệu plugin GEditor), API-15 §1 (JSON-RPC, `plane.hello` với `api_version`), UXD-13 và `docs/ui/*.dc.html`. Plugin chỉ nói chuyện với daemon qua JSON-RPC; không truy cập `.eide/` trực tiếp.
