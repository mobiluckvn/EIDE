# EIDE-UXC-31 — CHECKLIST UI/UX CHO TÁC TỬ HIỆN THỰC (v2.0)

| Thuộc tính | Giá trị |
|---|---|
| Mã tài liệu | EIDE-UXC-31 |
| Phiên bản | **2.0 — 23/09/2026** (v1.0 17/09; v1.3 21/09 sửa năm chỗ tài liệu sai theo [DEV-138], [DEV-139], [DEV-142] ×2, [DEV-143]; **v2.0** đồng bộ 26 mục DEVIATIONS theo `docs/sync/2026-09-22-UXC.md` — mỗi chỗ ghi rõ bản trước đã viết gì) |
| Người lập | Vũ Trí Công (hỗ trợ soạn thảo: Claude) |
| Người hướng dẫn | TS. Nguyễn Trung Hiếu |
| Khuôn khổ | Đề án tốt nghiệp Thạc sĩ ngành Kỹ thuật Điện tử — PTIT |
| Căn cứ | EIDE-UXD-13 v2.0 (đặc tả), EIDE-FTR-30 (tính năng), EIDE-POL-17 (chính sách), demo EIDE_UI_Demo_v2.html (mẫu tham chiếu hành vi) |
| Đối tượng đọc | Tác tử Claude khi hiện thực giao diện Swift/macOS. Mỗi mục `- [ ]` là MỘT việc kiểm được: làm xong thì đổi thành `- [x]` kèm mã commit. KHÔNG được đánh dấu mục chưa có bài kiểm chứng minh. |

## TÀI LIỆU NÀY KHÔNG SINH TỪ `docs/ho-so/nguon/` — sửa thẳng tệp Markdown

UXC-31 là tài liệu **viết tay**, khác 31 tài liệu docx và 4 Excel của bộ hồ sơ. Không có
`uxc.js`, không chạy `scripts/sinh_tai_lieu.sh` cho nó.

Tới 22/09/2026 chi tiết ấy làm nó **rơi khỏi quy trình đồng bộ**: `dong_bo_tai_lieu.py` lọc mã
tài liệu bằng `if m in NGUON`, mà `NGUON` chỉ liệt kê tài liệu có tệp sinh — nên 26 mục
DEVIATIONS đụng UXC-31 rơi hết vào nhóm "(chưa rõ)" và không sinh bản nháp nào. Đo lúc vá:
UXC là nhóm **lớn thứ hai** (26 mục, chỉ sau CDS 41). Tài liệu bị giao diện sửa nhiều nhất lại
là tài liệu duy nhất chưa từng được đồng bộ. Xem [DEV-188]; nay có danh mục `TU_VIET` riêng.

## TRẠNG THÁI ÁP DỤNG (tác tử cập nhật, 23/09/2026)

**25/25 màn đã nối · 0 màn chặn · vùng trao đổi mang trọn cuộc đối thoại.** Bốn màn S17–S20
từng ghi `CHẶN: chờ bo mạch` nay đã dựng ([DEV-186]): chúng hiện trạng thái rỗng CÓ LÝ DO như
B5 đòi, và không màn nào tự chạm phần cứng khi mở.

Ba con số đo được trên một dự án thật (`--do-noi-dung`, [DEV-183]): **58 mục nội dung đã hiện ·
0 mục chưa dựng · 19 mục không đo được** (màn rỗng đúng luật vì dự án chưa có hiện vật loại
ấy). Tự kiểm phiên: **78/78**. Bấm thử 21 nút: **0 nút không làm gì**.

Ba đợt đầu đã vào kho:

| Đợt | Commit | Nội dung |
|---|---|---|
| 0 | `c997796` | Đóng ba lỗi TÀI LIỆU của bản rà soát (R1/R4/R6). Ba trong sáu phát hiện là lỗi tài liệu chứ không phải lỗi sản phẩm: bộ chuyển dự án đã có từ DEV-113, điều hướng đã nhóm sẵn, Chat không nằm trong sidebar. |
| 1 | `c27d027` | Thẻ Run (R3) + sáu kiểu sự kiện sổ cái `run.*`. Ba lỗi im lặng 59–61. |
| — | `e04c0c6` | Bảng màu theo bản demo, sửa hai giá trị không đạt AA. |
| 2 (lõi) | `3e360a9` | `code.human_save`, `code.merge_conflict_resolve`, `project.watch`; merge 3 bên; 5 quy tắc chính sách; hai lỗ an toàn 62–63. |

**Bốn chỗ tài liệu này đã được sửa số theo đo đạc**, ghi lại để người đọc bản in cũ không lạc:
`23 màn` → **25** (mục 8 của chính tài liệu liệt kê S1–S25); `A0…A3` → **A0…A4** (APD-08 có năm
mức); `238 năng lực` → **241** (v2.0 thêm ba); `53 quy tắc` → **54** (P-EDIT-02 bắt buộc đi kèm
P-EDIT-04, xem DEV-125). Mục 1.5 (Dark Mode) BỎ theo quyết định chủ sản phẩm.

---

## QUY TẮC ĐỌC TÀI LIỆU NÀY (tác tử đọc trước tiên)

- [x] Đọc hết mục 0 (bất biến) trước khi viết dòng mã đầu tiên; mọi mục sau đều phải thoả bất biến mục 0.  ·  **✔ Mục 0 nay tick trọn 8/8 và cả tám đều có bài kiểm, không có mục nào tick bằng lời hứa**
- [ ] Thứ tự hiện thực bắt buộc: mục 0 → 1 → 2 → 7 (sổ cái + sự kiện) → 3 → 4 → 5 → 6 → 8 (từng màn) → 9 → 10 → 11. Không làm màn (mục 8) trước khi xong tầng sự kiện (mục 7).  ·  ****ĐÃ ĐI CHỆCH, ghi lại thay vì tick.** Đúng ở khúc đầu: §7 (tầng sự kiện) làm TRƯỚC mục 8 như tài liệu đòi. Sai ở khúc sau: 21 màn của mục 8 dựng xong rồi mới quay lại §3 và §4, ngược thứ tự `3 → 4 → 5 → 6 → 8`. Không hỏng gì, nhưng luật này không được tick bằng một lần đi chệch có hậu quả tốt**
- [x] Gặp mâu thuẫn giữa tài liệu này và UXD-13 v2.0 → UXD-13 v2.0 thắng; ghi mâu thuẫn vào sổ cái và báo người, KHÔNG tự chọn.  ·  **✔ Áp đúng ở [DEV-142]: §1.2/§1.3 của chính tài liệu này thua UXD-13 v2.0 về phông chữ và bán kính. Ghi DEVIATIONS và báo chủ sản phẩm, không tự chọn im lặng**
- [x] Mọi chuỗi chữ hiển thị: tiếng Việt; thuật ngữ tiếng Anh phải kèm giải thích tiếng Việt lần xuất hiện đầu (ví dụ "bảng lệnh (command palette)").  ·  **✔ Không chuỗi hiển thị nào bằng tiếng Anh; thuật ngữ Anh giữ nguyên trong dấu nháy ngược kèm giải thích (bảng lệnh, cổng, hộ chiếu…). Nhãn trợ năng cũng tiếng Việt — §9.2**
- [x] Không phát minh thêm màn, menu, nút ngoài danh mục này. Thiếu thì hỏi, thừa là lỗi.  ·  **✔ 25 màn đúng danh mục, có bài kiểm khoá con số ấy. **Một ngoại lệ, và nó được ghi**: nút gấp/bung cột phải (⟩/⟨) không có trong tài liệu — thêm vì §2.2 cho cột tự thu mà không cho đường bung lại, [DEV-139]. Theo CLAUDE.md quy tắc 3 thì ghi DEVIATIONS, không sửa im lặng**

---

## 0. BẤT BIẾN — VI PHẠM LÀ LỖI CHẶN (blocker), KHÔNG MERGE

- [x] **B1.** Giao diện KHÔNG có đường nào hiển thị một việc mà sổ cái không có sự kiện tương ứng. Mọi widget trạng thái (thẻ Run, badge, cột phải, timeline) là hàm chiếu của dòng sự kiện sổ cái — không giữ trạng thái nguồn riêng.  ·  **✔ qua bài kiểm PHỦ ĐỊNH của §10.2 — cách duy nhất đo được một mệnh đề phủ định mọi đường: cắt nguồn sự thật rồi khẳng định màn hình đứng yên. Một widget giữ trạng thái nguồn riêng sẽ tiếp tục nhúc nhích ở đó. Kèm ba phép đo cấu trúc: mọi màn lấy dữ liệu qua `EideManCoSo.nap`, mọi màn khai báo mình nghe gì, cột phải không nghe sổ cái**
- [x] **B2.** Nút bấm của người, lệnh của tác tử, lệnh từ bảng lệnh ⌘K: đi CÙNG MỘT đường gọi năng lực → cổng chính sách → sổ cái. Không có lối tắt riêng cho UI.  ·  **✔ c27d027 · mọi nút panel đi `caps.invoke` → Router → cổng → sổ cái**
- [x] **B3.** Vùng trao đổi không bao giờ bị màn nào thay thế, che, hay về chiều cao 0. Tối thiểu tuyệt đối 48 pt.  ·  **✔ Bài kiểm đi hết 21 màn: không màn nào ẩn, bóp hay chờm lên vùng trao đổi — `vungLamViec.minY ≥ dock.maxY` ở mọi màn. Sàn 48 pt là enum ba giá trị nên không đường nào lách xuống dưới, và thu gọn hết cỡ VẪN gửi được lệnh**
- [x] **B4.** Không ghi đè im lặng: mọi tình huống hai bản cùng tồn tại (người/tác tử, cũ/mới) phải đi qua merge 3 bên hoặc màn xung đột.  ·  **✔ 3e360a9 · `test_LUU_khi_tep_da_doi_tren_dia_thi_KHONG_ghi_de` + merge 3 bên**
- [x] **B5.** Không hiện dữ liệu giả. Màn thiếu dữ liệu → trạng thái rỗng gồm đúng 2 phần: (a) lý do rỗng, (b) bước kế tiếp.  ·  **✔ c997796 · bài tự kiểm mở CẢ 26 màn, khẳng định không màn nào vừa rỗng vừa im**
- [x] **B6.** Dữ liệu cũ phải nói là cũ: màn mất đồng bộ (seq lệch / mất daemon) hiện nhãn "Dữ liệu cũ — bấm để tải lại" trong ≤ 2 giây (tiêu chí N6).  ·  **✔ `HAN_CU = 2,0` s đạt N6, `NHIP_TIM = 1,0` s dày hơn hạn (nhịp thưa hơn hạn thì hạn không bao giờ đo tới). Dải nói rõ TRỄ BAO LÂU và có nút tải lại; mất daemon với nhảy quãng `seq` là hai câu khác nhau**
- [x] **B7.** Mỗi câu hỏi của người dùng có ĐÚNG MỘT nơi trả lời: "đang làm gì?" = thẻ Run; "việc gì chờ tôi?" = khối Chờ tôi; "đã xảy ra gì?" = Nhật ký. Mọi chỗ khác chỉ là bản chiếu (mirror), không mang dữ liệu riêng.  ·  **✔ Cột phải không có trong `EideDangKySuKien.BANG` — nó là bản chiếu, không nghe sổ cái. ĐÚNG MỘT màn nghe mọi loại sự kiện và đó là Nhật ký (S2). Mọi màn đã nối đều khai báo mình nghe gì**
- [x] **B8.** Dừng khẩn hạ về A0 và huỷ thao tác đang chờ trong < 1 giây, hoạt động ở MỌI trạng thái giao diện, kể cả khi modal đang mở.  ·  **✔ Chạy dưới 1 giây cả khi bảng lệnh đang mở lẫn khi màn chào phủ toàn cửa sổ — hai trạng thái modal duy nhất của khung. `--tu-kiem` 8/8b đo trên daemon thật**

## 1. HỆ THỐNG THIẾT KẾ (design tokens)

- [x] **1.1** Khai báo token màu tập trung một tệp, không hard-code trong view: `--red #BC2626` (hành động chính/PTIT), `--red2 #DE221A` (viền nhấn), `--ok #1D7A4F`, `--warn #8A5A00`, `--blue #1B5FA5` (tác tử đang chạy), nền `#F5F4F2`, panel `#FFFFFF`, chữ chính `#1B1B1B`, chữ phụ `#6B6B6B`, viền `#E2E0DC`.  ·  **✔ e04c0c6 · `EideTokensGenerated.swift` sinh từ `tokens.json`, không hard-code**
- [x] **1.2** Chữ: **IBM Plex Sans 13 pt** (UXD-13 v2.0 §7 `font.ui`, giãn dòng 1,45; dự phòng phông hệ khi máy chưa cài), phụ 11–12 pt, tiêu đề màn 16 pt; mã nguồn dùng **IBM Plex Mono 12 pt** (`font.mono`). *(v1.3 — v1.0 ghi "San Francisco / SF Mono 12,5", ba con số không có ở đâu trong bộ hồ sơ; [DEV-142].)*  ·  **✔ theo UXD-13 v2.0, KHÔNG theo §1.2. IBM Plex Sans 13 / IBM Plex Mono 12 + dự phòng phông hệ. §1.2 nói San Francisco 12,5 pt — ba con số không có ở đâu trong bộ hồ sơ; quy tắc đọc đầu tài liệu này cho UXD-13 v2.0 thắng. [DEV-142]**
- [x] **1.3** Bo góc: lấy từ bộ `radius` của UXD-13 v2.0 — **[6, 8, 10]**: nút 6 pt, panel 8 pt, **thẻ Run 10 pt**. Khoảng cách lưới bội số 4 pt (`space [4, 8, 12, 16, 24]`). *(v1.3 — v1.0 ghi "thẻ Run 9 pt", một con số không có trong bộ ấy; [DEV-142].)*  ·  **✔ `radius [6, 8, 10]` và `space [4, 8, 12, 16, 24]` — mọi khoảng cách là bội số 4 pt. "Thẻ Run 9 pt" của §1.3 không có trong bộ bán kính của UXD-13; mã dùng 10. [DEV-142]**
- [x] **1.4** Ngữ nghĩa màu cố định toàn app: xanh lá = có nguồn/đạt; đỏ = vi phạm/chặn/dừng; vàng = chờ người; xanh dương = tác tử đang làm. Không dùng chéo.  ·  **✔ Thẻ Run là chỗ cả bốn màu ngữ nghĩa cùng xuất hiện, nên cũng là chỗ một lần dùng chéo lộ rõ nhất: xanh lá bước đã đạt · xanh dương đang chạy · vàng chờ người · đỏ dừng. `EideTheRun.mauBuoc` tách ra làm hàm tĩnh để đo được**
- [x] **1.5** ~~Hỗ trợ Dark Mode~~  ·  **BỎ theo quyết định chủ sản phẩm 17/09/2026.** Sản phẩm chạy MỘT bảng màu sáng, ghim `.aqua` (DEV-114): UXD-13 khai đúng một bảng màu, nên bịa một bảng tối ở tầng mã là quyết định thương hiệu chứ không phải quyết định kỹ thuật. Phép kiểm tương phản ≥ 4,5:1 GIỮ NGUYÊN và đang chạy — xem DEV-124.
- [x] **1.6** Mọi trạng thái KHÔNG truyền đạt bằng màu đơn độc — luôn kèm ký hiệu hoặc chữ (chấm ●, vạch ▎, nhãn) để không phụ thuộc thị giác màu.  ·  **✔ DEV-117 · lề dùng ● và ▎ kèm màu, không dựa màu đơn độc**

## 2. KHUNG MÀN HÌNH (shell) — 5 VÙNG

- [x] **2.1** Bố cục: thanh trên (cao 46 pt, viền dưới 2 pt màu `--red2`) / hàng chính gồm: cột trái 198 pt · trung tâm co giãn · cột phải 236 pt. Trung tâm = thanh tab + vùng làm việc + vùng trao đổi.  ·  **✔ 46 / 198 / 236 pt trong `EideKhung.CAO_THANH_TREN`/`RONG_COT_TRAI`/`RONG_COT_PHAI` — khớp bản demo v2.0. **Nhưng token sinh từ `uxd.js` nói 52/212** — xem [DEV-137]**
- [x] **2.2** Cửa sổ tối thiểu 1100 × 700 pt; **tại hoặc dưới** ngưỡng bề ngang ấy thì cột phải thu thành dải icon 44 pt (badge vẫn hiện), KHÔNG được ẩn hẳn. Kèm một nút gấp/bung luôn hiện ở cả hai trạng thái. *(v1.3 — v1.0 ghi "dưới ngưỡng", mà một bề ngang tối thiểu đã cưỡng chế thì không bao giờ xuống dưới được, nên dải icon là mã chết; [DEV-139].)*  ·  **✔ `contentMinSize = 1100 × 700`; cột phải thu còn 44 pt với ba con số vẫn đọc được. §2.2 tự mâu thuẫn — một bề ngang tối thiểu đã cưỡng chế thì không bao giờ "dưới ngưỡng" được; đọc là TẠI ngưỡng, [DEV-139]. Ảnh `khung-toi-thieu.png` trong `--chup`**
- [x] **2.3** Ở cửa sổ cao 900 pt, vùng trao đổi mức chuẩn: vùng làm việc còn ≥ 50 % chiều cao (tiêu chí N7 — viết bài kiểm layout tự động).  ·  **✔ `EideBoCucTests.testN7…` — xanh ngay lần chạy đầu, tức bố cục vốn đã đạt; bài kiểm chốt nó lại. Kèm hai bất biến nữa: vùng trao đổi mở rộng và dải Dữ liệu cũ đều không ăn thủng sàn 200 pt**

### 2A. Thanh trên — đúng thứ tự trái → phải, không thêm bớt

- [x] **2A.1** Logo chữ "EIDE" (đậm, `--red`).  ·  **✔ `EideThanhTren`**
- [x] **2A.2** Bộ chuyển dự án: tên dự án + mũi tên ▾; bấm mở popover: danh sách `project.list` + ô lọc + nút "Dự án mới" (gọi `project.create`). Đổi dự án = thay toàn bộ ngữ cảnh, đóng hết tab, giữ nguyên bố cục.  ·  **✔ `EideChonDuAn` — ô lọc (không dấu) · danh sách `project.list` · nút Dự án mới. Đổi dự án đóng hết tab, bỏ thẻ Run, xoá badge; bố cục thì GIỮ NGUYÊN vì đó là lựa chọn của người**
- [x] **2A.3** Huy hiệu mức tự chủ: "Tự chủ A0…A4" — nền xanh dương khi A1–A4, nền đỏ khi A0. Bấm mở màn S25.  ·  **✔ `EideThanhTren.muc` là NSButton → `onMuc` → `moMan("ChinhSach")`; nền pill giữ nguyên bằng `attributedTitle`**
- [x] **2A.4** Bộ đếm "Chờ tôi n" — bấm cuộn tới khối Chờ tôi ở cột phải (không mở màn mới).  ·  **✔ `onDemCho` → `EideCotPhai.cuonToi(.cho)`; KHÔNG mở màn — có bài kiểm khoá điều đó**
- [x] **2A.5** Bộ đếm "Hoàn tác n" — bấm cuộn tới khối Hoàn tác được.  ·  **✔ `onDemHoanTac` → `cuonToi(.hoanTac)`. Cột vừa màn hình thì cuộn là lệnh không-làm-gì, nên tiêu đề khối luôn NHÁY — nút phải trả lời cả khi không có gì để cuộn**
- [x] **2A.6** Nút bảng lệnh "⌘K" (viền đứt) + phím tắt ⌘K toàn cục.  ·  **✔ nút ⌘K + phím tắt qua thanh menu thật**
- [x] **2A.7** Nút "■ Dừng khẩn" — luôn ở vị trí cuối cùng bên phải, nền `--red-bg`, chữ `--red`; hành vi theo B8; sau khi dừng, nút đổi thành "Đặt lại mức tự chủ" trỏ S25. **Câu "dừng" người GÕ cũng phải dừng thật** *(v2.0 — [DEV-155])*: `chat.send` có đường tắt xác định — câu khớp mẫu dừng gọi thẳng `policy.emergency_stop` TRƯỚC mọi lời gọi mô hình.  ·  **✔ nút Dừng khẩn cuối bên phải; `--tu-kiem` mục 8 đo nó không núp sau mức tự chủ. Đường tắt là bắt buộc chứ không phải tối ưu: bắt một lệnh dừng đi qua mô hình nghĩa là lệnh dừng có thể bị hiểu sai, chậm vài giây, hoặc hỏng cùng lúc với thứ người ta đang muốn dừng — đúng lúc nó cần chắc chắn nhất**
- [x] **2A.8** Hai bộ đếm 2A.4/2A.5 phái sinh từ cùng nguồn với cột phải (B7) — viết một selector chung, cấm hai phép đếm riêng.  ·  **✔ `EidePhien.choTheoMan` là nguồn DUY NHẤT, `_veBadge()` chiếu ra cột trái — không có phép đếm thứ hai**

### 2B. Cột trái — điều hướng 6 nhóm / 25 màn

- [x] **2B.1** Đúng 6 nhóm cấp một, mỗi nhóm 2–5 mục (tiêu chí N8); danh mục và thứ tự CHÍNH XÁC theo mục 8 dưới đây.  ·  **✔ dựng thẳng từ `EideManHinhDS.nhom`; `--tu-kiem` mục 13 đo 6 nhóm / 25 màn**
- [x] **2B.2** Tiêu đề nhóm: chữ hoa 10.5 pt màu nhạt; bấm tiêu đề gập/mở nhóm; trạng thái gập lưu theo dự án.  ·  **✔ gập/mở qua `nhomGap`. **Trạng thái gập CHƯA lưu theo dự án** — mở lại app là bung hết**
- [x] **2B.3** Mục đang mở: nền đậm hơn + vạch trái 3 pt `--red` + chữ đậm.  ·  **✔ `vachDo` + nền đậm + chữ đậm**
- [x] **2B.4** Huy hiệu số trên mục và trên tiêu đề nhóm = số việc "Chờ tôi" trỏ về màn ấy; phái sinh từ danh sách pending duy nhất (B7).  ·  **✔ `choTheoMan`, cùng nguồn với cột phải (B7)**
- [x] **2B.5** Khi tác tử tự mở màn: nhóm chứa tự bung + mục nhấp nháy nền vàng đúng 2 nhịp × 300 ms rồi thôi. Không nhấp nháy khi người tự bấm.  ·  **✔ `_nhapNhay`: `CABasicAnimation` 0,3 s × `repeatCount = 2`, chỉ khi `boiTacTu`**
- [x] **2B.6** Không mục nào ngoài 25 màn; "Chat" và "ReviewQueue" KHÔNG được xuất hiện ở đây (đã đổi vai — UXD-13 v2.0 §4).  ·  **✔ `EideManHinhDS` không có mục nào ngoài 25 màn; không có Chat/ReviewQueue**

### 2C. Vùng làm việc + tab

- [x] **2C.1** Tab như IDE: mở màn nào thêm tab đó (không trùng), đóng bằng ✕, kéo đổi thứ tự; tab đang mở nền trắng chữ đậm.  ·  **✔ `EideNutTab` tự chạy vòng sự kiện chuột (NSButton nuốt chuột nên `mouseDragged` ở cha không bao giờ chạy); quá 6 pt là KÉO, trong ngưỡng vẫn là BẤM. Chưa có đường bàn phím — ghi ở §9**
- [x] **2C.2** Không tab nào mở: hiện trạng thái rỗng "Vùng làm việc trống — chọn màn ở cột trái, hoặc ra lệnh để tác tử tự mở đúng màn." **Mở một dự án CÓ SẴN phải ẩn màn chào** *(v2.0 — [DEV-152])*, và chỉ ẩn SAU khi `project.open` chạy xong.  ·  **✔ "Vùng làm việc trống" trong `EideVungLamViec`. Tới 22/09 chỉ đường TẠO dự án gọi `anManChao()`, nên mọi đường mở một dự án có sẵn (`--du-an`, bộ lái kịch bản, nút mở dự án) để người dùng đứng lại ở màn tạo dự án trong khi daemon đã nối, tab đã thêm, phiên đã mở — chủ sản phẩm nhìn màn hình và hỏi đúng câu ấy: *"màn hình đang ở màn tạo dự án mà?"*. Ẩn SAU `project.open` vì đó là chỗ duy nhất biết dự án mở được thật: E6000/E6003 thì màn chào phải ở nguyên, nó là chỗ duy nhất người dùng làm được gì tiếp theo**
- [x] **2C.3** Quy tắc "không cướp màn": tác tử muốn mở màn trong khi người vừa TỰ chọn màn khác < 20 giây → tab mới mở ở nền (không chiếm focus) + nhấp nháy 2B.5; quá 20 giây → được chiếm focus.  ·  **✔ `EidePhien.GIU_MAN = 20`; `moMan` trả về CÓ CHIẾM ĐƯỢC hay không, vùng trao đổi nói hai câu khác nhau cho hai trường hợp. Mở ở nền vẫn tăng badge — tab nằm im không dấu hiệu thì còn tệ hơn cướp màn**
- [x] **2C.4** Mỗi màn khai báo header chuẩn: tiêu đề (16 pt) + dòng phụ ghi tên năng lực đứng sau (12 pt, màu phụ). Không màn nào không ghi năng lực (chống "màn mồ côi" — phát hiện R6).  ·  **✔ `moMan(tien:nangLucDs:)` dựng header cho MỌI màn — không màn nào tự dựng lấy**

### 2D. Vùng trao đổi (chat dock) — 3 trạng thái

- [x] **2D.1** Ba trạng thái chiều cao: thu gọn 48 pt (chỉ ô nhập) · chuẩn 220 pt · mở rộng tối đa 320 pt (kéo tay, nội dung vượt trần thì cuộn trong).  ·  **✔ `case thuGon = 48, chuan = 220, moRong = 320`**
- [x] **2D.2** Tự chuyển: (a) Run mới bắt đầu → về chuẩn; (b) người gõ trong editor liên tục 5 giây → thu gọn — **"liên tục" = không có quãng nghỉ nào quá 1,5 giây** *(v1.3 — v1.0 không định nghĩa, mà không định nghĩa được thì không đo được; [DEV-138])*; (c) không bao giờ đổi trạng thái trong lúc con trỏ đang ở ô lệnh.  ·  **✔ (a) đã có từ trước (`_theoRun` → `datCao(.chuan)`); (b) mới: `nguoiGo()` đo mốc thời gian, `GO_LIEN = 5` s, `NGAT_GO = 1,5` s [DEV-138]; (c) đã có từ trước — và nó sai im lặng: `nil === nil` → `true` khiến luật chặn cả những lần lẽ ra phải cho qua**
- [x] **2D.3** Hoạt ảnh chuyển ≤ 150 ms, ease-out; ba nút ▁▂▃ góc phải cho chuyển tay.  ·  **✔ Cả hai ĐÃ CÓ từ trước — ghi chú 20/09 của tôi sai. Chỉ thiếu `timingFunction`: mặc định là ease-in-ease-out, trên quãng 150 ms đọc thành một khựng nhẹ. Nay `.easeOut`**
- [x] **2D.4** Ô lệnh: placeholder gợi ý một lệnh mẫu theo pha hiện tại của dự án; Enter gửi; đang có Run chạy thì lệnh mới được báo "xếp hàng sau Run hiện tại".  ·  **✔ Tám lệnh mẫu theo pha BPD, mỗi câu gõ thẳng vào được; chưa biết pha thì về câu chung chứ KHÔNG đoán. Pha đọc từ cùng nguồn màn S3 dùng. Đang chạy thì báo "xếp hàng sau lượt chạy hiện tại"**
- [x] **2D.5** Bong bóng: người nền `--red-bg` căn phải ≤ 78 % rộng; tác tử nền trắng viền, căn trái; việc chờ người nền vàng có viền *(v2.0 — [DEV-130], khớp bản demo)*.  ·  **✔ `EideBongBong` — người nền đỏ nhạt căn phải, tác tử nền trắng viền căn trái, chờ người nền vàng**
- [x] **2D.6** Trước mọi chuỗi: tác tử in "Ý hiểu (chat.restate)" + danh sách bước dự kiến + hai nút "Đúng — làm đi" / "Sửa ý hiểu" (A1); mức A2–A3 tự chạy nhưng vẫn in ý hiểu.  ·  **✔ trọn — [DEV-140] đóng 21/09.** Thẻ Ý hiểu in câu `chat.restate` + bước đánh số; hai nút *Đúng — làm đi* / *Sửa ý hiểu* hiện khi `state == "planned"`. Chỗ dừng ấy nay có thật: CHAT-06 nhận `plan_only`, `chat.resume` chạy tiếp hoặc huỷ, và daemon quyết theo mức tự chủ ĐANG THI HÀNH — A0/A1 dừng, A2–A3 tự chạy nhưng vẫn in ý hiểu, đúng nguyên văn §2D.6

- [x] **2D.7** *(v2.0 — [DEV-146])* **Một lượt gõ LUÔN được trả lời.** Không dựng được chuỗi thì hiện bong bóng lỗi nêu `eide_code` + `message` THẬT của lõi, không im lặng. Câu "đang chạy" kèm trạng thái bằng tiếng người (`asked` → "DỪNG, đang chờ anh trả lời").  ·  **✔ Đo 21/09 bằng một phiên thật trên bài CNC trước khi vá: tám lượt gõ, KHÔNG một câu trả lời nào, và không một lỗi nào — mọi nhánh của `_gui` đều có điều kiện nên một `chat.send` không khớp nhánh nào đi qua lặng lẽ. Nay có nhánh cuối bắt hết**
- [x] **2D.8** *(v2.0 — [DEV-181])* **MỌI trao đổi diễn ra ở vùng trao đổi.** Tác tử hỏi gì thì câu hỏi hiện Ở ĐÂY kèm chỗ trả lời — không chỉ ở tab Làm rõ yêu cầu (S9). Thẻ hỏi mang **hai** biến thể, vì với người dùng cả hai đều là *tác tử đang hỏi tôi*: (a) **thiếu tham số** — câu hỏi bằng tiếng Việt, nút chọn sẵn khi EIDE biết tập giá trị đóng (`isa` đọc từ `docs/spec/isa/`), kèm ô gõ tự do; (b) **cổng chặn** — nút **Duyệt / Từ chối** kèm ô lý do đi thẳng vào `note` của API-15 §2. Ô gõ BỔ SUNG cho lựa chọn đã bấm, không ghi đè.  ·  **✔ `EideTheHoi`. Nguyên tắc chủ sản phẩm chốt 22/09/2026: *"Tất cả các trao đổi đều phải thực hiện ở vùng trao đổi. Agent hỏi gì thì phải hiển thị ở vùng trao đổi thì tôi mới biết trả lời chứ?"* và *"nút duyệt hoặc phê duyệt luôn ở ô trả lời chat"*. Trước đó màn hình in "Dừng ở `env.check` — cần anh cho biết: isa" rồi dừng: một TÊN THAM SỐ, không phải một câu hỏi, và không có chỗ nào để trả lời. [DEV-160] đưa câu hỏi sang tab S9 vì lý do đúng (bong bóng chat TRÔI) nhưng kết luận sai — chỗ TRA CỨU và chỗ ĐỐI THOẠI là hai nhu cầu khác nhau, phải có CẢ HAI**
- [x] **2D.9** *(v2.0 — [DEV-181])* Trả lời xong thì lượt cũ **chạy lại**, không nằm im. Câu trả lời vào lớp C2 của CXD-10 nên lượt mới không hỏi lại, và nó vẫn đi qua cổng chính sách như mọi lượt khác.  ·  **✔ `traLoiCauHoi` ghi `req.answer_clarification` rồi gửi lại câu gốc (`event.chat.report.van`). Thiếu `clar_id` thì KHÔNG vẽ ô trả lời — một ô không biết ghi câu trả lời vào đâu tệ hơn một dòng chữ, vì nó hứa**

### 2E. Thẻ Run (Run card) — đặc tả từng pixel hành vi

- [x] **2E.1** Sinh đúng MỘT thẻ cho mỗi Run, đặt trong dòng chat ngay dưới câu lệnh; cột phải chỉ chiếu một dòng (B7, tiêu chí N2).  ·  **✔ c27d027 · một thẻ mỗi `run_id`; nút của chuỗi mang mã CHUỖI**
- [x] **2E.2** Hàng đầu: "Run #id · tên việc" (đậm) + "bước i/n" + link "Mở chi tiết" (mở S2 lọc sẵn theo Run) + link "Dừng" (huỷ Run này, KHÔNG hạ mức tự chủ).  ·  **✔ c27d027 · `Run #7 · <câu người gõ> · 3/8 bước` + Mở chi tiết + Dừng**
- [x] **2E.3** Thanh tiến độ: n đoạn bằng nhau; xong = xanh lá, đang chạy = xanh dương, chờ người = vàng, chưa tới = xám. Dưới thanh: dòng trạng thái ghi bước hiện tại + tên năng lực dạng mã.  ·  **✔ `_mauDoan`: xong xanh lá · đang chạy xanh dương · chờ người vàng · chưa tới xám**
- [x] **2E.4** Trạng thái thẻ chỉ đổi khi nhận sự kiện `run.*` từ sổ cái — cấm setState trực tiếp từ luồng thực thi (B1). Số đoạn "xong" phải luôn bằng số `run.step_done` đã ghi (bài kiểm N2).  ·  **✔ c27d027 · thẻ chỉ đổi khi nhận `run.*`; số bước lấy từ `run.started`**
- [x] **2E.5** Run bị chặn (`run.blocked`): thẻ hiện "⏸ Chờ anh quyết…" + link mở đúng màn có việc; đồng thời mục xuất hiện ở khối Chờ tôi. Giải quyết xong → thẻ tự chạy tiếp, không cần người quay lại chat.  ·  **✔ `run.blocked` → trạng thái `.chan` + nói thiếu gì**
- [x] **2E.6** Run kết thúc: `run.done` → tác tử in báo cáo (chat.report_back) ngay dưới thẻ: sản phẩm, cổng đã qua, chi phí, các mục hoàn tác được. `run.cancelled` → thẻ ghi ai huỷ, lúc nào. **`run.done` KHÔNG đồng nghĩa với "xong"** *(v2.0 — [DEV-148])*: đọc `state` (`done`/`asked`/`cancelled`) và số `failed`; `asked` → trạng thái CHẶN và **không** hiện dấu ✅.  ·  **✔ `run.done` → `chat.report_back` in ngay dưới thẻ; `run.cancelled` ghi AI huỷ và LÚC NÀO. Cả hai chỉ một lần cho mỗi lượt chạy — `run.done` tới nhiều lần khi nghe lại sổ cái. Bốn chỗ từng nói SAI về việc tác tử vừa làm, [DEV-148]: dấu ✅ cho một lượt đang chờ người, số bước bị gán cứng `= tong`, tin báo mở màn lặp bốn lần giống nhau từng chữ, và thẻ Ý hiểu in cho cả lượt đã giao đi**
- [!] **2E.7** Chuỗi > 20 bước: thanh tiến độ gộp theo pha P0–P7, bấm pha bung chi tiết (quyết định cho câu hỏi mở #3 của UXD-13 v2.0).  ·  **CHẶN: chưa có chuỗi > 20 bước để đo; chuỗi dài nhất hiện là 16 (`code.feature`)**

### 2F. Cột phải — hàng đợi 3 khối cố định

- [x] **2F.1** Đúng 3 khối, đúng thứ tự: ĐANG CHẠY / CHỜ TÔI / HOÀN TÁC ĐƯỢC. Không khối nào khác.  ·  **✔ đúng ba khối, đúng thứ tự**
- [x] **2F.2** Khối rỗng vẫn hiện tiêu đề + một dòng lý do ("Trống — không việc nào chờ anh."), không ẩn khối (B5).  ·  **✔ "Trống — không việc nào chờ anh."**
- [x] **2F.3** CHỜ TÔI: mỗi mục = tiêu đề + lý do + đích; nền vàng nhạt; bấm mở đúng màn, cuộn tới đúng phần tử. Nguồn: danh sách pending duy nhất (B7).  ·  **✔ mục CHỜ TÔI có Duyệt / Từ chối, nguồn `queue.list`**
- [x] **2F.4** HOÀN TÁC ĐƯỢC: mỗi mục = nhãn + tác giả (`human:` / `agent:run-…`) + thời gian còn lại đếm ngược + nút "hoàn tác" (gọi `code.revert`/`project.rollback` đúng phạm vi). Hết hạn tự rời danh sách kèm sự kiện.  ·  **✔ nhãn + hạn đếm ngược + nút Hoàn tác gọi `undo.apply`**
- [x] **2F.5** Mục của NGƯỜI (lưu tệp) xuất hiện ở đây như mục của tác tử — cùng cơ chế, không phân biệt (UXD-13 v2.0 §7.4).  ·  **✔ Đo được, và lần đầu đo là KHÔNG ĐẠT: `code.human_save` ghi sổ cái mà không gọi `undo.register`, nên khối hoàn tác chỉ chứa việc của máy — đúng sự phân biệt §2F.5 cấm. Nay đăng ký `git_revert` như `code.merge` [DEV-141]**

## 3. LUỒNG LÀM QUEN (onboarding) — trả lời phát hiện R2

- [x] **3.1** Workspace chưa có dự án → toàn màn là màn hình chào: 1 câu chào, 1 đoạn giải thích trạng thái rỗng, 1 ô gợi ý mô tả dự án một câu, ĐÚNG MỘT nút **CHÍNH** "Tạo dự án đầu tiên (project.create)", và **một dòng nói dự án sẽ nằm ở đâu** kèm nút phụ *Đổi…* chọn thư mục. Không menu, không cột phải lúc này. *(v2.0 — v1.0 ghi "ĐÚNG MỘT nút" và bài kiểm ĐẾM nút; điều khoản nói về NGÃ RẼ, không nói về số lượng `NSButton`. Một nút phụ nằm trong dòng thông tin về chỗ lưu trả lời một câu hỏi người dùng đang có, nó không bắt họ cân nhắc gì. Phép đo chuyển sang đếm nút CHÍNH — nút nhận phím Enter — kèm trần 2 nút; [DEV-182].)*  ·  **✔ Đúng MỘT nút chính trong cả cây khung nhìn (bài kiểm đếm nút nhận Enter, không tin mắt); phủ toàn cửa sổ nên không cột phải lúc này; trạng thái rỗng hai phần của B5. Lõi đã nhận `project.create.dir` từ đầu, chỉ giao diện không có đường tới nó — và màn chào còn KHÔNG NÓI dự án sẽ nằm ở đâu, nên người dùng tạo xong phải đi tìm dự án của chính mình trong `~/eide`. Không chọn gì thì truyền `nil`, KHÔNG truyền chuỗi rỗng: `--dir ""` tạo dự án ở thư mục hiện hành của tiến trình**
- [x] **3.2** Tạo dự án xong ≤ 3 thao tác từ lúc mở app (tiêu chí N1 — viết bài kiểm đếm click).  ·  **✔ N1 đo bằng cách đi ĐÚNG đường người đi — điền ô rồi bấm nút tìm thấy trong cây khung nhìn, không gọi thẳng `taoDuAn`. **Hai** thao tác. Bỏ trống ô vẫn tạo được bằng câu gợi ý đang hiện**
- [x] **3.3** Sau tạo: mở S1, tác tử chào trong vùng trao đổi + chỉ đúng 3 thứ: nút demo/lệnh mẫu, ⌘K, nút Dừng khẩn. Không tour dài.  ·  **✔ `lamQuen()` mở S1 TRƯỚC rồi chào — câu chào nói về thứ đang hiện trên màn hình. Đúng ba mục, và có bài kiểm khẳng định KHÔNG có mục thứ tư**
- [x] **3.4** Ô lệnh 10 phút đầu: placeholder xoay vòng 3 lệnh mẫu thật chạy được với dự án vừa tạo.  ·  **✔ Ba lệnh mẫu của màn chào xoay mỗi 12 s trong 10 phút đầu, rồi TRẢ ô lại cho gợi ý theo pha của §2D.4. Hai luật cùng viết vào một ô nên phải nói rõ luật nào thắng lúc nào**

## 4. BẢNG LỆNH (command palette)

- [x] **4.1** ⌘K mở overlay giữa-trên màn; Esc đóng; focus vào ô tìm ngay khi mở.  ·  **✔ `mo()` xoá ô tìm và đặt tiêu điểm vào đó; Esc đóng. Nhánh `cancelOperation` cũ chỉ chạy khi con trỏ CÒN trong ô tìm — bấm vào một kết quả rồi đổi ý thì Esc không làm gì; nay `EideBangLenh` override thẳng**
- [x] **4.2** Nguồn dữ liệu: registry thật (**241** năng lực sau v2.0) + 25 màn; tìm theo tên VÀ mô tả, không phân biệt hoa thường, có dấu/không dấu tiếng Việt (tiêu chí N9).  ·  **✔ 25 màn + mọi năng lực đã hiện thực; tìm theo tên VÀ mô tả, không dấu, không phân biệt hoa thường. `--tu-kiem` 13/14 đo trên registry thật (244 năng lực, không phải 241)**
- [x] **4.3** Chọn năng lực → đi qua `policy.decide` như mọi lời gọi (B2); toast hiện kết quả cổng (**APPROVE / ASK / REJECT** + mã quy tắc) *(v1.3 — v1.0 ghi "DENY", một từ `PolicyGate` không bao giờ phát ra; [DEV-143])*. Chọn màn → mở màn.  ·  **✔ `EideToast` — quyết định · năng lực · cổng · mã quy tắc · lý do, thiếu trường thì NÓI thiếu. Đồng thời ghi một dòng vào vùng trao đổi, vì một thông báo chỉ sống 5 giây là thông báo không xem lại được. §4.3 viết "DENY" — một từ cổng KHÔNG BAO GIỜ phát ra, [DEV-143]**
- [x] **4.4** Năng lực cần tham số bắt buộc → mở form tham số sinh từ hợp đồng CDS-12, không cho gọi thiếu.  ·  **✔ `EideFormThamSo` sinh từ `caps.describe`, không từ bảng chép tay. Nút Chạy chết tới khi đủ ô bắt buộc và nói rõ thiếu ô nào; trường `integer` gửi đi là SỐ; ô tuỳ chọn để trống thì không gửi**

## 5. TRÌNH SOẠN THẢO (S14) — nơi người và tác tử gặp nhau

- [x] **5.1** Lề trái mỗi dòng có hằng số phần cứng: chấm xanh ● = có `eide:fact` (hover hiện fact + nút mở nguồn đúng trang PDF); vạch đỏ ▎ = không nguồn, dòng nền hồng nhạt + chú thích "← hằng số không trỏ fact — G-FACT chặn merge".  ·  **✔ S14 · lề `NSRulerView`: ● xanh có `eide:fact`, ▎đỏ không nguồn. Hai dấu khác nhau cả HÌNH lẫn MÀU — bản in đen trắng và mắt mù màu vẫn phân biệt được**
- [x] **5.2** Tác tử chèn/sửa chú thích fact → sự kiện tới → lề cập nhật ngay, KHÔNG cần người thao tác (tiêu chí N10).  ·  **✔ S14 · `apDung` của §7.3 — màn ĐẦU TIÊN hiện thực nó; lề dựng lại từ `code.constant_guard` khi sự kiện tới**
- [x] **5.3** Người gõ tự do trong buffer; buffer bẩn (dirty) → băng vàng "Bộ đệm có sửa CHƯA LƯU của anh — tác tử muốn ghi tệp này sẽ bị hỏi (P-EDIT-01)".  ·  **✔ S14 · băng vàng nêu đúng P-EDIT-01; so NỘI DUNG chứ không đếm lần gõ, nên gõ rồi hoàn về như cũ là hết bẩn**
- [x] **5.4** Nút "Lưu" gọi `code.human_save`: commit `human:<tên>` + sự kiện `human.file_save` + mục Hoàn tác — xuất hiện đủ ba nơi trong ≤ 1 giây (tiêu chí N3). Luôn APPROVE mọi mức tự chủ (P-EDIT-02).  ·  **✔ S14 · `code.human_save` kèm `by: human:<tên>`; commit + seq hiện ngay, mục hoàn tác vào cột phải**
- [x] **5.5** Lưu gặp tệp trên đĩa đã đổi từ lúc mở → lỗi **`E6004 FILE_STALE`** (tên `E-SAVE-STALE` không vào được `errors.json`; chủ sản phẩm chốt 17/09) → tự chuyển luồng merge (mục 6), TUYỆT ĐỐI không ghi đè (B4).  ·  **✔ S14 · KHÔNG có nút ghi đè — một nút như thế biến merge ba bên thành tuỳ chọn. E6004 dẫn sang S15, bản của người nằm nguyên trong bộ đệm**
- [x] **5.6** Tác tử đang sửa tệp người đang xem → băng xanh "🤖 Tác tử đang sửa tệp này (Run #n, bước k/m) — xem diff trực tiếp"; người vẫn gõ được.  ·  **✔ S14 · băng XANH, người vẫn gõ được; sự kiện của tệp KHÁC không làm phiền**
- [x] **5.7** Tự lưu (autosave): mặc định TẮT; bật trong cài đặt thì các lần tự lưu liên tiếp squash thành một commit khi người rời tệp (quyết định cho câu hỏi mở #1).  ·  **✔ Cài đặt là `preferences.yaml` (PROJECT-09) khoá `autosave` — **mặc định TẮT** vì không có khoá thì không có tự lưu. Gộp bằng `git commit --amend`, chỗ DUY NHẤT trong cả kho viết lại lịch sử, và có bốn điều kiện chặn: HEAD phải mang trailer `Eide-Autosave`, cùng tác giả, chạm ĐÚNG một tệp, và là tệp này. "Khi người rời tệp" chính là lúc điều kiện thứ ba hỏng — mạch gộp tự kết thúc, không sự kiện nào phải phát, không trạng thái nào phải nhớ. Gộp xong thì HUỶ mục hoàn tác cũ: nó trỏ vào một commit đã biến mất**

## 6. CÙNG SỬA — MERGE 3 BÊN VÀ XUNG ĐỘT MÃ

- [x] **6.1** Tác tử gọi năng lực ghi mã trên tệp có buffer bẩn của người → cổng trả ASK, modal đúng 2 lựa chọn: "Lưu bản của tôi rồi tác tử tiếp tục" / "Tác tử chờ — tôi sửa tiếp" (P-EDIT-01). Không lựa chọn thứ ba.  ·  **✔ `EideModalHoi` — đúng hai nút, có bài kiểm ĐẾM nút trong cây khung nhìn để không có nút thứ ba lọt vào. Vế thuận làm HAI việc đúng thứ tự: lưu bản của người xong mới duyệt cho tác tử; đảo thứ tự thì chính việc của tác tử biến mất lặng lẽ. Lưu hỏng thì KHÔNG duyệt**
- [x] **6.2** Hai chuỗi sửa cùng tệp → merge 3 bên trên tổ tiên chung; vùng không giao nhau tự hợp (commit merge ghi 2 cha); vùng giao nhau → dựng xung đột (P-EDIT-03).  ·  **✔ 3e360a9 · `git merge-file --diff3`; `test_MERGE_ba_ben_tu_hop_vung_khong_giao`**
- [x] **6.3** Màn xung đột MÃ tái dùng đúng component màn Xung đột tri thức (S8): hai vế cùng hàng — "Người sửa hh:mm" / "Tác tử Run #n" — nút Chọn A / Chọn B / Soạn tay; lựa chọn ghi qua `code.merge_conflict_resolve` kèm tên người (một component, hai nguồn dữ liệu — cấm viết màn riêng).  ·  **✔ S15 · dựng `EideTheXungDot` y như S8, có bài kiểm tìm đúng lớp ấy trong cây khung nhìn. Nút thứ ba là *Soạn tay*, và nó KHÔNG gọi năng lực — nó đưa người về S14**
- [x] **6.4** Hoàn tác 3 mức chạy đúng: 1 commit; cả Run (revert chọn lọc `agent:run-<id>/*`, GIỮ commit người xen giữa — bài kiểm N4); về known-good (`project.rollback`).  ·  **✔ Ba mức chạy THẬT, `src/eide/undo_handlers.py`. Ba chỗ hỏng cùng chặn mục này và cả ba đều im lặng — `undo_handlers` rỗng, `code.merge` commit không mang tác giả máy-đọc-được (nên phép chọn lọc N4 luôn trả rỗng), và `rollback` từ chối chạy trên MỌI dự án thật vì `.eide/` chưa theo dõi. [DEV-144]. Bài kiểm N4 dàn đúng cảnh: tác tử ghi hai tệp, người sửa một tệp XEN GIỮA, hoàn tác cả lượt phải giữ nguyên tệp của người**
- [x] **6.5** Bài kiểm phủ định N5: dàn cảnh hai bên sửa cùng vùng, khẳng định KHÔNG tồn tại nhánh mã nào ghi đè không qua 6.2/6.3.  ·  **✔ `test_N5_khong_duong_nao_ghi_de…` — không chứng minh được "KHÔNG tồn tại nhánh nào" (mệnh đề phủ định mọi đường, cùng hình dạng B1), nhưng BA lối ghi mà giao diện và tác tử thực sự dùng thì kiểm được, và cả ba phải từ chối: người lưu trên tệp đã đổi (E6004), tác tử gọi `code.human_save` (P-EDIT-04), `code.modify` ngoài phạm vi (E8000)**

## 7. ĐỒNG BỘ SỰ KIỆN (nền của mọi thứ — làm TRƯỚC các màn)

- [x] **7.1** Kênh: giao diện subscribe thông báo JSON-RPC theo LOẠI sự kiện; mỗi màn khai báo tĩnh danh sách loại nó cần — có bảng đăng ký kiểm được, không màn nào subscribe "tất cả". **Một lượt nạp đã CŨ phải rút lui** *(v2.0 — [DEV-164])*.  ·  **✔ 9fa1ffc · `EideDangKySuKien.BANG`; 22 loại lấy từ `EideMethod` (sinh từ openrpc.json), không chép tay. Nhật ký khai `TAT_CA` — hằng số CÓ TÊN, không phải `"*"` — vì §8 S2 đòi "nghe mọi loại"; có bài kiểm đòi ĐÚNG MỘT màn được dùng nó. **Lượt nạp cũ**: `nap` đánh số thế hệ và mang theo TASK bằng `@TaskLocal`; `doc()` ném `LoiNapCu` ngay sau mỗi `await` nếu lượt nạp đã bị một lượt mới thay thế. Không có nó thì hai lượt nạp chạy song song cùng ghi vào một cây khung nhìn, và thứ người dùng thấy là lượt nào về sau — không phải lượt nào mới hơn, [DEV-164]**
- [x] **7.2** Mỗi màn giữ `seq` sổ cái của lần vẽ gần nhất; sự kiện đến áp tuần tự; phát hiện nhảy quãng hoặc mất daemon → nhãn "Dữ liệu cũ — bấm để tải lại" ≤ 2 giây (B6/N6); bấm = query lại từ seq đã có. **Nhảy quãng chỉ báo khi hở LỚN (> 8)** *(v2.0 — [DEV-163])*.  ·  **✔ 9fa1ffc · phát hiện nhảy quãng ở `EidePhien` chứ không ở từng màn (`seq` là số TOÀN CỤC); nhảy quãng có câu RIÊNG khác mất daemon; dải có nút Tải lại bấm được. Ngưỡng `> 8` chứ không `≥ 2`: `KHONG_LEN_UI` bỏ qua vài loại bản ghi vận hành nên `seq` **vốn** hở vài số trong đường chạy bình thường — báo mọi hở là dựng một cảnh báo luôn sáng, và một cảnh báo luôn sáng là một cảnh báo người ta thôi đọc, [DEV-163]**
- [~] **7.3** Sự kiện tới màn đang ĐÓNG → chỉ tăng badge nhóm; tới màn đang MỞ → vẽ lại đúng phần liên quan (diff render, không reload cả màn).  ·  **NỬA ĐẦU XONG** 9fa1ffc. Hai lần sửa sau đó, và cả hai vì badge ĐẾM SAI chứ không vì nó thiếu: badge nay đếm **đơn vị việc** (tập khoá `chain` mà `Ledger.append` đóng dấu lên mọi bản ghi) thay vì đếm sự kiện — một lời gọi sinh sáu bản ghi thì nó là MỘT việc, [DEV-156]; và bộ lọc "tiếng vọng" nhận cả `action_cap` chứ không chỉ `cap`, nếu không thì chính lời gọi mà một màn phát ra để tự vẽ lại làm tăng badge của màn ấy, [DEV-169].  **NỬA SAU MỚI MỘT MÀN**: `EideManCoSo.apDung` là điểm mở rộng, mặc định trả `false` để đếm được còn bao nhiêu màn chưa làm — tới 23/09/2026 chỉ **màn Nhật ký (S2)** hiện thực nó (chèn một hàng lên đầu, ~6 ms trên 120 hàng, thay cho 7,68 s nạp lại). Các màn còn lại vẫn nạp lại cả màn, nhưng các lượt nạp nay **gộp** được: sự kiện tới trong lúc đang nạp chỉ ĐÁNH DẤU, xong rồi nạp thêm đúng một lượt — trước đó cờ chờ bị gỡ trước khi nạp nên hai sự kiện liền nhau xếp hai lượt nạp chồng lên nhau, [DEV-166]
- [x] **7.4** `project.watch` chạy nền: tệp đổi ngoài EIDE → `human.file_external` kèm diff tóm tắt; bỏ qua thay đổi do chính tác tử vừa ghi (đối chiếu hash).  ·  **✔ 3e360a9 · `project.watch`, lọc theo BĂM nội dung chứ không theo thời gian**
- [x] **7.5** Lượt kế của tác tử: `memory.compose` nhận khối "thay đổi của người từ lượt trước" (tệp + diff tóm tắt); người sửa trúng vùng thuộc kế hoạch đang chạy → kích `plan.replan`, cấm ghi đè.  ·  **✔ Khối C5 "người đã sửa tay từ lượt trước" dựng từ sổ cái `human.file_save` kể từ `run.done` gần nhất. Luật lập lại: `code.human_save` đánh dấu `replan_for` NGAY TRÊN bản ghi lưu — API-15 khai 34 kiểu sự kiện và `plan.replan_needed` không nằm trong đó; thêm một kiểu là sửa hợp đồng cho một dữ kiện vốn thuộc về sự việc đã có. Orchestrator đọc dấu ấy trước mỗi nút và gọi `plan.replan(reason="human_change")`**
- [x] **7.6** Diff tóm tắt đưa vào ngữ cảnh: ≤ 200 dòng gửi nguyên văn; hơn thì tóm tắt bằng mô hình, ghi rõ "đã tóm tắt" (quyết định cho câu hỏi mở #2).  ·  **✔ `TRAN_DONG_DIFF = 200`: dưới trần gửi nguyên văn, trên trần tóm tắt bằng mô hình và **nói ra là đã tóm tắt** kèm số dòng thật. Mô hình hỏng thì lùi về phép cắt xác định (giữ `---`/`+++`/`@@`) chứ không ném — một lượt chạy không được chết vì phần tóm tắt ngữ cảnh. Ghi `human_diff:summarize` vào `compressions` theo CXD-10 §5**

## 8. TỪNG MÀN — THÀNH PHẦN BẮT BUỘC (mỗi màn: năng lực đứng sau · thành phần · trạng thái rỗng · sự kiện subscribe)

> **Mục này được ĐO, không đọc bằng mắt** *(v2.0 — [DEV-183])*. `EideApp --do-noi-dung <dự án>`
> mở từng màn trên một dự án THẬT và tìm dấu hiệu của từng thành phần bắt buộc liệt kê dưới
> đây; bảng kỳ vọng sinh ra từ `docs/EIDE-VUNG-MAN-HINH.xlsx` sang
> `docs/spec/ui/man_can_hien.json`, nên tài liệu và phép đo không tách nhau được.
>
> Phép đo trả **bốn** nhóm, không một con số:
>
> | nhóm | nghĩa | làm đỏ cổng? |
> |---|---|---|
> | đã hiện | tìm thấy dấu hiệu | — |
> | **CHƯA DỰNG** | màn CÓ dữ liệu mà mục vẫn không hiện | có |
> | **MÀN HỎNG** | màn rỗng vì lời gọi NÉM LỖI | có |
> | không đo được | màn rỗng vì dự án chưa có hiện vật loại ấy | không |
>
> Bản đầu gộp cả bốn thành "thiếu" và báo *"28 hiện, 49 thiếu"* trên một dự án có 0 fact, 0 hộ
> chiếu, 0 yêu cầu — phần lớn 49 mục ấy là màn đang hiện ĐÚNG trạng thái rỗng của B5. Tin vào
> con số ấy là đi viết lại 21 màn không hỏng, và viết xong con số vẫn y nguyên vì dấu hiệu cần
> tìm nằm trong DỮ LIỆU chứ không trong mã. Chạy lại trên dự án có 290 fact thì lộ ra chuyện
> thứ hai: **18/20 màn "rỗng" thật ra đang in `E6003: store user_version=7, cần 10`** — một sản
> phẩm HỎNG được báo cáo là "màn rỗng"; và chuyện thứ ba: hai lượt chạy liên tiếp ra `32/24/21`
> rồi `31/27/19` vì phép đo chụp lúc màn còn đang nạp. **Một cổng chập chờn dạy người ta chạy
> lại cho tới khi xanh.**
>
> Phép đo cũng phải đọc **nhãn NÚT**, không chỉ `NSTextField`: màn Bản đồ luồng vẽ tám pha
> P0–P7 thành tám nút, và một đầu dò mù với nút báo "chưa dựng 3/3" cho một màn hiện đủ cả tám.

### Nhóm 1 · DỰ ÁN
- [x] **S1 Tổng quan** — `project.status`, `session.state`, `budget.state`, `queue.list`, `view.timeline`, `passport.browse` · **bốn khối trả lời bốn câu** *(v2.0 — [DEV-185])*: ① tính năng `n passing / n tổng` + cái đang làm dở; ② **đang chờ tôi** = số mục + 3 cái gần nhất, bấm được sang chỗ XỬ LÝ được; ③ chip/board đã ghim + ISA + trạng thái hộ chiếu; ④ 3 việc tác tử vừa làm xong (có giờ) · rỗng: "dự án mới — chưa có feature; bước kế: ra lệnh đầu tiên" · nghe: `run.*`, `human.*`, thay đổi pending/undo.  ·  **✔ Trường lõi CHƯA lưu hiện thành dòng riêng (DEV-110), không nuốt. Tới 22/09/2026 màn hiện mã phiên và ngân sách — hai thứ đúng và KHÔNG nằm trong bốn câu §8 hỏi; đo bằng `--do-noi-dung` trên một dự án có 290 fact: 4/6 mục không hiện. Bấm một mục chờ thì sang chỗ XỬ LÝ được (mục cổng → vùng trao đổi, nơi có nút Duyệt của §2D.8), không sang chỗ xem. Hai nguồn cho "đang chờ tôi" vì việc chờ có hai loại ở hai chỗ — mục bị cổng chặn (`queue.list`) và điểm cần làm rõ đang mở; cộng tay hai con số là đúng lỗi [DEV-151]**
- [x] **S2 Nhật ký** — `view.timeline` · dòng thời gian MỌI sự kiện (máy + người), mới nhất trên; **cột: lúc · ai · loại · năng lực · kết quả · chi phí · chi tiết**; **năm lối lọc**: tất cả / chỉ việc của tôi / chỉ việc tác tử tự làm / chỉ lỗi / chỉ cổng *(v2.0 — [DEV-185])* · rỗng: "sổ cái chưa có sự kiện; bước kế: tạo dự án/ra lệnh" · nghe: mọi loại.  ·  **✔ `view.timeline` kèm `limit` (DEV-132); cắt ở LÕI chứ không ở bên vẽ. Bộ lọc đọc `by`/`kind`, KHÔNG đọc chữ trong mô tả — mô tả do bốn nguồn khác nhau viết, và một bộ lọc bám vào câu chữ sẽ im lặng bỏ sót đúng lúc nguồn thứ năm xuất hiện. "Chỉ lỗi" gồm CẢ lời gọi hỏng lẫn bản ghi mang mã lỗi: hai cách một việc hỏng để lại dấu, bỏ một là bỏ đúng nửa số lỗi. Lọc ra rỗng KHÁC sổ cái rỗng, và màn nói rõ là bộ lọc đang che. Bộ lọc áp cả cho hàng chèn thêm của §7.3 — không áp thì bảng mang nhãn "Chỉ lỗi" chứa một dòng không lỗi, và người đọc tin vào nhãn**
- [x] **S3 Bản đồ luồng** — `view.timeline` lọc theo pha (hết màn mồ côi) · sơ đồ P0→P7, pha hiện tại tô đậm, bấm pha mở S2 lọc sẵn; **mỗi pha ghi CỔNG canh nó + quyết định gần nhất của cổng ấy kèm lý do**; **bước đang bị chặn hiện nổi bật kèm CÁCH GỠ** *(v2.0 — [DEV-185])* · nghe: `run.*`.  ·  **✔ tám pha P0–P7 rút từ `bpd.js`, có bài kiểm Python đối chiếu ngược HAI CHIỀU cho cả bảng cổng ↔ pha của BPD §10 — bịa thêm một cổng là dọa người dùng bằng cổng không có, bỏ sót một cổng là giấu chỗ công việc sẽ dừng. Pha hiện tại SUY từ sổ cái chứ không từ một biến trạng thái. Nói ra số lời gọi NGOÀI pha (68/244 năng lực có trong tám quy trình). Bấm pha: chưa truyền được bộ lọc sang S2, màn nói thẳng. Cách gỡ viết RIÊNG cho từng cổng và phân biệt ASK với REJECT — "duyệt ở vùng trao đổi" đúng với ASK và vô dụng với REJECT, vì cổng từ chối thì không có gì để duyệt. Không cổng nào chặn thì màn NÓI RA điều đó: sự vắng mặt của một cảnh báo không phân biệt được với "màn quên kiểm"**

### Nhóm 2 · TRI THỨC
- [x] **S4 Nhập tài liệu** — `archive.*`, `ingest.*`, `extract.*` · vùng kéo-thả + bảng nguồn (loại, số fact trích, trạng thái duyệt theo chính sách, **AI ĐƯA VÀO**, **GIẤY PHÉP**) + **lượt nhập gần nhất** dựng từ sổ cái · rỗng: "chưa nhập tài liệu nào; kéo PDF/SVD/BOM vào đây" · nghe: `ingest.*`.  ·  **✔ vùng kéo-thả + đường ống classify → dedupe → extractor → index_text; bảng nguồn từ `archive.sources` (ARCHIVE-08) — tới 20/09 màn KHÔNG có bảng nguồn thường trực, chỉ có lượt nhập hiện tại, [DEV-134]. Hai cột thêm ở v2.0 ([DEV-185]): **tài liệu tác tử TỰ TẢI phải phân biệt được với tệp người đưa vào** — chỉ `search.fetch` điền `fetched_at` (DDD-14 §2), nên chính cột ấy là câu trả lời, và đó là câu G-SRC tồn tại để hỏi; **giấy phép trống ghi `CHƯA RÕ`**, không phải dấu gạch, vì G-SRC-01 duyệt theo `allowed_licenses` và một ô trống là thứ người duyệt phải nhìn thấy**
- [x] **S5 Hộ chiếu chip** — `passport.query`, `view.provenance` · bảng thanh ghi: tên, địa chỉ HỆ 16, tầng (vàng/bạc/nâu), cột nguồn bấm mở ĐÚNG TRANG PDF kèm bbox · ô mâu thuẫn hiện ⚠ trỏ S8 · rỗng: "chưa ghim chip; bước kế: nhập datasheet hoặc target.detect".  ·  **✔ địa chỉ hệ 16, tầng, nguồn kèm `tr.<trang> [bbox]`; ⚠ trỏ S8. Ô nguồn bấm được còn thiếu — [DEV-133]**
- [x] **S6 Hộ chiếu mạch** — `board.*`, `diagram.pinmap` · bảng net/chân/chức năng/kiểm xung đột + trạng thái khai báo mạch lab (`board.mark_lab`) ghi TÊN NGƯỜI khai · rỗng: "chưa có schematic/BOM".  ·  **✔ bảng chân + `check_pins` + ràng buộc; khai báo lab đòi CẢ HAI ô xác nhận. Trạng thái lab chưa đọc lại được — [DEV-135]**
- [x] **S7 Bản đồ tri thức & hỏi đáp** — `view.kg_map`, `view.rag_ask` · đồ thị thu phóng (node fact/tài liệu/mã, cạnh CITES/USES/CONFLICT) + ô hỏi; câu trả lời BẮT BUỘC kèm trích dẫn nhấp mở nguồn, không nguồn thì trả "không đủ căn cứ" · nghe: `kg.*`.  ·  **✔ đồ thị bố cục cột TẤT ĐỊNH; câu trả lời không trích dẫn thì KHÔNG hiện; `not_found` là kết quả đúng**
- [x] **S8 Xung đột tri thức** — `kg.conflicts`, `kg.resolve_conflict` · danh sách xung đột mở; mỗi cái: hai vế CÙNG HÀNG, mỗi vế = giá trị + tầng + nguồn bấm được + nút chọn; đã quyết → dòng lịch sử ghi tên người + thời điểm · rỗng: "không còn xung đột mở — quyết định gần nhất: …" · component này DÙNG CHUNG cho xung đột mã (6.3).  ·  **✔ `EideTheXungDot` — component dùng chung với §6.3; `pending` nói rõ chưa ghi store**

### Nhóm 3 · THIẾT KẾ
- [x] **S9 Làm rõ yêu cầu** — `chat.restate`, `chat.clarify`, `req.answer_clarification` · thẻ ý hiểu + chuỗi bước + câu hỏi gộp (mỗi câu có mặc định an toàn, người bỏ qua được) + **ô trả lời ngay trên dòng** + lịch sử trả lời; câu đã trả lời xếp xuống dưới, không mất.  ·  **✔ chỉ ĐỌC (`queue.list` + `chat.history`), không gọi năng lực sinh lúc mở; nút Bỏ qua không quyết hộ. Màn này là **sổ ghi** — "còn treo những gì"; vùng trao đổi (§2D.8) là **cuộc trò chuyện**. Cùng một câu hỏi hiện ở CẢ HAI chỗ, trả lời ở đâu cũng được: [DEV-160] đưa câu hỏi về đây vì bong bóng chat TRÔI — lý do đúng, kết luận sai, đáng lẽ là cả hai. Nút chuỗi bị chặn ghi câu hỏi về đây kèm `clar_id` băm theo NỘI DUNG nên chạy lại chuỗi không đẻ bản sao, [DEV-178]. **Chỉ ghi lỗi CÓ ĐƯỜNG RA** (E2000/E4001/E6003/E3000): E5000/E5002 là mô hình trả sai schema và E7001 là chuyện của máy — đưa chúng lên đây là biến tab này thành sọt rác lỗi, và một tab đầy thứ không hành động được là một tab người ta thôi mở**
- [x] **S10 Yêu cầu & kiến trúc** — `req.*`, `arch.*` · bảng FR/NFR với cột khả thi (`req.ground_hw` — căn cứ phần cứng thật) + danh sách ADR (quyết định kiến trúc) kèm fact trích dẫn.  ·  **✔ `view.artifacts`; cột KHẢ THI ba trạng thái; ADR không trích dẫn bị đánh dấu**
- [x] **S11 Lược đồ** — `diagram.*` · khung xem 6 loại lược đồ + chỉ báo đồng bộ hai chiều mã ↔ hình (`diagram.sync`): lệch thì băng vàng + nút đồng bộ.  ·  **✔ `stale` đọc cả dạng INTEGER lẫn Bool; lược đồ lệch kéo lên thành băng + nút đồng bộ**
- [x] **S12 Kế hoạch** — `plan.create`, `plan.sufficiency` · bảng bước: mô tả + CỘT FACT TRÍCH DẪN (bấm mở nguồn) + `missing[]` hiện thành khối "còn thiếu" đầu bảng, **mỗi mục nói rõ thiếu CÁI GÌ và ai trả lời được** + **quyết định cổng G1**: ai quyết, quy tắc nào, lúc nào · rỗng: "chưa có kế hoạch cho feature này".  ·  **✔ khối "còn thiếu" lên ĐẦU bảng, có bài kiểm so vị trí. Ba loại thiếu có BA hình dạng vì chúng dẫn tới ba hành động khác nhau — đi tìm tài liệu, cài công cụ, hay trả lời tác tử; gộp vào một câu chung là xoá đúng phần khiến người dùng biết làm gì tiếp. Trước [DEV-171] dòng ấy đọc ra *"CÒN THIẾU 2 căn cứ: {3 khoá}, {3 khoá}"* — đúng số lượng, không một chữ nào nói thiếu cái gì. **Nút duyệt G1 KHÔNG ở màn này**: chủ sản phẩm chốt 22/09/2026 *"nút duyệt hoặc phê duyệt luôn ở ô trả lời chat"*, nên màn HIỆN quyết định và chỉ sang vùng trao đổi (§2D.8). Hai nút duyệt cho cùng một việc ở hai chỗ là hai chỗ phải giữ đồng bộ, và là hai câu trả lời khác nhau cho câu "tôi duyệt ở đâu"**
- [x] **S13 Tài liệu** — `doc.*` · bảng tài liệu sinh (trạng thái, mục stale) + nút sinh lại; `doc.style_check`: tiếng Việt ưu tiên + mọi khẳng định có nguồn.  ·  **✔ `uncited` tách riêng khỏi vấn đề văn phong — nó là loại duy nhất làm tài liệu không kiểm được**

### Nhóm 4 · MÃ NGUỒN
- [x] **S14 Trình soạn thảo** — toàn bộ mục 5 của checklist này.  ·  **✔ toàn bộ mục 5 (6/7 — xem 5.7)**
- [x] **S15 Diff & cổng merge** — `code.review`, `code.merge` · khung diff hai cột + bảng cổng (G-FACT, G1/G3/G4/G5): mỗi cổng trạng thái ✅/⛔/⏳ + lý do chặn bấm mở đúng chỗ; **bảng BẰNG CHỨNG cho G3: 4 công cụ kiểm + reviewer khác hãng** *(v2.0 — [DEV-185])*; merge xong hiện mã commit + tác giả máy-đọc-được + trailer seq + hạn hoàn tác.  ·  **✔ §6.3; bảng cổng ba trạng thái kèm LÝ DO. Bảng cổng nói cổng đã quyết GÌ; bảng bằng chứng nói DỰA VÀO ĐÂU — thiếu nó thì người duyệt G3 bấm "Duyệt" mà không thấy thứ mình đang duyệt, và POL-17 sinh ra chính là để chuyện đó không xảy ra. Bốn công cụ hiện ĐỦ bốn dòng kể cả khi chưa chạy cái nào: chỉ liệt kê công cụ đã có báo cáo thì một bảng trống đọc như "không có yêu cầu nào", trong khi sự thật là "bốn yêu cầu, chưa cái nào chạy". Reviewer khác hãng là điều kiện THỨ NĂM và nó không phải một công cụ — gộp cùng bảng vì với người duyệt cả năm là một danh sách kiểm; tách ra hai bảng là để họ quên mất cái thứ năm**

### Nhóm 5 · CHẠY THỬ
- [x] **S16 Mô phỏng** — `sim.*` · log UART thật từ simavr + bảng kỳ vọng ĐẠT/TRƯỢT · rỗng: "chưa có lượt mô phỏng; bước kế: tác tử chạy sim.run ở bước N".  ·  **✔ đọc `tool.report` của `sim.run` từ sổ cái, KHÔNG tự chạy mô phỏng lúc mở. Giữ nguyên BA trạng thái kỳ vọng — `unverified` tách khỏi `failed`; hết giờ tách khỏi chạy xong**
> **v2.0 — bốn màn dưới đây HẾT CHẶN** ([DEV-186]). Tới 22/09/2026 cả bốn có mặt trong menu cột
> trái mà **không có lớp màn nào**: bấm vào mở ra một vùng làm việc trống, không một dòng chữ.
> Menu hứa 25 màn, sản phẩm giao 21. Bộ dò nút `--do-nut` không thấy suốt nhiều phiên vì nó
> duyệt MENU, còn `moMan()` chỉ canh danh sách menu nên vẫn trả `true` — rồi nó đếm nút trên
> một vùng trống và báo "0 nút chết trên 25 màn". Một phép đo đứng về phía lời hứa thay vì về
> phía sản phẩm. Lỗ ấy nay do `--do-noi-dung` bịt: nó hỏi `EidePhien.MAN`, không hỏi `moMan()`.
>
> **"Chặn vì chờ bo mạch" KHÁC "không dựng được".** Ba trong bốn màn sẽ rỗng trên máy không cắm
> gì, và đó là trạng thái ĐÚNG theo B5 — chúng nói ra cần gì để có dữ liệu. Thứ sai là một vùng
> trống không nói gì.
>
> **Không màn nào TỰ chạm phần cứng khi mở.** `discover.probe`/`bus_scan` gửi tín hiệu xuống
> board, `debug.experiment` nạp firmware thí nghiệm, `bench.run` tiêu token thật. Mở màn mà tự
> chạy là tác động lên phần cứng (hoặc ví tiền) của người dùng vì họ bấm nhầm một mục menu —
> đúng thứ POL-17 G-OPS tồn tại để chặn, và giao diện không được đi vòng qua nó. Mọi thao tác
> ấy nằm sau MỘT nút có nhãn nói rõ nó làm gì. Ngoại lệ `discover.ports`: nó chỉ liệt kê cổng
> USB của máy phát triển, không gửi gì xuống board, nên chạy được lúc mở màn.

- [x] **S17 Dò board** — `discover.ports`, `discover.probe`, `discover.chip_id`, `discover.bus_scan`, `discover.power`, `discover.link_speed` · bảng cổng/probe (thiết bị · VID:PID · loại · driver · serial) + đối chiếu ID đọc được với chip đã ghim: **KHỚP / LỆCH** · rỗng khi không board: "Không cổng USB/serial nào đang cắm" (B5, không dữ liệu giả) · nghe: `discover.changed`, `run.progress`.  ·  **✔ `EideManDoBoard`. Cột DRIVER nói "THIẾU DRIVER" chứ không để trống — đó là lý do thường gặp nhất khiến board cắm rồi mà không nạp được. KHỚP/LỆCH là câu quan trọng nhất của cả màn: nạp firmware của chip A lên chip B là cách hỏng board nhanh nhất, và `passport_match` là chỗ duy nhất biết**
- [x] **S18 Log & serial** — `debug.log_stats`, `debug.ask_at`, `serial.*` · thống kê log (mẫu lặp · số lần · mức) + **khoảng TRỐNG thời gian** + lọc theo mẫu lỗi + hỏi-tại-dòng (trả lời kèm fact/mã liên quan); serial trực tiếp qua `event.serial.line` · nghe: `serial.line`, `notice`, `run.progress`.  ·  **✔ `EideManLog`. Hai nửa trả lời hai câu khác nhau — log ĐÃ GHI nói *"vừa rồi có gì bất thường"*, serial trực tiếp nói *"ngay bây giờ nó đang nói gì"*; gộp vào một khung cuộn chung là đánh mất phân biệt ấy. Khoảng trống thời gian là bằng chứng của treo/reset — thứ dễ bỏ sót nhất khi đọc log bằng mắt, vì nó là chỗ KHÔNG có gì**
- [x] **S19 Gỡ lỗi probe** — `debug.hypothesize`, `debug.experiment`, `debug.propose_fix` · giả thuyết xếp hạng kèm **bằng chứng ủng hộ VÀ phản bác** + EvidencePack (log · số đo · thanh ghi) + thí nghiệm tiếp theo tác tử đề nghị · nghe: `run.progress`, `gate.opened`, `serial.line`.  ·  **✔ `EideManGoLoi`. Cột PHẢN BÁC trống nhiều hơn cột ỦNG HỘ, và đó là một TÍN HIỆU chứ không phải khiếm khuyết trình bày: giả thuyết chưa ai tìm cách bác bỏ là giả thuyết chưa được kiểm. Chỗ hữu ích của DEBUG-03 nằm ở thí nghiệm PHÂN BIỆT — phép thử mà hai giả thuyết hàng đầu cho hai kết quả khác nhau**
- [x] **S20 Bench** — `bench.run`, `bench.badge` · bài CF/BF/BC (correctness-first · build-first · budget-constrained) + huy hiệu đạt được, so với lần chạy trước · nghe: `tool.report`, `model.call`, `run.progress`.  ·  **✔ `EideManBench`. Màn KHÔNG tự chạy bench khi mở: `bench.run` gọi mô hình nhiều lượt cho cả bộ tác vụ, tức tiêu tiền thật — cùng lý do đã viết cho `sim.run` ở S16, và ở đây cái giá đo bằng USD chứ không bằng giây CPU. Cột "so với lần trước" giữ DẤU: `+0.03` và `0.03` là hai câu khác nhau khi con số có thể âm**

### Nhóm 6 · HỆ THỐNG
- [x] **S21 Môi trường** — `env.doctor`, `tools.lock` · bảng công cụ: phiên bản, hash khớp lockfile ✅/⚠; nút sửa KHÔNG bao giờ chạy sudo.  ·  **✔ `env.check` theo ISA; BA trạng thái (đạt / chưa cài / **có nhưng CŨ**) vì `ok=false` gộp mất hai việc khác hẳn nhau. Nút sửa gọi `env.guide_install` (R0) — KHÔNG `env.install` (R4), có bài kiểm cấm**
- [x] **S22 Mô hình & chi phí** — `budget.state`, `view.timeline` · bảng **vai trò → mô hình** + KPI chi phí vòng/ngày + thanh hạn mức + **lượt gọi mô hình gần nhất: vai trò · token vào/ra · giá · độ trễ** *(v2.0 — [DEV-185])*; chạm hạn mức → sự kiện + băng cảnh báo, không chạy tiếp im lặng.  ·  **✔ `budget.state` + thanh hạn mức; `sap_het` giữ BA giá trị (`nil` = chưa biết). Bảng vai trò → mô hình có từ [DEV-136]. Bảng lượt gọi là thứ nằm giữa hai cái kia: bảng trên nói CẤU HÌNH, con số chi phí nói TỔNG, còn câu người mở màn này hỏi khi thấy hoá đơn cao hơn dự tính là **lượt nào đã tiêu**. Đọc từ sổ cái chứ không từ một bộ đếm riêng — cùng nguồn với `budget.state`, nên hai con số trên cùng màn hình không bao giờ lệch. Lượt HỎNG cũng hiện và hiện rõ là hỏng: một lượt gọi lỗi vẫn tốn token vào ở phần lớn nhà cung cấp**
- [x] **S23 Công cụ tự tạo** — `tool.*` · bảng công cụ tạm/thăng cấp; đường thăng cấp bắt buộc qua sandbox + bài kiểm (G-TOOL), hiện số lần dùng đạt.  ·  **✔ đếm lượt ĐẠT theo TOOL-08 (≥3 lần, 0 lỗi); dựng từ sự kiện `tool.report`, tức LỊCH SỬ CHẠY chứ không phải danh mục — [DEV-136]**
- [x] **S24 Registry** — `registry.*` · bảng gói .hkp: chữ ký, license, huy hiệu (verified/bench).  ·  **✔ `registry.search`; cột huy hiệu đứng TRƯỚC phiên bản vì badge là thứ đắt nhất hệ thống. Không chữ ký là CẢNH BÁO, không phải ô trống**
- [x] **S25 Chính sách tự chủ** — `policy.rules` · bảng **54** quy tắc (49 + P-EDIT-01/02/**03/04** + P-RUN-01) chỉ-đọc + mức hiện tại + đổi mức qua `policy.set_autonomy` + **trạng thái niêm phong, nói CẢ khi đã ký** *(v2.0 — [DEV-185])*; ký lại chính sách ghi rõ "làm bằng lệnh dòng lệnh, không phải năng lực".  ·  **✔ bảng quy tắc chỉ-đọc, đọc từ quy tắc PolicyGate ĐANG NẠP chứ không từ nội dung `rules.yaml` — hai thứ ấy khác nhau đúng lúc quan trọng nhất, khi niêm không khớp. Niêm CHƯA ký nói TRƯỚC bảng. Từ v2.0 niêm ĐÃ ký cũng nói: người mở màn chính sách ra để kiểm tra an toàn cần nghe một câu KHẲNG ĐỊNH — "niêm đang khớp" — chứ không phải sự vắng mặt của một cảnh báo, vì không có gì phân biệt "đã ký" với "màn quên kiểm" nếu cả hai đều im**

## 9. TRỢ NĂNG (accessibility) & BÀN PHÍM

- [x] **9.1** Điều hướng đủ bằng bàn phím: ⌘K bảng lệnh; ⌘1…⌘6 nhảy nhóm; ⌘W đóng tab; ⌘S = `code.human_save`; Esc đóng overlay.  ·  **✔ ⌘K · ⌘1…⌘6 nhảy sáu nhóm (mở màn ĐẦU của nhóm, không chỉ cuộn tới tiêu đề) · ⌘W đóng tab và hết tab thì KHÔNG đóng cửa sổ · ⌘S `code.human_save` (không mở S14 thì NÓI RA) · Esc đóng overlay. Đặt trong MENU chứ không bắt phím thô — phím tắt không ai tìm ra được là phím tắt chỉ tác giả dùng**
- [x] **9.2** Mọi phần tử tương tác có nhãn trợ năng tiếng Việt cho VoiceOver; thứ tự focus theo thứ tự thị giác.  ·  **✔ Bài kiểm quét cây khung nhìn của CẢ 21 màn. Bản đầu xanh giả: `accessibilityLabel()` mặc định trả về chính `title`, nên `▁` có nhãn `▁` và bài kiểm mù đúng với nhóm nút nó sinh ra để bắt. Siết lại thì lộ 4 nút × 21 màn — nay có nhãn tiếng Việt thật**
- [x] **9.3** Modal ASK: focus nhốt trong modal, Esc = lựa chọn an toàn ("Tác tử chờ"), KHÔNG bao giờ Esc = đồng ý.  ·  **✔ Tiêu điểm nhốt trong modal (Tab chạy vòng giữa hai nút), tiêu điểm ĐẦU ở vế an toàn, Esc = vế an toàn. Nút mặc định (Enter) cũng là vế an toàn — một phím Enter gõ vội không được duyệt cho tác tử ghi đè**
- [x] **9.4** Hoạt ảnh tôn trọng "Giảm chuyển động" của hệ điều hành (tắt nhấp nháy 2B.5, giữ đổi màu tĩnh).  ·  **✔ Ba chỗ hoạt ảnh đều theo cờ hệ điều hành. `EideDock.datCao` là chỗ BỎ SÓT và nó là hoạt ảnh lớn nhất cửa sổ — khối 272 pt trượt: người bật cờ vì chuyển động làm chóng mặt vẫn nhận đúng chuyển động mạnh nhất**

## 10. BÀI KIỂM NGHIỆM THU — ánh xạ N1–N10 (chạy trên cửa sổ thật, không headless)

- [x] **10.1** N1 → bài kiểm 3.2 · N2 → 2E.4 · N3 → 5.4 · N4 → 6.4 · N5 → 6.5 · N6 → 7.2 · N7 → 2.3 · N8 → 2B.1 · N9 → 4.2 · N10 → 5.2. Mỗi bài kiểm là một kịch bản tự động thao tác UI thật + đọc sổ cái đối chiếu.  ·  **✔ 10/10.** Bảng N1…N10 là MÃ chứ không phải bảng chép tay: tám ô Swift đối chiếu với runtime, hai ô LÕI đối chiếu bằng `test_bang_tieu_chi_N1_N10_tro_dung_bai_kiem_python`. Nó bắt được ngay hai ô tôi điền từ trí nhớ, rồi bắt tiếp chính nó nói sai chiều ngược lại (hàm `async` lộ ra ObjC dưới tên `…WithCompletionHandler:`). N4/N5 từng để trống vì chặn ở `undo_handlers`; gỡ 21/09, [DEV-144] — ghi chú lịch sử giữ lại trong `TUNG_CHAN`
- [x] **10.2** Thêm bài kiểm phủ định cho B1: tắt daemon → khẳng định KHÔNG widget nào tự đổi trạng thái.  ·  **✔ Cắt nguồn sự thật rồi khẳng định màn hình ĐỨNG YÊN — một widget giữ trạng thái nguồn riêng sẽ tiếp tục nhúc nhích ở đây. Kèm vế ngược: nghe được sự kiện thật thì PHẢI đổi, không thì bài trên xanh cho cả một giao diện chết hẳn**
- [x] **10.3** Toàn bộ 10 bài chạy trong CI trước mọi merge nhánh giao diện; trượt 1 bài = chặn merge.  ·  **✔ có điều kiện.** Job `eide-ui` trong `.github/workflows/ci.yml`: bản sinh khớp spec → `swift build` → bài kiểm → `--tu-kiem` trên cửa sổ thật. KHÔNG `continue-on-error` — §10.3 nói "trượt 1 bài = chặn merge", và một cổng không chặn được gì thì nó là một dòng log. Job này **thiếu từ 18/09**: gói `apps/eide` ra đời trong kho mà CI không hề đụng tới. **CHƯA xác nhận được job chạy xanh trên GitHub** — máy này không có `gh`, nên tôi chỉ kiểm được YAML hợp lệ và các lệnh chạy đúng ở cục bộ. Hai chỗ có thể hỏng trên runner: `--tu-kiem` cần một phiên đồ hoạ để dựng `NSWindow`, và `python` trần (đã thêm `setup-python`). Chủ sản phẩm xem lần chạy CI kế tiếp rồi chốt

## 11. ĐỊNH NGHĨA HOÀN THÀNH (Definition of Done) CHO MỖI MÀN

- [x] **11.1** Header chuẩn 2C.4 ✔ · trạng thái rỗng 2 phần ✔ · danh sách sự kiện subscribe khai báo ✔ · nhãn stale hoạt động ✔ · trợ năng 9.2 ✔ · ảnh chụp so mẫu demo EIDE_UI_Demo_v2.html không lệch cấu trúc ✔ · một dòng trong bảng theo dõi: mã màn, commit, ngày, người/tác tử làm.  ·  **✔ `docs/BANG-THEO-DOI-MAN.md` — bảng SINH từ `EideManHinhDS` + `EidePhien.MAN` + `git log`, `--kiem` chạy trong `make check` và CI. Nó bắt lỗi ngay lần sinh đầu: bản đầu so TÊN LỚP với TIỀN TỐ và ra "0 nối · 21 chưa" — một bảng sai toàn tập, đọc vẫn trôi chảy. Năm phép kiểm còn lại của §11.1 là BÀI KIỂM chứ không phải ô đánh dấu, ghi rõ ở cuối bảng**
- [x] **11.2** Tác tử KHÔNG đánh dấu `[x]` mục nào thiếu bằng chứng (bài kiểm hoặc ảnh chụp); mục bị chặn ghi `[!]` + lý do + hỏi người.  ·  **✔ `scripts/kiem_checklist.py` — luật về hành vi của tác tử nay là phép kiểm chứ không phải câu trong tài liệu, vì để nó ở dạng câu là để nó phụ thuộc vào trí nhớ của chính cái nó ràng buộc. Ba luật: `[x]` phải có bằng chứng sau `·`; `[!]` phải có lý do; `[x]` không được đi kèm chữ "CHƯA". Lần chạy đầu bắt hai chỗ — mục 1.5 và **S18 bị để `[ ]` trong khi S17/S19/S20 đã là `[!]`****

---
*Hết EIDE-UXC-31 v2.0 — 23/09/2026. Tài liệu này đặt tại `docs/EIDE-UXC-31_Checklist_UI_UX_cho_tac_tu.md` trong repo và được trỏ từ CLAUDE.md; nó VIẾT TAY, không sinh từ `docs/ho-so/nguon/` — xem khối đầu tài liệu và [DEV-188]. Bản Word đối chiếu sinh từ tệp này khi cần nộp hồ sơ.*
