# Đồng bộ tài liệu UXC — bản nháp 2026-09-23

Tài liệu VIẾT TAY phải sửa: `docs/EIDE-UXC-31_Checklist_UI_UX_cho_tac_tu.md`  ·  phiên bản đích: **(nêu trong từng mục)**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Tài liệu này **không** sinh từ `docs/ho-so/nguon/` — sau khi chủ sản phẩm duyệt thì sửa thẳng tệp Markdown ở trên, rồi đổi trạng thái mục thành `Đã cập nhật tài liệu (nêu trong từng mục)`.

## DEV-193 — Mở

**Mục bị ảnh hưởng:** UXC-31 §8 (77 mục "DỮ LIỆU PHẢI HIỆN") — không đổi điều khoản; CDS-12.4 SIM-01 (`sim.build_platform` GHI thư mục nền tảng), VIEW-14, REGISTRY-01; quy tắc B5

**Hiện tại (điều mã buộc phải khác):** Bộ dò nội dung **vào cổng** (`make check`) sau khi đạt 77/77; nó tự dựng dự án mẫu khi không truyền `DUAN`. Bốn luật đo mới: in BẰNG CHỨNG từng mục (`--chi-tiet`); bỏ khớp nằm trong câu PHỦ ĐỊNH; không vắt cửa sổ nhìn lui qua ranh giới dòng; chỉ gọi "không đo được" khi MỌI mục của màn đều trượt. Nguyên tắc dựng màn: **dấu hiệu là NHÃN KHỐI, và khối phải tự in nhãn kể cả khi rỗng**.

**Vì sao:** Rà soát 77 mục ngày 23/09/2026 theo yêu cầu chủ sản phẩm. Phép đo cũ sai theo **năm** cách, và cả năm chỉ lộ ra khi bắt nó in bằng chứng: (a) dương tính giả — `engine` khớp câu "engine hiện có không có kênh để nhìn", `Bước` khớp "vẫn chạy được từng bước"; (b) âm tính giả — `constant-guard` báo thiếu trong khi màn in "hằng số phần cứng KHÔNG trỏ fact"; (c) câu trạng thái RỖNG cũng khớp, nên màn chỉ cần nói "chưa có gì" là qua mọi phép đo; (d) luật phủ định vá (c) lại vắt sang dòng trước và giết mục "Cổng G1" của S13 — **tôi suýt kết luận nó cần sửa lõi**; (e) một màn vẽ ba khối rồi nói "khối thứ tư chưa có dữ liệu" bị xoá sổ cả màn.

**Nội dung đề xuất:** Sửa PHÉP ĐO trước, màn sau — thứ tự ấy là bắt buộc: mọi con số trước đó đều không dùng được để quyết định. Bộ dò vào cổng vì lý do ngược với lý do nó từng đứng ngoài: khi còn 28/77 thì một cổng luôn đỏ là một cổng bị tắt; đủ rồi thì để ngoài cổng nghĩa là mục đầu tiên rụng đi sẽ không ai thấy. Đo ngược đã chạy: tắt một khối → 76/77, đỏ đúng chỗ. Ba lỗi SẢN PHẨM tìm ra trong lượt này: màn Hộ chiếu mạch kết thúc sớm khi `diagram.pinmap` rỗng, vứt luôn xung đột chân và khối lab — đúng cái bẫy mà chú thích ngay dưới đã cảnh báo, chỉ nằm ở guard TRÊN; màn Mô phỏng gọi `sim.build_platform` (một năng lực GHI) lúc mở màn, phạm chính nguyên tắc "không màn nào tự chạm phần cứng khi mở" — nay ĐỌC `sim/platform.json` đã dựng; và `S10` gọi `req.detect_conflict` thiếu tham số bắt buộc nên in "✓ có ngưỡng đo" cho MỌI yêu cầu, một câu trả lời SAI tệ hơn hẳn ô trống.

**Mã liên quan:** `EideDoNoiDung.swift`, `EideApp/main.swift`, `scripts/gen_man_can_hien.py`, `scripts/du_an_mau.py` (mới), `Makefile`, và tám màn: S1 S3 S6 S8 S10 S11 S12 S13 S14 S16 S22

---
