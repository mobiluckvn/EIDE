# Đồng bộ tài liệu DDD — bản nháp 2026-09-07

Nguồn sinh phải sửa: `docs/ho-so/nguon/ddd.js + ddd_model.py`  ·  phiên bản đích: **v1.3**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Nó **không** sửa docx và **không** sửa `docs/spec/`. Sau khi chủ sản phẩm duyệt: sửa nguồn sinh → `scripts/sinh_tai_lieu.sh ddd` → đổi trạng thái mục thành `Đã cập nhật tài liệu v1.3`.

## DEV-056 — Mở

**Mục bị ảnh hưởng:** DDD-14 §5 bảng lịch migration, dòng `0004_m2_engineering_hw` ("… user_version=3")

**Hiện tại (điều mã buộc phải khác):** Tài liệu ghi tệp `0004_m2_engineering_hw` đặt `user_version=3`; mã đặt 4. Store suy phiên bản từ SỐ HIỆU TỆP (`migrations()` đọc `f.name[:4]`), còn §5 đếm SỐ MIGRATION ĐÃ CHẠY của store chính — hai cách khớp nhau ở 0001→1 và 0002→2, rồi rẽ đôi tại 0004 vì số 0003 đã bị `0003_m1_index` và `0003b_m1_session` dùng cho hai cơ sở dữ liệu RIÊNG (index.sqlite, session.sqlite) không nằm trong dãy của store

**Vì sao:** Giữ quy tắc đồng nhất `user_version == số hiệu tệp` (đã ghi ở `store.py` dòng 31 từ Sprint 1). Đọc `PRAGMA user_version` của một store bất kỳ là biết ngay tệp nào chạy sau cùng — với cách đếm của §5 thì phải biết thêm tệp nào thuộc dãy store, tệp nào là cơ sở dữ liệu bên cạnh. Quan trọng hơn: cách đếm dễ vỡ về sau — bất kỳ số hiệu nào bị một cơ sở dữ liệu riêng dùng tiếp (hoặc một migration bị bỏ) đều làm LỆCH toàn bộ phiên bản phía sau, trong khi quy tắc đồng nhất thì ổn định. Đổi bây giờ không tốn gì: 0001 và 0002 cho cùng kết quả ở cả hai cách, nên không store nào đang tồn tại phải đánh số lại

**Nội dung đề xuất cho v1.3:** Sửa DDD-14 §5: dòng `0004_m2_engineering_hw` đổi "user_version=3" → "user_version=4"; `0005_m3_debug` đổi "user_version=4" → "user_version=5". Thêm một câu vào §5 nêu quy tắc: `user_version` LUÔN bằng số hiệu tệp migration chạy sau cùng của store chính; các số hiệu cấp cho cơ sở dữ liệu riêng (0003 index, 0003b session) bị bỏ trống trong dãy này một cách cố ý

**Mã liên quan:** `src/eide_core/migrations/0004_m2_engineering_hw.sql, src/eide_core/store.py (LATEST_VERSION), tests/test_store.py`

---

## DEV-057 — Mở

**Mục bị ảnh hưởng:** POL-17 §2 quy tắc G-SRC-01 (`source.kind in ["svd","atdf","edc","binding","pdf_vendor"]`); DDD-14 §2 Source (enum `kind` gồm `pdf`, KHÔNG có `pdf_vendor`)

**Hiện tại (điều mã buộc phải khác):** Hai bộ từ vựng `kind` cùng tên nhưng khác nghĩa và không tài liệu nào nói ra: `Source.kind` của DDD-14 mô tả ĐỊNH DẠNG (`svd`, `pdf`, `html`…), còn G-SRC-01 chờ `pdf_vendor` — không phải định dạng mà là một PHÁN XÉT ("PDF lấy từ chính trang hãng"). Truyền thẳng `Source.kind` vào cổng thì mọi datasheet hãng đều trượt G-SRC-01 và rơi xuống G-SRC-99 (ASK)

**Vì sao:** Phân biệt của POL-17 là ĐÚNG và đáng giữ: PDF từ st.com và PDF từ một diễn đàn cùng định dạng nhưng khác hẳn mức tin cậy, mà chính sách quan tâm cái sau. Cái thiếu là chỗ nói rõ hai bảng này khác nhau và ai chịu trách nhiệm ánh xạ. Mất khá lâu mới tìm ra vì mọi đặc trưng đều trông đúng — domain đúng, license đúng, size đúng — chỉ `kind` lệch một từ, và quy tắc không khớp thì im lặng rơi xuống mặc định chứ không báo gì. Cùng họ với DEV-011/DEV-012/DEV-032: quyết định đúng, lý do sai, và người đọc decision_log bị dẫn đi sửa nhầm chỗ

**Nội dung đề xuất cho v1.3:** Bổ sung một câu vào POL-17 §2 (hoặc §1 phần đặc trưng): "`source.kind` trong quy tắc dùng từ vựng CHÍNH SÁCH, không phải enum `Source.kind` của DDD-14 §2; `pdf_vendor` nghĩa là PDF tải từ tên miền chính hãng. Bên gọi chịu trách nhiệm ánh xạ." Kèm bảng ánh xạ hai chiều. Phía mã đã hiện thực trong `_kind_chinh_sach` và có test; ghi vào store vẫn dùng enum DDD-14. Cân nhắc thêm: `trusted_sources` mặc định có `github.com/cmsis-svd` nhưng SVD thật phục vụ từ `raw.githubusercontent.com`, nên nguồn SVD vàng phổ biến nhất vẫn rơi vào ASK — sửa danh sách trắng là việc của chủ sản phẩm (WI-257) vì nó cần ký lại

**Mã liên quan:** `src/eide/caps/search.py::_kind_chinh_sach, dac_trung_nguon`

---
