# Đồng bộ tài liệu CDS — bản nháp 2026-09-10

Nguồn sinh phải sửa: `docs/ho-so/nguon/cds.js + cds_data_*.py`  ·  phiên bản đích: **v1.4**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Nó **không** sửa docx và **không** sửa `docs/spec/`. Sau khi chủ sản phẩm duyệt: sửa nguồn sinh → `scripts/sinh_tai_lieu.sh cds` → đổi trạng thái mục thành `Đã cập nhật tài liệu v1.4`.

## DEV-072 — Mở

**Mục bị ảnh hưởng:** CDS-12.1 DOC-03 steps[0] ("Doxygen/tree-sitter → chữ ký + chú thích; … ; md/html") đối chiếu chính input_schema của DOC-03 (chỉ có `src`) và pyproject.toml (dependencies)

**Hiện tại (điều mã buộc phải khác):** Hai điểm. (1) Chữ ký và chú thích đọc bằng một bộ phân tích NỘI BỘ viết trong `doc.py` (quét câu lệnh ngoài thân hàm, chỉ họ C), không gọi Doxygen cũng không dùng tree-sitter: cả hai đều không có trong `pyproject.toml` và `doxygen` không có trong `COMMON_TOOLS`. Tệp ngoài họ C (`.rs`, `.py`, `.swift`…) trả E4001 kèm `candidates=[env.guide_install, env.install]`. (2) Đầu ra chỉ Markdown, không có nhánh html.

**Vì sao:** (1) Bắt buộc Doxygen thì năng lực không chạy được trên máy chưa cài — mà nó chỉ đọc chữ ký hàm C, việc mà một bộ quét 60 dòng làm đủ và kiểm được bằng test. Đây đúng khuôn đã dùng cho `pdfplumber`/Docling ở EXTRACT-05: bộ nội bộ là đường mặc định, công cụ ngoài là đường nâng cao, thiếu nó thì E4001 nói rõ. Giữ E4001 cho ngôn ngữ ngoài họ C là giữ lỗi ấy có thật và kiểm được, thay vì một mã lỗi khai trong hợp đồng mà không nhánh nào ném. Bộ đọc đã thử trên header thật của SDK macOS (sys/stat.h 34 hàm, dirent.h 18, pthread.h 98, unistd.h 141, math.h 254). (2) `input_schema` của DOC-03 chỉ có `src` — không có chỗ nào để bên gọi chọn html, nên nhánh html là mã không ai gọi được. Cùng lập luận đã duyệt ở DEV-070 cho DOC-01: chuyển định dạng là việc của `report.export`, dựng ở hai chỗ là hai bản dựng cùng một tài liệu.

**Nội dung đề xuất:** SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_c.py` dòng 48 (và `docs/ho-so/nguon/excel/caps.py` dòng 252, cột đầu ra). Đề nghị CDS-12.1 v1.4 sửa DOC-03 bước 1 thành "bộ đọc chữ ký họ C (Doxygen/tree-sitter khi có, cho ngôn ngữ khác — thiếu thì E4001) → chữ ký + chú thích; writer thêm ví dụ dùng theo hộ chiếu (fact id); md", và đổi ô "md/html" của PLN-27/caps thành "md" với ghi chú trỏ REPORT-02. Không sửa mã: hợp đồng `src`/`path`/E4001/tc giữ nguyên.

**Mã liên quan:** `src/eide/caps/doc.py::api_ref, _ham_public, _cau_lenh_ngoai, _bo_macro_bao, DUOI_C`

---

## DEV-073 — Mở

**Mục bị ảnh hưởng:** CDS-12.1 DOC-06 input_schema.format (enum `docx|md`) đối chiếu CDS-12.5 REPORT-02 bước 1 v1.3 ("chuyển sang định dạng yêu cầu — đây là chỗ DUY NHẤT dựng docx/pdf (DEV-070)")

**Hiện tại (điều mã buộc phải khác):** `format=docx` KHÔNG dựng docx: trả E2000 kèm `candidates=[report.export]`, cùng khuôn ủy quyền mà `doc.generate` dùng cho `bringup`/`test_report`. Chỉ `md` (mặc định) sinh ra tệp.

**Vì sao:** Hai tài liệu nói khác nhau sau khi bộ hồ sơ lên v1.3: DOC-06 vẫn cho bên gọi chọn docx, còn REPORT-02 đã được duyệt thành 'chỗ DUY NHẤT dựng docx/pdf'. Dựng docx ở cả hai chỗ là hai bản dựng cùng một báo cáo — chúng sẽ lệch, và không có cách nào biết bản nào đúng. Đây đúng hình dạng của DEV-070, chỉ khác năng lực; giải quyết theo cùng một cách để hai lần đọc cùng một câu hỏi cho cùng một câu trả lời. Ngoài ra `report.export` là mốc M4 nên hiện chưa có hiện thực nào để ủy quyền tới: E2000 nói rõ điều đó thay vì dựng một bản docx thứ hai.

**Nội dung đề xuất:** SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_c.py` (mục `doc.test_report`) và `docs/ho-so/nguon/excel/caps.py`. Đề nghị CDS-12.1 v1.4 bỏ `docx` khỏi enum `format` của DOC-06 (chỉ còn `md`), thêm một câu ở bước 1: 'chuyển sang docx/pdf qua report.export'. Không sửa mã.

**Mã liên quan:** `src/eide/caps/doc.py::test_report`

---

## DEV-074 — Mở

**Mục bị ảnh hưởng:** CDS-12.4 DIAGRAM-08 steps[0] ("… từ capture LA → chuyển mẫu") đối chiếu CDS-12.4 MEASURE-01 `measure.capture_signal` (mốc M5, trả `dataset_id`)

**Hiện tại (điều mã buộc phải khác):** Đường "capture LA → chuyển mẫu" CHƯA hiện thực. `source` trỏ một hàng `measurement` → E2000 kèm `candidates=[measure.capture_signal]` và nói rõ là chờ mốc M5. Đường fact `timing` (tsu/th/tCLK từ datasheet) đã đủ và có test.

**Vì sao:** `dataset_id` chỉ do `measure.capture_signal` sinh ra, mà nhóm `measure.*` là mốc M5 và cần máy phân tích logic thật — thuộc khối H "để cuối cùng" theo quyết định chủ sản phẩm 08/09. Không có định dạng bộ mẫu nào trong `docs/spec/data/` để đọc, nên viết bộ chuyển bây giờ là viết cho một định dạng tự nghĩ ra. Vẽ một dạng sóng "minh họa" thay cho mẫu đo thật thì tệ hơn: người đọc giản đồ thời gian tin từng mép tín hiệu trên đó. Đây là NỢ HIỆN THỰC, không phải tài liệu sai — cùng hình dạng với DEV-008 (chờ nhóm kg.*).

**Nội dung đề xuất:** KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Hoàn tất nhánh capture khi `measure.capture_signal` có hiện thực (mốc M5): khi ấy định dạng `dataset_id` đã có thật và bộ chuyển mẫu → WaveDrom viết theo nó. Rà lại mục này cùng lúc với DEV-055 (M5).

**Mã liên quan:** `src/eide/caps/diagram.py::timing, _la_dataset`

---

## DEV-075 — Mở

**Mục bị ảnh hưởng:** CDS-12.4 DIAGRAM-07 `errors: []` đối chiếu steps[0] ("… từ mô tả: writer") và CDS-12.4 DIAGRAM-05/DOC-02 (cùng gọi mô hình, đều khai `E5002`)

**Hiện tại (điều mã buộc phải khác):** `diagram.flow` ném E5002 khi mô hình trả về lưu đồ không dựng được (sai dòng mở đầu hoặc lệch ngoặc), trong khi `errors` của DIAGRAM-07 là danh sách RỖNG.

**Vì sao:** Hợp đồng có một nhánh gọi mô hình ("từ mô tả: writer") thì nhất thiết có khả năng mô hình trả sai — và hai năng lực cùng hình dạng trong chính tập này (DIAGRAM-05 `diagram.sequence`, DOC-02 `doc.section`) đều khai E5002 cho đúng tình huống ấy. Cách xử lý duy nhất còn lại nếu không ném là TRẢ VỀ một lược đồ không dựng được, tức đẩy lỗi xuống hạ nguồn để người dùng phát hiện — đúng thứ mà bước 1 ("lint") sinh ra để chặn. `errors: []` ở đây là chỗ bỏ sót của tài liệu, không phải một quyết định.

**Nội dung đề xuất:** SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_c.py` (mục `diagram.flow`). Đề nghị CDS-12.4 v1.4 thêm `E5002` vào `errors` của DIAGRAM-07, cho khớp DIAGRAM-05 và DOC-02. Không sửa mã.

**Mã liên quan:** `src/eide/caps/diagram.py::flow, _flow_tu_mo_ta`

---

## DEV-076 — Mở

**Mục bị ảnh hưởng:** CDS-12.2 EXTRACT-09 steps[0] ("Bảng pinout (Pin, Name, Type) hoặc **hình package (vision)** → pin_function; package") và ask_when "Từ hình"

**Hiện tại (điều mã buộc phải khác):** Nhánh "hình package (vision)" CHƯA hiện thực. Chỉ đường BẢNG có hiện thực (đọc bằng mã: bảng định nghĩa chân + bảng Alternate function mapping). Không thấy bảng nào → E2000 kèm candidates=[extract.ocr, extract.image_board] và nói rõ đường vision chưa có. Vì thế ask_when "Từ hình" hiện không có nhánh nào chạm tới.

**Vì sao:** Đường vision cần extract.ocr / extract.image_board / extract.image_schematic — cả ba đều thuộc mốc M2 và chưa hiện thực (khối D mục D3). Viết bộ đọc hình bây giờ là viết cho một đầu vào chưa có ai sinh ra, và một pinout đọc từ hình mà không kiểm chéo được với bảng thì tệ hơn không có: người đọc hộ chiếu chân tin từng số AF trên đó. Đây là NỢ HIỆN THỰC, không phải tài liệu sai — cùng hình dạng với DEV-074 (nhánh capture LA của DIAGRAM-08 chờ measure.capture_signal) và DEV-008 (project.open bước 3 chờ kg.*).

**Nội dung đề xuất:** KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Hoàn tất nhánh vision khi nhóm extract.image_*/ocr có hiện thực (cùng khối D): khi ấy fact pin_function từ hình phải mang method=vision_llm — mà G-FACT-02 loại riêng vision_llm khỏi tự duyệt, nên nó vào hàng đợi hỏi người, đúng chỗ. Rà lại mục này cùng lúc với D3.

**Mã liên quan:** `src/eide/caps/extract.py::pdf_pinout`

---

## DEV-077 — Mở

**Mục bị ảnh hưởng:** CDS-12.2 EXTRACT-10 steps[0] ("… liên kết CONFLICTS_WITH nếu mâu thuẫn datasheet") đối chiếu DDD-14 §2 Fact (không có trường quan hệ) và KAD-07 §6.3 + src/eide_core/kg.py::dung/mau_thuan

**Hiện tại (điều mã buộc phải khác):** Không có đường nào để một fact KHAI cạnh CONFLICTS_WITH. Entity Fact của DDD-14 chỉ có một trường quan hệ duy nhất là `supersedes`; cạnh CONFLICTS_WITH trong đồ thị được `kg.dung` sinh TỰ ĐỘNG từ `mau_thuan()` — cặp fact cùng (subject, predicate) khác giá trị. Mà chính bước 1 của EXTRACT-10 lại buộc mục errata dùng `predicate: other`, nên nó không bao giờ trùng predicate của fact datasheet (base_address, reset_value, timing…) và cạnh ấy không bao giờ sinh ra.

**Vì sao:** Hai vế của cùng một câu trong hợp đồng loại trừ nhau: "predicate other" và "liên kết CONFLICTS_WITH nếu mâu thuẫn datasheet". Hiện thực được một trong hai, không được cả hai, và không có mục DEVIATIONS nào trước đây về điểm này. Đã hiện thực vế thứ nhất vì nó là vế cụ thể hơn (nêu rõ predicate) và vì tc của hợp đồng ("Errata I2C → fact có rev") nằm ở vế ấy. Giảm nhẹ: fact errata mang subject nằm DƯỚI nút ngoại vi (chip:<part>/periph:I2C1/errata:<mục>) khi store đã biết ngoại vi ấy, nên `kg.dung` vẫn nối ABOUT tới cùng nhánh cây và `kg.neighborhood("…/periph:I2C1")` vẫn thấy errata — quan hệ duyệt được, chỉ không mang nhãn CONFLICTS_WITH.

**Nội dung đề xuất:** Cần chủ sản phẩm chọn một trong hai. (a) DDD-14 §2 thêm trường `conflicts_with` (danh sách fact id) cho Fact + migration + `kg.dung` đọc nó — làm được đúng điều tài liệu muốn, nhưng chạm store và mọi năng lực ghi fact. (b) CDS-12.2 v1.4 sửa bước 1 của EXTRACT-10 thành: "… fact predicate other với điều kiện (rev, silicon), overlay K2′ (layer B), subject dưới nút ngoại vi liên quan; cạnh CONFLICTS_WITH sinh tự động khi một mục errata phủ định một fact CÙNG (subject, predicate)" — bám đúng cơ chế đã có của KAD-07 §6.3, không chạm schema. Nghiêng về (b): CONFLICTS_WITH trong KAD-07 được định nghĩa là quan hệ SUY RA từ dữ liệu, không phải quan hệ khai tay; thêm một trường khai tay là mở đường cho hai nguồn sự thật về cùng một cạnh.

**Mã liên quan:** `src/eide/caps/extract.py::pdf_errata, _fact_errata; src/eide_core/kg.py::dung`

---

## DEV-078 — Mở

**Mục bị ảnh hưởng:** CDS-12.2 EXTRACT-17 steps[1] ("Chuẩn hóa MPN (bỏ hậu tố gói), gộp trùng, đếm qty")

**Hiện tại (điều mã buộc phải khác):** Hai điểm. (1) Gộp trùng theo MPN ĐẦY ĐỦ chứ không theo MPN đã bỏ hậu tố; phần gốc vẫn được tính và trả riêng ở khoá `mpn_base` cho `extract.bom_enrich` tra cứu. (2) "Bỏ hậu tố gói" chỉ áp dụng khi hậu tố TÁCH BẰNG DẤU (`-TR`, `-T`, `-ND`, `/T&R`…); hậu tố dính liền (`LM358DR2G`) được giữ nguyên.

**Vì sao:** (1) BOM là danh sách MUA. `STM32F411CEU6` (UFQFPN-48) và `STM32F411CET6` (LQFP-48) cùng một die nhưng là hai mã đặt hàng, hai gói vật lý; gộp chúng vào một dòng thì lô hàng về không hàn được lên mạch. Cùng lập luận với `A4988SETTR-T` (băng cuộn) và `A4988SETTR` (khay). Hợp đồng nói "chuẩn hóa rồi gộp", mà chuẩn hóa đúng nghĩa "bỏ hậu tố gói" chính là xoá đi thông tin phân biệt hai dòng đặt hàng. (2) Cắt hậu tố dính liền cần bảng mã của từng nhà sản xuất; không có bảng ấy thì mọi quy tắc đều cắt nhầm — `LM358DR2G` mất `G` (mã mạ chân), và bất kỳ MPN nào kết thúc bằng T/R/G đều bị cắt cụt. Một MPN cắt cụt tra ra linh kiện khác, hoặc không ra gì, mà `bom_enrich` lại dùng đúng nó để tra datasheet.

**Nội dung đề xuất:** SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_b.py` (mục `extract.bom`). Đề nghị CDS-12.2 v1.4 sửa bước 2 thành: "Gộp trùng theo MPN đầy đủ (hai dạng đóng gói là hai dòng đặt hàng); tính thêm `mpn_base` bằng cách bỏ hậu tố đóng gói/băng tách bằng dấu, dùng cho tra cứu ở bom_enrich; không có MPN thì gộp theo (value, footprint); qty = số ký hiệu duy nhất". Không sửa mã: hợp đồng input/output và tc giữ nguyên.

**Mã liên quan:** `src/eide/caps/extract.py::bom, _gop_bom, _mpn_goc`

---

## DEV-079 — Mở

**Mục bị ảnh hưởng:** CDS-12.2 EXTRACT-17 steps[0] ("… image: cartographer …")

**Hiện tại (điều mã buộc phải khác):** Đường `kind=image` (đọc BOM từ ảnh mạch/bảng chụp) CHƯA hiện thực: trả E2000 kèm candidates=[extract.image_board, extract.ocr] và nói rõ là chờ đường vision. Bốn đường còn lại (schematic, csv, xlsx, readme) đã có.

**Vì sao:** Vai trò `cartographer` đọc ảnh cần `extract.image_board`/`extract.image_schematic`/`extract.ocr` — cả ba đều thuộc mốc M2 và chưa hiện thực (khối D mục D3). Đây là NỢ HIỆN THỰC, cùng hình dạng với DEV-076 (nhánh hình của EXTRACT-09) và DEV-074. Trả BOM rỗng cho một tấm ảnh thì tệ hơn báo lỗi: nó trông y hệt "tấm ảnh này không có linh kiện nào".

**Nội dung đề xuất:** KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Hoàn tất nhánh ảnh cùng lúc với khối D3; rà lại mục này cùng DEV-076.

**Mã liên quan:** `src/eide/caps/extract.py::bom`

---

## DEV-080 — Mở

**Mục bị ảnh hưởng:** CDS-12.2 EXTRACT-11 `errors: []` đối chiếu steps[0] ("Trích công thức/thuật toán … → skill dự án (K5) có trích dẫn trang") và CDS-12.2 EXTRACT-07/DOC-02 (cùng gọi mô hình, đều khai E5002)

**Hiện tại (điều mã buộc phải khác):** `extract.pdf_formula` ném E5002 khi mô hình không trả mã C, trong khi `errors` của EXTRACT-11 là danh sách RỖNG.

**Vì sao:** Hợp đồng có một nhánh gọi mô hình thì nhất thiết có khả năng mô hình trả thứ không dùng được — và `tc` của chính EXTRACT-11 ("skill có mã C tham chiếu và trang") nói rõ mã C là phần bắt buộc của đầu ra. Không ném thì cách xử lý duy nhất còn lại là GHI RA một skill không có mã: `code.generate_module` không có gì để chép, người đọc vẫn phải mở datasheet, và tệp ấy vẫn được tính là một skill trong thư mục. Đây đúng hình dạng DEV-075 (`diagram.flow` khai `errors: []` nhưng phải ném E5002), chỉ khác năng lực.

**Nội dung đề xuất:** SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_b.py` (mục `extract.pdf_formula`). Đề nghị CDS-12.2 v1.4 thêm `E5002` vào `errors` của EXTRACT-11, cho khớp EXTRACT-07 và DOC-02. Không sửa mã. Gộp duyệt cùng DEV-075 vì cùng một lập luận.

**Mã liên quan:** `src/eide/caps/extract.py::pdf_formula`

---
