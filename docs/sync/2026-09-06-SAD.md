# Đồng bộ tài liệu SAD — bản nháp 2026-09-06

Nguồn sinh phải sửa: `docs/ho-so/nguon/sad.js`  ·  phiên bản đích: **v1.3**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Nó **không** sửa docx và **không** sửa `docs/spec/`. Sau khi chủ sản phẩm duyệt: sửa nguồn sinh → `scripts/sinh_tai_lieu.sh sad` → đổi trạng thái mục thành `Đã cập nhật tài liệu v1.3`.

## DEV-003 — Mở

**Mục bị ảnh hưởng:** PLN-27 WI-001; SAD-03 ADR (kế thừa hkw-core)

**Hiện tại (điều mã buộc phải khác):** Kho eide viết mới hoàn toàn, không đổi tên/kế thừa mã hkw-core M0; lõi tách package eide_core/ ngay từ đầu; CLI dùng argparse thay vì typer/rich

**Vì sao:** Quyết định của chủ sản phẩm 05/09/2026: làm mới hoàn toàn; giảm phụ thuộc để chạy trên 3 nền tảng

**Nội dung đề xuất cho v1.3:** Sửa PLN-27 WI-001 thành 'khởi tạo kho eide'; ghi ADR-16 trong SAD-03 v1.3; DEP-26 bỏ bước migrate .hkw→.eide

**Mã liên quan:** `toàn kho`

---
