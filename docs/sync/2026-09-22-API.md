# Đồng bộ tài liệu API — bản nháp 2026-09-22

Nguồn sinh phải sửa: `docs/ho-so/nguon/api.js`  ·  phiên bản đích: **v2.0**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Nó **không** sửa docx và **không** sửa `docs/spec/`. Sau khi chủ sản phẩm duyệt: sửa nguồn sinh → `scripts/sinh_tai_lieu.sh api` → đổi trạng thái mục thành `Đã cập nhật tài liệu v2.0`.

## DEV-092 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §3 bảng mã lỗi (`E1003 UNAUTHORIZED`, `E6002 CONFLICT`) đối chiếu `errors[]` của cả 238 hợp đồng trong CDS-12

**Hiện tại (điều mã buộc phải khác):** Hai mã lỗi có trong bảng API-15 §3 mà **KHÔNG hợp đồng năng lực nào khai** trong `errors[]`. Đo 13/09/2026 bằng cách quét cả 238 hợp đồng: `E4003` được 4 năng lực khai (đều cần board, nên chưa ném là đúng lịch); `E1003` và `E6002` thì không ai.

**Vì sao:** Không phải nợ hiện thực mà là mã lỗi KHÔNG CÓ CHỦ — không năng lực nào được phép ném chúng, vì `errors[]` của hợp đồng là danh sách đóng. Ép một năng lực ném E6002 sẽ làm nó trái chính hợp đồng của mình: `passport.query`, `kg.conflicts` và `req.ground_hw` đều khai `errors: []`, tức chúng KHÔNG ném gì — và đó là thiết kế đúng, vì một câu truy vấn tri thức không nên hỏng chỉ vì store có một xung đột chưa ai giải. `E1003` ("Token REST sai") thì chưa có bề mặt nào để ném: daemon là JSON-RPC qua stdio, không có token.

**Nội dung đề xuất:** Hai hướng, cần chủ sản phẩm chọn. (a) **Gán chủ**: thêm `E6002` vào `errors[]` của `kg.resolve_conflict` (nơi duy nhất một xung đột thật sự chặn việc), và giữ `E1003` là mã DÀNH SẴN cho bề mặt REST tương lai — ghi rõ điều đó vào API-15 §3 để nó thôi trông như một mã bị bỏ quên. (b) **Bỏ khỏi bảng** nếu thấy chúng không thuộc về đâu cả. Nghiêng về (a): một mã dành sẵn có ghi chú thì đọc được, còn một mã không ai khai thì mỗi lần rà lại phải điều tra lại từ đầu — lần này là lần thứ hai.

**Mã liên quan:** `docs/spec/api/errors.json`, `docs/spec/cds.json`

---

## DEV-093 — Đã duyệt

**Mục bị ảnh hưởng:** UXD-13 §2 bảng màn hình (màn 20 "Models (Mô hình & chi phí)", năng lực `policy`, `gateway`) đối chiếu API-15 §1 (57 phương thức JSON-RPC) và §5 (26 kiểu sự kiện sổ cái)

**Hiện tại (điều mã buộc phải khác):** Màn 20 hiện ĐƯỢC ÍT HƠN tài liệu mô tả: chỉ `cost_today` (từ `project.status`) và mức tự chủ (từ `autonomy.get`). Bảng vai trò → mô hình, chi phí theo từng lượt gọi, và độ trễ mỗi vai trò thì không hiện được.

**Vì sao:** **API-15 không có phương thức nào cho màn này.** Quét cả 57 phương thức: không cái nào trả cấu hình mô hình hay chi tiết chi phí. Dữ liệu ấy TỒN TẠI và đầy đủ — `model.call` trong sổ cái mang `role, model_id, request_hash, prompt_hash, tokens_in, tokens_out, cache_read_tokens, latency_ms, cost_usd, stop_reason` — nhưng nó không nằm trong 16 phương thức `event.*` của API-15 §1, nên `SU_KIEN` của daemon cũng không map nó, và panel không nhận được gì theo thời gian thực. Khoảng trống này chỉ lộ ra khi có người ngồi dựng đúng màn hình ấy: bảng §2 liệt kê màn, bảng §1 liệt kê phương thức, và không ai đối chiếu hai bảng với nhau. Đây cũng là lý do dựng cả 23 màn đáng làm dù 29 năng lực còn chờ board — viết giao diện là phép kiểm chéo rẻ nhất giữa hai tài liệu.

**Nội dung đề xuất:** Không lấp bằng cách vẽ một bảng mô hình từ dữ liệu không có: `ModelsView` hiện đúng thứ lấy được và NÓI RA phần thiếu kèm đường xem thật (`eide ledger --kind model.call`). Đề xuất API-15 v1.x thêm `models.list` (vai trò → mô hình → giá, đọc từ `models.yaml`) và `cost.today` (tổng hợp `model.call` theo vai trò), hoặc `event.model.call` nếu muốn panel theo dõi chi phí trực tiếp — nghiêng về hai phương thức đọc thay vì một sự kiện, vì một lượt gọi mô hình có thể xảy ra hàng chục lần một phút và panel không cần từng cái, nó cần TỔNG. Chờ chủ sản phẩm quyết. **ĐÓNG 15/09/2026.** API-15 nay có `event.model.call` (DEV-107) và `view.timeline` (VIEW-12, `ref: memory.ledger`) trả bản ghi sổ cái thô — đủ để màn gom `model.call` theo vai trò mà không cần thêm phương thức RPC nào. `ModelsView` đổi nguồn: đọc SỔ CÁI thay vì `models.yaml` + `project.status`. Khác biệt là khác biệt về nghĩa, không về dữ liệu: cấu hình nói tác tử ĐƯỢC PHÉP dùng mô hình nào, sổ cái nói nó ĐÃ dùng gì — và người trả tiền hỏi câu thứ hai. Không hiện nội dung prompt (`TRUONG_KHONG_DAY` lược từ daemon). Sắp theo chi phí giảm dần vì người mở màn này muốn biết tiền đi đâu.

**Mã liên quan:** `apps/geditor/Sources/EIDEKit/EideSystemViews.swift::ModelsView`, `src/eide/daemon/rpc.py::SU_KIEN`

---

## DEV-095 — Đã duyệt

**Mục bị ảnh hưởng:** CDS-12 `output_schema` của cả 238 hợp đồng; API-15 §1 `caps.describe`

**Hiện tại (điều mã buộc phải khác):** Bài test đối chiếu khung nhìn với hợp đồng phải ĐỌC CẢ `description` của schema, không chỉ `properties` — vì phần lớn cấu trúc kết quả không nằm trong `properties`.

**Vì sao:** **181 chỗ trong `output_schema` là object tự do** (`{"type": "object"}` không kèm `properties`), đo 13/09/2026 bằng cách quét đệ quy cả 238 hợp đồng. Trong đó 46 chỗ đặt cấu trúc thật vào `description` dạng danh sách tên (`project.status.report` → "features, gates_open, undo_items, cost_today, autonomy, target"; `debug.log_stats.stats` → "top_patterns[], time_span, gaps[], levels{}"), và **135 chỗ không nói gì cả**. Hệ quả đo được: `jsonschema` chấp nhận bất cứ object nào ở những chỗ ấy, nên phép kiểm `output_schema` của Router không phát hiện được một hiện thực trả sai tên trường — và đó đúng là hình dạng của hai lỗi đã xảy ra (`DocView` đọc `sections[]` không tồn tại, `SimView` đọc `report.expectations` trong khi tên thật là `metrics.expect`). Không phải lỗi của ai: CDS-12 sinh từ bảng Excel, nơi cột "đầu ra" là một ô văn bản.

**Nội dung đề xuất:** KHÔNG sửa 238 hợp đồng bằng tay. Hai bước, theo thứ tự rẻ-trước: (a) bộ sinh `cds.js` đọc `description` dạng danh sách tên và sinh `properties` tối thiểu (`{type: object}` cho tên trần, `{type: array}` cho `ten[]`) — 46 chỗ được máy-kiểm ngay, không ai phải gõ gì; (b) 135 chỗ còn lại thì điền dần khi có người chạm tới hợp đồng ấy, ưu tiên những cái có khung nhìn đọc (đã liệt kê trong `EideKhungNhinDocSchemaTests.bang`). Tạm thời bài test tự tách tên từ `description`, và nó cố ý KHÔNG im lặng về phần không kiểm được: schema không mô tả gì thì khung nhìn được cho qua, vì một dương tính giả còn tệ hơn không kiểm — nó dạy người ta bỏ qua test. Chờ chủ sản phẩm quyết (a).

**Mã liên quan:** `apps/geditor/Tests/EIDEKitTests/EideE2ETests.swift::EideKhungNhinDocSchemaTests`

---

## DEV-100 — Đã duyệt

**Mục bị ảnh hưởng:** APD-08 §1 (duyệt mục chờ là quyết định của người) và §4.1 (thang quyết định năm tầng); UXD-13 U2 (hàng đợi hai danh sách); API-15 §2 `gate.decide`

**Hiện tại (điều mã buộc phải khác):** Nhánh nhường cho `actor == "human"` nằm ở TẦNG 5 của `decide`, tức sau khi quy tắc theo cổng (tầng 3) và quy tắc chung (tầng 4) đã trả lời. Nhưng tầng 5 chỉ tới khi KHÔNG quy tắc nào khớp — mà một mục vào hàng đợi thì gần như luôn vì một quy tắc ASK ở tầng 3/4 vừa khớp. Hệ quả: `Router.quyet_dinh()` duyệt xong chạy lại với `actor="human"`, quy tắc cũ khớp lại, ASK lại. Hàng đợi U2 không đóng được mục nào.

**Vì sao:** Đo 14/09/2026 trên dự án thật: `search.fetch` một header ESP-IDF từ `raw.githubusercontent.com` rơi vào G-SRC-99; `eide queue approve` → "CHỜ NGƯỜI (G-SRC · G-SRC-99)" — cùng câu hỏi, run_id mới. Duyệt bao nhiêu lần cũng thế. Chính docstring của `Router.quyet_dinh` mô tả hành vi mà mã không có: "PolicyGate tầng 5 trả APPROVE cho người gọi trực tiếp, nên lời gọi không quay lại hàng đợi một lần nữa". Test `test_hang_doi.test_duyet_thi_chay_tiep_khong_hoi_lai` xanh suốt vì nó dùng `kg.resolve_conflict` — một T2 gắn cổng `*`, đúng ca DUY NHẤT rơi xuống tầng 5. Bài test chọn trúng ngoại lệ và chứng minh cho trường hợp không xảy ra trong đời thật.

**Nội dung đề xuất:** Đã sửa, và sửa hẹp: ở tầng 3/4, quy tắc ASK gặp `actor == "human"` thì thành APPROVE và MƯỢN id quy tắc, để `decision_log` còn truy được người đã trả lời câu hỏi nào. REJECT thì không nhường — duyệt là trả lời một câu HỎI, còn một quy tắc REJECT không hỏi gì cả. Cổng nào đã tự viết điều kiện về `actor` trong POL-17 thì nhánh chung tránh ra (`_cong_tu_xu_nguoi`, đọc từ `when` chứ không liệt kê tên cổng): G-WL-01 nói người ký được duyệt KHI NIÊM KHỚP, nên S48 ("người ký nhưng băm không khớp niêm" → ASK) vẫn đúng. CÒN MỞ, cần chủ sản phẩm quyết: vòng lặp vẫn còn ở ngưỡng cứng tầng 2 (HARD-R4 và "lớp vượt mức tự chủ") — hai chỗ ấy ép ASK trước khi cổng được hỏi, nên người duyệt một năng lực R4 vẫn bị hỏi lại mãi. Chỉ chạm hai năng lực R4 (`target.erase_fuse`, `registry.publish` công khai) và cả hai cần phần cứng hoặc mạng thật, nên chưa gặp — nhưng nới ranh giới R4 là quyết định của người, không phải của tác tử.

**Mã liên quan:** `src/eide_core/policy.py::decide, _cong_tu_xu_nguoi`

---

## DEV-107 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §1 (bảng 16 phương thức `event.*`); UXD-13 U2 (hàng đợi, thanh tự chủ luôn hiện); APD-08 §2 (mức A0–A4) §5 (leo thang)

**Hiện tại (điều mã buộc phải khác):** API-15 §1 thêm NĂM sự kiện: `event.gate.decided`, `event.model.call`, `event.tool.report`, `event.project.changed`, `event.chat.intent` (16 → 21). Giao diện thêm màn **Nhật ký hoạt động** (sidebar 21 → 22 màn), đường **mở dự án EIDE**, ô **đổi mức tự chủ** và **băng leo thang** trên `AutonomyBar`.

**Vì sao:** Chủ sản phẩm duyệt 14/09/2026 sau bản phân tích `docs/GIAM-SAT-UI.md`: *"toàn bộ các tính năng khi hoạt động cần được show lên giao diện để người dùng có thể giám sát và tham gia vào quá trình nếu muốn"*. Đo được trước khi làm: **13 trong 26 kiểu sự kiện sổ cái không có đường nào lên giao diện**, và 16 sự kiện API-15 hiện có chỉ kể những thứ ĐÒI người làm gì đó. Ba cái thiếu đau nhất: `model.call` (tác tử tiêu bao nhiêu token), `tool.report` (build/sim chạy ra sao), `policy.escalate` (tác tử đang dừng chờ người). Riêng `gate.decision` còn bị lọc bỏ khi quyết định KHÁC ASK — nên người không thấy tác tử tự duyệt những gì, mà ở mức tự chủ cao thì đó chính là thứ cần nhìn nhất. Về giao diện: không màn nào trong 23 màn UXD-13 trả lời được câu "tác tử vừa làm gì" — cả 23 đều là màn chuyên đề; và `AutonomyBar` hiện mức nhưng không đổi được, nên núm điều khiển chính của APD-08 chỉ dùng được bằng cách sửa `.eide/autonomy.yaml` rồi khởi động lại daemon.

**Nội dung đề xuất:** Đề nghị API-15 v1.x ghi năm sự kiện mới vào bảng §1 (đã thêm qua literal `EV` của `api.js`, không sửa tay openrpc.json) và nói rõ nguyên tắc phân đôi: 16 sự kiện gốc là **việc cần người**, năm cái mới là **việc tác tử tự làm xong**. UXD-13 §2 cần thêm màn "Nhật ký hoạt động" vào bảng 23 màn (nay 24 với cả `ReviewQueue`), và §4 mô tả `AutonomyBar` có ô chọn mức. Hai ràng buộc phải giữ nếu tài liệu đổi: (a) `event.model.call` KHÔNG mang `prompt`/`context`/`messages` và `context.bundle` không lên UI — sổ cái đã che khoá API, nhưng che khoá khác với không gửi mã nguồn người dùng ra một cửa sổ có thể đang chia sẻ màn hình; (b) `nhatKy.them` gọi TRƯỚC `switch` trong `nhanSuKien`, vì để nó trong một `case` thì mỗi sự kiện thêm về sau là một chỗ có thể quên — có test đọc mã nguồn canh đúng vị trí ấy.

**Mã liên quan:** `docs/ho-so/nguon/api.js (literal EV); src/eide/daemon/rpc.py::SU_KIEN; apps/geditor/Sources/EIDEKit/EideNhatKyView.swift, EideDuAn.swift, EideViews.swift::AutonomyBar`

---

## DEV-110 — Đã duyệt

**Mục bị ảnh hưởng:** MEM-11 §2 (M2 gồm "quyền theo phiên (R4)" và "target đang cắm"); API-15 §2

**Hiện tại (điều mã buộc phải khác):** Thêm phương thức `session.state` trả M2 của phiên đang mở. Payload có `permits[]` và `board` theo đúng MEM-11 §2, nhưng **hai trường ấy luôn RỖNG**: `SessionMemory` chỉ lưu `session_id, project, opened_at, autonomy_effective, stopped, turns, undo_items, closed_at, summary` — không có quyền R4 và không có board. Kèm khoá `thieu[]` liệt kê chính hai tên ấy, và màn Tổng quan hiện câu giải thích.

**Vì sao:** USECASE §4 mục 7, chủ sản phẩm duyệt 15/09/2026. Trước đó giao diện không hiện gì về M2 — người dùng không có cách nào biết phiên này đã cấp những quyền gì, mà quyền R4 theo phiên là thứ nguy hiểm nhất còn hiệu lực: nó cho tác tử làm một việc KHÔNG hoàn tác được mà không hỏi lại. Ba cách xử lý khoảng trống, và hai cách đầu tệ hơn: (a) bỏ hẳn hai khoá — giao diện không biết chúng tồn tại và thiếu sót biến mất khỏi tầm nhìn, đúng khuôn DEV-093 nơi một khoảng trống nằm im vì không ai hỏi tới; (b) bịa dữ liệu, ví dụ đọc `discover.ports` rồi gọi đó là "board đang cắm" — board DÒ ĐƯỢC khác board đã GẮN vào phiên, và trộn hai thứ là nói sai về quyền đang có hiệu lực; (c) trả rỗng kèm khoá `thieu`, giao diện hiện đúng thứ có thật và nói ra phần chưa có.

**Nội dung đề xuất:** Đề nghị MEM-11 v1.x giữ nguyên định nghĩa M2 (nó đúng) và ghi rõ hiện thực còn nợ hai trường. Hai trường ấy cần: (1) `permits[]` — `policy.permit` (POLICY-02, T2) cấp quyền R4 theo phiên, nhưng chưa ai ghi kết quả vào `SessionMemory`; thêm một lời gọi ở `Router.quyet_dinh` khi quyết định là cấp quyền. (2) `board` — `target.detect` gắn board vào phiên, nhưng nhóm `target.*` cần phần cứng nên chưa hiện thực; làm cùng lúc với `discover.chip_id` (mốc M2). Khi hai trường có dữ liệu, `session.state` và màn Tổng quan không phải đổi một dòng nào — chúng đã đọc sẵn.

**Mã liên quan:** `src/eide_core/memory.py::SessionMemory; src/eide/daemon/rpc.py::session_state; docs/ho-so/nguon/api.js`

---

## DEV-111 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §1 (JSON-RPC 2.0 qua stdio — không nói gì về nhiều lời gọi chồng nhau); `docs/spec/api/openrpc.json`

**Hiện tại (điều mã buộc phải khác):** `EideClient` nay TUẦN TỰ HOÁ mọi lời gọi bằng một mutex bất đồng bộ (`CheckedContinuation`): lời gọi thứ hai chờ lời gọi thứ nhất đọc xong phản hồi rồi mới ghi vào stdin. Một lời gọi một lúc, dù JSON-RPC 2.0 cho phép chồng.

**Vì sao:** Trước bản sửa, hai lời gọi cùng lúc làm TREO client vĩnh viễn (lỗi im lặng số 35). Gốc rễ là một hiểu sai về `actor` trong Swift: actor tuần tự hoá phần mã GIỮA các điểm treo, không phải cả hàm — tới `await` là nhả quyền, nên hai lời gọi cùng ghi stdin rồi cùng đọc stdout, mỗi bên nhận câu trả lời của bên kia và cả hai chờ một phản hồi không bao giờ tới. Trong giao diện: một màn đứng im mãi mãi, không lỗi, không xoay. Lối đi ĐÚNG theo JSON-RPC là khớp phản hồi theo `id` và cho phép chồng — nhưng nó cần một vòng đọc nền và một bảng `id → continuation`, tức một tầng đồng thời nữa trong đúng chỗ vừa sinh ra lỗi này. Tuần tự hoá đổi lấy: mất song song (một lời gọi chậm chặn các lời gọi sau) để lấy một bất biến kiểm được bằng mắt. Với tải thật của giao diện — người bấm, một màn nạp một năng lực — hàng chờ hầu như luôn rỗng.

**Nội dung đề xuất:** Đề nghị API-15 v1.x NÓI RA chuyện này, vì hôm nay nó im lặng và sự im lặng ấy là chỗ lỗi sinh ra: ghi rõ daemon có chấp nhận nhiều lời gọi chồng nhau trên một ống stdio hay không, và nếu có thì phản hồi được phép trả ra KHÔNG theo thứ tự hay không. Hai câu ấy quyết định client phải viết kiểu gì. Nếu sau này cần song song thật (ví dụ tải nhiều màn cùng lúc khi mở dự án lớn), đổi mutex thành bảng `id → continuation` cộng một vòng đọc nền — và khi ấy phải có test bắn ≥3 lời gọi chồng nhau kèm hạn giờ, như test đang có cho 2 lời gọi.

**Mã liên quan:** `apps/geditor/Sources/EIDEKit/EideClient.swift::vaoHang, roiHang, goi`

---

## DEV-116 — Đã duyệt

**Mục bị ảnh hưởng:** UXD-13 §2 màn 13 (mockup `docs/ui/Sim.dc.html` — log console, bảng SIL↔HIL, bảng quét); CDS-12.4 SIM-05/SIM-08; API-15 §2 ("tool nặng trả job_id"); SEC-25 §2 (sandbox cắt mọi kênh không khai báo); APD-08 §5 (leo thang khi còn <20% ngân sách)

**Hiện tại (điều mã buộc phải khác):** Màn Mô phỏng: log UART ra khối mã đầy đủ, quét tham số ra bảng có hàng tốt nhất được tô, so sánh SIL↔HIL ra bảng bốn cột, tên kỳ vọng dựng từ `{kind, pattern}`. Panel THEO DÕI việc nặng qua `job.status` thay vì coi `running` là lỗi. Sandbox cắt stdin của tiến trình con. Client bỏ qua SIGPIPE. Hội thoại bị ẩn nay nhường chiều cao cho màn.

**Vì sao:** Chụp màn Mô phỏng trên luồng AVR thật (qemu-system-avr, 6/6 kỳ vọng) lộ ra NĂM lỗi xếp chồng, mỗi lỗi chỉ thấy sau khi sửa lỗi trước. (a) `sim.run` là việc nặng nên trả `job_id` + `status: running`, và panel coi mọi trạng thái khác `done` là lỗi — màn hiện *"chưa chạy được (trạng thái running)"*, một câu SAI phát ra đúng lúc việc đang chạy; người dùng bấm lại và khởi động một qemu thứ hai. (b) Sửa xong thì việc không bao giờ kết thúc qua daemon dù CLI chạy 25 giây: **sandbox không chuyển hướng stdin**, nên `qemu-system-avr` — vốn đọc stdin như console nối tiếp — thừa kế stdin của daemon, tức chính ỐNG JSON-RPC, và ăn mất lệnh của giao diện. Đây là lỗ SEC-25: stdin thừa kế là một kênh vào không khai báo, và bất kỳ công cụ nào đọc stdin đều rút được lưu lượng RPC của tiến trình cha. (c) Khi daemon chết vì chuyện trên, `FileHandle.write` bắn SIGPIPE và **giết cả EIDE** — mã thoát 141, không hộp thoại, không log, cửa sổ biến mất giữa lúc chạy mô phỏng. (d) `job.status` trả `result` là CẢ CapabilityRun (`asdict(run)`), client bọc thêm một lớp nữa, nên màn nhận `{status, result}` thay cho `{report}` và kết luận *"không trả về kỳ vọng nào"* sau một lượt chạy ĐẠT 6/6. (e) Hội thoại `isHidden = true` vẫn giữ nguyên chiều cao trong Auto Layout, mà mép dưới mọi màn buộc vào mép trên hội thoại — nên khối log bị cắt ngang dòng đầu trong khi nửa dưới cửa sổ trống trơn. Lỗi (e) ảnh hưởng MỌI màn, không riêng Mô phỏng. Ngoài ra `make check` đỏ 4 bài lúc 06:55 giờ Việt Nam: sổ cái đóng dấu UTC còn `budget_state` so với `date.today()` địa phương, nên **mọi chi phí từ nửa đêm tới 7 giờ sáng không được tính vào ngân sách hôm nay** — và APD-08 §5 leo thang theo chính con số ấy.

**Nội dung đề xuất:** Đề nghị API-15 v1.x ghi rõ vòng đời một việc nặng ở phía KHÁCH: `caps.invoke` trả `{status: running, job_id}`, bên gọi hỏi `job.status` tới khi `state` khác `running`, và `job.status.result` là CẢ CapabilityRun chứ không phải phần thân — chỗ này không nói rõ thì mỗi bên gọi tự đoán một kiểu. SEC-25 §2 nên liệt kê stdin vào danh sách kênh sandbox phải cắt, cùng mạng và hệ tệp. UXD-13 nên ghi rằng bốn trạng thái `pending`/`running`/`cancelled`/`rejected` là BỐN câu khác nhau cho người dùng, không phải một câu "chưa chạy được". Và ngưỡng ngân sách nên nói rõ "ngày" là ngày nào — hiện thực nay tính theo múi giờ máy người dùng, vì đó là thứ họ hiểu khi đọc "hạn mức ngày".

**Mã liên quan:** `src/eide_core/sandbox.py` (stdin=DEVNULL); `src/eide/daemon/rpc.py::budget_state, _ngay_dia_phuong`; `apps/geditor/Sources/EIDEKit/EideClient.swift` (SIGPIPE); `EidePanel.swift::theoDoiViec, docLoi, anHoiThoai`; `EideCodeViews.swift::SimView`

---

## DEV-120 — Đã duyệt

**Mục bị ảnh hưởng:** UXD-13 §1 (khung cửa sổ) + §2 (23 màn); DEV-098 (GEditor và EIDE là MỘT cửa sổ); API-15 §1 `event.job.progress` `{job_id, pct, log_tail[]}`; UXD-13 U1 (ô trò chuyện)

**Hiện tại (điều mã buộc phải khác):** **Vùng trao đổi người ↔ tác tử thành bất biến.** Trình soạn thảo nay là MỘT MÀN NẰM TRONG panel (`datManNgoai`, cùng bộ ràng buộc `_rangBuocVungMan` với 23 màn kia) thay vì một khung nhìn thay thế panel; hội thoại có SÀN 220 pt và TRẦN 320 pt nên không co mất cũng không nuốt vùng làm việc. Tác tử chạy một năng lực thì màn của nhóm ấy **tự mở và được focus** (`_theoTacTu` + `onTacTuMoMan` đẩy cả lựa chọn ở sidebar), `code.*` mở thẳng trình soạn thảo. `event.job.progress` nay mang thêm `cap`. Thẻ tiến độ nhận thông điệp đã tạo ra nó.

**Vì sao:** Chủ sản phẩm nêu ba điều ngày 16–17/09/2026: *"giao diện để người và máy cùng trao đổi là phải có và bất biến"*; *"tự hiện thì không đúng mà tự mở và focus vào phần đó"*; *"phân chia lại màn hình để đảm bảo giao diện người agent luôn hiện"*. Trước đó ba điều ấy không đồng thời đúng được: chọn màn `Code` ở sidebar **THAY** cả panel bằng trình soạn thảo, nên đúng lúc tác tử sửa mã — lúc người cần giám sát nhất — ô trò chuyện biến mất khỏi màn hình. Bước 12 của vòng giao diện kiểm cả ba cùng lúc và lần chạy đầu báo `HỎNG: tác tử chạy code.* mà màn là Env`. Sửa xong lộ tiếp bốn lỗi im lặng, cả bốn chỉ thấy trên ảnh chụp cửa sổ thật. (a) Hội thoại không có TRẦN nên cao theo nội dung: với năm thẻ đang chạy nó chiếm gần nửa cửa sổ và trình soạn thảo còn một dải BỐN DÒNG — cây dự án bị bóp tới mức chỉ còn hai ô tìm kiếm. (b) `_bao_job` biết `cap` của việc nền nhưng KHÔNG phát nó, nên mọi việc nền đều vô danh: thẻ ghi `Đang chạy job_038c110c8b65…` và màn chuyên biệt không tự mở được cho đúng những việc CHẠY LÂU (build, sim, flash) — tức là đúng những việc người dùng cần được dẫn tới màn của chúng nhất. (c) `hienTienDo` tạo thẻ rồi **vứt thông điệp đã tạo ra nó** (`init` chỉ gọi `capNhat([:])`), nên một lượt chạy mà panel chỉ nhận đúng một thông điệp sẽ để lại một thẻ VĨNH VIỄN ghi "đang chạy" mang mã băm: đo được năm thẻ như thế cùng lúc, và trạng thái `done`/`failed` nằm trong chính thông điệp bị bỏ. Một lượt chạy đã xong mà giao diện nói đang chạy là lời nói sai về việc máy vừa làm — đúng thứ màn giám sát tồn tại để không xảy ra. (d) Thẻ giữ dải chip cao 28 pt kể cả khi không có nút nào, ba thẻ rỗng ăn 270 pt vùng trao đổi.

**Nội dung đề xuất:** Đề nghị UXD-13 v1.x ghi vùng trao đổi vào §1 như một phần BẤT BIẾN của khung cửa sổ, kèm sàn và trần, và ghi rõ trình soạn thảo là màn thứ 23 nằm trong panel chứ không phải một bề mặt song song — đây là hệ quả cuối cùng còn thiếu của DEV-098. §2 nên ghi luật tự-mở-và-focus: màn mở theo NHÓM NĂNG LỰC tác tử đang chạy, và bị hoãn 20 giây sau khi người tự chọn màn khác (`GIU_MAN_NGUOI_CHON`) — một giao diện đổi màn ngay dưới tay người đang đọc là giao diện không dùng được. API-15 §1 nên thêm `cap` vào payload `event.job.progress`: thiếu nó thì giao diện không có tên để gọi việc nền, và không có cách nào biết việc thuộc nhóm năng lực nào.

**Mã liên quan:** `apps/geditor/Sources/EIDEKit/EidePanel.swift::datManNgoai, hienManNgoai, _rangBuocVungMan, _theoTacTu, TRAN_HOI_THOAI, hienTienDo`; `GEditorApp/MainWindowController.swift::_gnPanel, chonManEide`; `EIDEKit/EideRunCards.swift::RunProgressCard`; `src/eide/daemon/rpc.py::_bao_job`

---

## DEV-123 — Đã duyệt

**Mục bị ảnh hưởng:** UXD-13 bản sửa đổi v2.0 §1.1 R3 (tiến độ rải bốn chỗ), §5 (thẻ Run), §9.1 (sự kiện sổ cái mới), §9.2 P-RUN-01; API-15 §7 (bảng sự kiện sổ cái) và §5 (`event.run.progress`); CDS-12.6 CHAT-06

**Hiện tại (điều mã buộc phải khác):** **Thẻ Run.** Sáu kiểu sự kiện sổ cái mới cho vòng đời MỘT LƯỢT CHẠY: `run.started` (mang số Run, câu lệnh gốc, và CẢ KẾ HOẠCH), `run.step_started`/`run.step_done`, `run.blocked`, `run.done`/`run.cancelled`. Mỗi nút của chuỗi nay mang mã của CHUỖI trong `event.run.progress` (Router ghi kèm `chain`, daemon đổi `run_id`), nên 16 bước gộp về MỘT thẻ thay vì 16 thẻ rời. Thẻ hiện `Run #7 · <câu người gõ> · 3/8 bước`, thanh tiến độ, chip mỗi bước, nút Dừng và Mở chi tiết (mở Nhật ký lọc theo mã Run). `chat.orchestrate` nhận thêm `text` để thẻ gọi tên việc bằng chính câu người viết.

**Vì sao:** Bản rà soát v2.0 §1.1 R3: *"tiến độ của tác tử hiện rải ở bốn chỗ — không chỗ nào là nguồn chính"*. Đúng, và nguyên nhân nằm ở tầng sự kiện chứ không ở giao diện: sổ cái chỉ có `cap.run.*` — nói về MỘT LỜI GỌI năng lực — mà không có kiểu nào nói về một LƯỢT CHẠY, nên giao diện phải tự suy tiến độ chuỗi từ các mảnh rời và không bao giờ trả lời được câu "còn bao nhiêu bước". Ba lỗi im lặng lộ ra khi làm. (a) **Mọi sự kiện lên giao diện HAI LẦN.** Daemon đăng ký cùng một hàm nhận cho cả `theo_doi()` (in-process) lẫn `theo_doi_tep()` (đọc tệp); `TheoDoiTep` có lọc trùng theo `seq` nhưng nó chỉ biết một `seq` SAU KHI tự đọc dòng ấy — tức sau khi đã phát lần hai. Đo được 29 thông báo cho 15 bản ghi. Sống từ 14/09 tới nay vì thẻ khoá theo `run_id` và mọi phép cập nhật đều luỹ đẳng; chỉ lộ ra khi thẻ bắt đầu ĐẾM bước. (b) **Bản vá đầu của (a) dùng `is` để so hàm nhận** — mà mỗi lần truy cập một phương thức ràng buộc trong Python lại sinh một đối tượng mới, nên phép so luôn sai và phép khử trùng im lặng không chạy; nó qua sạch 1 612 bài kiểm trong khi daemon thật vẫn phát đôi. (c) **Dải chip của thẻ không hiện gì**: `hang` là `documentView` không có ràng buộc nên đứng ở kích thước 0 — tám chip được dựng, nằm đủ trong cây khung nhìn, và không cái nào nhìn thấy được.

**Nội dung đề xuất:** Đề nghị API-15 §7 giữ nguyên phần vừa thêm và ghi rõ ranh giới hai họ sự kiện: `cap.run.*` là MỘT LỜI GỌI, `run.*` là MỘT LƯỢT CHẠY, và `run_id` trong `event.run.progress` luôn là mã lượt chạy — lời gọi lẻ mang mã của chính nó, nút trong chuỗi mang mã của chuỗi. Không nói rõ điều này thì mỗi bên nhận tự đoán, và cách đoán tự nhiên nhất (một thẻ cho mỗi `run_id` thấy được) cho ra đúng lỗi 16-thẻ. DPS-09 §4.4 nên ghi P-RUN-01 vào phần hợp đồng của Orchestrator chứ không để ở tài liệu giao diện: "phát `run.started` trước hành động đầu tiên" là ràng buộc của bên SINH sự kiện. Ghi thêm một bài học về hạ tầng: hai đường phát cùng tới một người nhận thì phép khử trùng phải nằm ở chỗ GHI, không nằm ở chỗ đọc — chỗ đọc luôn biết muộn.

**Mã liên quan:** `docs/ho-so/nguon/api.js` (bảng LED + EV); `docs/ho-so/nguon/cds_data_b.py` (`chat.orchestrate` nhận `text`); `src/eide_core/ledger.py::append, theo_doi_tep, TheoDoiTep.danh_dau`; `src/eide_core/router.py::invoke` (mang `chain`); `src/eide/caps/chat.py::orchestrate, _so_run`; `src/eide/daemon/rpc.py::SU_KIEN, _su_kien, chat_send`; `apps/geditor/Sources/EIDEKit/EideRunCards.swift`; `EidePanel.swift::hienTienDo`; `EideNhatKyView.swift::tenSuKien`

---

## DEV-125 — Đã duyệt

**Mục bị ảnh hưởng:** UXD-13 v2.0 §1.1 R5, §6–§8, Q1, Q2; UXC-31 §0 B4, §5, §6, §7.4; POL-17 §1–2 (rules.yaml) và §8 (situations); API-15 §6 (errors) và §7 (ledger); CDS-12.1/12.3; CON-28 §4 (trailer commit)

**Hiện tại (điều mã buộc phải khác):** **Người và tác tử cùng sửa tệp — phần lõi.** Ba năng lực mới (238 → **241**): `code.human_save` (nút Lưu của trình soạn thảo đi qua Router, commit tác giả `human:<tên>` kèm trailer `Eide-Ledger-Seq`, sự kiện `human.file_save`, mục hoàn tác), `code.merge_conflict_resolve` (người quyết TỪNG vùng giao nhau), `project.watch` (tệp đổi ngoài EIDE → `human.file_external`). Merge ba bên dùng `git merge-file --diff3`. Năm quy tắc chính sách P-EDIT-01…04 + P-RUN-01 và năm tình huống S49–S53. Hai mã lỗi mới **E6004 FILE_STALE** và **E6005 FILE_READONLY** thay cho tên phi số `E-SAVE-STALE`/`E-SAVE-RO` mà hai tài liệu v2.0 đề xuất (chủ sản phẩm chốt 17/09/2026).

**Vì sao:** R5 của bản rà soát là phát hiện giá trị nhất: người gõ mã là hành vi THƯỜNG XUYÊN NHẤT trong sản phẩm mà sổ cái không thấy — không lời gọi năng lực, không mục hoàn tác, không mô hình xung đột — nên tác tử ghi đè bản sửa của người trong im lặng, và nguyên tắc "giao diện chỉ hiện việc sổ cái có" thủng đúng ở chỗ đông người qua lại nhất. Làm phần này lộ ra **hai lỗ an toàn có sẵn**, cả hai đều nằm ở chỗ chính sách đọc dữ liệu vào. (a) **`actor` trong biểu thức quy tắc đọc từ ĐẶC TRƯNG, không từ tham số Router truyền xuống.** `_env()` chỉ chép `features` vào môi trường biểu thức, mà `features` một phần suy từ THAM SỐ của lời gọi — nghĩa là mọi quy tắc viết `actor == "human"` đứng trên một giá trị bên gọi đặt được. Tệ hơn: khi không ai nhét `actor` vào đặc trưng thì `_Env.__missing__` trả một đối tượng rỗng, nên `actor == "human"` luôn False còn `actor != "human"` luôn True — **`G-WL-01` chưa bao giờ khớp được và `G-WL-02` khớp cho tất cả**, tức cổng danh sách trắng từ chối cả NGƯỜI. Không bài kiểm nào thấy vì mọi tình huống trong `situations.py` đều tự nhét `actor` vào đặc trưng. (b) **Khối `cap` bị đặc trưng ghi đè được.** `{"cap": {...}, **features}` — `**features` đứng SAU nên một `features["cap"]` thay được cả `id` lẫn `risk` mà cổng dùng để quyết. Chưa ai khai thác, nhưng `cap.is_human_surface` thêm ở v2.0 biến chỗ này thành đường tự phong "tôi là người". Nay khối `cap` dựng từ REGISTRY và ghi sau cùng. Ngoài ra: yêu cầu "`code.human_save` APPROVE ở mọi mức, kể cả A0" đụng ngưỡng cứng tầng 2 — đo ở tình huống S50 ra `ASK HARD-A0`. Miễn trừ RẤT HẸP ở tầng 2 và KHÔNG ở tầng 1: `A0` là một mức người đặt, còn `stopped` là dừng khẩn đang có hiệu lực.

**Nội dung đề xuất:** **Đề nghị sửa ba con số trong hai tài liệu v2.0.** (1) POL-17 v2.0 có **54** quy tắc, không phải 53: `P-EDIT-02` (APPROVE khi người) BẮT BUỘC đi kèm `P-EDIT-04` (REJECT khi không phải người). Viết mỗi vế APPROVE thì tác tử gọi `code.human_save` là ghi được mọi tệp không qua cổng nào — đúng hình dạng lỗ mà `G-WL-02` dựng lên để chặn, và lý do "cổng kiểm máy, không kiểm người" chỉ đúng khi hệ thống PHÂN BIỆT được hai thứ ấy. (2) Tổng năng lực 238 → 241; con số 238 còn nằm ở 94 chỗ trong bộ hồ sơ, nên đây là việc của đợt đồng bộ tài liệu chứ không sửa rải được. (3) UXD-13 v2.0 §7.1 nói kiểm tệp cũ bằng `seq` bản mở; hiện thực so NỘI DUNG, vì một `git checkout` ghi lại đúng nội dung cũ vẫn đổi thời gian, và hệ tệp phân giải một giây thì hai lần ghi liền nhau không phân biệt được. Ghi thêm một ràng buộc nên vào POL-17 §1: **nguồn của `actor` là bên GỌI, không phải đặc trưng** — và mọi đặc trưng do tham số suy ra không được phép ghi đè khối `cap`.

**Mã liên quan:** `docs/ho-so/nguon/api.js` (ERR + LED), `pol.js` (RULES + SIT), `cds_data_b.py`, `docs/spec/caps.json`; `src/eide_core/policy.py::NANG_LUC_BE_MAT_NGUOI, decide, _env`; `src/eide_core/router.py::invoke`; `src/eide_core/git.py::tac_gia, commit, LENH_CHO_PHEP`; `src/eide/caps/code.py::human_save, merge_conflict_resolve, hop_nhat_ba_ben, tach_vung`; `src/eide/caps/project.py::watch`; `src/eide/daemon/rpc.py::_ctx_cho`; `tests/situations.py` (S49–S53)

---

## DEV-126 — Đã duyệt

**Mục bị ảnh hưởng:** UXC-31 §0 B6 và §7.1–7.2; tiêu chí nghiệm thu N6; API-15 §2 (thông báo `event.*` đi chung ống với câu trả lời); DEV-102 (bộ theo dõi tệp sổ cái để thấy việc của tiến trình khác)

**Hiện tại (điều mã buộc phải khác):** Thêm **nhịp tim 1 giây** tới daemon và dải **"Dữ liệu cũ — bấm để tải lại"** neo trên cùng vùng làm việc, hiện khi quá 2 giây không nghe được daemon. Vòng giao diện thêm bước 16: GIẾT daemon thật rồi khẳng định dải hiện ra — bài kiểm PHỦ ĐỊNH duy nhất của vòng.

**Vì sao:** **Kênh sự kiện chỉ được bơm khi có một lời gọi đang chạy** (lỗi im lặng 64). API-15 cho `event.*` đi chung ống với câu trả lời, và `EideClient.goi` là chỗ DUY NHẤT đọc ống ấy — một quyết định đúng và có ghi lý do (hai bên cùng đọc một ống là cách mất thông điệp), nhưng hệ quả thì không ai viết ra: **khi người dùng ngồi yên, giao diện điếc hoàn toàn.** Rà lại `EidePanel` thì mọi `lamMoi()` đều do người bấm; không có nhịp nào. Nghĩa là tác tử chạy qua CLI ở tiến trình khác, sổ cái đầy sự kiện, bộ theo dõi tệp dựng ở DEV-102 phát đủ — và cửa sổ đứng im cho tới khi người bấm một cái gì đó. Cả cơ chế giám sát ấy dừng lại đúng ở tầng vận chuyển, và không bài kiểm nào thấy vì mọi bài đều CHỦ ĐỘNG gọi. Chọn nhịp tim thay vì viết lại tầng vận chuyển thành luồng đọc riêng: rẻ hơn nhiều, không đụng mô hình đồng thời của một actor đang chạy đúng, và cho luôn phép phát hiện mất daemon mà B6 cần. Trong lúc một lời gọi dài chạy (mô phỏng 25 giây) thì nhịp tim xếp hàng sau nó, nhưng đúng lúc ấy ống ĐANG được đọc nên sự kiện vẫn tới ngay — hai cơ chế bù nhau kín. Kèm một lỗi thứ tự dựng cây (65): màn neo `top` vào `daiCu.bottom` trong khi dải chưa vào cây khung nhìn → AppKit ném `NSException` "no common ancestor" và ứng dụng sập ngay lúc mở. Cùng họ bẫy với `contentView` phải đặt trước `documentView` của DEV-116.

**Nội dung đề xuất:** Đề nghị API-15 §2 ghi rõ rằng kênh `event.*` là kênh ĐƯỢC BƠM, không phải kênh đẩy: bên nhận phải tự giữ một nhịp, và nói ra chu kỳ tối đa nó chấp nhận. Một dòng "daemon đẩy sự kiện lên giao diện" đọc như một lời hứa về độ trễ mà tầng vận chuyển không giữ được, và khoảng cách giữa lời hứa ấy với sự thật đã sống bốn ngày. UXC-31 §7.2 đòi phát hiện "nhảy quãng `seq`" — KHÔNG hiện thực phần ấy và đây là lý do: `KHONG_LEN_UI` lọc bớt bản ghi trước khi phát, nên quãng trống trong `seq` là chuyện BÌNH THƯỜNG và một bộ dò quãng sẽ báo động liên tục. Tín hiệu dùng được là mất kết nối, và `seq` LÙI (sổ cái bị xoay hoặc thay) — đề nghị §7.2 sửa theo hướng ấy.

**Mã liên quan:** `apps/geditor/Sources/EIDEKit/EideDaiCu.swift` (mới); `EidePanel.swift::batNhipTim, capNhatDaiCu, NHIP_TIM, HAN_DU_LIEU_CU`; `EideClient.swift::Transport.gietDeTest`; `GEditorApp/VongGiaoDien.swift` (bước 16); `Tests/EIDEKitTests/EideBoCucMoiTests.swift`

---

## DEV-153 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §3 (bảng 30 mã lỗi — không có mã cho lỗi nội bộ); DDD-14 §5 (lịch migration); POL-17 §5

**Hiện tại (điều mã buộc phải khác):** Bốn bản vá tìm ra khi chạy vòng cộng tác THẬT trên giao diện. ① `Daemon.handle` bắt mọi ngoại lệ, không chỉ `EideError/KeyError/TypeError` — trả lỗi JSON-RPC `-32603` kèm loại ngoại lệ và in vết ra stderr, KHÔNG gắn `eide_code`. ② `Router.invoke` đặt `ctx.extra["cap_run_id"]` để năng lực biết mã lượt chạy của chính nó. ③ `Router.hoan_tac` niêm lại store sau khi hoàn tác. ④ Migration 0009 thêm `clarification_answer.run_id` (store v8 → v9).

**Vì sao:** ① Chỉ ba loại ngoại lệ được bắt, nên một câu SQL trỏ vào cột không tồn tại làm vòng phục vụ thoát và DAEMON CHẾT — người dùng mất TOÀN BỘ giao diện vì một lời gọi hỏng, màn hình chỉ hiện "Dữ liệu cũ — không nghe được daemon 6 giây qua", và không chỗ nào nói lời gọi nào đã giết nó. Không gắn `eide_code` vì bảng 30 mã không có mã nào cho "lỗi nội bộ không lường trước", và mượn một mã sẵn có (E8002 SENSITIVE_UPLOAD chẳng hạn) là nói dối phía giao diện về loại sự cố — nó tra bảng để hiện CÁCH XỬ LÝ. ② Router đăng ký mọi mục hoàn tác bằng `run_id`, nên một năng lực muốn hoàn tác ở mức chi tiết hơn "cả lời gọi" phải ghi lại mã ấy cùng dữ liệu nó vừa ghi; không có thì bộ hoàn tác cầm một mã mà không biết nó trỏ vào dòng nào ("Không đọc được `undo_ref` `fdfafbb4e45f`"). ③ `hoan_tac` gọi thẳng hàm xử lý chứ không qua `invoke`, nên `_niem_lai` không chạy. Với ba loại cũ điều đó vô hại — chúng đều gọi lại một NĂNG LỰC nên lời gọi bên trong tự niêm; `restore_answer` ghi thẳng vào store và là loại đầu tiên làm thế. Hệ quả: hoàn tác xong thì lần mở dự án kế tiếp hỏng với E6000 — phép kiểm toàn vẹn tố cáo chính EIDE. ④ Cột này đáng lẽ nằm trong 0008. Sửa thẳng 0008 là SAI và tôi đã làm sai một lần: **một migration đã áp là bất biến** — store nào đã ở v8 sẽ không bao giờ nhận cột mới vì `user_version` đã là 8.

**Nội dung đề xuất:** Thêm một mã lỗi cho "lỗi nội bộ không lường trước" vào API-15 §3 ở v1.3 (đề nghị `E1005 INTERNAL`), và ghi vào DDD-14 §5 quy tắc migration đã áp là bất biến.

**Mã liên quan:** `src/eide/daemon/rpc.py::Daemon.handle`; `src/eide_core/router.py::invoke, hoan_tac`; `src/eide_core/migrations/0009_m3_clar_answer_run.sql`; `tests/test_daemon.py`, `tests/test_undo.py`

---

## DEV-154 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §1 (`chat.send`, `event.chat.restated`, `event.chat.report`); CDS-12.1 CHAT-06 ("bất đồng bộ theo thiết kế: tiến độ đi qua sự kiện"); UXC-31 §2D, §2D.4, §2D.6

**Hiện tại (điều mã buộc phải khác):** **Bốn thay đổi kiến trúc ở tầng tích hợp Agent↔UI.** ① Một VÒNG ĐỌC NỀN duy nhất trong `EideDaemon`. ② Bỏ mutex một-lời-gọi-một-lúc, khớp câu trả lời theo `id` của JSON-RPC. ③ Hạn thời gian theo phương thức, và ống đứt làm hỏng MỌI lời gọi đang bay. ④ `chat.send` trả về NGAY với `state: "running"`; chuỗi chạy ở luồng nền, kết quả tới bằng `event.chat.restated` + `event.chat.report` (hai sự kiện openrpc đã khai sẵn, mở rộng mô tả). `plan_only` giữ đồng bộ. Kèm một khoá cho cả ống ra, và hai bộ kiểm giao diện mới (`--do-nut`, `--bam-thu`).

**Vì sao:** Rà theo yêu cầu của chủ sản phẩm ngày 22/09. ① `onSuKien` được gọi ở ĐÚNG MỘT CHỖ — trong vòng đọc của `goi()` — nên sự kiện chỉ tới được khi có một lời gọi đang bay; thứ cứu tình huống ấy là nhịp tim, tức tiến độ của tác tử tới màn hình nhờ một hiệu ứng phụ của phép kiểm sống-chết. ② `chat.send` chạy đồng bộ cả chuỗi (2–4 phút) và mutex chặn mọi thứ khác: `autonomy.get`, `queue.list`, `undo.list`, nhịp tim — sau 6 giây màn hình tự treo biển "Dữ liệu cũ" trong khi daemon hoàn toàn khỏe, mọi màn đứng ở "Đang đọc…". ④ là gốc của ②, và nó TRÁI với chính hợp đồng CHAT-06. **Hai bộ kiểm mới đến từ một câu của chủ sản phẩm**: *"gần như các nút không dùng được một nút nào cả"*. `--do-nut` (dò nút không có target/action) trả 0 — tức nút CÓ nối; `--bam-thu` (bấm thật, so vân tay màn hình trước/sau) tìm ra 3 nút im, và cả ba im vì CÙNG một lý do: đòi ô nhập có nội dung nhưng vẫn trông bấm được khi ô trống. Sửa bằng một cơ chế dùng chung (`EideNutTheoO`) chứ không vá ba chỗ. Bộ đo cũng tự lộ hai điểm mù của chính nó: ban đầu nó bỏ qua hình học (tố oan ba nút đổi cỡ vùng trao đổi) và không chờ màn nạp xong (25 màn góp đúng 0 nút).

**Nội dung đề xuất:** Ghi vào API-15 §1 rằng `chat.send` là bất đồng bộ và hai sự kiện mang gì; ghi vào UXC-31 §2D.4 rằng gợi ý ô lệnh suy từ hiện vật của dự án chứ không phải hằng số.

**Mã liên quan:** `src/eide/daemon/rpc.py::chat_send, _chay_chuoi_nen, serve_stdio`; `apps/eide/Sources/EideLoi/EideDaemon.swift`; `apps/eide/Sources/EideGiaoDien/EidePhien.swift`, `EideDock.swift`; `apps/eide/Sources/EideApp/KichBan.swift`; `apps/eide/Sources/EideGiaoDien/{EideDoNutChet,EideNutTheoO}.swift`

---

## DEV-156 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §5 (sự kiện sổ cái); UXC-31 §7.3 (badge cho màn đóng)

**Hiện tại (điều mã buộc phải khác):** `Ledger.append` đóng dấu LƯỢT CHẠY đang thi hành lên mọi bản ghi (`chain`), đặt từ trước bản ghi đầu tiên của mỗi lời gọi và gỡ ở cả bốn đường ra. Badge cột trái đếm ĐƠN VỊ VIỆC (tập khoá) thay vì đếm sự kiện.

**Vì sao:** Đo 22/09/2026 trên bài CNC: ba câu người gõ ra huy hiệu `DỰ ÁN 993`, `Nhật ký 470`, `Mã nguồn 713` trên một dự án có đúng 7 yêu cầu. Con số ấy không sai về số học nhưng trả lời một câu hỏi không ai hỏi, và người ta thôi nhìn badge. Gốc sâu hơn giao diện: 633 bản ghi sổ cái từ ba câu, trong đó 48 bản ghi (`model.call`, `context.bundle`, `undo.register`, `intent`) KHÔNG mang mã lượt chạy — tức không truy được một lời gọi mô hình thuộc về câu lệnh nào, đúng câu hỏi đầu tiên người ta hỏi khi nhìn hoá đơn. Ba lần sửa mới đúng, và cả ba lần đều là cùng một sai lầm — tin một điều kiện nghe hợp lý mà chưa kiểm trên dữ liệu thật: (a) đặt dấu SAU `cap.run.start` nên `gate.decision` rơi vào đơn vị khác (177 khoá / 75 khoá); (b) bỏ qua bản ghi đã có `run_id` — nhưng `run_id` là mã LỜI GỌI, `chain.run_id` là mã ĐƠN VỊ VIỆC; (c) gộp theo `kind` làm hai lần `store.write` rời nhau thành một.

**Nội dung đề xuất:** Ghi vào API-15 §5 rằng mọi bản ghi mang `chain` của lượt chạy sinh ra nó; UXC-31 §7.3 nói rõ badge đếm đơn vị việc.

**Mã liên quan:** `src/eide_core/ledger.py::append, dat_chuoi_dang_chay`; `src/eide_core/router.py::invoke`; `apps/eide/Sources/EideGiaoDien/EidePhien.swift::khoaViec, laDoiTrangThai`

---

## DEV-162 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §5, §7 (sổ cái nối chuỗi băm); DDD-14 §2 Ledger

**Hiện tại (điều mã buộc phải khác):** `Ledger.append` nay khoá cả khối *gán số → nối băm → ghi tệp* bằng `threading.RLock`.

**Vì sao:** Sổ cái KHÔNG có khoá nào, trong khi [DEV-154] cùng ngày đã làm daemon chạy đa luồng: chuỗi chạy ở luồng nền trong khi vòng lặp chính phục vụ `view.timeline`, `queue.list`, `plane.hello`. Ba thứ phải đổi cùng nhau mà không cái nào được bảo vệ — `self._seq += 1` không nguyên tử, `_last_hash` bị ghi đè chéo, hai dòng xen vào nhau giữa tệp. Phát hiện bằng ẢNH CHỤP cửa sổ thật giữa một lượt CNC: giao diện treo biển "thiếu 1 bản ghi sổ cái (seq 22…22)". Thứ hỏng không phải một badge — sổ cái là một CHUỖI BĂM, mỗi bản ghi mang `prev_hash` của bản trước, và đó là toàn bộ bảo đảm "không ai sửa được lịch sử mà không bị phát hiện". Chuỗi gãy thì `verify()` báo hỏng và bằng chứng của cả phiên mất giá trị. `RLock` chứ không `Lock`: người quan sát in-process chạy NGAY trong `append`, nên một người quan sát ghi sổ lại sẽ tự khoá chính mình. Phép đo được KIỂM CHỨNG trước khi tin: bỏ khoá → test đỏ, có khoá → xanh. Một bài kiểm đồng thời xanh mà không bắt được lỗi thì vô giá trị.

**Nội dung đề xuất:** Ghi vào API-15 §7 rằng `append` phải nguyên tử khi daemon đa luồng.

**Mã liên quan:** `src/eide_core/ledger.py::Ledger.append`; `tests/test_ledger.py`

---

## DEV-163 — Đã duyệt

**Mục bị ảnh hưởng:** UXC-31 §7.2 (dải "Dữ liệu cũ", phát hiện nhảy quãng `seq`); API-15 §5

**Hiện tại (điều mã buộc phải khác):** Bộ dò nhảy quãng `seq` chỉ báo khi hở LỚN (> 8), thay vì báo mọi hở ≥ 2.

**Vì sao:** Giao diện giả định MỌI bản ghi sổ cái đều sinh một sự kiện lên màn hình. Sai: daemon cố ý chỉ chuyển tiếp những loại người dùng cần thấy, và `context.bundle`, `session.open` không có tên sự kiện nào. Đo 22/09/2026 giữa một lượt CNC — 5/78 bản ghi không sinh sự kiện, giao diện treo biển "thiếu 1 bản ghi sổ cái (seq 22…22)" trong khi tệp sổ cái HOÀN TOÀN LÀNH: 78 bản ghi, không thiếu, không trùng, `verify()` trả `(True, 0)`. Một cảnh báo toàn vẹn kêu sai TỆ HƠN không có cảnh báo: nó kêu ở mọi phiên bình thường, người dùng học cách bỏ qua, rồi lần sổ cái gãy THẬT thì nó kêu và không ai nhìn. Nới ngưỡng chứ KHÔNG chép danh sách loại sang giao diện: giao diện không được biết loại nào daemon chuyển tiếp — đó là chi tiết của phía kia, và một bản sao ở đây là một chỗ sẽ trôi. Ghi kèm: bản vá khoá sổ cái [DEV-162] cùng ngày vẫn ĐÚNG và CẦN (bỏ khoá thì test mất bản ghi), nó chỉ không phải nguyên nhân của dòng cảnh báo này. Hai lỗi khác nhau, cùng một triệu chứng — và tôi đã sửa cái thứ nhất rồi tưởng đã xong.

**Nội dung đề xuất:** Ghi vào UXC-31 §7.2 rằng phép dò nhảy quãng chịu được hở nhỏ do bản ghi nội bộ; hoặc đề xuất daemon gửi kèm `seq_bo_qua` để giao diện dò chính xác mà không cần biết bảng loại.

**Mã liên quan:** `apps/eide/Sources/EideGiaoDien/EidePhien.swift::_theoSeq, NGUONG_HO`; `apps/eide/Tests/EideGiaoDienTests/EideDongBoSuKienTests.swift`

---

## DEV-167 — Đã duyệt

**Mục bị ảnh hưởng:** API-15 §5 (sự kiện sổ cái)

**Hiện tại (điều mã buộc phải khác):** Daemon chỉ đổi `run_id`/`node_id` khi bản ghi thật sự là một NÚT của chuỗi; lời gọi lẻ không còn mang khoá `node_id` rỗng. Giao diện đọc `node_id` bằng `as? String` thay vì `!= nil`.

**Vì sao:** Từ [DEV-156] mọi bản ghi sổ cái đều mang `chain`, và với lời gọi LẺ thì dấu ấy chỉ có `{run_id, cap}`. Bản trước gán `node_id = None` vô điều kiện; trong JSON đó là `null`, và Swift đọc `null` thành `NSNull` — thứ KHÁC `nil`. Bộ lọc `p["node_id"] != nil` vì thế cho lọt MỌI lời gọi lẻ, mỗi cái sinh một thẻ Run riêng — thẻ ấy không bao giờ nhận `run.started` nên đứng mãi ở "đang lập kế hoạch", và không bao giờ nhận `run.done` nên thanh tiến độ chạy vĩnh viễn. Đo bằng ảnh chụp cửa sổ thật: hai thẻ `cded59a8`, `648c5d33` treo suốt phiên — mã 12 hex của lời gọi lẻ, không phải mã chuỗi (`r_…`). Vá CẢ HAI đầu: sửa một bên là đủ hết triệu chứng, nhưng cùng nhầm lẫn ấy sẽ quay lại ở khoá khác — `null` đi qua JSON là chuyện thường xuyên. Một khoá có mặt với giá trị rỗng và một khoá vắng mặt là hai điều khác nhau ở mọi ngôn ngữ nhận. **Hệ quả thứ TƯ của một thay đổi của tôi trong ngày** (ba cái trước từ [DEV-154]).

**Nội dung đề xuất:** Ghi vào API-15 §5 rằng sự kiện không mang khoá có giá trị rỗng.

**Mã liên quan:** `src/eide/daemon/rpc.py::_tu_so_cai`; `apps/eide/Sources/EideGiaoDien/EidePhien.swift::_theoRun`; `tests/test_daemon.py`

---

## DEV-169 — Đã duyệt

**Mục bị ảnh hưởng:** UXC-31 §7.3 (badge cho màn đóng); API-15 §5

**Hiện tại (điều mã buộc phải khác):** Bộ lọc "tiếng vọng" nay nhận CẢ `action_cap`, không chỉ `cap`. Bỏ dòng `p["chain"] != nil` thêm ở [DEV-156] — mã chết. Sửa hai bài kiểm badge dùng payload không tồn tại trên đường dây.

**Vì sao:** Router ghi năng lực bị xét vào `action_cap` (để phân biệt "năng lực bị xét" với "năng lực đang chạy"), nên phép hỏi `p["cap"] != nil` của bộ lọc không thấy nó và MỌI `gate.decision` đi thẳng qua — kể cả quyết định cho một lời gọi ĐỌC mà chính một màn vừa phát ra để tự vẽ. Mỗi lần màn tự vẽ lại thành một "việc mới cho anh". Đo 22/09/2026 trên bài CNC: `gate.decision` góp 109 khoá việc trên tổng 119, và badge hiện `Nhật ký 120` trên một dự án có 5 yêu cầu. Sau bản vá: DỰ ÁN 993 → 125 → **21**, Nhật ký 470 → 120 → **16**, các màn thiết kế về **5** — đúng số lượt người gõ. Dòng `p["chain"] != nil` là MÃ CHẾT vì daemon `pop("chain")` trước khi gửi; một điều kiện không bao giờ đúng nằm trong bộ lọc là chỗ người đọc sau sẽ tin nhầm. Và nó che mất lỗi: hai bài kiểm badge gửi `chain` nguyên trong gói — **một hình dạng không tồn tại trên đường dây** — nên chúng xanh suốt trong khi đo một giao thức tưởng tượng.

**Nội dung đề xuất:** Ghi vào API-15 §5 rằng `gate.decision` dùng `action_cap`, và mọi bên nhận phải coi nó tương đương `cap` khi phân biệt tiếng vọng.

**Mã liên quan:** `apps/eide/Sources/EideGiaoDien/EidePhien.swift::laDoiTrangThai`; `apps/eide/Tests/EideGiaoDienTests/EideDongBoSuKienTests.swift`

---

## DEV-176 — Mở

**Mục bị ảnh hưởng:** API-15 §2 `gate.decide` ("Người quyết định mục ASK"); APD-08 §4.1 (hàng chờ và duyệt), §1 (duyệt là quyết định của người); UXD-13 U2 (nút duyệt/từ chối); POL-17 §2 G1, §7 (POLICY-06 học từ `human_answer`); CDS-12.3 CODE-01 (grounding "G1 approved")

**Hiện tại (điều mã buộc phải khác):** `Router.quyet_dinh` nay nhận thêm khoá dạng `<run_id>:<gate>` — cổng do HANDLER chạy — và giao cho `cong_phu_handlers[gate]`, đăng ký từ ngoài y như `undo_handlers`. Hiện thực cho G1: sửa `decision` trong tệp kế hoạch, giữ quyết định cũ của máy ở `truoc`, và đánh dấu điểm cần làm rõ do chính cổng ấy sinh ra là đã trả lời. `plan.create` ghi thêm `run_id` vào quyết định để tra ngược được từ khoá về đúng kế hoạch.

**Vì sao:** `quyet_dinh()` được viết để chạy tiếp một LỜI GỌI đang treo — nó còn giữ `(cap, params, ctx)` trong hàng chờ và chỉ việc gọi lại với `actor="human"`. Cổng phụ không có gì để chạy tiếp: lời gọi sinh ra nó đã `done` từ lâu, và thứ người duyệt là **hiện vật** (bản kế hoạch), không phải một lời gọi. Đo 22/09/2026 trên bài nhấp nháy LED: cổng G1-03 "Đổi kiến trúc" trả ASK vì bước 1 chạm `clock` (đặt `F_CPU`) — đúng thứ POL-17 bắt hỏi người. Nhờ [DEV-171] câu hỏi HIỆN ĐƯỢC ở tab Làm rõ yêu cầu và có dòng `decision_log`; bấm duyệt thì nhận E2000. Người dùng đọc được câu hỏi và **không có đường nào trả lời nó** — chuỗi đứng vĩnh viễn ở đúng chỗ tác tử làm đúng. Đây là mắt xích cuối của chuỗi [DEV-171] → [DEV-174a] → [DEV-175]: ba mục trước làm tác tử hỏi ĐÚNG câu và hỏi ĐƯỢC; mục này làm người trả lời được. | Router không tự biết cách duyệt vì hiện vật thuộc về từng nhóm năng lực (G1 phán lên tệp kế hoạch của `plan.*`), và `eide_core` không được phụ thuộc `eide.caps` — cùng lập luận đã viết cho `undo_handlers`. Từ chối KHÔNG xoá kế hoạch: một kế hoạch bị bác vẫn là dữ liệu người dùng cần đọc lại và `plan.replan` cần làm điểm xuất phát. Câu trả lời của nút Duyệt ghi cả vào `clarification_answer` chứ không chỉ đổi `status`, nên nó **hoàn tác được** bằng `restore_answer` ([DEV-151]) — người bấm nhầm có đường lùi. **Đo vòng đầy đủ trên dự án thật, qua daemon:** cổng chặn `E3000` → `gate.decide approve` → `code.generate_module` sinh ra `src/main.c` (`DDRB

**Nội dung đề xuất:** = (1 << DDB5);`) → `avr-gcc 14.3.0` dịch thành `main.elf`, **136 byte flash** trên ATmega328P. 13 bài pytest; tắt bản vá thì 9 bài đỏ. Đề xuất API-15 §2 v1.3 ghi rõ `gate.decide` nhận CẢ hai loại khoá — `run_id` của mục trong hàng chờ, và `<run_id>:<gate>` của cổng do handler chạy.

**Mã liên quan:** `src/eide_core/router.py::quyet_dinh, _quyet_dinh_cong_phu, cong_phu_handlers`; `src/eide/gate_handlers.py` (mới); `src/eide/caps/req.py::ghi_tra_loi`; `src/eide/caps/plan.py::create`; đăng ký ở `daemon/rpc.py`, `cli.py`, `mcp/server.py`; `tests/test_duyet_cong_phu.py`

---

## DEV-178 — Mở

**Mục bị ảnh hưởng:** UXC-31 §8 S9 (tab Làm rõ yêu cầu); DPS-09 §4.4 (nút chuỗi, `on_ask`); API-15 §5 `run.blocked`

**Hiện tại (điều mã buộc phải khác):** Nút chuỗi HỎNG kèm lời khuyên DÙNG ĐƯỢC nay cũng sinh một dòng `clarification`, y như nút bị chặn vì thiếu tham số ở [DEV-160]. Chỉ ghi các mã có đường ra — E2000, E4001, E6003, E3000 — và nói rõ `candidates` là năng lực nào chạy tiếp được.

**Vì sao:** Chạy lại bài CNC toàn trình qua giao diện 22/09/2026 (12 bước, 147 lời gọi năng lực, 0,0553 USD, 7 yêu cầu có mã): chuỗi *"Vẽ lược đồ"* chạy `view.artifacts(module)` → **0 mục** → `diagram.architecture` hỏng với *"Chưa có module nào — chạy `arch.decompose` trước; sơ đồ kiến trúc phải vẽ từ ModuleGraph có thật, không đoán"*. Thông điệp ấy ĐÚNG và DÙNG ĐƯỢC NGAY — `arch.decompose` không đòi hộ chiếu (grounding `—`), nên người dùng chạy được liền. Nhưng store của dự án khi ấy có **4 điểm cần làm rõ** (`req.detect_conflict` ×3, `arch.style_select` ×1) và **không cái nào là nó**. Người dùng đóng cửa sổ trò chuyện, mở tab, và thứ đang chặn họ không có ở đó — đúng cái trôi mà [DEV-160] viết ra để chặn, chỉ ở một nhánh khác.

**Nội dung đề xuất:** Chỉ ghi lỗi CÓ ĐƯỜNG RA: E5000/E5002 (mô hình trả sai schema) và E7001 là chuyện của máy, không phải việc người làm được. Đưa chúng lên tab Làm rõ yêu cầu là biến chỗ ấy thành sọt rác lỗi, và một tab đầy thứ không hành động được là một tab người ta thôi mở. Mã điểm cần làm rõ băm theo nội dung ([DEV-151]) nên chạy lại chuỗi không đẻ bản sao. 6 bài pytest; tắt bản vá thì 4 bài đỏ. **Ghi chú về chính bài CNC:** chuỗi dừng ở `arch.style_select` cần `passport` — dự án chưa ghim chip, và đó là quyết định của chủ sản phẩm chứ không phải lỗi. Câu hỏi ấy ĐÃ lên tab đúng (`CL-70b3efe903 · open · arch.style_select`).

**Mã liên quan:** `src/eide/caps/chat.py::_ghi_loi_chan_chuoi`, `orchestrate`; `tests/test_loi_chan_chuoi.py`

---

## DEV-180 — Mở

**Mục bị ảnh hưởng:** CDS-12.2 SEARCH-09 `search.reference_projects` (`output_schema`: `{templates}`, `additionalProperties: false`); API-15 §3 (E1004 OUTPUT_SCHEMA, xem [DEV-002])

**Hiện tại (điều mã buộc phải khác):** Trả `{"templates": […]}` thay vì `{"candidates": […], "warning": …}`.

**Vì sao:** Chủ sản phẩm gõ thẳng vào app một câu mô tả việc CNC LAN→USB 22/09/2026; chuỗi Z-01 chạy tới bước 2 và sổ cái ghi `search.reference_projects · failed · E1004`. Hợp đồng SEARCH-09 khai đúng MỘT trường `templates` và `additionalProperties: false`; hiện thực trả `candidates` kèm `warning` — **sai tên khoá VÀ thừa một khoá**, nên `Registry.validate_output` bác **mọi lần gọi**, không riêng khi registry rỗng. Hình dạng cũ chép từ `search.registry` — năng lực ấy đúng là trả `candidates`. Hai hợp đồng khác nhau, trông giống nhau, và **không bài kiểm nào chạy `search.*` qua Router** nên nó sống tới lúc có người dùng thật.

**Nội dung đề xuất:** Sửa MÃ cho khớp hợp đồng, không sửa hợp đồng cho khớp mã (CLAUDE.md quy tắc 1). Bỏ `warning` vì schema cấm trường lạ; thông tin "registry chưa có mẫu nào" không mất — `templates: []` nói điều ấy, và lớp giao diện đã có cơ chế "màn rỗng CÓ LÝ DO". Đề nghị SEARCH-09 v1.3 cân nhắc thêm `warning` vào `output_schema`: phân biệt "registry rỗng" với "không mẫu nào khớp" là hai việc khác nhau cho người dùng — cùng lập luận với [DEV-173].

**Mã liên quan:** `src/eide/caps/search.py::reference_projects`

---

## DEV-181 — Mở

**Mục bị ảnh hưởng:** UXC-31 §2D (vùng trao đổi), §8 S9/S10; API-15 `chat.send.result.cho_nguoi[]` và `event.chat.report` (schema `type: object` + mô tả, KHÔNG liệt kê trường); DPS-09 §4.3 (nút thiếu dữ kiện thì hỏi người); CXD-10 §2 lớp C2

**Hiện tại (điều mã buộc phải khác):** Mục `cho_nguoi[]`/`waiting[]` mang thêm `clar_id`, `hoi` (câu hỏi tiếng Việt), `truong[].lua_chon` (tập giá trị hợp lệ); `event.chat.report` mang thêm `van` (câu gõ gốc). Vùng trao đổi vẽ THẺ HỎI có ô trả lời và nút chọn, thay cho một dòng chữ.

**Vì sao:** Chủ sản phẩm dùng app thật 22/09/2026: chuỗi dừng, vùng trao đổi in *"Dừng ở `env.check` — cần anh cho biết: isa"*, và hai câu họ gõ lại là *"Lượt chạy DỪNG, đang chờ anh trả lời. Tôi cần tìm chỗ nào để trả lời?"* rồi *"isa cho cái gì? Tôi cần bạn tư vấn mà"*. Ba lỗ cùng lúc: (a) câu hỏi chỉ là TÊN THAM SỐ thô, không phải câu hỏi; (b) `isa` có đúng ba giá trị hợp lệ đọc được từ `docs/spec/isa/` nhưng không giá trị nào được đưa ra; (c) không có chỗ nào TRONG vùng trao đổi để trả lời — [DEV-160] đã chuyển câu hỏi sang tab Làm rõ và **chỉ** sang đó. Nguyên tắc chủ sản phẩm chốt: *"Tất cả các trao đổi đều phải thực hiện ở vùng trao đổi. Agent hỏi gì thì phải hiển thị ở vùng trao đổi thì tôi mới biết trả lời chứ?"*

**Nội dung đề xuất:** Thêm trường vào hai payload có schema mở (`type: object` + mô tả), nên KHÔNG phá hợp đồng và không đổi tên phương thức, mã lỗi hay schema trong `docs/spec/` — vẫn ghi vào đây vì API-15 phải tả được trường mới ở v1.3. Câu hỏi hiện ở CẢ HAI chỗ (vùng trao đổi + tab Làm rõ): [DEV-160] đúng ở lý do (bong bóng chat TRÔI) nhưng sai ở kết luận — chỗ để tra cứu và chỗ để đối thoại là hai nhu cầu khác nhau. Trả lời xong thì GỬI LẠI câu gốc (`van`) thay vì "chạy tiếp từ nút đang dở": lượt mới đọc câu trả lời qua lớp C2 nên không hỏi lại, và nó vẫn đi qua cổng chính sách như mọi lượt khác thay vì có một đường vòng riêng. Nút Duyệt/Từ chối của cổng chặn dùng CÙNG thẻ ấy — chủ sản phẩm chốt *"nút duyệt hoặc phê duyệt luôn ở ô trả lời chat"*; với người dùng, thiếu tham số và cổng chặn đều là *tác tử đang hỏi tôi*. Thiếu `clar_id` thì KHÔNG vẽ ô trả lời — một ô không biết ghi vào đâu tệ hơn một dòng chữ, vì nó hứa.

**Mã liên quan:** `src/eide/caps/chat.py::_ghi_cau_hoi_chuoi`, `src/eide/daemon/rpc.py::_cho_gi` + `_chay_chuoi_nen`, `apps/eide/Sources/EideGiaoDien/EideTheHoi.swift` (mới), `EidePhien.swift::_hienCauHoi`/`traLoiCauHoi`

---

## DEV-184 — Mở

**Mục bị ảnh hưởng:** CDS-12.3 PROJECT-02 (*"cũ → E6003, KHÔNG tự di trú — hợp đồng giao việc ấy cho Orchestrator gọi `eide migrate`"*); DDD-14 §5; API-15 §3 E6003 MIGRATION_REQUIRED

**Hiện tại (điều mã buộc phải khác):** Giao diện bắt E6003 lúc mở dự án và hiện một THẺ có nút *Di trú ngay* chạy `eide migrate`, thay cho một dòng chữ bảo người dùng ra dòng lệnh gõ. Lõi KHÔNG đổi: `project.open` vẫn ném E6003 và vẫn không tự di trú.

**Vì sao:** Đo 22/09/2026 bằng bộ dò [DEV-183] trên dự án `doc-cam-bien-dht22…` (290 fact, tạo trước [DEV-170]): **18 trong 20 màn mở ra trống**, mỗi màn in `E6003: store user_version=7, cần 10`. Vùng trao đổi có nói đúng cách sửa — *"→ chạy `eide migrate` trong thư mục dự án"* — nhưng cách ấy nằm ngoài ứng dụng: người dùng phải mở Terminal, biết đường dẫn dự án, và biết `eide` nằm ở đâu. Mọi dự án tạo trước [DEV-170] đều rơi vào tình trạng này, tức là mọi dự án của chủ sản phẩm tính tới hôm nay.

**Nội dung đề xuất:** Giữ nguyên hợp đồng: PROJECT-02 nói lõi không tự di trú, và lý do của điều khoản ấy đúng — di trú là thao tác đổi dữ liệu, nó phải do NGƯỜI quyết, và nó sao lưu trước khi chạy. Thứ thiếu không phải một quyết định tự động mà là một CÁI NÚT: hợp đồng giao việc cho "Orchestrator", và ở bản desktop thì giao diện chính là chỗ ấy. Chạy qua đường `eide migrate` một lần (cùng đường `project new` đang dùng) chứ không thêm phương thức RPC — di trú xong thì store đổi schema dưới chân daemon đang chạy, nên phải mở lại dự án, và một lệnh một lần làm việc đó gọn hơn một RPC giữa phiên.

**Mã liên quan:** `apps/eide/Sources/EideGiaoDien/EidePhien.swift::_moThat`, `EideTheHoi.swift`

---
