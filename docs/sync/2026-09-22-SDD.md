# Đồng bộ tài liệu SDD — bản nháp 2026-09-22

Nguồn sinh phải sửa: `docs/ho-so/nguon/sdd.js`  ·  phiên bản đích: **v1.3**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Nó **không** sửa docx và **không** sửa `docs/spec/`. Sau khi chủ sản phẩm duyệt: sửa nguồn sinh → `scripts/sinh_tai_lieu.sh sdd` → đổi trạng thái mục thành `Đã cập nhật tài liệu v1.3`.

## DEV-145 — Đã duyệt

**Mục bị ảnh hưởng:** CDS-12.3 PROJECT-01 (`project.create` — "tạo .eide/ với cấu trúc SDD-04 §6") và PROJECT-02 (`project.open` — "PRAGMA user_version → nếu cũ: E6003, Orchestrator gọi `eide migrate`"); SDD-04 §6; DDD-14 §5

**Hiện tại (điều mã buộc phải khác):** `project.create` nay **tự di trú store lên bản mới nhất** (`store.migrate`) ngay lúc tạo dự án. Trước đó nó chỉ `mkdir` thư mục `.eide/store/` rỗng, nên mọi dự án mới đều mở không được. Ba test mã hoá tiền đề cũ đã sửa, trong đó `test_chua_migrate_bao_E6003_chu_khong_no` bị **đảo chiều** thành `test_du_an_vua_tao_MO_DUOC_ngay`; nhánh E6003 vẫn được phủ bằng một test xoá store đi.

**Vì sao:** Đo ngày 21/09/2026 bằng một phiên giao diện THẬT (bài kiểm gateway LAN→USB cho bộ điều khiển CNC Fangling F2300B, `--kich-ban`): tạo dự án xong, `project.open` ném E6003 ở lượt mở đầu tiên, và từ đó **tám lượt gõ liên tiếp không nhận được một câu trả lời nào**. Đây là cùng một lỗ hổng với `.git` đã vá hôm 17/09 (DEV-125): `SUBDIRS` tạo cái vỏ, không ai tạo cái ruột. Tài liệu viết "Orchestrator gọi `eide migrate`" — nhưng trong sản phẩm có giao diện thì không có Orchestrator nào gọi nó, không có nút nào trên màn hình, và `eide migrate` là một lệnh dòng lệnh mà người dùng giao diện không có đường nào biết tới. Một lời hứa "tự di trú" giao cho một tầng không tồn tại thì không ai thực hiện. Điều đáng ghi hơn cả: **ba test đều XANH suốt trong khi sản phẩm hỏng** — chúng mô tả đúng mã và bảo vệ đúng hành vi sai, vì không test nào đi hết đường từ "tạo dự án" tới "làm được việc".

**Nội dung đề xuất:** Sửa PROJECT-01 ở CDS-12.3 v1.3: thêm `store.migrate` vào hậu điều kiện ("dự án sau khi tạo phải MỞ ĐƯỢC ngay"). PROJECT-02 giữ nguyên E6003 — nó vẫn đúng cho store bị xoá hoặc bản cũ.

**Mã liên quan:** `src/eide/caps/project.py::create`; `tests/test_project_open.py`; `tests/test_kg.py`

---

## DEV-182 — Mở

**Mục bị ảnh hưởng:** UXC-31 §3.1 (*"ĐÚNG MỘT nút chính"* ở màn chào), §8 S1; CDS-12.1 PROJECT-01 (`input_schema.dir` — đã có sẵn, không đổi); SDD-04 §6 `defaults.project_dir`

**Hiện tại (điều mã buộc phải khác):** Màn chào thêm một DÒNG *"Lưu tại: …"* kèm nút phụ *Đổi…* mở `NSOpenPanel` chọn thư mục; `onTao` mang thêm thư mục và đi xuống `project.create.dir`.

**Vì sao:** Chủ sản phẩm yêu cầu 22/09/2026: *"Khi tạo dự án cần cho phép chọn thư mục lưu dự án"*. Lõi đã hỗ trợ từ đầu (`PROJECT-01.input_schema.dir`, `eide project new --dir`), chỉ giao diện không có đường tới nó — và màn chào còn KHÔNG NÓI dự án sẽ nằm ở đâu, nên người dùng tạo xong phải đi tìm dự án của chính mình trong `~/eide`.

**Nội dung đề xuất:** §3.1 nói về NGÃ RẼ, không nói về số lượng `NSButton`: *"mọi nút thứ hai là một ngã rẽ họ phải cân nhắc trước khi biết sản phẩm làm gì"*. Một nút phụ nằm trong dòng thông tin về chỗ lưu không phải ngã rẽ — nó trả lời một câu hỏi người dùng đang có. Nên giữ nguyên điều khoản, đổi PHÉP ĐO: bài kiểm chuyển từ đếm nút sang đếm nút CHÍNH (nút nhận phím Enter), và thêm trần 2 nút để không lặng lẽ mọc thêm cái thứ ba. Vẫn đúng MỘT đường tạo dự án — nút *Tạo mới* trong popover chuyển dự án quay về chính màn chào này. Đề nghị UXC-31 §3.1 v1.3 viết lại thành "đúng một nút CHÍNH" và mô tả dòng chỗ lưu.

**Mã liên quan:** `apps/eide/Sources/EideGiaoDien/EideManChao.swift`, `EidePhien.swift::taoDuAn`, `apps/eide/Tests/EideGiaoDienTests/EideLamQuenBangLenhTests.swift`

---
