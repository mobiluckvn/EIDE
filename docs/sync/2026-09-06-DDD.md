# Đồng bộ tài liệu DDD — bản nháp 2026-09-06

Nguồn sinh phải sửa: `docs/ho-so/nguon/ddd.js + ddd_model.py`  ·  phiên bản đích: **v1.3**

Bản nháp này do `scripts/dong_bo_tai_lieu.py` sinh từ `docs/DEVIATIONS.md`. Nó **không** sửa docx và **không** sửa `docs/spec/`. Sau khi chủ sản phẩm duyệt: sửa nguồn sinh → `scripts/sinh_tai_lieu.sh ddd` → đổi trạng thái mục thành `Đã cập nhật tài liệu v1.3`.

## DEV-006 — Mở

**Mục bị ảnh hưởng:** DDD-14 §5 bảng lịch migration (đối chiếu §2.25 và MEM-11 §2)

**Hiện tại (điều mã buộc phải khác):** Bảng lịch §5 có một dòng cho cơ sở dữ liệu RIÊNG `index.sqlite` (0003) nhưng không có dòng tương đương cho `session.sqlite`, nên đọc §5 một mình sẽ tưởng bảng `session` bị bỏ sót

**Vì sao:** **Ghi nhận đầu (06/09/2026) SAI và đã sửa cùng ngày.** Ban đầu tôi đặt `session` vào migration 0002 của store.sqlite vì tưởng §5 bỏ quên. Thật ra DDD-14 §2.25 ghi rõ "Phiên làm việc (session.sqlite, không commit)" và MEM-11 §2 xếp M2 vào `session.sqlite` — nó thuộc cơ sở dữ liệu khác, đúng như `rag_chunk` thuộc index.sqlite. Đã gỡ `session` khỏi 0002 và dựng `session_db.sql` riêng. Sửa được vì 0002 chưa phát hành (v0.1.0), không có store thật nào cần di trú

**Nội dung đề xuất cho v1.3:** Bổ sung vào DDD-14 §5 một dòng cho `session.sqlite` giống dòng 0003 của `index.sqlite`, để đọc §5 một mình là đủ biết bảng nào ở đâu

**Mã liên quan:** `src/eide_core/migrations/session_db.sql`, `0002_m1_policy_runtime.sql`

---

## DEV-010 — Mở

**Mục bị ảnh hưởng:** DDD-14 §2 thực thể Preference (bảng `preference`) vs §6 `preferences.yaml`; CDS-12.3 PROJECT-09

**Hiện tại (điều mã buộc phải khác):** Tài liệu có HAI nơi lưu cùng một thứ với cùng hình dạng ({value, scope, learned_from, ttl_days}) — bảng `preference` (§2, khóa chính (key, scope)) và tệp `preferences.yaml` (§6) — nhưng không nói nơi nào là chuẩn cho phạm vi nào

**Vì sao:** Phải chọn một cách nhất quán, và phép thử là ngữ nghĩa của `scope`: bảng `preference` nằm trong store của TỪNG dự án, nên tùy chọn phạm vi `user` ghi ở dự án A sẽ không đọc được ở dự án B — trái đúng nghĩa "user"

**Nội dung đề xuất cho v1.3:** `scope=project` → bảng `preference` trong store dự án; `scope=user` → `preferences.yaml` trong thư mục cấu hình người dùng (platformdirs). Đề xuất DDD-14 v1.3 ghi rõ phân công này ở cả §2 và §6

**Mã liên quan:** `src/eide/caps/project.py::preferences`

---

## DEV-015 — Mở

**Mục bị ảnh hưởng:** SDD-04 §6 `models.yaml`; DDD-14 §6 (models.yaml "từ M0"); PRS-16 §2

**Hiện tại (điều mã buộc phải khác):** `models.yaml` được cả ba tài liệu nhắc tới nhưng không có trong `docs/spec/`; và SDD-04 §6 dùng BÍ DANH ("gemini-flash", "claude-opus") chứ không phải mã model thật, lại không nói giá lấy ở đâu

**Vì sao:** Không có tệp thì Gateway không chọn được mô hình; không có ánh xạ bí danh thì không gọi được API nào; không có giá thì `daily_budget_usd` không cưỡng chế được và `model.call.cost_usd` luôn bằng 0

**Nội dung đề xuất cho v1.3:** Thêm `docs/spec/models.yaml` — phần `roles` và `policy` chép ĐÚNG SDD-04 §6; bổ sung `aliases` (bí danh → mã model thật, đã đối chiếu API 06/09/2026) và `pricing` (USD/1M token). Cùng khuôn mẫu với `defaults.yaml` của DEV-001. Đề xuất SDD-04 v1.3 ghi rõ hai phần bổ sung này

**Mã liên quan:** `docs/spec/models.yaml`

---
