# Nhật ký sai khác giữa mã và tài liệu (DEVIATIONS)

Quy tắc: mọi chỗ mã **buộc phải khác** bộ hồ sơ v1.2 đều ghi ở đây **trước khi commit**. Không sửa tài liệu trực tiếp trong docx; tài liệu được sinh lại từ `ho-so/nguon/` sau khi chủ sản phẩm duyệt. Thêm mục bằng `/sai-khac` hoặc `python scripts/new_deviation.py`.

Trạng thái: `Mở` → `Đã duyệt` (chủ sản phẩm đồng ý sửa tài liệu) | `Bác` (sửa mã cho khớp tài liệu) → `Đã cập nhật tài liệu vX.Y`.

| Mã | Ngày | Tài liệu / mục | Mã nguồn | Sai khác | Lý do | Đề xuất | Trạng thái |
|---|---|---|---|---|---|---|---|
| DEV-001 | 2026-09-05 | POL-17 §2 rules.yaml (`thresholds.*`, `trusted_sources`, `allowed_licenses`) | `src/eide_core/policy.py`, `docs/spec/policy/defaults.yaml` | rules.yaml tham chiếu ngưỡng nhưng giá trị mặc định chỉ có trong schema autonomy.json (POL-17 §5) và ví dụ SDD-04 §6; `allowed_licenses` không có giá trị mặc định ở đâu | Cần tệp mặc định để PolicyGate chạy được khi dự án chưa có `.eide/autonomy.yaml` | Thêm `spec/policy/defaults.yaml` (ngưỡng theo schema; `allowed_licenses` = MIT, BSD-2/3, Apache-2.0, CC-BY-4.0, vendor-doc) và ghi vào POL-17 §5 ở v1.3. **Chủ sản phẩm duyệt và KÝ 06/09/2026 (WI-257):** Vũ Trí Công ký nguyên bốn danh sách đang có (6 tên miền hãng, 12 gói công cụ, 6 license, `boards` rỗng) — niêm `docs/spec/policy/defaults.sig`, băm `5b6768bb2a377939…`; đồng thời hạ mức tự chủ mặc định **A3 → A2** (tác tử tự làm tới lớp R2; mọi thao tác chạm phần cứng đều hỏi người). Từ nay ngưỡng và danh sách không còn là giả định của tác tử. Sửa tay `defaults.yaml` mà không chạy `eide policy sign` sẽ làm PolicyGate **bỏ hẳn** ba danh sách và mọi thứ rơi về ASK — xem `whitelist.py`. Còn lại: ghi giá trị mặc định vào POL-17 §5 ở v1.3 | POL-17 §5 v1.2 nay ghi rõ giá trị mặc định và cơ chế kế thừa niêm. | Đã cập nhật tài liệu v1.2 |
| DEV-002 | 2026-09-05 | API-15 §3 errors.json | `src/eide_core/registry.py::validate_output` | Không có mã lỗi cho trường hợp *kết quả năng lực* không khớp `output_schema` (E5002 chỉ dành cho đầu ra mô hình) | Registry kiểm output mọi lời gọi để bắt lỗi hiện thực sớm (STP-05) | Tạm dùng E6001 SCHEMA_VIOLATION; đề xuất thêm E1004 OUTPUT_SCHEMA ở API-15 v1.3 | Chủ sản phẩm duyệt 07/09/2026. API-15 §3 v1.5 thêm `E1004 OUTPUT_SCHEMA`; `registry.validate_output` bỏ cách mượn E6001, và nay có test phủ nhánh ấy (trước đó không test nào phủ). | Đã cập nhật tài liệu v1.5 |
| DEV-054 | 2026-09-07 | POL-17 §2 (cổng G1, G-FACT) đối chiếu §2 câu "đặc trưng do năng lực cung cấp khi gọi Router" | `src/eide_core/registry.py::_gate_tu_spec` | Ánh xạ cổng cấp Router loại `G-FACT` (từ DEV-041) và nay loại thêm `G1` | Mọi cổng đều xét một HIỆN VẬT — G-OPS xét `board`/`artifact`, G-SRC xét `source`, G3 xét `patch`, G5 xét `pkg`, G1 xét `plan`, G-FACT xét `fact`. Khác biệt nằm ở chỗ hiện vật ấy có TỒN TẠI TRƯỚC lời gọi hay không: `target.flash` nhận `artifact` làm tham số nên Router đánh giá được trước khi gọi; còn `plan.create` SINH RA bản kế hoạch mà G1 xét, và `kg.review_facts` xét từng fact nó nạp lên — hiện vật chưa tồn tại lúc Router quyết định, nên hỏi cổng ở cửa chỉ cho ra quy tắc bắt hết, luôn ASK. Đo được: `plan.replan` và `plan.create` rơi vào `G1-99` và không bao giờ chạy | Loại hai cổng "sinh ra hiện vật" khỏi tầng Router; hai năng lực ấy hỏi cổng BÊN TRONG đúng như hợp đồng viết (CDS-12.1 PLAN-03 "policy.decide(G1) với đặc trưng steps/all_cited/…"). **Cách tổng quát hơn** — Router chỉ áp cổng khi năng lực khai đủ `features_provided` mà cổng ấy hỏi (POL-17 §2 mô tả đúng cơ chế này) — cần nối `features_provided` vào PolicyGate, chưa làm. Đề xuất POL-17 v1.4 phân biệt rõ hai loại cổng | Đã cập nhật tài liệu v1.3 |
| DEV-053 | 2026-09-07 | DEV-29 §4 (vòng đồng bộ tài liệu) | `scripts/sinh_tai_lieu.sh` (cách dùng) | Chạy `sinh_tai_lieu.sh <ten> >/dev/null 2>&1` rồi coi như đã sinh — nhưng bộ sinh có thể hỏng CÚ PHÁP và thoát khác 0, mà đầu ra đã bị nuốt | Xảy ra ba lần trong một phiên làm việc, mỗi lần mất vài phút truy ngược: docx cứ hiện phiên bản cũ trong khi nguồn đã đúng, và không có tín hiệu nào cho biết vì sao. Lần cuối là một dấu `;` thừa do chính tôi để lại lúc đặt tên literal `CHUOI_MAU` | Quy tắc dùng: **không nuốt đầu ra của `sinh_tai_lieu.sh`**; nếu cần yên tĩnh thì `|| echo LỖI` hoặc kiểm mã thoát. Cổng `--kiem` cũng cần phân biệt "lệch nguồn" với "không sinh được" — hiện cả hai đều cho `TỔNG LỆCH: 0` khi không có dòng nào để so, nên vòng quét cuối phải kiểm thêm rằng có ít nhất một dòng `=`. Đã áp dụng trong lần quét 07/09/2026 | Bác |
| DEV-052 | 2026-09-07 | DDD-14 §2 Run (`intent_id TEXT REFERENCES intent(id)`) | `src/eide_core/router.py`, `src/eide/caps/chat.py::_ghi_run` | Năm chỗ trong Router và một chỗ trong `chat.orchestrate` bắt `except sqlite3.Error: pass` — quá rộng | Lý do có thật: store cũ chưa có bảng (user_version thấp) không được làm hỏng chính lời gọi, và bắt người chạy `eide migrate` trước khi được làm bất cứ việc gì là cái giá quá cao cho một dòng thống kê. Nhưng `sqlite3.Error` gồm CẢ lỗi lập trình. Đã xảy ra thật: `_ghi_run` truyền tên ý định (`"kg.build"`) vào `intent_id` vốn là khóa ngoại, mọi câu chèn hỏng vì ràng buộc, và KHÔNG dòng nào được ghi qua nhiều lần chạy mà không ai thấy — chỉ lộ khi chạy tay câu SQL với ngoại lệ không bị nuốt | Thu hẹp cả sáu chỗ về `sqlite3.OperationalError` (thiếu bảng/cột) — đúng trường hợp cần bỏ qua, và mọi lỗi khác nổi lên. `intent_id` để NULL cho tới khi có mã ghi bảng `intent`: `chat.parse_intent` hiện chỉ ghi sự kiện nhật ký. Không sửa tài liệu — đây là mã sai, không phải tài liệu sai | Bác |
| DEV-051 | 2026-09-07 | DPS-09 §4.4 ("Orchestrator lập đồ thị chuỗi bằng mô hình lập kế hoạch (vai trò planner)"); CDS-12.6 CHAT-06 bước 1 | `src/eide/caps/chat.py::_dung_chuoi` | `chat.orchestrate` chọn chuỗi MẪU theo `trigger_intents`, nhưng khi không mẫu nào khớp thì chưa gọi vai trò `planner` — nó dựng một chuỗi một nút từ chính ý định | Vai trò `planner` (PRS-16 §2) cần `plan.create` và nhóm `plan.*`, đều thuộc mốc M2 chưa hiện thực. Hai điều khiến việc hoãn này chấp nhận được: (a) §4.4 nói thẳng mẫu tồn tại "để mô hình bám theo thay vì sáng tác", nên đường mẫu mới là đường CHÍNH; (b) phép kiểm deterministic ở bước 2 chạy giống hệt cho cả hai nguồn chuỗi, nên khi planner có thì không phải sửa gì ở tầng kiểm | Thêm nhánh planner khi nhóm `plan.*` có. **Một giới hạn nữa phải nói ra**: mẫu §4.4 là chuỗi RÚT GỌN viết cho người đọc — có nhánh điều kiện bằng văn xuôi (`[template? registry.pull : req.elicit]`) và có `extract.*` là cả một nhóm — nên hiện chỉ giữ những bước là năng lực có thật và đã hiện thực. `nguon_chuoi` trong `run.report` nói rõ chuỗi đến từ mẫu nào, để không ai tưởng một chuỗi 12 bước rút còn 3 là đã chạy đủ 12  **ĐÓNG 07/09/2026:** nhóm `plan.*` hiện thực đủ 7/7, và `chat.orchestrate` nay gọi vai trò `planner` qua `plan.create` khi không mẫu nào khớp. Gọi QUA ROUTER chứ không gọi thẳng hàm: `plan.create` là T1* và hỏi cổng G1 bên trong, nên một kế hoạch thiếu tri thức hay đổi kiến trúc dừng ở đó chứ không lặng lẽ thành chuỗi. Thứ tự chọn nguồn chuỗi: mẫu §4.4 → ý định-là-năng-lực → planner; ý định trùng tên năng lực là câu trả lời CHẮC CHẮN, nhờ mô hình lập kế hoạch cho một việc một bước là mời ảo giác vào chỗ không cần. `nguon_chuoi` trong `run.report` ghi rõ chuỗi đến từ đâu. | Bác |
| DEV-050 | 2026-09-07 | UXD-13 §4 QueueList ("hành động duyệt/từ chối/hoàn tác/**hàng loạt**") | `apps/geditor/Sources/EIDEKit/EideViews.swift::ReviewQueueView` | Ba hành động đầu đã có; **hàng loạt CHƯA**, có chủ ý | Duyệt hàng loạt một chồng mục ASK lẫn lộn nhiều cổng và nhiều lớp rủi ro chính là cách biến cổng chính sách thành một con dấu — người bấm một nút để chấp nhận những thứ họ chưa đọc, và mỗi mục trong đó là một quyết định riêng mà APD-08 dựng cả năm tầng để buộc phải cân nhắc | Nếu làm, phải gom theo CÙNG cổng + CÙNG quy tắc, để người duyệt một LOẠI quyết định chứ không phải một đống; và số mục trong nhóm phải hiện ra trước khi bấm. Đó là một thiết kế cần bàn, không phải một nút thêm vào cho đủ danh sách. Đề xuất UXD-13 v1.2 ghi rõ ràng buộc ấy vào §4 thay vì để chữ "hàng loạt" đứng một mình | Đã cập nhật tài liệu v1.3 |
| DEV-049 | 2026-09-07 | DDD-14 §2 CapabilityRun (bảng `capability_run` lưu `args_hash`, không lưu tham số) | `src/eide_core/router.py::_cho` | Một mục ASK giữ tham số trong BỘ NHỚ tiến trình (`Router._cho`) để `gate.decide` chạy tiếp được. Bảng `capability_run` chỉ lưu `args_hash` — dùng cho toàn vẹn, không dùng để chạy lại | Hệ quả: **hàng đợi không sống qua lần khởi động lại daemon**. Người thấy một mục chờ hôm nay, tắt máy, mở lại thì mục ấy biến mất — mà UXD-13 U2 dựng hàng đợi làm nơi "người vào xem việc đang chờ", tức nó phải bền. Lưu `args_hash` thay vì tham số là lựa chọn có lý của DDD-14 (tham số có thể lớn hoặc chứa dữ liệu nhạy cảm), nhưng khi ấy phải có chỗ khác giữ chúng | Hai đường đi tiếp, cần chủ sản phẩm chọn: (a) thêm cột `args` vào `capability_run` và chấp nhận lưu tham số trong store — đơn giản, nhưng đưa dữ liệu có thể nhạy cảm vào một bảng vốn chỉ giữ băm; (b) giữ tham số trong `run.working` (M1 WorkingMemory, MEM-11 §3) vốn đã sinh ra để "tiếp tục sau tắt máy" — đúng chỗ hơn về ý nghĩa, nhưng phải nối WorkingMemory vào Router. Nghiêng về (b). Trong lúc chờ: mục chờ chỉ dùng được trong một phiên daemon, và điều đó phải nói ra ở UI chứ không để người phát hiện bằng cách mất việc  **Quyết định 07/09/2026 — chọn phương án (b), và đặc tả đã chọn sẵn:** `WorkingMemory` (MEM-11 §3) vốn CÓ hai trường `pending_question` và `asked_at`, còn MEM-11 §2 định nghĩa M1 là "RAM + run.state (SQLite) ĐỂ TIẾP TỤC SAU TẮT MÁY". Chỗ đúng đã được thiết kế từ đầu; Router chỉ chưa nối vào. Hai lý do nữa nghiêng về (b) chứ không phải thêm cột `args`: (1) tham số đi qua `che_bi_mat` trước khi ghi — một mục chờ nằm trong store hàng tuần là chỗ tệ nhất để một khóa API nằm lại, mà bảng `capability_run` vốn chỉ giữ băm nên không có bước lọc nào; (2) tham số ở cùng chỗ với run nó thuộc về, không nhân thành bản thứ hai. ĐÃ LÀM: mục chờ sống qua lần khởi động lại, duyệt được bằng `eide queue approve` từ một tiến trình khác, và có test dựng Router MỚI để chứng minh. | | Đã cập nhật tài liệu — MEM-11 (không đổi — đặc tả đã đúng) |
| DEV-048 | 2026-09-07 | DDD-14 §2 DecisionLog (bảng `decision_log`); POL-17 §7 ("chạy trên DecisionLog 30 ngày") | `src/eide_core/router.py::_ghi_decision_log` | Bảng `decision_log` có trong migration 0002 từ mốc M1 nhưng **KHÔNG mã nào ghi vào nó**. Router chỉ ghi sự kiện `gate.decision` vào nhật ký | POLICY-06 học ngưỡng đọc đúng bảng ấy, và hai cột nó cần — `human_answer`, `undone_at` — là những thứ được điền SAU khi người trả lời một mục ASK hoặc hoàn tác một việc đã APPROVE. Nhật ký chỉ-thêm không mang được cập nhật ấy; đó chính là lý do DDD-14 dựng một bảng riêng bên cạnh nhật ký. Không có bảng được ghi thì POLICY-06 luôn trả về danh sách rỗng, và trả rỗng trông y hệt "không có gì để đề xuất" | Router ghi một dòng `decision_log` cho mỗi quyết định, khi có dự án. Lỗi khi ghi KHÔNG làm hỏng chính quyết định (store cũ chưa có bảng là trường hợp thật, và bắt chạy `eide migrate` trước khi được làm bất cứ việc gì là cái giá quá cao cho một dòng thống kê). Còn nợ: `human_answer` và `undone_at` chưa ai cập nhật — cần `gate.decide` (người duyệt mục ASK) và `undo.apply` nối ngược về `decision_log.id`, thuộc phần hàng đợi ở Sprint sau  **07/09/2026:** vòng đã khép — `Router.quyet_dinh` (API-15 `gate.decide`) điền `human_answer` và `Router.hoan_tac` (`undo.apply`) điền `undone_at`. Có test chạy 22 lần duyệt thật rồi gọi `policy.learn_thresholds` và thấy đề xuất — không còn học từ dữ liệu bơm tay. Câu trả lời được ghi TRƯỚC khi chạy tiếp: người đã duyệt là sự thật độc lập với kết quả lần chạy sau đó. Còn lại: `decision_log` chưa ghi khi lời gọi không thuộc dự án nào (ví dụ `project.create`) — đúng thiết kế, vì lúc ấy chưa có store nào để thuộc về. | Bác |
| DEV-047 | 2026-09-07 | CDS-12.3 PROJECT-02 bước 2 ("Kiểm hash store vs ledger.last_hash → lệch: E6000"); DDD-14 §5 | `src/eide_core/store.py::BANG_VAN_HANH` | `content_digest` băm MỌI bảng của store. Từ khi Router ghi `decision_log` (DEV-048), băm ấy đổi ở **mỗi lời gọi năng lực**, nên niêm phong lệch ngay và `project.open` báo E6000 cho một kho hoàn toàn lành | Đưa bảng vận hành vào niêm thì phải niêm lại sau TỪNG lời gọi — và một niêm phong phải viết lại liên tục thì không còn là niêm phong. Niêm trả lời câu hỏi "tri thức có bị sửa ngoài cổng không?", mà `decision_log`/`capability_run`/`run`/`intent`/`error_ledger` thì chính cổng tự ghi vào, theo thiết kế | Loại năm bảng ấy khỏi `content_digest`. **Không mất bảo vệ**: mọi quyết định cũng vào nhật ký `gate.decision`, và nhật ký là chuỗi băm nối tiếp — mạnh hơn một niêm phong đơn; ai sửa `decision_log` để giấu một quyết định vẫn lộ khi đối chiếu bảng với nhật ký. Đề xuất DDD-14 v1.3 chia rõ bảng TRI THỨC (được niêm) và bảng VẬN HÀNH (không niêm, đối chiếu bằng nhật ký)  **Quyết định 07/09/2026: giữ nguyên cách tách.** Đề xuất DDD-14 v1.3 chia rõ hai loại bảng — TRI THỨC (được niêm, đối chiếu bằng `content_digest`) và VẬN HÀNH (không niêm, đối chiếu bằng nhật ký). Việc tách này còn làm được một điều nữa: xóa `decision_log` để dọn dẹp không làm hỏng niêm, nên người dùng dọn được lịch sử vận hành mà không mất khả năng kiểm tra tri thức. | | Đã cập nhật tài liệu — DDD-14 v1.3 |
| DEV-046 | 2026-09-07 | UXD-13 U1 ("ô lệnh có gợi ý `/` liệt kê năng lực có `ui`"); DDD-14 §2 bảng `capability` cột `ui` | `docs/spec/ui/screens.json`, `src/eide_core/registry.py::_ui_tu_spec`, `src/eide/daemon/rpc.py::caps_list` | U1 dựa vào một trường `ui` trên mỗi năng lực, nhưng **0/238 năng lực trong `cds.json` có trường ấy**. DDD-14 khai cột `ui` cho bảng `capability` và TOOL-06 đòi khai nó khi đăng ký năng lực tạm, song `cds_data_*.py` không mang nó cho 238 năng lực gốc | Quy tắc U1 vì thế không áp dụng được như viết: lọc theo một trường luôn rỗng cho ra một menu rỗng. Nhưng nguồn thông tin CÓ tồn tại — bảng §2 của UXD-13 ánh xạ 23 màn hình sang nhóm năng lực (`chat.*`) hoặc năng lực đơn (`project.status`). Cùng khuôn mẫu DEV-025/029/043: dữ liệu nằm trong một bảng của tài liệu nhưng không có dạng máy đọc được, nên phần mã cần nó phải chép tay hoặc bỏ qua | Sinh `ui/screens.json` từ literal `S` của `uxd.js`; `CapabilitySpec.man_hinh` suy `ui` từ đó (`ui` khai tay vẫn thắng, dành cho năng lực `user.*` tự tạo); `caps.list` trả thêm `desc` và `ui` để plugin không phải gọi `caps.describe` 238 lần chỉ để dựng một menu. Kết quả đo: **199/238** năng lực có màn hình, **27** vừa có màn hình vừa đã hiện thực — đó là tập gợi ý `/` thật sự dùng được. Đề xuất UXD-13 v1.1 nói rõ `ui` suy từ bảng §2, hoặc CDS-12 bổ sung cột `ui` cho 238 năng lực | Bác |
| DEV-045 | 2026-09-07 | API-15 §3 (danh sách 16 tool "nhanh" trong văn xuôi) đối chiếu literal `quick` cuối `api.js` | `docs/ho-so/nguon/api.js` | Cùng một tài liệu có HAI danh sách tool nhanh, khác cả số lượng lẫn tên. Văn xuôi §3: 16 tên không khớp năng lực nào (`acquire_request`, `review_patch`, `build`, `size`, `static`, `test`, `feature_status`, `discover_scan`). Literal `quick` sinh ra `mcp_tools.json`: 12 id năng lực thật (`kg.request`, `code.review`, `env.build`, `target.flash`…) | Cùng hình dạng DEV-030 (§3 vs §4 của POL-17): văn xuôi và literal trôi khỏi nhau trong cùng một tài liệu, và literal mới là thứ sinh ra tệp. Người đọc §3 sẽ đi tìm một tool tên `review_patch` không tồn tại | Đề xuất API-15 v1.6 in danh sách §3 RA TỪ literal `quick` thay vì viết lại bằng tay — cùng cách `pol.js` in bảng quy tắc ra từ `RULES`. Chưa sửa: đây là thay đổi văn xuôi thuần, gộp vào lượt đồng bộ tài liệu sau  **Quyết định 07/09/2026:** API-15 v1.6 in danh sách §3 RA TỪ literal `quick`, như `pol.js` in bảng quy tắc ra từ `RULES`. Gộp vào lượt đồng bộ tài liệu sau. | | Đã cập nhật tài liệu — API-15 v1.6 |
| DEV-044 | 2026-09-07 | API-15 §3 ("schema chính là input_schema của năng lực"; khối mã ví dụ có `inputSchema`) | `docs/spec/api/mcp_tools.json`, `src/eide/mcp/server.py::dung_tool` | `mcp_tools.json` chỉ mang `name`/`description`/`capability`/`risk` — KHÔNG có `inputSchema`, dù §3 nói schema là input_schema của năng lực và chính khối mã ví dụ của §3 có trường ấy | Một client MCP đọc tệp ấy không biết gọi tool với đối số nào. Nhưng sinh sẵn schema vào tệp cũng sai: sẽ có HAI bản schema cho cùng một năng lực (`cds.json` và `mcp_tools.json`) và chúng sẽ trôi khỏi nhau — đúng bài học DEV-018 | Chia hai nửa, mỗi nửa một nguồn: `mcp_tools.json` giữ phần CHỌN LỰA (tool nào "hay dùng" — quyết định biên tập thuộc tài liệu); `inputSchema` gắn lúc khởi động từ registry, đúng chữ §3 "sinh từ registry lúc khởi động". Không có bảng schema thứ hai. **Kèm theo**: chỉ phơi năng lực ĐÃ HIỆN THỰC — 7/15 lúc này. Một tool trỏ tới năng lực chưa có handler trả E1001 cho mọi lời gọi nhưng vẫn ăn một suất trong trần 20 và vẫn chiếm chỗ trong phần mô tả mà mô hình phải đọc. `bo_qua()` liệt kê cái bị bỏ và `eide doctor` in ra, để danh sách không trông như đã đủ | Bác |
| DEV-043 | 2026-09-07 | CXD-10 §4.5 (bảng trọng số vị từ PRED_W) | `docs/ho-so/nguon/cxd.js::CXD.pred_weight`, `docs/spec/context/budgets.json` | Công thức xếp hạng của Graph-RAG có HAI bảng trọng số — `EDGE_W` (theo cạnh) và `PRED_W` (theo vị từ). `EDGE_W` nằm trong khối máy đọc được và sinh ra `context/budgets.json`; `PRED_W` chỉ nằm trong văn xuôi ngay câu sau | Hai nửa của cùng một công thức ở hai dạng khác nhau: một nửa có cổng chống trôi, một nửa phải chép tay vào mã. Đúng khuôn mẫu DEV-025 (mã màu UI) và DEV-029 (ngân sách ngữ cảnh) — và cả ba đều là bảng SỐ, loại nội dung dễ chép sai nhất mà không ai thấy | Đưa `pred_weight` vào literal `CXD` cạnh `edge_weight`, sinh ra `context/budgets.json`, cổng `--kiem` của `make check` phủ luôn. Thêm khóa `_mac_dinh: 0.5` cho vị từ ngoài bảng — DDD-14 cho phép `predicate = other`, và văn xuôi §4.5 không nói giá trị nào cho trường hợp ấy. Đề xuất CXD-10 v1.1 in bảng ra từ literal thay vì viết lại trong văn xuôi | | Đã cập nhật tài liệu — CXD-10 v1.1 |
| DEV-042 | 2026-09-07 | DDD-14 §2 RagChunk (cột `embedding`, "float32[] qua Gateway"); CXD-10 §4.5 | `src/eide_core/rag.py` | `RagIndex` dùng FTS5 và khớp `graph_nodes`, chưa dùng embedding | Đường vào của mọi truy vấn ở mốc M1 là một IRI (`chip:st.stm32f411ce/periph:I2C1`), không phải câu hỏi ngôn ngữ tự nhiên. Với đầu vào ấy, khớp chính xác `graph_nodes` cộng FTS5 trả lời đúng và rẻ; nhúng một lời gọi mô hình cho mỗi đoạn chỉ để so cosine với một chuỗi định danh là tốn tiền mà không thêm thông tin | Giữ cột `embedding` và `them()` nhận nó, nên `view.rag_ask` (câu hỏi ngôn ngữ tự nhiên, mốc M2) dùng cùng bảng mà không phải di trú. Không sửa tài liệu — đây là nợ hiện thực có chủ ý, không phải tài liệu sai. Đề xuất CXD-10 v1.1 nói rõ hai đường vào khác nhau: RAG theo IRI (deterministic, M1) và RAG theo câu hỏi (embedding, M2)  **Quyết định 07/09/2026: giữ FTS5, KHÔNG thêm embedding lúc này.** CXD-10 v1.1 nói rõ hai đường vào khác nhau — RAG theo IRI (deterministic, M1) và RAG theo câu hỏi ngôn ngữ tự nhiên (embedding, M2, cho `view.rag_ask`). Cột `embedding` giữ nguyên nên không phải di trú khi tới M2. | | Đã cập nhật tài liệu — CXD-10 v1.1 |
| DEV-041 | 2026-09-07 | APD-08 §3.1; POL-17 §2 (cổng của năng lực) | `src/eide_core/registry.py::gate`, `_gate_tu_spec` | Cổng được ánh xạ theo NHÓM năng lực — một suy đoán của mã vì `cds.json` không có trường `gate`. Đo 07/09/2026: ánh xạ ấy áp cổng cho **73 năng lực** trong khi đặc tả nêu cổng cho **10**; ba nhóm nguyên vẹn (`passport` 8, `discover` 12, `measure` 3) nhận một cổng mà không hợp đồng nào trong đó từng nhắc tới | Đây là DEV-026 lặp lại ở quy mô lớn hơn, lộ ra khi nhóm `kg.*` được hiện thực: `kg.request` — tạo một yêu cầu nhận tri thức — bị hỏi bằng `G-FACT-99` "bạc dưới ngưỡng / OCR / thiếu nguồn hai", câu chẳng liên quan gì tới việc nó làm. Lỗi nằm im tới giờ vì mọi năng lực đã hiện thực trước đó đều rơi vào nhóm không được ánh xạ, tức cổng `*` | Quét `steps`/`ask_when` của cds.json tìm tên cổng trong bảng quy tắc POL-17 §2 — mỗi mục trong bảng nay truy được về một câu trong hợp đồng. Còn **12 năng lực** có cổng ở tầng Router, mỗi cái là một hành động thật (tải nguồn, nạp firmware, merge mã, phát hành gói, chạy công cụ). **G-FACT bị loại khỏi tầng Router theo BẢN CHẤT chứ không phải theo ngoại lệ**: nó là cổng của một FACT, không phải của một hành động — mọi quy tắc của nó hỏi thuộc tính dữ liệu (`fact.tier`, `fact.confidence`, `fact.second_source`), nên năng lực HỎI nó cho từng fact chứ không ĐI QUA nó. Cả ba năng lực đặc tả gắn G-FACT đều vậy. Không sửa tài liệu — đây là mã đoán sai, không phải tài liệu sai | Bác |
| DEV-040 | 2026-09-07 | APD-08 §4.1 tầng 2 và POL-17 §1 hàng T2: "R0 → APPROVE(R0) (bỏ qua tầng sau)" | `src/eide_core/policy.py::decide` | Nhánh R0 của tầng 2 **chưa bao giờ được hiện thực**. Mọi lời gọi R0 rơi xuống quy tắc bắt hết của cổng, vốn luôn là ASK | **Lệch quyết định.** Đo 07/09/2026: **43 năng lực R0** ở bảy cổng đều bị hỏi người — gồm `kg.build`, `search.rank`, `discover.chip_id`, `code.size`, `measure.power`. R0 là lớp CHỈ ĐỌC, nên đây là hỏi người về những việc không gây hiệu ứng nào. Lỗi nằm im vì mọi năng lực hiện thực trước Sprint 2 đều gắn cổng `*`, nơi tầng 5 trả APPROVE cho T1; `kg.*` là nhóm đầu tiên có cổng thật | Hiện thực nhánh R0, nhưng KHÔNG "bỏ qua tầng sau" theo nghĩa đen: (a) quy tắc trong dải chặn (priority ≤ 5) vẫn thắng kể cả khi chỉ ASK — `TOOL-03` chặn công cụ chưa test, mà lớp rủi ro của công cụ tự viết SUY TỪ HIỆU ỨNG NÓ KHAI BÁO và `effects_ok` tồn tại đúng vì khai báo ấy có thể sai; `G-SRC-07` hỏi khi dự án nhạy cảm dùng nguồn đòi gửi dữ liệu ra ngoài, mà vài `search.*` mang nhãn R0 vì "chỉ đọc" — đọc từ một dịch vụ bên ngoài. (b) Quy tắc APPROVE khớp thì MƯỢN mã và lý do, không thì `TOOL-01` thành quy tắc chết và decision_log mất câu giải thích (tình huống S41 bắt được đúng điều này). Cùng nguyên tắc DEV-012: dải chặn chỉ siết. Đề xuất APD-08 v1.3 và POL-17 §1 ghi rõ hai ngoại lệ ấy  **Quyết định 07/09/2026:** APD-08 v1.3 và POL-17 §1 ghi rõ HAI ngoại lệ của nhánh R0 — quy tắc trong dải chặn (ưu tiên ≤ 5) vẫn thắng kể cả khi chỉ ASK, và quy tắc APPROVE khớp thì mượn mã cùng lý do. Không ghi ra thì lần hiện thực sau sẽ đọc "bỏ qua tầng sau" theo nghĩa đen và mở lại đúng hai lỗ hổng ấy. | | Đã cập nhật tài liệu — POL-17 v1.3 |
| DEV-039 | 2026-09-07 | CDS-12.2 KG-01 bước 1 ("NetworkX từ store (M0)") | `src/eide_core/kg.py` | Hợp đồng nêu đích danh NetworkX; hiện thực dùng từ điển kề + BFS của thư viện chuẩn | Mọi phép toán mà chín năng lực `kg.*` cần đều nông: đếm nút/cạnh (KG-01), gom fact cùng subject+predicate (KG-02), lần ngược một–hai bước (KG-03), BFS **≤ 2 bước** có trần 500 nút (KG-04). Không phép nào cần đường đi ngắn nhất, thành phần liên thông, độ trung tâm hay bố cục — tức không dùng gì của NetworkX ngoài `dict` của nó. Thêm một phụ thuộc để dùng `dict` thì SAD-03 ADR-16 ("ít phụ thuộc thì chạy được trên cả ba nền tảng") không còn nghĩa | Giữ thư viện chuẩn. Thêm NetworkX khi `view.kg_map`/`diagram.kg_view` cần thuật toán thật (bố cục, phân cụm) — lúc ấy có lý do cụ thể, không phải vì tài liệu nhắc tên nó một lần. Đề xuất CDS-12.2 v1.2 đổi bước 1 thành "dựng đồ thị từ store; cache theo băm nội dung" và bỏ tên thư viện khỏi hợp đồng: chọn thư viện là việc của thiết kế chi tiết, không phải của hợp đồng năng lực  **Quyết định 07/09/2026:** CDS-12.2 v1.2 đổi bước 1 của KG-01 thành "dựng đồ thị từ store; cache theo băm nội dung" và bỏ tên thư viện khỏi hợp đồng — chọn thư viện là việc của thiết kế chi tiết, không phải của hợp đồng năng lực. | | Đã cập nhật tài liệu — CDS-12.2 v1.2 |
| DEV-038 | 2026-09-07 | KAD-07 §6.3 (bảng chín cạnh của đồ thị tri thức) | `src/eide_core/kg.py::CANH` | Bảng cạnh KHÔNG có cạnh nào nối một Fact với chính subject mà nó nói về. Chín cạnh nêu ra nối Fact với Source (CITES), CodeUnit (CITES), Fact khác (SUPERSEDES, CONFLICTS_WITH) và Measurement (EVIDENCED_BY) | Với đúng chín cạnh ấy, đồ thị KHÔNG liên thông theo hướng cần dùng: câu hỏi "biết gì về `chip:…/periph:I2C1`" không trả lời được bằng duyệt đồ thị, vì fact về I2C1 không nối tới nút I2C1. Mà đó chính là việc của KG-04 `kg.neighborhood`, có tc ghi "Có PB6/PB7 và BME280" | Thêm cạnh `ABOUT` (Fact → subject IRI). Tối thiểu và không thay cạnh nào đang có. Đề xuất KAD-07 v1.3 bổ sung `ABOUT` vào bảng §6.3  **Quyết định 07/09/2026:** KAD-07 v1.3 bổ sung cạnh `ABOUT` (Fact → subject IRI) vào bảng §6.3. Không cạnh ấy thì đồ thị không liên thông theo hướng cần dùng, và KG-04 mất đúng việc của nó. | | Đã cập nhật tài liệu — KAD-07 v1.3 |
| DEV-037 | 2026-09-07 | DEV-29 §4 (sinh lại tài liệu từ nguồn) | `docs/ho-so/nguon/excel/build_plan.py`, `build_uc_v12.py`, `scripts/sinh_tai_lieu.sh`, `scripts/so_tai_lieu.py` | Bốn tệp Excel của bộ hồ sơ có bộ sinh ở `nguon/excel/`, nhưng hai trong số đó đọc `caps.json`/`cds.json` từ **`/home/claude/eide-docs/`** — đường dẫn tuyệt đối của MỘT MÁY KHÁC. Chúng chưa từng chạy được trong kho này. Ngoài ra `--kiem` so xlsx bằng `cmp`, mà xlsx cũng là zip có dấu thời gian nên luôn báo lệch | Cùng họ DEV-018 và DEV-034: quy trình đồng bộ có, công cụ thực hiện nó thì hỏng ở chỗ không ai chạy tới. Phát hiện khi cần sửa PLN-27 cho DEV-003 | Bỏ đường dẫn tuyệt đối (đọc ở cwd của thư mục dàn dựng); thêm bí danh `sinh_tai_lieu.sh excel` chạy `build_plan.py`; mở rộng bộ so sánh nội dung sang xlsx và đổi tên `so_docx.py` → `so_tai_lieu.py`. Bộ so sánh đọc CÔNG THỨC chứ không đọc giá trị đã tính: tệp vừa sinh chưa từng được Excel mở nên cache rỗng, so theo giá trị báo 21 chỗ lệch giả trong khi thật ra chỉ có 2. Ba tệp Excel còn lại CHƯA nối vào: `build_uc_v12.py` cần đầu ra của `build_xlsx.py` (chuỗi hai bước), `build_caps.py` và `build_audit.py` chưa đối chiếu được với bản trong kho — nối vào khi chưa chứng minh sinh lại đúng thì cổng sẽ báo lệch giả | Bác |
| DEV-036 | 2026-09-07 | DEV-29 §4 và §8 tiêu chí 3 ("sau mỗi đồng bộ, phiên bản tài liệu tăng") | `docs/ho-so/nguon/eide_common.js::meta` | Hàm `meta()` — dùng bởi 12 tài liệu gốc — viết cứng `version: '1.2'` và ba hàng lịch sử 1.0/1.1/1.2, không có tham số nào để thêm hàng mới. Chỉ `metaNew()` (19 tài liệu bổ sung) mới nhận `lichSu`. | Nghĩa là vòng đồng bộ **không nâng phiên bản được cho nhóm tài liệu gốc**, tức tiêu chí §8.3 không đạt được cho SDD-04, SAD-03, SRS-02, URD-01, STP-05, BPD-06, KAD-07, APD-08, DPS-09, PDA-00, CON-28, DEP-26. Phát hiện theo cách xấu nhất: tôi chèn hàng lịch sử vào `history10` — chỗ duy nhất trông có vẻ nhận được — và docx ra "1.0 → 1.1 → **1.1** → 1.2", một hàng 1.1 thứ hai nằm TRƯỚC hàng 1.1 gốc. Trước đó, ở DPS-09 tôi truyền đối số thứ sáu cho `meta()` và nó bị **bỏ qua im lặng** vì chữ ký khác `metaNew()`. Cùng họ DEV-018: quy trình đồng bộ có nhưng công cụ thực hiện nó thì hỏng | Thêm tham số `lichSu13` vào `meta()`, nối SAU hàng 1.2, và cập nhật `version` cùng thuộc tính "Phiên bản" theo hàng cuối — cùng cách `metaNew` đã làm. Đã dùng thật: SDD-04 lên v1.3, DPS-09 lên v1.1 (tài liệu này ghim ở 1.0 vì ra đời cùng bộ v1.1, nên cắt ba hàng mặc định rồi nối riêng). Không sửa tài liệu — đây là công cụ hỏng | Bác |
| DEV-035 | 2026-09-07 | DPS-09 §4.1 (enum ý định, 19 giá trị) đối chiếu danh mục 238 năng lực / 27 nhóm | `docs/spec/dialog/intent.schema.json`, `docs/spec/dialog/commands.jsonl` | Ý định `big_command` mang HAI nghĩa khác nhau: (a) lệnh thật sự nhiều bước trải nhiều nhóm ("làm hết đi", "bộ tài liệu đầy đủ"), và (b) lệnh MỘT bước thuộc nhóm năng lực chưa có ý định riêng. Đo được: **14 trong 27 nhóm không có ý định nào** — `registry`, `bench`, `measure`, `passport`, `kg`, `tool`, `search`, `archive`, `extract`, `board`, `plan`, `report`, `chat`, `memory` | Phát hiện khi rà DEV-022: ba câu 45/46/50 của bộ 50 câu ("đóng gói lên registry", "chạy benchmark", "đo dòng tiêu thụ") mang `intent=big_command` nhưng `is_big=false` — người gán nhãn thấy đúng là lệnh đơn, chỉ không có ý định nào để gán. **Tôi đã đọc nhầm chỗ ấy thành lỗi của cột `is_big` và sửa ba dòng sang `true`, tức dịch chuyển nhầm cột**; đã trả về nhãn gốc 07/09/2026. Hệ quả còn lại: một lệnh `registry.publish` (R4, cổng G5) và một lệnh `measure.power` (R2) tới Orchestrator dưới cùng một nhãn, nên tầng hiểu lệnh không phân biệt được chúng bằng ý định | Chủ sản phẩm quyết định 07/09/2026 hoãn sang sau: thêm ý định bây giờ buộc gán nhãn lại cả 50 câu và viết lại lớp C0, rủi ro làm tụt TC-59 đang ở 100%. Giải quyết khi làm CHAT-06 `chat.orchestrate` (M2) — lúc ấy Orchestrator phải chọn năng lực cho từng ý định nên vấn đề tự lộ ra dưới dạng một bảng ánh xạ thiếu. Trong lúc chờ: DPS-09 §4.1 v1.1 nói rõ `big_command` KHÔNG kéo theo `is_big=true`, và `dialog/intents.md` (lớp C0) mang cùng câu ấy để mô hình không suy sai  **Quyết định 07/09/2026: giữ hoãn tới CHAT-06, nhưng chốt HƯỚNG để lần sau không phải cân nhắc lại** — thêm ý định cho từng nhóm năng lực còn thiếu, không mở rộng nghĩa của `big_command`. Lý do: `big_command` mô tả HÌNH DẠNG của lệnh (nhiều bước), còn nhóm năng lực mô tả LĨNH VỰC; trộn hai trục vào một enum thì mỗi lần thêm nhóm lại phải sửa nghĩa của một nhãn đang dùng. Khi làm: gán nhãn lại bộ 50 câu và đo lại TC-59 trước/sau. | Đã cập nhật tài liệu v1.3 |
| DEV-034 | 2026-09-06 | DEV-29 §4 (đối chiếu tài liệu với nguồn sinh) | `scripts/sinh_tai_lieu.sh::chep`, `scripts/so_docx.py` | `--kiem` so docx bằng `cmp` (so BYTE). docx là tệp zip, mỗi mục mang dấu thời gian nén, nên hai lần sinh từ CÙNG một nguồn cho ra hai tệp khác byte | Cổng đối chiếu vì thế báo "LỆCH nguồn" cho **mọi** tài liệu, kể cả tài liệu đang đồng bộ hoàn hảo — đo 06/09/2026: pol, prs, cxd đều ✗ dù pol và prs khớp từng khối. Một cổng luôn đỏ là một cổng không ai đọc, và nó che mất lần lệch thật; DEV-018 dựng ra vòng đồng bộ này chính là để tránh chỗ ấy | Thêm `scripts/so_docx.py` so theo KHỐI (đoạn văn và bảng, giữ đánh dấu đậm/nghiêng, bỏ dấu thời gian và id quan hệ); `chep()` dùng nó cho `.docx` và `cmp` cho phần còn lại, và in ra khối lệch thay vì chỉ báo ✗. Kết quả quét toàn bộ 22 bộ sinh: 8 khớp, 14 lệch **đúng một khối** — mục tham khảo cuối mà `eide_common.js` mới thêm. Không tài liệu nào lệch nội dung thật. Không sửa tài liệu — đây là công cụ hỏng, không phải tài liệu sai | Bác |
| DEV-033 | 2026-09-06 | POL-17 §2 (bảng quy tắc, `G-OPS-04` ưu tiên 10 vs `G-OPS-05` ưu tiên 5) | `src/eide_core/policy.py::_Ns.__bool__` | **Lệch quyết định, không phải lệch nhãn.** `_Ns` khai trong docstring "thuộc tính thiếu trả None (đặc trưng chưa biết ⇒ không khớp APPROVE)", nhưng nó chỉ giữ lời ở nhánh thuộc tính. Một đặc trưng viết dưới dạng TÊN TRẦN mà không ai cung cấp thì `_Env.__missing__` trả `_Ns({})`, và object không có `__bool__` nên nó mang tính ĐÚNG. Trong 46 quy tắc chỉ có một tên trần kiểu bool: `needs_sudo` của `G-OPS-05` | `needs_sudo` luôn đúng ⇒ `G-OPS-05` (ưu tiên 5, ASK) luôn khớp ⇒ che hẳn `G-OPS-04` (ưu tiên 10, APPROVE) ⇒ **không gói nào trong `trusted_packages` được duyệt tự động**, và cả danh sách gói tin cậy trở thành vô nghĩa. Cùng họ với DEV-012 (quy tắc chết) nhưng nặng hơn vì nó vô hiệu hóa một trong ba danh sách trắng mà WI-257 vừa được ký. 45 tình huống §7 vẫn xanh suốt vì không tình huống nào bỏ trống `needs_sudo`. Phát hiện khi đo lại G-OPS sau khi ký danh sách trắng, không phải khi đọc mã | Đã sửa: `_Ns.__bool__` trả `bool(self._d)`, nên tên trần vắng mặt và thuộc tính vắng mặt cùng là SAI. Không sửa tài liệu — bảng quy tắc §2 vốn đúng, động cơ làm sai. **Còn nợ**: `features` khai trong mỗi quy tắc dùng từ vựng KHÔNG QUALIFY (`G-OPS-01` khai `passed_g3` trong khi biểu thức viết `artifact.passed_g3`), nên chưa cưỡng chế được điều POL-17 §2 hứa — "đặc trưng do năng lực cung cấp" — bằng cách bỏ qua quy tắc thiếu đặc trưng. Đề xuất POL-17 v1.3 thống nhất từ vựng `features` với đường dẫn trong `when` để động cơ phân biệt được "đặc trưng SAI" với "đặc trưng CHƯA CUNG CẤP" | POL-17 §1 v1.2 nay ghi thành quy tắc: đặc trưng chưa được cung cấp luôn coi là SAI, kể cả dạng tên trần. | Đã cập nhật tài liệu v1.2 |
| DEV-032 | 2026-09-06 | POL-17 §2 quy tắc `G-OPS-06` (`not board.lab`, ưu tiên 20) | `docs/spec/policy/rules.yaml` (chưa sửa) | `G-OPS-06` không có điều kiện nào về `op`, nên nó khớp MỌI hành động qua cổng G-OPS, kể cả `op == "install"` vốn chẳng liên quan gì tới board | Không lệch quyết định (mọi nhánh đều ra ASK), nhưng lệch LÝ DO. **Phạm vi hẹp hơn ghi nhận đầu:** khi danh sách trắng ĐÃ KÝ thì `G-OPS-05` bắt trước và lý do đúng. Chỉ khi danh sách CHƯA KÝ — tức ngay sau khi ai đó sửa tay `autonomy.yaml`, đúng lúc người ta cần lý do chính xác nhất — thì `trusted_packages` bị bỏ, `G-OPS-05` không đánh giá được, và mọi lệnh cài rơi vào `G-OPS-06` với dòng "Board chưa đánh dấu lab (đề nghị đánh dấu nếu không có cơ cấu chấp hành)". Người duyệt đọc dòng ấy sẽ đi đánh dấu board — tức được mời làm đúng một thao tác nới lỏng chính sách, cho một vấn đề không nằm ở board. Cùng họ với DEV-011/DEV-012: lý do trong nhật ký là thứ người dựa vào để quyết | Thêm điều kiện `op` vào `G-OPS-06` để nó chỉ nói về thao tác trên board: `not board.lab and op in ["flash","reset","rtt","read_mem","write_ram","experiment","experiment_no_actuator"]` — đúng bảy `op` có thể tới được ưu tiên 20 (tập của `G-OPS-01` cộng `experiment`, vì 02/03 đã bắt phần còn lại ở ưu tiên 1). Đã đo: `install` chuyển từ `G-OPS-06` sang `G-OPS-99`, `flash` lên board chưa lab giữ nguyên `G-OPS-06`. **Kèm theo, ở phía mã**: khi danh sách trắng chưa ký thì PolicyGate nên ghi hẳn lý do ấy vào quyết định thay vì để lại chữ "Mặc định" trống nghĩa — người đọc decision_log cần biết cổng đang chạy thiếu danh sách. Chưa sửa: chủ sản phẩm chọn duyệt tài liệu theo nhóm, nên mục này chờ cùng các mục POL khác | Chủ sản phẩm duyệt 06/09/2026 phương án sửa cả hai phía. POL-17 §2 v1.2: `G-OPS-06` thêm điều kiện `op`. Phía mã: `PolicyGate._ly_do` nối thêm "danh sách trắng chưa được ký (chạy `eide policy sign`)" vào lý do của quy tắc bắt hết khi niêm không đạt. | Đã cập nhật tài liệu v1.2 |
| DEV-031 | 2026-09-06 | POL-17 §3 ("ghi băm nội dung + user + thời điểm vào decision_log"; "hành động R4 theo **cổng riêng**") đối chiếu API-15 §5 `ledger_events.json` và POL-17 §2 | `src/eide_core/whitelist.py::KIND, GATE_WL` | §3 hứa hai thứ mà không tài liệu nào định nghĩa: (a) một bản ghi decision_log cho việc ký — nhưng 23 kiểu sự kiện của API-15 §5 không có kiểu nào cho nó; (b) một "cổng riêng" cho việc đổi danh sách — nhưng bảng quy tắc §2 không có cổng nào như vậy trong 46 quy tắc (chỉ có G-SRC, G-FACT, G1, G3, G-OPS, G4, G5, G-TOOL, `*`) | `Ledger.append` từ chối kiểu ngoài `ledger_events.json` (E6001), nên phải chọn một kiểu đã có. Không ghi gì thì mất luôn bước đối chiếu chéo `.sig` ↔ nhật ký — mà đó chính là thứ duy nhất làm cho niêm không chỉ là một tệp cạnh một tệp | Tạm dùng `gate.human {gate_id, decision, by, note}` với `gate_id: "G-WL"`, cộng thêm khóa `hash` (ngoài bốn trường tài liệu nêu) vì `kiem()` phải so được băm; để băm trong `note` dạng văn xuôi thì bước đối chiếu thành ra phân tích chuỗi tự do. Đề xuất API-15 v1.3 thêm kiểu `policy.sign {hash, by, keys, alg}` và POL-17 §2 định nghĩa cổng `G-WL` — cùng hình dạng với đề xuất của DEV-013 | Chủ sản phẩm duyệt 06/09/2026 phương án định nghĩa đủ cả hai. POL-17 §2 v1.2 thêm cổng `G-WL` (ba quy tắc; tác tử chạm danh sách là REJECT chứ không phải ASK) và §8 thêm S46–S48 phủ cổng ấy; API-15 §7 v1.2 thêm kiểu `policy.sign {hash, by, keys[], alg}`. `whitelist.py` bỏ cách tạm `gate.human`. | Đã cập nhật tài liệu v1.2 |
| DEV-030 | 2026-09-06 | POL-17 §3 ("Ba danh sách trong autonomy.yaml: `trusted_sources`, `trusted_packages`, `lab_boards`") vs §4 (schema autonomy.json) | `docs/spec/policy/{defaults.yaml,autonomy.schema.json}`, `src/eide_core/whitelist.py::KHOA_NIEM` | §3 và §4 của CÙNG MỘT tài liệu mâu thuẫn về danh sách thứ ba. §3 gọi nó là `lab_boards` (mảng board id); §4 không có khóa ấy, đặt board ở `boards` (object: id → {lab, has_actuator, autonomy, reason}) và khai `additionalProperties: false` | §4 là schema chuẩn tắc và `additionalProperties: false` nên nó **từ chối** đúng cái tên mà §3 bảo dùng — làm theo §3 thì tệp không hợp lệ. §4 cũng giàu hơn và có sẵn `has_actuator`, đúng thứ §3 đòi người xác nhận khi đánh dấu board lab. Trước WI-257 chưa ai kiểm autonomy.yaml theo schema nên mâu thuẫn này nằm im | Theo §4: dùng `boards`, để RỖNG có chủ ý vì đánh dấu board lab đòi người xác nhận hai điều chỉ người cầm board mới biết. Sinh `policy/autonomy.schema.json` từ §4 (`AUTONOMY_SCHEMA` trong `pol.js`) và đưa vào cổng chống trôi của `make check`; test chốt rằng schema từ chối `lab_boards`. Ở mức A2 hiện tại `boards` chưa đổi gì: thao tác R3 đã bị ngưỡng cứng ép hỏi người trước khi G-OPS được hỏi tới. Đề xuất POL-17 v1.3 sửa văn xuôi §3 thành `boards` cho khớp §4 | Chủ sản phẩm duyệt 06/09/2026. POL-17 §3 v1.2 sửa `lab_boards` → `boards`, nói "bốn danh sách", và bổ sung ba điểm về giới hạn của niêm. | Đã cập nhật tài liệu v1.2 |
| DEV-029 | 2026-09-06 | CXD-10 §3 (bảng ngân sách, thứ tự cắt, hằng ước lượng token), §4.2, §4.5 | `docs/ho-so/nguon/cxd.js`, `docs/spec/context/budgets.json`, `src/eide_core/composer.py` | Bảng ngân sách theo vai trò, thứ tự cắt, hằng "3,5 ký tự ≈ 1 token", trọng số cạnh Graph-RAG và k0 chỉ tồn tại trong VĂN XUÔI §3–§5 — không có dạng máy đọc được, dù Composer, `models.yaml` và tài liệu đều cần chúng | Cùng khuôn mẫu DEV-015/DEV-021/DEV-025: một bảng số nằm trong văn xuôi thì mã phải chép tay, và bản chép sẽ trôi. Riêng ở đây còn một hệ quả nữa — §9 của CXD-10 nói về "đo lường và tinh chỉnh" ngân sách, mà không có tệp cấu hình thì không tinh chỉnh được gì | `cxd.js` sinh `context/budgets.json`; `composer.py` đọc từ đó, không viết cứng số nào. `--kiem` trong `make check` và CI. Đề xuất CXD-10 v1.3 trỏ §3 sang `context/budgets.json` | Bác |
| DEV-028 | 2026-09-06 | SDD-04 §6 `models.yaml` `policy.fallback_on` | `src/eide_core/gateway.py::run`, `ModelPort.co_khoa` | Vai trò có Claude đứng đầu (`planner`, `architect`, `reviewer`, `debugger`, `writer`) KHÔNG chạy được trên máy chỉ có khóa Gemini: thiếu khóa bị coi là một lỗi gọi, mà `no_key` không nằm trong `fallback_on`, nên Gateway dừng thay vì dùng ứng viên sau | Phát hiện khi chạy ToolForge thật: `tool.need` (vai trò planner) chết ngay dù Gemini đứng ngay sau và dùng được. Thêm `no_key` vào `fallback_on` sẽ che mất lỗi cấu hình thật (khóa sai, khóa hết hạn) — không phải cách sửa đúng | Phân biệt VẮNG MẶT với HỎNG: `ModelPort.co_khoa()` cho biết nhà cung cấp đã cấu hình chưa, và `run` loại ứng viên chưa cấu hình khỏi danh sách TRƯỚC khi gọi — không ghi vào ledger như một lỗi, không tiêu một lượt lui. Không nhà cung cấp nào cấu hình thì báo E5000 nêu rõ cần khóa nào. Đề xuất SDD-04 v1.3 ghi rõ phân biệt này ở §6 | Bác |
| DEV-027 | 2026-09-06 | CDS-12.3 TOOL-01 `output_schema.reuse` | `src/eide/caps/tool.py::need` | Hiện thực đầu trả `reuse` là một `tool_id` hoặc `null`; hợp đồng khai MẢNG object ("công cụ/năng lực gần khớp") | Lỗi của hiện thực, không phải của tài liệu — Router bắt bằng E6001 ngay lần chạy thật đầu tiên | Trả danh sách ứng viên kèm điểm. Đúng hơn về mặt sản phẩm: người quyết định dùng lại cần thấy các ứng viên, chứ không phải một lựa chọn đã bị máy chốt sẵn | Bác |
| DEV-026 | 2026-09-06 | CDS-12.3 TOOL-05 bước 2 (`gate=G-TOOL`); POL-17 §2 quy tắc G-TOOL; `cds.json` không có trường `gate` | `src/eide_core/registry.py::CapabilitySpec.gate`, `src/eide/caps/tool.py::run` | Mã gán CẢ nhóm `tool.*` vào cổng G-TOOL, nhưng mọi quy tắc G-TOOL đều hỏi `tool.tested`/`tool.effects_ok`/`tool.uses_ok` — tức đặc trưng của một công cụ ĐANG ĐƯỢC CHẠY. Hệ quả: `tool.write` luôn bị REJECT bởi quy tắc TOOL-03 (`not tool.tested`), vì một công cụ chưa viết thì đương nhiên chưa test | Vòng luẩn quẩn giống DEV-020: năng lực tạo ra thứ mà cổng đòi hỏi lại bị chính cổng ấy chặn. CDS-12.3 chỉ nói `gate=G-TOOL` ở ĐÚNG MỘT chỗ — TOOL-05 bước 2 | `tool.*` đi cổng chung `*` ở tầng Router; riêng `tool.run` tự hỏi G-TOOL với đặc trưng THẬT của công cụ, đúng như TOOL-05 bước 2 mô tả — đó cũng là nơi duy nhất các đặc trưng ấy tồn tại. Đề xuất: `cds.json` mang thêm trường `gate` cho từng năng lực để ánh xạ không còn là suy đoán của mã | Bác |
| DEV-025 | 2026-09-06 | UXD-13 §7 (token thiết kế PTIT) | `docs/ho-so/nguon/uxd.js`, `docs/spec/ui/tokens.json`, `scripts/gen_ui_swift.py` | Token thiết kế chỉ tồn tại trong VĂN XUÔI §7 — không có dạng máy đọc được, dù ba chỗ dùng chúng: 23 mockup HTML, panel EIDE trong GEditor (Swift/AppKit) và chính tài liệu | Chép tay màu vào Swift nghĩa là lần xác nhận nhận diện tiếp theo (WI-258) phải sửa hai nơi và một nơi sẽ bị quên — mà "quên" ở đây là một sản phẩm có hai sắc đỏ khác nhau trên cùng màn hình. Ngoài ra U10 ("tương phản ≥ 4,5:1 WCAG 2.2 AA") là một câu không ai đo được nếu ngưỡng không nằm ở dạng máy đọc | `uxd.js` nay sinh `ui/tokens.json` (16 màu + font, space, radius, layout, `contrastMin`); `scripts/gen_ui_swift.py` sinh `EideTokensGenerated.swift`; `--kiem` nằm trong `make check` và CI. Có test Swift ĐO tương phản thật cho mọi cặp màu chữ/nền. Đề xuất UXD-13 v1.3 trỏ §7 sang `ui/tokens.json` | Bác |
| DEV-024 | 2026-09-06 | API-15 §3 (mã lỗi kèm cách xử lý) | `src/eide/daemon/rpc.py::_err` | Daemon trả `-32602` (tham số sai) TRẦN, không kèm `data.eide_code`, trong khi API-15 §3 có sẵn E1000 INVALID_ARGS đúng cho trường hợp ấy | Test round-trip Swift bắt được: client nhận `-32602` thì không tra được mã tài liệu, nên phía giao diện chỉ có một con số âm để đưa cho người dùng — trong khi §3 quy định mỗi mã lỗi đi kèm CÁCH XỬ LÝ | Đính `{eide_code: E1000, name: INVALID_ARGS}` vào `error.data`. Không sửa tài liệu — đây là hiện thực còn thiếu, không phải tài liệu sai | Bác |
| DEV-023 | 2026-09-06 | API-15 §2 `openrpc.json` (OpenRPC 1.3 §methods) | `docs/ho-so/nguon/api.js`, `docs/spec/api/openrpc.json` | Hai mục `methods[].name` chứa HAI tên gộp bằng " / ": `event.undo.registered / event.undo.expired` và `event.doc.stale / event.diagram.stale`. OpenRPC 1.3 quy định `name` là MỘT tên phương thức | Một client sinh từ tài liệu ấy không gọi được cái nào trong hai, và trình biên dịch Swift báo lỗi cú pháp ngay khi sinh — đó là cách phát hiện. `openrpc.json` khai 55 phương thức nhưng thật ra đặt tên 57 | Tách thành bốn mục riêng ở `api.js` rồi sinh lại (`scripts/sinh_tai_lieu.sh api`): nay 57 mục, không mục nào chứa dấu `/`. `ledger_events.json` cũng dùng lối gộp "a / b" nhưng ở đó là định dạng riêng và `Ledger.event_kinds()` đã tách sẵn — không phải lỗi | Bác |
| DEV-022 | 2026-09-06 | PRS-16 §7 (bộ 50 câu lệnh có nhãn); DPS-09 §7 ("is_big đúng 100%") | `docs/spec/dialog/commands.jsonl`, `tests/test_chat.py::test_tc59_bo_50_cau_lenh_that` | Cột `is_big` của fixture TỰ MÂU THUẪN. Ba dòng có `intent=big_command` nhưng `is_big=false` (câu 45, 46, 50) — trái chính định nghĩa "lệnh gồm nhiều bước" của §4.1, vì `big_command` theo nghĩa là lệnh trải nhiều nhóm. Và bốn dòng cùng `intent=project.create` nhận nhãn True/False/False/True (câu 1, 32, 33, 34) mà không quy tắc phân biệt nào được nêu ở đâu | Ngưỡng "is_big đúng 100%" của TC-59 vì thế KHÔNG đạt được: mô hình đúng theo định nghĩa vẫn bị chấm sai. Đã đo: sau khi sửa ba dòng 45/46/50 ở nguồn, `is_big` lên 48/50, hai câu còn lại là 1 và 34 | Đã sửa ba dòng 45/46/50 tại `prs.js` theo định nghĩa của §4.1. Đề xuất PRS-16 v1.3: (a) gán lại `is_big` bằng một QUY TẮC ghi ra được (big_command ⟹ true; lệnh nở thành chuỗi mẫu §4.4 ⟹ true) thay vì gán tay từng dòng; (b) tách một bộ câu GIỮ RIÊNG chưa dùng để chỉnh prompt, vì `dialog/intents.md` đã được viết dựa trên chính 50 câu này nên con số intent 100% là số regression, không phải ước lượng khái quát | **Ghi nhận đầu SAI CHIỀU, đã sửa 07/09/2026.** Ba câu 45/46/50 mang `intent=big_command` nhưng `is_big=false` KHÔNG phải mâu thuẫn của cột `is_big`: chúng là lệnh một bước thuộc nhóm registry/bench/measure vốn không có ý định riêng (DEV-035), nên người gán nhãn đã đúng cả hai cột. Tôi đổi `is_big` sang `true` cho "khớp" là dịch chuyển nhầm cột, và còn viết bảng `INTENT_MO_TA` cho khớp nhãn mình vừa đổi thay vì đối chiếu §4.1. Đã trả về nhãn gốc. Chủ sản phẩm duyệt phương án viết PHÉP THỬ thay cho gán tay: lệnh gọi toàn bộ một chuỗi mẫu §4.4 → true, lệnh gọi một nút của chuỗi → false (PRS-16 §7 v1.1, DPS-09 §4.1 v1.1, và lớp C0 mang cùng câu ấy). **Đo với Gemini thật 07/09/2026: intent 50/50 = 100%, is_big 48/50.** Hai câu còn lại thuộc hai loại khác nhau và không được trộn: #13 "Thêm tính năng đọc MPU6050" — mô hình trả true, fixture ghi false, mà "thêm tính năng" đúng là tên chuỗi Z-05 nên khả năng cao FIXTURE sai, chờ chủ sản phẩm gán lại; #34 "Create a new project for a balancing robot" — mô hình trả false trong khi câu 1 là đúng lệnh ấy bằng tiếng Việt và nó trả true, tức MÔ HÌNH trả lời khác nhau cho cùng một lệnh ở hai thứ tiếng, và DPS-09 có trường `lang` vì sản phẩm nhận cả hai. Còn lại: (b) tách một bộ câu giữ riêng chưa dùng để chỉnh prompt, vì `intents.md` viết dựa trên chính 50 câu này nên 100% là số regression | Đã cập nhật tài liệu v1.1 |
| DEV-021 | 2026-09-06 | DPS-09 §4.1 (enum ý định) đối chiếu POL-17 GEN-03; CXD-10 (lớp C0); PRS-16 §7 câu 47 | `docs/ho-so/nguon/dps.js`, `docs/spec/dialog/{intent.schema.json,intents.md}`, `src/eide/caps/chat.py::_c0` | Hai lỗ hổng cùng một chỗ. (a) Enum ý định KHÔNG có `project.delete`, trong khi POL-17 `GEN-03` lại có hẳn quy tắc chặn `action.is_delete_project` — nên bộ 50 câu buộc phải gán "Xóa dự án test-1" thành `project.create`, tức dạy tầng hiểu lệnh đọc "xóa" thành "tạo". (b) `prompts/intent.md` viết "chọn năng lực không có trong danh sách C0" — tức GIẢ ĐỊNH lớp C0 được cấp — nhưng C0 không tồn tại ở đâu trong `docs/spec/` | (a) là lỗi an toàn: một tác tử hiểu "xóa dự án" thành "tạo dự án" là đúng thứ GEN-03 sinh ra để chặn, mà lại chặn không được vì ý định đã sai từ đầu. (b) đo được bằng số: không có C0, TC-59 chỉ đạt **76%**; có C0, đạt **96–100%** | Thêm `project.delete` vào enum và gán lại câu 47; thêm bảng `INTENT_MO_TA` + `INTENT_PHAN_BIET` cạnh enum trong `dps.js`, sinh ra `dialog/intents.md`, và `chat.parse_intent` nạp nó làm C0. `scripts/gen_dialog_spec.js --kiem` kiểm enum và bảng mô tả phủ nhau HAI CHIỀU. Đề xuất DPS-09 v1.3 ghi `project.delete` vào §4.1 và mô tả lớp C0 của vai trò intent | Chủ sản phẩm duyệt 07/09/2026. DPS-09 §4.1 v1.1 ghi `project.delete`, mô tả lớp C0 kèm ba con số đo được (76% / 82% / 100%), và nói rõ `big_command` không kéo theo `is_big=true`. | Đã cập nhật tài liệu v1.1 |
| DEV-020 | 2026-09-06 | CDS-12.6 CHAT-04 `chat.clarify` mức T2; APD-08 §4.1 tầng 5 | `src/eide_core/registry.py::TIER_SUA` | `chat.clarify` khai mức **T2** — "cần người duyệt" — nhưng nó CHÍNH LÀ cơ chế hỏi người | Vòng luẩn quẩn: PolicyGate trả ASK, lời gọi rơi vào hàng đợi với một câu hỏi CHƯA TỪNG ĐƯỢC DỰNG, và người chờ một câu hỏi không bao giờ hiện ra. Hệ quả là D3 của DPS-09 ("gộp mọi điểm mơ hồ thành một câu") không chạy được, và chỉ số §7 "≤ 1 câu hỏi trên một lệnh" không đo được | Coi `chat.clarify` là T1 qua bảng `TIER_SUA` — một nơi duy nhất, có tên, test soi được — thay vì rải ngoại lệ trong PolicyGate. Đề xuất CDS-12.6 v1.3 đổi CHAT-04 thành T1: nó không gây hiệu ứng nào ngoài việc hiện một câu hỏi, còn quyền quyết vẫn thuộc người trả lời | Chủ sản phẩm duyệt 07/09/2026. CDS-12.6 v1.1 đổi CHAT-04 thành T1; `TIER_SUA` nay RỖNG — không năng lực nào còn chạy khác mức hợp đồng khai. | Đã cập nhật tài liệu v1.1 |
| DEV-019 | 2026-09-06 | DEV-29 §4 (sinh lại tài liệu từ nguồn) | `docs/ho-so/nguon/dps.js`, `scripts/gen_dialog_spec.js` | `dps.js` mở đầu bằng `require('./dialog.json')` nhưng **không có tệp `dialog.json` nào trong kho** — nên DPS-09 chưa từng sinh lại được, khác với `pol.js` và `prs.js` đã chạy thật | Không sinh lại được thì mọi mục DEVIATIONS chạm DPS-09 sẽ nằm Mở vĩnh viễn (cùng hình dạng với DEV-018) | Tạm thời: `scripts/gen_spec_tu_nguon.js` rút thẳng literal `INTENT_SCHEMA`/`INTENT_MO_TA` ra khỏi `dps.js` — chúng không phụ thuộc `dialog.json` — nên `docs/spec/dialog/` vẫn sinh lại và đối chiếu được. **ĐÃ ĐÓNG 06/09/2026:** chủ sản phẩm xác nhận tệp gốc đã mất và duyệt dựng lại từ chính docx. `scripts/khoi_phuc_dialog_json.py` rút ba khóa `WHERE`/`RULES`/`SCENARIOS` từ bảng docx, dựng ngược đánh dấu `**đậm**` từ run, và bóc phần in đậm mà `T()` TỰ thêm cho hàng tiêu đề và cột đầu (`bold: i === 0`) — giữ lại phần ấy thì bản dựng lại sinh ra `****D1****`. **Bằng chứng đúng:** sinh lại DPS-09 rồi so nội dung với bản trong kho — **157/157 khối trùng khớp**; khác biệt duy nhất là một mục tham khảo đến từ `eide_common.js`, và CXD-10 (không hề dùng `dialog.json`) lệch đúng cùng khối ấy. `tests/test_dialog_json.py` chốt hình dạng mà `dps.js` giả định | Đã cập nhật tài liệu — nguồn khôi phục |
| DEV-018 | 2026-09-06 | DEV-29 §4 và CLAUDE.md §"Đồng bộ tài liệu" ("sửa nguồn sinh … sinh lại bằng `node <ten>.js`"); DEV-29 §8 tiêu chí 3 ("phiên bản tài liệu tăng") | `docs/ho-so/nguon/package.json`, `scripts/sinh_tai_lieu.sh`, `scripts/dong_bo_tai_lieu.py`, `docs/ho-so/nguon/eide_common.js::metaNew` | Vòng lặp đồng bộ tài liệu mà quy trình quy định **không chạy được** như viết: (a) bộ sinh cần gói npm `docx` nhưng kho không có `package.json` nào; (b) `node <ten>.js` ghi docx và `policy/*` vào THƯ MỤC HIỆN TẠI, nên chạy trong `nguon/` thì rải tệp sinh lẫn vào mã nguồn, chạy ở gốc kho thì ghi đè `docs/spec` bằng đường vòng; (c) bộ sinh đọc `caps.json`/`cds.json`/`ddd.json` từ cwd mà không ai nói ra; (d) `metaNew` viết cứng phiên bản `1.0` cho mọi tài liệu | Không sửa được thì tiêu chí §8.3 ("sau mỗi đồng bộ, phiên bản tài liệu tăng") không bao giờ đạt, và mọi mục DEVIATIONS sẽ nằm `Mở` vĩnh viễn — đúng thứ nhật ký sai khác sinh ra để tránh | Thêm `package.json` (docx ^9.7.1, đã cài và chạy thật), `scripts/sinh_tai_lieu.sh` (dựng thư mục dàn dựng sạch, chạy bộ sinh ở đó, chép có chủ đích từng tệp về; có `--kiem` để so mà không ghi), `scripts/dong_bo_tai_lieu.py` (sinh bản nháp `docs/sync/` theo bước 1-2 của lệnh), và tham số `lichSu` cho `metaNew` để nâng phiên bản TỪNG tài liệu. Đã chứng minh trọn vòng: sửa `pol.js` → sinh lại → POL-17 lên v1.1 và `rules.yaml` khớp từng byte. Đề xuất DEV-29 §4 v1.3 mô tả đúng ba bước này thay vì `node <ten>.js` | Bác |
| DEV-017 | 2026-09-06 | SEC-25 §2 ("`resource` (Linux/macOS) giới hạn CPU 60 s, **RAM 1 GB**, tệp mở 256, kích thước ghi 2 GB") | `src/eide_core/sandbox.py::_dat_gioi_han`, `_CanhRSS` | Trên macOS `RLIMIT_AS` KHÔNG dùng được: đặt ở bất kỳ giá trị nào — đã thử cả 2 GB — đều làm hỏng chính lời gọi `exec`, vì Python trên macOS đặt trước một vùng địa chỉ rất lớn. Ba giới hạn còn lại (`RLIMIT_CPU`, `RLIMIT_FSIZE`, `RLIMIT_NOFILE`) hoạt động bình thường | Đo trực tiếp 06/09/2026 trên macOS 26 arm64, không tra tài liệu: `RLIMIT_CPU` → SIGXCPU; `RLIMIT_FSIZE` → Errno 27; `RLIMIT_NOFILE` → Errno 24; `RLIMIT_AS` → `SubprocessError` ngay ở `preexec_fn`. macOS là nền tảng thứ tự 1 (PLATFORM.md) nên không thể bỏ giới hạn RAM | Canh RAM bằng RSS ở tiến trình CHA (`_CanhRSS`, chu kỳ 0,2 s, vượt → SIGKILL + `violations: [rss]`) thay cho `RLIMIT_AS`. Canh ở cha vì một công cụ đang ăn hết RAM là đúng thứ không nên nhờ nó tự dừng. Đề xuất SEC-25 v1.3 ghi rõ: RAM giới hạn bằng RSS ở tiến trình cha trên macOS, `RLIMIT_AS` chỉ dùng được trên Linux | Chủ sản phẩm duyệt 07/09/2026. SEC-25 §2 v1.1 ghi rõ: trên macOS RAM canh bằng RSS ở tiến trình cha (chu kỳ 0,2 s, vượt → SIGKILL), `RLIMIT_AS` chỉ dùng được trên Linux; ba giới hạn còn lại giữ nguyên. | Đã cập nhật tài liệu v1.1 |
| DEV-016 | 2026-09-06 | API-15 §7 (bộ lọc bí mật trước khi ghi ledger) | `src/eide_core/ledger.py::che_bi_mat` | Tài liệu nêu bộ lọc nhưng SPRINT-01 xếp nó vào Sprint 2 (ghi chú WI-008); nay đã hiện thực sớm | WI-013 làm khóa API đi qua tiến trình lần đầu. Ledger là append-only và chống sửa: một khóa lọt vào thì không gỡ ra được nữa mà không phá chuỗi hash — và vẫn phải đổi khóa. Chờ tới Sprint 2 là chờ quá một sự kiện không hoàn tác được | Đã che ở tầng `Ledger.append`, TRƯỚC khi băm, nên chuỗi hash vẫn liền. Giữ 8 ký tự đầu để còn truy được là khóa nào. Không sửa tài liệu — đây là làm sớm, không phải làm khác | Bác |
| DEV-015 | 2026-09-06 | SDD-04 §6 `models.yaml`; DDD-14 §6 (models.yaml "từ M0"); PRS-16 §2 | `docs/spec/models.yaml` | `models.yaml` được cả ba tài liệu nhắc tới nhưng không có trong `docs/spec/`; và SDD-04 §6 dùng BÍ DANH ("gemini-flash", "claude-opus") chứ không phải mã model thật, lại không nói giá lấy ở đâu | Không có tệp thì Gateway không chọn được mô hình; không có ánh xạ bí danh thì không gọi được API nào; không có giá thì `daily_budget_usd` không cưỡng chế được và `model.call.cost_usd` luôn bằng 0 | Thêm `docs/spec/models.yaml` — phần `roles` và `policy` chép ĐÚNG SDD-04 §6; bổ sung `aliases` (bí danh → mã model thật, đã đối chiếu API 06/09/2026) và `pricing` (USD/1M token). Cùng khuôn mẫu với `defaults.yaml` của DEV-001. Đề xuất SDD-04 v1.3 ghi rõ hai phần bổ sung này | Chủ sản phẩm duyệt 07/09/2026. SDD-04 §6 v1.3 ghi rõ `aliases` và `pricing` cùng hệ quả khi thiếu từng phần. | Đã cập nhật tài liệu v1.3 |
| DEV-014 | 2026-09-06 | POL-17 §5-6; DEP-26 §3 (một daemon mỗi người dùng, nhiều dự án); API-15 §7 | `src/eide/cli.py::_router`, `src/eide_core/undo.py` | Mục hoàn tác của `project.create` nằm ở ledger NGƯỜI DÙNG chứ không ở ledger dự án, vì lúc gọi thì dự án chưa tồn tại. Tài liệu không nói cửa sổ hoàn tác có phạm vi nào | Hai ledger là hệ quả của mô hình DEP-26 §3, không phải lỗi; nhưng `policy.undo_window` gọi trong dự án sẽ KHÔNG thấy mục tạo dự án, còn gọi ngoài dự án thì thấy | Giữ nguyên phân chia (sự kiện liên-dự-án ở ledger người dùng, sự kiện dự án ở ledger dự án) và ghi rõ vào POL-17 §5 ở v1.3. **Màn hình ReviewQueue (UXD-13) phải gộp hai nguồn** — nếu chỉ đọc ledger dự án thì người dùng không bao giờ thấy nút hoàn tác cho việc tạo dự án | Chủ sản phẩm duyệt 06/09/2026: giữ phân chia hai nhật ký. POL-17 §5 v1.2 ghi rõ phạm vi cửa sổ hoàn tác và yêu cầu ReviewQueue gộp cả hai nguồn. | Đã cập nhật tài liệu v1.2 |
| DEV-013 | 2026-09-06 | API-15 §5 `ledger_events.json`; CDS-12.5 POLICY-04 bước "ghi ledger" | `src/eide/caps/policy.py::escalate` | Không có kiểu sự kiện nào cho leo thang: danh sách 15 kiểu không có `escalate` hay `policy.escalate`, mà POL-17 §6 lại có hẳn một máy trạng thái leo thang | Lớp `Ledger` từ chối kiểu không có trong `ledger_events.json` (E6001), nên phải chọn một kiểu đã có | Tạm dùng `question` với `{question_id, reason, channels, escalated: true}` — leo thang chính là đưa một câu hỏi tới người qua kênh phù hợp. Đề xuất API-15 v1.3 thêm kiểu `policy.escalate {ref, reason, channels, level}` để phân biệt câu hỏi thường với câu hỏi đã leo thang | Chủ sản phẩm duyệt 07/09/2026. API-15 §7 v1.3 thêm kiểu `policy.escalate {ref, reason, channels[], level}`; `policy.py` bỏ cách tạm mượn kiểu `question`. | Đã cập nhật tài liệu v1.3 |
| DEV-012 | 2026-09-06 | APD-08 §4.1 tầng 2 vs POL-17 quy tắc `G-OPS-02`, `G5-02`; situations S31, S39 | `src/eide_core/policy.py::decide` | Hai quy tắc có lý do ghi "(R4)" không bao giờ thắng: tầng ngưỡng cứng bắt mọi hành động R4 và trả `HARD-R4` trước khi xét quy tắc cổng. Fixture ghi `ASK G-OPS-02` / `ASK G5-02`, máy trả `ASK HARD-R4` | Quyết định GIỐNG NHAU (đều ASK) nên không phải lỗi an toàn; chỉ là quy tắc chết và nhãn trong nhật ký khác điều tài liệu hứa. Người đọc `decision_log` sẽ không thấy G-OPS-02 bao giờ | Chủ sản phẩm duyệt 06/09/2026. ĐÃ SỬA theo phương án (b): tầng 2 tra quy tắc cổng trước và mượn `rule_id` + lý do, nhưng CHỈ từ dải chặn (priority ≤ 5) — quy tắc ưu tiên lớn hơn là lời khuyên chung, dùng nó làm lý do cho hành động R4 là nói nhỏ đi mức nghiêm trọng. Ngưỡng cứng chỉ siết, không nới: quy tắc APPROVE gặp ngưỡng cứng vẫn ra ASK. Còn lại: APD-08 §4.1 và POL-17 v1.3 ghi rõ tầng 2 mượn lý do từ dải chặn | POL-17 §1 v1.2 nay có đoạn mô tả tầng T2 mượn mã quy tắc và lý do từ dải chặn (ưu tiên ≤ 5). | Đã cập nhật tài liệu v1.2 |
| DEV-011 | 2026-09-06 | POL-17 §2 `rules.yaml` `G-SRC-01` priority 10 vs `G-SRC-06` priority 25; situations.jsonl S06 | `docs/spec/policy/rules.yaml` (chưa sửa), `tests/test_policy_situations.py::LECH` | **Lệch quyết định, không phải lệch nhãn.** S06 ("A3; bosch-sensortec.com; match_score 0,4") được fixture ghi là `ASK G-SRC-06`, nhưng PolicyGate trả `APPROVE G-SRC-01`: quy tắc "nguồn hãng tin cậy" (priority 10) thắng trước quy tắc "tài liệu có thể không đúng linh kiện" (priority 25), nên `match_score` không bao giờ được xét khi domain đã tin cậy | Hậu quả: một datasheet của biến thể chip KHÁC, tải từ đúng trang hãng, được tự động nhận vào mà không hỏi ai. Mọi fact trích từ nó sau đó mang nhãn "nguồn tin cậy" — đúng kiểu nhiễm tri thức mà G-SRC sinh ra để chặn | Sửa `rules.yaml`: hạ `G-SRC-06` xuống priority 5 (ngang `G-SRC-03` hash lệch và `G-SRC-07` nhạy cảm — cùng loại "chặn trước khi tin"), HOẶC thêm `source.match_score >= thresholds.source_match_min` vào điều kiện `G-SRC-01`. Chủ sản phẩm duyệt 06/09/2026 (uỷ quyền quyết định). ĐÃ SỬA phương án hạ priority: `pol.js` dòng G-SRC-06 25→5, sinh lại `docs/spec/policy/rules.yaml` bằng `node scripts/gen_policy_spec.js`. S06 nay ra đúng `ASK G-SRC-06`. Đã sinh lại POL-17 docx bằng `scripts/sinh_tai_lieu.sh pol` — tài liệu lên **v1.1** kèm dòng lịch sử | Đã cập nhật tài liệu v1.1 |
| DEV-010 | 2026-09-06 | DDD-14 §2 thực thể Preference (bảng `preference`) vs §6 `preferences.yaml`; CDS-12.3 PROJECT-09 | `src/eide/caps/project.py::preferences` | Tài liệu có HAI nơi lưu cùng một thứ với cùng hình dạng ({value, scope, learned_from, ttl_days}) — bảng `preference` (§2, khóa chính (key, scope)) và tệp `preferences.yaml` (§6) — nhưng không nói nơi nào là chuẩn cho phạm vi nào | Phải chọn một cách nhất quán, và phép thử là ngữ nghĩa của `scope`: bảng `preference` nằm trong store của TỪNG dự án, nên tùy chọn phạm vi `user` ghi ở dự án A sẽ không đọc được ở dự án B — trái đúng nghĩa "user" | `scope=project` → bảng `preference` trong store dự án; `scope=user` → `preferences.yaml` trong thư mục cấu hình người dùng (platformdirs). Đề xuất DDD-14 v1.3 ghi rõ phân công này ở cả §2 và §6 | Chủ sản phẩm duyệt 07/09/2026 phương án phân theo scope. DDD-14 v1.1 ghi rõ ở CẢ §2 (bảng `preference` giữ riêng scope=project) và §6 (`preferences.yaml` giữ scope=user). | Đã cập nhật tài liệu v1.1 |
| DEV-009 | 2026-09-06 | CDS-12.3 PROJECT-09 `input_schema.value` và trường "Ví dụ gọi" | `src/eide/caps/project.py::preferences`, `tests/test_project_preferences.py` | Hợp đồng khai `"value": {"type": "object"}` nhưng ví dụ ngay bên cạnh là `{"op":"set","key":"probe","value":"stlink","scope":"user"}` — giá trị là chuỗi. Ví dụ của chính hợp đồng KHÔNG qua nổi input_schema của nó: Router chặn bằng E1000 trước khi vào handler | Router kiểm input theo `input_schema` (API-15 E1000) nên một trong hai vế phải sai; không thể hiện thực cả hai | Bám `input_schema` (object) vì nó khớp hình dạng Preference của DDD-14 {value, learned_from, ttl_days}; ví dụ dùng dạng `{"value":{"value":"stlink"}}`. Đề xuất v1.3 sửa **ví dụ** trong CDS-12.3 cho khớp schema (không nới schema thành scalar, vì learned_from của D8 cần chỗ đứng). Không sửa `cds.json` — tệp sinh | **Ghi nhận đầu SAI ở hai chỗ, đã sửa 07/09/2026.** (a) "Không sửa `cds.json` — tệp sinh" đúng, nhưng giữa chừng tôi kết luận ngược lại rồi sửa thẳng `cds.json`; thật ra nó sinh từ `caps.json` + `cds_data_*.py` qua `gen_cds.py`, và bản sinh lại khớp từng byte. (b) Phương án "sửa ví dụ cho khớp schema" là sửa nhầm bên: DDD-14 §2 khai `value` kiểu `object` với chú thích "JSON", còn `learned_from`/`ttl_days` là trường NGANG HÀNG — nên ví dụ `"value":"stlink"` vốn đúng, chỗ sai là input_schema chép chữ `object` sang JSON Schema nơi nó chỉ nhận đối tượng. Chủ sản phẩm duyệt phương án nới schema. Đã thêm ký hiệu kiểu `json` vào `gen_cds.py`; CDS-12.3 v1.1. | Đã cập nhật tài liệu v1.1 |
| DEV-008 | 2026-09-06 | CDS-12.3 PROJECT-02 bước 3 và 4 | `src/eide/caps/project.py::open_project` | Bước 3 "tái dựng KG từ cache hoặc từ store" chưa hiện thực. **Bước 4 đã xong 06/09/2026** cùng WI-007: project.open mở SessionMemory mới, đóng phiên trước và trả `previous_session` | Nhóm `kg.*` chưa có ở Sprint 1 (mốc M1 theo PLN-27); phần còn lại của hợp đồng đứng độc lập và `tc` kiểm được ngay | Hoàn tất bước 3 khi nhóm `kg.*` có; (`summary.undo_open` đã có nguồn thật từ 06/09/2026 — POLICY-03 UndoService.) Không sửa tài liệu — đây là nợ hiện thực, không phải tài liệu sai  Vẫn Mở: chờ nhóm `kg.*` ở mốc M1 (PLN-27). Rà lại 07/09/2026 — không có gì để sửa trong tài liệu, hợp đồng PROJECT-02 đúng.  **ĐÓNG 07/09/2026:** nhóm `kg.*` đã hiện thực (8/9 năng lực; `kg.evidence` thuộc mốc M2), và `project.open` bước 3 nay dựng đồ thị lúc mở dự án. Lỗi khi dựng KHÔNG chặn việc mở: đồ thị là khung nhìn dựng lại được, nên một store lạ phải để người dùng mở được dự án mà đi sửa — báo `kg.ok=false` kèm lý do. Cả năm bước của PROJECT-02 nay có hiện thực. | Bác |
| DEV-007 | 2026-09-06 | CDS-12.3 PROJECT-02 bước 2 ("Kiểm hash store vs ledger.last_hash"); API-15 §5 `store.write` | `src/eide_core/store.py` (`content_digest`, `write_seal`, `verify_seal`) | Tài liệu nêu phép kiểm nhưng không định nghĩa "hash store" được tính thế nào, lưu ở đâu, và ghi vào ledger lúc nào — `store.write` chỉ có {batch_id, n_facts, n_conflicts, actor, reason}, không có trường hash | Không có cơ chế thì E6000 không hiện thực được, mà `tc` của PROJECT-02 đòi đúng nhánh đó | Thêm niêm phong `store.sqlite.seal.json` {content_hash, ledger_last_hash, user_version, at}, ghi sau mỗi lần ghi qua cổng (hiện tại: `migrate`). `content_hash` băm NỘI DUNG LOGIC (bảng theo tên, dòng đã sắp) chứ không băm byte tệp — WAL, checkpoint và VACUUM đổi tệp mà không đổi dữ liệu, băm byte sẽ báo động giả mỗi lần mở. Đề xuất bổ sung trường `hash` vào `store.write` và mô tả niêm phong vào DDD-14 §5 ở v1.3 | Chủ sản phẩm duyệt 07/09/2026. DDD-14 §5 v1.1 mô tả niêm phong `store.sqlite.seal.json` và nói rõ vì sao băm nội dung logic chứ không băm byte; API-15 §7 v1.4 thêm trường `hash` vào `store.write`. | Đã cập nhật tài liệu v1.1 |
| DEV-006 | 2026-09-06 | DDD-14 §5 bảng lịch migration (đối chiếu §2.25 và MEM-11 §2) | `src/eide_core/migrations/session_db.sql`, `0002_m1_policy_runtime.sql` | Bảng lịch §5 có một dòng cho cơ sở dữ liệu RIÊNG `index.sqlite` (0003) nhưng không có dòng tương đương cho `session.sqlite`, nên đọc §5 một mình sẽ tưởng bảng `session` bị bỏ sót | **Ghi nhận đầu (06/09/2026) SAI và đã sửa cùng ngày.** Ban đầu tôi đặt `session` vào migration 0002 của store.sqlite vì tưởng §5 bỏ quên. Thật ra DDD-14 §2.25 ghi rõ "Phiên làm việc (session.sqlite, không commit)" và MEM-11 §2 xếp M2 vào `session.sqlite` — nó thuộc cơ sở dữ liệu khác, đúng như `rag_chunk` thuộc index.sqlite. Đã gỡ `session` khỏi 0002 và dựng `session_db.sql` riêng. Sửa được vì 0002 chưa phát hành (v0.1.0), không có store thật nào cần di trú | Bổ sung vào DDD-14 §5 một dòng cho `session.sqlite` giống dòng 0003 của `index.sqlite`, để đọc §5 một mình là đủ biết bảng nào ở đâu | DDD-14 §5 v1.2 thêm dòng `0003b_m1_session` cho `session.sqlite`, giống dòng 0003 của `index.sqlite`. | Đã cập nhật tài liệu v1.2 |
| DEV-005 | 2026-09-06 | PLATFORM.md §ma trận (`macos-13` Intel) | `.github/workflows/ci.yml` | Runner `macos-13` (Intel) của GitHub Actions đã ngừng phục vụ; không còn runner Intel miễn phí để chạy ma trận như PLATFORM.md mô tả | Vẫn phải chứng minh sản phẩm chạy trên Mac Intel (nền tảng thứ tự 1) | Phủ Intel bằng hai đường: (a) `arch -x86_64` trên runner arm64 và trên máy chủ sản phẩm — đã có Rosetta + Python universal2 ở `/usr/local`; (b) với phần Swift, cross-compile x86_64 rồi kiểm bằng `lipo` như `apps/geditor/scripts/build-universal.sh` đã làm. Sửa PLATFORM.md ở v1.3 | Chủ sản phẩm duyệt 07/09/2026. `docs/PLATFORM.md` nay ghi rõ hai đường phủ Intel và **điều chúng không phủ**: lỗi chỉ xuất hiện trên phần cứng Intel thật mà Rosetta che mất (hành vi thời gian, thứ tự bộ nhớ). | Đã cập nhật tài liệu |
| DEV-004 | 2026-09-06 | SEC-25 §quản lý bí mật; GPI-23 §1 (GEditor là phụ thuộc ngoài) | `apps/geditor/**`, `.gitignore`, `apps/geditor/scripts/check_no_secrets.py` | GEditor được **chuyển hẳn** vào kho EIDE (`apps/geditor/`) thành một phần sản phẩm, không phải phụ thuộc ngoài như GPI-23 giả định. Kèm theo: khóa riêng ký cập nhật Sparkle (`secrets/sparkle_eddsa_private.txt`) có trong kho Geditor gốc đã **không** được mang sang, và ngoại lệ tương ứng trong `check_no_secrets.py` đã bị gỡ | Chủ sản phẩm quyết định 06/09/2026: hai kho đi hai hướng phát triển khác nhau nên fork thay vì tham chiếu. Khóa riêng không được nhân thêm nơi lưu (SEC-25) | Sửa GPI-23 §1 ở v1.3: GEditor là thành phần trong kho, bỏ phần "khung plugin bên thứ ba". Với kho Geditor gốc: **chủ sản phẩm quyết định 06/09/2026 KHÔNG đổi khóa Sparkle** — chấp nhận dùng chung. Ghi lại để sau này truy được: khóa ấy là quyền ký bản cập nhật tự động, nên ai có nó cũng ký được một binary bất kỳ thành "GEditor x.y" và mọi bản đã cài sẽ tự nuốt; nó cũng được `actions/checkout` chép lên runner GitHub mỗi lần push. Rủi ro bị chặn bởi việc kho đang private — nếu kho đổi sang public hoặc cấp quyền đọc cho người ngoài thì phải đổi khóa trước. Phía EIDE giữ nguyên: khóa riêng không được mang sang, `.gitignore` chặn `secrets/*_private*` | Đã cập nhật tài liệu v1.3 |
| DEV-003 | 2026-09-05 | PLN-27 WI-001; SAD-03 ADR (kế thừa hkw-core) | `toàn kho` | Kho eide viết mới hoàn toàn, không đổi tên/kế thừa mã hkw-core M0; lõi tách package eide_core/ ngay từ đầu; CLI dùng argparse thay vì typer/rich | Quyết định của chủ sản phẩm 05/09/2026: làm mới hoàn toàn; giảm phụ thuộc để chạy trên 3 nền tảng | Sửa PLN-27 WI-001 thành 'khởi tạo kho eide'; ghi ADR-16 trong SAD-03 v1.3; DEP-26 bỏ bước migrate .hkw→.eide | Chủ sản phẩm duyệt 07/09/2026. SAD-03 v1.3 thêm ADR-16 (kho viết mới, `eide_core/` tách từ đầu, argparse); PLN-27 WI-001 đổi thành "khởi tạo kho eide"; DEP-26 TC-DP-04 bỏ bước `.hkw` → `.eide`. Sinh lại PLN-27 còn cho thấy vòng tài liệu-là-mã chạy đúng: cột phân công của WI-161 tự đổi từ "AI làm/người duyệt" sang "AI làm" nhờ tier CHAT-04 T2→T1 lan từ `caps.json` (DEV-020). | Đã cập nhật tài liệu v1.3 |
| DEV-055 | 2026-09-07 | TGT-19 §bảng ISA (rv32imac "M2", xtensa-esp32 "M2", pic16/pic18 "M5", avr8 "M0"); STP-05 TC-TG-01/TC-48 | `docs/spec/isa/ (chỉ có armv7e-m.yaml, avr8.yaml), tests/test_env_policy.py` | Toàn bộ phần còn lại của công việc ISA gộp về mốc M5 cùng PIC, thay vì M2 như TGT-19 định: (a) manifest rv32imac và xtensa-esp32 chưa viết; (b) schema cho ISA manifest chưa có, nên TC-TG-01 "5 profile nạp qua schema" và TC-48 "thêm rv32 không sửa core" chưa hiện thực được; (c) avr8 tuy ở mốc M0 và ĐÃ có manifest nhưng không test nào chạm tới — test env.check duy nhất dùng armv7e-m | Chủ sản phẩm quyết định 07/09/2026: dồn ISA về một mốc để làm một lần thay vì rải ba mốc. Có cơ sở kỹ thuật: cả rv32imac lẫn xtensa-esp32 đều cần chuỗi công cụ và trình mô phỏng chưa ai cài (riscv-none-elf-gcc, ESP-IDF, QEMU riscv32/xtensa), nên "test" chúng ở M2 mà không có board và không có toolchain sẽ chỉ là nạp YAML — đúng việc mà schema + TC-48 làm được rẻ hơn nhiều. RIÊNG avr8 là món nợ thật chứ không phải việc tương lai: nó ở mốc M0, manifest đã nằm trong kho, và hiện KHÔNG có gì báo nếu tệp ấy sai cú pháp hay thiếu khóa — cùng loại hỏng im lặng với DEV-052/DEV-053 | Sửa TGT-19 §bảng ISA: rv32imac và xtensa-esp32 chuyển M2 → M5, ghi rõ lý do (phụ thuộc toolchain/mô phỏng chưa có). Ở M5 làm gói ba việc: (1) docs/spec/isa/isa.schema.json + test nạp MỌI tệp trong docs/spec/isa/ qua schema — avr8 tự có test từ đó; (2) TC-48: thả rv32imac.yaml vào rồi khẳng định env.check hiểu nó mà không sửa dòng mã nào trong src/, tức chứng minh "ISA là dữ liệu, không phải mã"; (3) manifest xtensa-esp32 và pic16. PIC giữ nguyên M5 và giữ nguyên hạn chế đã ghi trong TGT-19: XC8 là trình dịch đóng và MPLAB sim không tự động hóa được, nên PIC không chạy được vòng "mô phỏng trước phần cứng" mà defaults.sim_first đòi — đây là hạn chế hệ sinh thái, không phải việc chưa làm | Đã cập nhật tài liệu TGT-19 v1.1 |
| DEV-056 | 2026-09-07 | DDD-14 §5 bảng lịch migration, dòng `0004_m2_engineering_hw` ("… user_version=3") | `src/eide_core/migrations/0004_m2_engineering_hw.sql, src/eide_core/store.py (LATEST_VERSION), tests/test_store.py` | Tài liệu ghi tệp `0004_m2_engineering_hw` đặt `user_version=3`; mã đặt 4. Store suy phiên bản từ SỐ HIỆU TỆP (`migrations()` đọc `f.name[:4]`), còn §5 đếm SỐ MIGRATION ĐÃ CHẠY của store chính — hai cách khớp nhau ở 0001→1 và 0002→2, rồi rẽ đôi tại 0004 vì số 0003 đã bị `0003_m1_index` và `0003b_m1_session` dùng cho hai cơ sở dữ liệu RIÊNG (index.sqlite, session.sqlite) không nằm trong dãy của store | Giữ quy tắc đồng nhất `user_version == số hiệu tệp` (đã ghi ở `store.py` dòng 31 từ Sprint 1). Đọc `PRAGMA user_version` của một store bất kỳ là biết ngay tệp nào chạy sau cùng — với cách đếm của §5 thì phải biết thêm tệp nào thuộc dãy store, tệp nào là cơ sở dữ liệu bên cạnh. Quan trọng hơn: cách đếm dễ vỡ về sau — bất kỳ số hiệu nào bị một cơ sở dữ liệu riêng dùng tiếp (hoặc một migration bị bỏ) đều làm LỆCH toàn bộ phiên bản phía sau, trong khi quy tắc đồng nhất thì ổn định. Đổi bây giờ không tốn gì: 0001 và 0002 cho cùng kết quả ở cả hai cách, nên không store nào đang tồn tại phải đánh số lại | Sửa DDD-14 §5: dòng `0004_m2_engineering_hw` đổi "user_version=3" → "user_version=4"; `0005_m3_debug` đổi "user_version=4" → "user_version=5". Thêm một câu vào §5 nêu quy tắc: `user_version` LUÔN bằng số hiệu tệp migration chạy sau cùng của store chính; các số hiệu cấp cho cơ sở dữ liệu riêng (0003 index, 0003b session) bị bỏ trống trong dãy này một cách cố ý | Đã cập nhật tài liệu DDD-14 v1.4 |
| DEV-057 | 2026-09-07 | POL-17 §2 quy tắc G-SRC-01 (`source.kind in ["svd","atdf","edc","binding","pdf_vendor"]`); DDD-14 §2 Source (enum `kind` gồm `pdf`, KHÔNG có `pdf_vendor`) | `src/eide/caps/search.py::_kind_chinh_sach, dac_trung_nguon` | Hai bộ từ vựng `kind` cùng tên nhưng khác nghĩa và không tài liệu nào nói ra: `Source.kind` của DDD-14 mô tả ĐỊNH DẠNG (`svd`, `pdf`, `html`…), còn G-SRC-01 chờ `pdf_vendor` — không phải định dạng mà là một PHÁN XÉT ("PDF lấy từ chính trang hãng"). Truyền thẳng `Source.kind` vào cổng thì mọi datasheet hãng đều trượt G-SRC-01 và rơi xuống G-SRC-99 (ASK) | Phân biệt của POL-17 là ĐÚNG và đáng giữ: PDF từ st.com và PDF từ một diễn đàn cùng định dạng nhưng khác hẳn mức tin cậy, mà chính sách quan tâm cái sau. Cái thiếu là chỗ nói rõ hai bảng này khác nhau và ai chịu trách nhiệm ánh xạ. Mất khá lâu mới tìm ra vì mọi đặc trưng đều trông đúng — domain đúng, license đúng, size đúng — chỉ `kind` lệch một từ, và quy tắc không khớp thì im lặng rơi xuống mặc định chứ không báo gì. Cùng họ với DEV-011/DEV-012/DEV-032: quyết định đúng, lý do sai, và người đọc decision_log bị dẫn đi sửa nhầm chỗ | Bổ sung một câu vào POL-17 §2 (hoặc §1 phần đặc trưng): "`source.kind` trong quy tắc dùng từ vựng CHÍNH SÁCH, không phải enum `Source.kind` của DDD-14 §2; `pdf_vendor` nghĩa là PDF tải từ tên miền chính hãng. Bên gọi chịu trách nhiệm ánh xạ." Kèm bảng ánh xạ hai chiều. Phía mã đã hiện thực trong `_kind_chinh_sach` và có test; ghi vào store vẫn dùng enum DDD-14. Cân nhắc thêm: `trusted_sources` mặc định có `github.com/cmsis-svd` nhưng SVD thật phục vụ từ `raw.githubusercontent.com`, nên nguồn SVD vàng phổ biến nhất vẫn rơi vào ASK — sửa danh sách trắng là việc của chủ sản phẩm (WI-257) vì nó cần ký lại | Đã cập nhật tài liệu POL-17 v1.4 |
| DEV-058 | 2026-09-07 | CXD-10 §4.5 (truy hồi lai); CDS-12.4 VIEW-05 bước 1 ("không có chunk đủ điểm (< 0,35) → not_found") | `src/eide_core/rag.py::RagIndex.tim, src/eide/caps/view.py::rag_ask` | Điểm FTS bị ĐẢO NGƯỢC từ Sprint 2 và không ai thấy. `bm25()` của SQLite trả số ÂM, càng âm càng khớp; bản đầu chuẩn hóa `1/(1+abs(bm25))`, nên đoạn khớp MẠNH ra điểm gần 0 còn đoạn khớp YẾU ra 1,0. Thêm nữa, bm25 SUY BIẾN trên kho nhỏ: BM25 nhân IDF, mà từ có mặt trong mọi tài liệu thì IDF = 0 — kho một tài liệu luôn cho bm25 = 0 với mọi khớp | Thứ hạng trả về vẫn ĐÚNG vì SQL đã `ORDER BY bm25 ASC`, nên lỗi im lặng hoàn toàn suốt thời gian `memory.retrieve` là bên dùng duy nhất — nó chỉ cần thứ tự. Chỉ khi VIEW-05 dùng điểm làm NGƯỠNG thì hậu quả mới lộ: với thang đảo, mọi đoạn khớp yếu đạt 1,0 và quy tắc "< 0,35 → not_found" thoái hóa thành phép kiểm "có đoạn nào không", tức tác tử trả lời dựa trên một đoạn gần như không liên quan thay vì nói không biết. Đây đúng là loại lỗi mà EIDE sinh ra để chống: một con số trông hợp lý, dùng được, và sai | Đã sửa: điểm = trung bình của bm25 chuẩn hóa ĐỒNG BIẾN (`1 - 1/(1+abs(bm25))`) và ĐỘ PHỦ (tỷ lệ từ khóa câu hỏi xuất hiện trong đoạn). Độ phủ không suy biến trên kho nhỏ và giải thích được cho người dùng ("đoạn này chứa 4/5 từ bạn hỏi"). `RagIndex.tim` nay trả thêm hai cột `bm25` và `coverage` để `view.rag_trace` hiển thị đúng những con số đã quyết định thứ hạng. Tài liệu: CXD-10 §4.5 nên nêu rõ thang điểm là ĐỒNG BIẾN và liệt kê các thành phần (bm25, coverage, vector, graph) — hiện §4.5 chỉ nói "truy hồi lai" mà không định nghĩa thang, nên hai bên hiện thực hai kiểu đều "đúng tài liệu" | Đã cập nhật tài liệu CXD-10 v1.2 |
| DEV-059 | 2026-09-07 | DPS-09 §4.4 (năm chuỗi mẫu) → `docs/spec/dialog/chains.json`, trường `buoc` | `src/eide/caps/chat.py::_dung_chuoi, scripts/kiem_chuoi_chuan.py` | Chín tên năng lực trong `chains.json` KHÔNG có trong danh mục 238: archive.classify (thật: ingest.classify), archive.explore (archive.list/query), code.module (code.generate_module), code.test (code.generate_tests hoặc code.test_host), discover.probes (discover.probe), env.install_tool (env.install), passport.build (passport.import), passport.pull (registry.pull), sim.build (sim.build_platform). Trường `buoc` là bản tách của chuỗi văn xuôi viết CHO NGƯỜI ĐỌC, không phải danh sách id máy dùng được — nó còn chứa nhánh điều kiện viết bằng chữ ("[template? registry.pull : req.elicit]") và ký hiệu nhóm ("extract.*") | Hệ quả đo được: `chat.orchestrate` bám mẫu trước rồi mới nhờ planner (DPS-09 §4.4 "để mô hình bám theo thay vì sáng tác"), nhưng vì chín bước ấy không phân giải được, phần khung mà mẫu đóng góp nhỏ hơn hẳn ý định của §4.4 — và đúng những bước then chốt (dựng hộ chiếu, sinh module, dò probe) lại rơi vào tay planner, tức về lại chỗ "sáng tác" mà mẫu sinh ra để tránh. `_dung_chuoi` đã xử lý đúng (chỉ nhận bước phân giải được) và ghi rõ giới hạn trong docstring, nên đây không phải lỗi mã; đây là chỗ TÀI LIỆU thiếu một tầng máy đọc được | Thêm vào `dps.js` một trường `nodes` cho mỗi chuỗi: danh sách `{id, cap, when?, on_ask?}` với `cap` là id THẬT trong danh mục, giữ nguyên `chuoi`/`buoc` làm bản cho người đọc. Nhánh điều kiện biểu diễn bằng `when` như DPS-09 §4.4 đã định nghĩa cho ChainNode, còn `extract.*` giãn thành các id cụ thể theo loại nguồn. `scripts/kiem_chuoi_chuan.py --json` đã có sẵn phép đối chiếu, nên thêm nó vào `make check-spec` để một tên gõ sai trong chuỗi mẫu bị bắt ngay thay vì im lặng biến mất khỏi khung | Đã cập nhật tài liệu DPS-09 v1.3 |
| DEV-060 | 2026-09-08 | CDS-12.3 ENV-03 (lớp rủi ro "R4→T1 theo danh sách trắng", errors [E3000,E4000,E4004]); POL-17 §2 G-OPS-04 (APPROVE khi package in trusted_packages); APD-08 §2 (mọi R4 luôn hỏi người) | `src/eide/caps/env.py::install, src/eide_core/policy.py::PolicyGate.decide (tầng 2), tests/test_env_install.py` | Hai điểm của hợp đồng ENV-03 không đạt được như tài liệu mô tả. (1) G-OPS-04 là quy tắc CHẾT cho env.install: APD-08 §2 đặt ngưỡng cứng "mọi hành động R4 luôn hỏi người" ở tầng TRƯỚC quy tắc cổng, nên một gói nằm trong trusted_packages vẫn ra ASK — mức T1 mà danh mục hứa không bao giờ đạt tới. Hiện thực giữ nguyên ngưỡng cứng và ghim thực tế ấy bằng test. (2) install ném E4001 TOOL_MISSING ở ba nhánh không nằm trong errors của ENV-03: công cụ đóng (XC8/IAR/Keil), máy không có trình quản lý gói nào, và xin ghim phiên bản với trình quản lý không ghim được. | Nới ngưỡng cứng R4 là sửa hạt nhân an toàn của cả hệ, không phải sửa một năng lực — nó chạm mọi năng lực R4 khác (xóa flash, fuse, phát hành công khai), nên phải do chủ sản phẩm quyết chứ không phải do người viết mã ENV-03 quyết. Về E4001: ba nhánh ấy đều là "EIDE không làm được việc này" chứ không phải "công cụ chạy rồi thất bại" (E4000) hay "cần người duyệt" (E3000); dùng E4000 cho chúng là báo sai loại lỗi và làm hỏng cách xử lý ở phía gọi (E4001 có handling "Gợi ý env.install_tool", đúng ý ở đây là chỉ sang env.guide_install). | Điểm (1) — chọn một trong hai: (a) giữ nguyên, sửa CDS-12.3 ENV-03 thành "R4, luôn ASK" và bỏ G-OPS-04 khỏi POL-17 vì nó không bao giờ khớp; (b) cho danh sách trắng ĐÃ KÝ thắng ngưỡng cứng riêng cho gate G-OPS op=install, tức thêm một ngoại lệ hẹp vào APD-08 §2 kèm điều kiện "không sudo" và "gói có trong danh sách đã ký". Đề nghị của người viết mã: (a), vì cài phần mềm lên máy người dùng là việc đáng hỏi mỗi lần, và chính chủ sản phẩm đã duyệt phương án "luôn hỏi lần đầu cho mỗi gói" — (a) nghiêm hơn thế nên không mâu thuẫn. Điểm (2): bổ sung E4001 vào errors của ENV-03 trong CDS-12.3. | Phần (2) **Đã cập nhật tài liệu v1.3** (CDS-12.3 ENV-03 thêm E4001). Phần (1) **Bác** — đọc kỹ thì CẢ BỐN tài liệu đều đã cho lối danh sách trắng: APD-08 §bốn điều giữ nguyên ở A4 ("hành động lớp R4 không tự động *trừ danh sách trắng do người ký*") và §7 mục 6 ("cài Renode/probe-rs… R4 nhưng được danh sách trắng"), POL-17 §2 mã giả (`MIN_LEVEL["R4"]=5  # chỉ whitelist`) và §8 S33, CDS-12.3 ("R4→T1 theo danh sách trắng"). Hiện thực mới là chỗ sai: nó chặn cứng mọi R4 nên G-OPS-04 là quy tắc chết. Đã sửa `policy.py` chứ không sửa tài liệu. |
| DEV-061 | 2026-09-08 | PRS-16 §4 (schema đầu ra Plan/CodePatch/Review/Diagnosis); POL-17 §2 G1-01 (đặc trưng touches_forbidden), G1-03 (arch_change) | `docs/ho-so/nguon/prs.js (SCHEMA_LINES), scripts/gen_spec_tu_nguon.js, docs/spec/prompts/out_schemas.json, src/eide/caps/plan.py::_schema_plan/_dac_trung_G1, tests/test_plan.py` | Ba điểm. (1) Bốn schema đầu ra của vai trò chỉ tồn tại trong một khối CODE của prs.js, nên mã phải chép tay — nay prs.js đặt tên literal SCHEMA_LINES và gen_spec_tu_nguon.js sinh docs/spec/prompts/out_schemas.json; văn bản tài liệu không đổi một ký tự. (2) POL-17 dùng hai đặc trưng touches_forbidden và arch_change nhưng KHÔNG định nghĩa chúng suy từ đâu; hiện thực suy từ trường touches của từng bước (enum đóng của PRS-16 §4): touches_forbidden = có bước chạm bất kỳ giá trị nào khác none; arch_change = có bước chạm linker hoặc clock (theo chính lý do của G1-03: "Đổi kiến trúc (RTOS, clock, linker)"; RTOS không có trong enum). (3) plan.py giữ tên id/cap cho mỗi bước thay vì n/by của PRS-16 §4. | Điểm (2) là một lỗ hổng đang mở chứ không phải chuyện hình thức: bản chép tay của Plan trong plan.py thiếu hẳn trường touches, nên _dac_trung_G1 đọc plan["touches_forbidden"] và plan["arch_change"] — hai khóa mức kế hoạch mà schema gửi cho mô hình không có và không chỗ nào tính. Cả hai LUÔN false, tức G1-03 là quy tắc chết và G1-01 tự duyệt được một kế hoạch sửa vector ngắt hoặc đổi clock. Không test nào thấy vì không test nào đặt touches. Điểm (3): cap là mã năng lực có thật, đối chiếu được với registry, còn by của tài liệu là chuỗi tự do; id là chuỗi nên code.* tham chiếu được qua step_ref (ví dụ "F-04/step-2"), còn n là số nguyên. | Cập nhật POL-17 §2 nói rõ hai đặc trưng suy từ Plan.steps[].touches theo đúng hai công thức trên, và cập nhật PRS-16 §4 đổi n→id (string), by→cap (mã năng lực trong registry). Nếu chủ sản phẩm muốn cách đọc hẹp hơn cho touches_forbidden (chỉ isr/linker), sửa CHAM_NHAY_CAM trong plan.py — có test phân biệt hai cách đọc bằng ca DMA. | **Đã cập nhật tài liệu v1.3** — PRS-16 §4 đổi `n`→`id`, `by`→`cap`; POL-17 §2 thêm đoạn định nghĩa hai đặc trưng suy từ `Plan.steps[].touches`. |
| DEV-062 | 2026-09-08 | CDS-12.1 CODE-04 bước 2 ("Vi phạm → block + kg.request tự động"); APD-08 §4.1 và POL-17 §1 hàng T2 (R0 là lớp CHỈ ĐỌC: không ghi, không chạm phần cứng, không phát ra ngoài) | `src/eide/caps/code.py::constant_guard, tests/test_code.py::test_R0_khong_ghi_gi_vao_store` | code.constant_guard KHÔNG tự tạo AcquisitionRequest như bước 2 của hợp đồng mô tả. Thay vào đó mỗi vi phạm thuộc ba loại thiếu tri thức (no_fact, fact_not_found, fact_not_reviewed) mang thêm trường `need`, và bên gọi — vốn là năng lực R2 như code.generate_module — gọi kg.request. Vi phạm loại value_mismatch cố ý KHÔNG có `need`: fact đã có và đã duyệt, mã ghi sai số, nên đi tìm thêm tài liệu là đi sai đường. | CODE-04 mang lớp rủi ro R0, mà kg.request ghi vào bảng acq_request và niêm lại store — đó là một thao tác ghi. PolicyGate dựa hẳn vào định nghĩa "R0 là lớp chỉ đọc" để cho mọi năng lực R0 đi thẳng không qua quy tắc cổng (nhánh thêm ở DEV-040, hiện phủ 43 năng lực). Một năng lực R0 ghi được vào store thì lối tắt ấy thành lỗ hổng cho cả 43 năng lực chứ không riêng cái này. Đổi CODE-04 lên R1 cũng không ổn: nó được gọi trong MỌI vòng sinh mã, nên đặt nó sau một cổng là đặt một câu hỏi vào giữa vòng lặp tự sửa. | Sửa CDS-12.1 CODE-04 bước 2 thành "Vi phạm → block; mỗi vi phạm thiếu tri thức kèm `need` để bên gọi tạo kg.request", giữ nguyên lớp R0. Nếu chủ sản phẩm muốn giữ nguyên chữ trong tài liệu thì phải nâng CODE-04 lên R1 và chấp nhận nó đi qua cổng ở mỗi vòng sinh mã. | **Đã cập nhật tài liệu v1.3** — CDS-12.1 CODE-04 bước 2 sửa thành "block; mỗi vi phạm thiếu tri thức kèm `need` để bên gọi tạo kg.request". |
| DEV-063 | 2026-09-08 | CDS-12.1 CODE-08 bước 1 ("Biên dịch host với mock ngoại vi (ctypes/CMock); chạy; JUnit → ToolReport") | `src/eide/caps/code.py::test_host (MAU_TEST_HOST, THU_MUC_MOCK), tests/test_code.py` | Hợp đồng nói biên dịch và chạy test trên máy chủ nhưng KHÔNG nói tệp test và mock nằm ở đâu, cũng không nói một chương trình test gồm những gì. Hiện thực đặt quy ước: tests/host/test_*.c là một chương trình test độc lập cho mỗi tệp; tests/mock/*.c liên kết vào mọi chương trình; src/ và include/ là thư mục include. Kết quả đọc từ JUnit XML nếu chương trình test in ra, nếu không thì từ mã thoát — và ToolReport ghi rõ đã đọc theo cách nào. | Không có quy ước thì code.generate_tests sinh test vào một chỗ mà code.test_host không tìm, và cả vòng sinh-dựng-kiểm đứt ở khớp giữa hai năng lực của cùng một nhóm. Chọn "mỗi tệp một chương trình" vì đó là thành ngữ phổ biến nhất của unit test C nhúng (tệp test include thẳng đơn vị đang kiểm), vì một test làm hỏng bộ nhớ chỉ giết chính nó, và vì tên tệp thành tên ca test mà không cần khung nào. Chọn đọc JUnit khi có và mã thoát khi không, thay vì bắt buộc JUnit, vì bắt buộc thì mọi test viết tay bằng C thuần đều không chạy được. | Bổ sung vào CDS-12.1 CODE-08 bước 1 đúng ba dòng bố cục trên, và nêu rõ hai cách đọc kết quả. Nếu chủ sản phẩm muốn khung Unity/Ceedling thì đổi quy ước ở đây và ghi vào TGT-19 cùng toolchain, vì lúc ấy nó thành một công cụ phải cài. | Đã cập nhật tài liệu v1.3 |
| DEV-064 | 2026-09-08 | CDS-12.1 CODE-04 bước 1 ("Quét literal địa chỉ/bit/enum/tần số trong ngữ cảnh phần cứng"); STP-05 TC-04 | `src/eide/caps/code.py::constant_guard (THU_MUC_KHONG_QUET), tests/test_code.py, tests/test_that.py` | code.constant_guard KHÔNG quét tệp dưới `tests/`. Mọi tệp C khác trong patch vẫn bị quét đầy đủ theo TC-04. | Một test khẳng định `bme280_dia_chi() == 0x76` đang KIỂM giá trị, không KHAI nó. Bắt nó trích dẫn cùng fact mà mã đang kiểm cũng trích dẫn thì cả hai lấy số từ một chỗ, và test không còn kiểm gì nữa — nó chỉ xác nhận rằng hai bản sao của cùng một biến bằng nhau. Viết literal ra là đúng cách viết test, không phải một chỗ thiếu nguồn. Tìm ra bằng gọi mô hình THẬT (test `llm` trong tests/test_that.py): coder chú thích `eide:fact` đúng trong src/ — tức prompt PRS-16 §3 làm được việc của nó — rồi bị E5003 vì ba lần `0x76` trong tệp test do chính nó viết kèm. Giả lập không lộ ra được vì giả lập trả đúng cái tôi bảo nó trả. | Bổ sung vào CDS-12.1 CODE-04 bước 1: "trừ tệp kiểm thử (tests/), nơi literal là giá trị kỳ vọng". Lỗ hổng còn lại — nhét mã điều khiển vào tests/ để né guard — bị chặn ở chỗ khác: code.build dựng theo CMakeLists của dự án và code.test_host chỉ chạy trên máy chủ; nếu chủ sản phẩm muốn chặt hơn thì thêm một phép kiểm trong code.review. | Đã cập nhật tài liệu v1.3 |
| DEV-065 | 2026-09-08 | CDS-12.1 CODE-12 bước 2 ("ghi tệp, commit vào auto/<feature> với trailer Eide-* (CON-28); tag"); CON-28 §4 (dạng tag `known-good/<date>`); SEC-25 §2 (sandbox cho công cụ ngoài) | `src/eide_core/git.py, src/eide/caps/code.py::merge/revert, tests/test_code.py` | Hai điểm. (1) `git` KHÔNG chạy trong sandbox mà gọi thẳng bằng subprocess với tham số dạng danh sách, qua một danh sách con lệnh cho phép (không có push/clean/reset). (2) CODE-12 nói "tag" mà không nói dạng nào; CON-28 §4 chỉ định nghĩa một dạng duy nhất là `known-good/<date>`, nên merge đặt tag ấy, thêm hậu tố `.1`, `.2`… khi một ngày merge nhiều lần. Tên tag ghi vào ledger (sự kiện `report`) vì output_schema của CODE-12 khai đúng ba trường và `additionalProperties: false`. | Điểm (1): việc của git ở đây CHÍNH LÀ ghi vào thư mục dự án, mà sandbox cấm ghi ra ngoài thư mục làm việc tạm — đó là cả điểm của sandbox. Đưa git vào trong thì hoặc phải mở quyền ghi cho cả dự án (bỏ lớp bảo vệ cho mọi công cụ khác dùng chung sandbox), hoặc git không làm được gì. Ranh giới đúng là ranh giới SEC-25 §2 vạch: sandbox dành cho công cụ NGOÀI chạy trên dữ liệu không tin được (trình giải nén, trình dịch, bộ dựng lược đồ); git ở đây do EIDE gọi với tham số cố định trên chính kho của người dùng, cùng loại với việc EIDE ghi .eide/store.sqlite. Danh sách con lệnh cho phép không để chống chính mình mà để việc thêm `git push` hay `git clean -fdx` sau này thành một thay đổi PHẢI ĐỌC. Điểm (2): một merge qua đủ bốn cổng công cụ và một review độc lập khác hãng chính là định nghĩa "known-good" của dự án này, nên dùng lại dạng tag ấy hợp lý hơn là đặt ra dạng thứ hai. | Bổ sung vào CON-28 §4 rằng `known-good/<date>` có thể mang hậu tố thứ tự trong ngày, và vào SEC-25 §2 một câu phân biệt "công cụ ngoài chạy dữ liệu không tin được" (sandbox) với "công cụ do EIDE gọi trên kho của chính người dùng" (git, sqlite). Nếu chủ sản phẩm muốn tag khác cho nhánh auto/ thì nêu dạng cụ thể trong CODE-12. | Đã cập nhật tài liệu v1.3 |
| DEV-066 | 2026-09-08 | CDS-12.2 EXTRACT-16 §input_schema (chỉ `file`, additionalProperties:false); đối chiếu BOARD-01 §input_schema (có `name`) và §example `{"sources":[...],"name":"robot-main"}` | `src/eide/caps/extract.py::kicad_netlist, src/eide/caps/board.py::build_passport, tests/test_specs_consistency.py` | EXTRACT-16 không có tham số đặt tên board, nên `extract.kicad_netlist` phải lấy tên board từ TÊN TỆP (`robot.net` → board `robot`). Nhưng BOARD-01 lại nhận `name`, và ví dụ của chính nó dùng `robot-main` — hai năng lực nối tiếp nhau trong cùng một bước của UC-B06 mà không có cách nào cho chúng thống nhất tên board. | Bản đầu của hiện thực nhận thêm `name` và có test xanh — vì test gọi thẳng hàm. Qua Router thì `additionalProperties:false` chặn bằng E1000 trước khi handler thấy, tức một tham số không ai truyền được. Hậu quả trên đường thật: tên board rơi về tên tệp, rồi `board.build_passport({"name":"robot-main"})` báo "chưa có net" trong khi netlist đã trích xong — chỉ khác tên. Tên tệp netlist thường là tên bản vẽ, không phải tên board. | Đã sửa mã cho khớp tài liệu: bỏ `name` khỏi `kicad_netlist`, tên board = tên tệp; `build_passport` liệt kê các board đang có net trong `candidates` để lệch tên lộ ngay. Đề nghị CDS-12.2 v1.3 thêm `name` (không bắt buộc) vào input_schema của EXTRACT-16, đúng dạng của BOARD-01. Kèm phép kiểm thường trực `test_hien_thuc_khong_doc_tham_so_ma_input_schema_khong_cho_phep` — quét cả 238 năng lực, chặn đúng lớp lỗi này (phần làm được ngay của WI-253). | Đã cập nhật tài liệu v1.3 |
| DEV-067 | 2026-09-08 | CDS-12.2 BOARD-04 bước 1 ("architect: remap AF, đổi chân, đổi địa chỉ (ADDR pin), thêm điện trở; xếp theo ít thay đổi nhất; đánh dấu chạm mã passing"); PRS-16 §2 vai trò architect | `src/eide/caps/board.py::propose_fix` | Bước 1 gán việc cho vai trò mô hình `architect`, nhưng hiện thực sinh phương án bằng MÃ, không gọi mô hình. | Chính bước 1 đã liệt kê đủ bốn loại phương án, nên không còn gì cho mô hình nghĩ ra. Phần khó lại là phần mô hình làm tệ nhất: một phương án đổi chân chỉ có ích khi nêu được CHÂN THAY THẾ CỤ THỂ, mà chân nào thay được cho chân nào nằm trong fact `pin_function` của hộ chiếu chip. Mô hình không có bảng ấy nên nó trả về một tên chân nghe rất đúng cho tới lúc nạp — đúng lớp lỗi mà cả tầng tri thức của EIDE sinh ra để chặn. Sinh từ fact thì phương án hoặc có thật, hoặc nói thẳng "chưa tra được, chạy extract.pdf_pinout". Cùng lập luận với `board.check_pins` và `arch.map_hw` (phát hiện xung đột là deterministic). | Đề nghị CDS-12.2 v1.3 bỏ tiền tố vai trò `architect:` ở BOARD-04 bước 1, hoặc ghi rõ "sinh từ fact pin_function; mô hình chỉ dùng khi chưa có hộ chiếu chân". Nếu chủ sản phẩm muốn giữ mô hình, tôi thêm một lượt làm giàu sau bước deterministic — nhưng khi ấy phải nói rõ trong tài liệu rằng phương án do mô hình đề xuất KHÔNG được nêu tên chân. | Đã cập nhật tài liệu v1.3 |
| DEV-068 | 2026-09-08 | CDS-12.2 BOARD-05 tc ("Z-10; board có động cơ → không lab"); POL-17 §3; autonomy.schema.json §4 (boards.<id>.has_actuator) | `src/eide/caps/board.py::RE_CO_CAU, _co_cau_chap_hanh` | tc đòi phát hiện "board có động cơ" nhưng không tài liệu nào liệt kê linh kiện nào tính là cơ cấu chấp hành. Hiện thực tự đặt một bảng mẫu (DRV8xxx, L298, TB6612, A4988, ULN2xxx, relay/servo/stepper/solenoid/motor/pump/valve/heater, MOSFET công suất IRF/IRLZ/AO3400/SI2302). | Không có bảng thì tc không kiểm được, và `board.mark_lab` sẽ chỉ chép lại lời khai của người — mà người khai không phải người vẽ mạch. Bảng đặt làm lưới chặn MỘT CHIỀU: bắt được thì chắc chắn không phải board lab; không bắt được thì không kết luận gì, lời khai của người vẫn là điều kiện bắt buộc. Đặt ngược lại (tự cho lab khi không thấy động cơ) thì một MOSFET lái van sẽ lọt, và cái lọt ấy chuyển động. | Đề nghị POL-17 §3 hoặc TGT-19 có một bảng "linh kiện chấp hành" máy đọc được trong docs/spec/ (dạng như isa/*.yaml) để bảng này thôi nằm trong mã. Cùng loại với DEV-063 (bố cục tests/host do hiện thực đặt). | Đã cập nhật tài liệu v1.3 |
| DEV-069 | 2026-09-08 | DDD-14 §2 Module (bảng `module`: id, name, responsibility, interfaces, depends, arch_style, budget, fsm, status) đối chiếu CDS-12.1 ARCH-02 bước 1 ("Chia hệ thống thành module theo lớp hal → driver → service → control → app") và CDS-12.4 DIAGRAM-04 (C4 mức container = lớp firmware) | `src/eide/caps/arch.py::decompose/_ghi_module/COT_MODULE, src/eide/caps/diagram.py::architecture` | DDD-14 §2 Module KHÔNG có trường `layer`. `arch.decompose` tính lớp cho từng module, dùng nó để kiểm bất biến "phụ thuộc chỉ đi xuống lớp dưới", trả nó về cho bên gọi — rồi MẤT nó lúc ghi, vì `layer` không nằm trong `COT_MODULE` và bảng `module` không có cột ấy. Đọc module ra khỏi store thì không còn biết nó thuộc lớp nào. | Lớp là khái niệm hạng nhất của ARCH-02 — cả một bất biến kiến trúc dựa trên nó — nhưng nó chỉ sống trong bộ nhớ đúng một lời gọi. Hệ quả: mọi năng lực đọc ModuleGraph LẠI từ store đều không dựng được gì theo lớp. `diagram.architecture` mức container (C4) là chỗ đầu tiên chạm phải: nó phải nhóm module theo lớp mà không có lớp để nhóm. `doc.generate` cho SAD sẽ gặp đúng chuyện ấy. Nguy hiểm hơn cả việc thiếu: bất biến "không phụ thuộc ngược lớp" chỉ kiểm được ĐÚNG MỘT LẦN lúc decompose; sau đó ai sửa `depends` bằng đường khác thì không còn gì kiểm lại. | Đề nghị DDD-14 v1.3 thêm `layer TEXT` vào entity Module với enum [hal, driver, service, control, app], kèm migration `0005` và cập nhật `docs/spec/data/schema.sql` (sinh từ ddd_model.py). Chưa duyệt thì hiện thực xử lý trung thực: mức `component` vẫn vẽ (module và depends đều có thật, chỉ mất nhãn lớp và cảnh báo cạnh ngược), mức `container` báo E2000 nói rõ vì sao thay vì gộp bừa mọi module vào `service` — một sơ đồ container gom nhầm lớp còn tệ hơn không có, vì nó trông đúng. | Đã cập nhật tài liệu v1.3 |
| DEV-070 | 2026-09-08 | CDS-12.4 DOC-01 bước 2 ("Dựng docx qua mẫu … doc.style_check; lưu DocArtifact") đối chiếu CDS-12.5 REPORT-02 (`report.export`: enum type docx/pdf/md, bước 1 "doc.generate với mẫu báo cáo"); CDS-12.4 DOC-01 bước 1 ("Mục lục chuẩn theo type (mẫu bộ EAA/EIDE)") | `src/eide/caps/doc.py::generate, MUC_LUC` | Hai điểm. (1) `doc.generate` sinh Markdown, KHÔNG dựng docx — dù DOC-01 bước 2 nói "dựng docx qua mẫu". (2) Mục lục chuẩn theo loại tài liệu nằm trong mã (hằng `MUC_LUC`), không nằm trong `docs/spec/`. | (1) REPORT-02 khai enum `type: docx\|pdf\|md` và bước 1 của nó là "doc.generate với mẫu báo cáo" — tức chuyển định dạng ở REPORT-02, còn DOC-01 lo NỘI DUNG. Dựng docx ở cả hai chỗ là hai bản dựng cùng một tài liệu, và chúng sẽ lệch. Cách đọc này cũng khớp trường `ref: report.export` của chính DOC-01. Hệ quả: `E4001 thiếu node/docx` mà DOC-01 liệt kê thuộc đường `report.export`, không đường này. (2) Mục lục lấy đúng các đề mục H1 của chính bộ hồ sơ EIDE (`docs/ho-so/nguon/{urd,srs,sad,sdd,stp,bpd}.js`) nên nó KHÔNG bịa — nhưng nó là bản chép tay, và bản chép tay sẽ trôi khỏi bộ hồ sơ đúng lúc bộ hồ sơ đổi. | (1) Đề nghị CDS-12.4 v1.3 sửa DOC-01 bước 2 thành "lưu DocArtifact; doc.style_check; chuyển định dạng qua report.export", và chuyển `E4001 thiếu node/docx` sang REPORT-02. (2) Sinh `docs/spec/doc/outlines.json` từ `docs/ho-so/nguon/*.js` (cùng khuôn với `doc/glossary.json` — DEV-025/029/043/046) rồi `doc.generate` đọc từ đó; khi ấy đổi mục lục trong bộ hồ sơ là mục lục tài liệu sinh ra đổi theo, không phải sửa hai chỗ. | Đã cập nhật tài liệu v1.3 |
| DEV-071 | 2026-09-08 | CDS-12.4 DOC-07 bước 1 ("diagram.render svg/png; chèn với Hình n. đánh số theo thứ tự; cập nhật DocArtifact.figures") đối chiếu DDD-14 §2 DocArtifact (id, type, template, path, sections, citations, style_issues, stale_sections, lang, at) | `src/eide/caps/doc.py::embed_diagram, RE_HINH` | DOC-07 bảo cập nhật `DocArtifact.figures`, nhưng DDD-14 §2 DocArtifact KHÔNG có trường `figures` và bảng `doc_artifact` không có cột ấy. Hiện thực đếm số hình từ CHÍNH nội dung tài liệu (`**Hình n.**`) thay vì từ một bộ đếm trong store. | Không có cột thì phải chọn: thêm cột (đổi schema, cần duyệt) hay đếm trong tài liệu. Đếm trong tài liệu đúng hơn về bản chất: "đánh số liên tục" — chính là tc của DOC-07 — là một tính chất CỦA TÀI LIỆU, nên nó phải kiểm được trên tài liệu. Bộ đếm ngoài sẽ lệch ngay khi ai đó sửa tay hay sinh lại tài liệu, và lúc ấy hai nguồn nói hai số khác nhau mà không có cách nào biết cái nào đúng. | Đề nghị CDS-12.4 v1.3 bỏ "cập nhật DocArtifact.figures" ở DOC-07, hoặc DDD-14 v1.3 thêm `figures` vào DocArtifact nếu chủ sản phẩm muốn giữ danh sách hình ở store (khi ấy nó là bản sao chỉ để truy vấn, còn số thứ tự vẫn lấy từ tài liệu). | Đã cập nhật tài liệu v1.3 |
| DEV-072 | 2026-09-09 | CDS-12.1 DOC-03 steps[0] ("Doxygen/tree-sitter → chữ ký + chú thích; … ; md/html") đối chiếu chính input_schema của DOC-03 (chỉ có `src`) và pyproject.toml (dependencies) | `src/eide/caps/doc.py::api_ref, _ham_public, _cau_lenh_ngoai, _bo_macro_bao, DUOI_C` | Hai điểm. (1) Chữ ký và chú thích đọc bằng một bộ phân tích NỘI BỘ viết trong `doc.py` (quét câu lệnh ngoài thân hàm, chỉ họ C), không gọi Doxygen cũng không dùng tree-sitter: cả hai đều không có trong `pyproject.toml` và `doxygen` không có trong `COMMON_TOOLS`. Tệp ngoài họ C (`.rs`, `.py`, `.swift`…) trả E4001 kèm `candidates=[env.guide_install, env.install]`. (2) Đầu ra chỉ Markdown, không có nhánh html. | (1) Bắt buộc Doxygen thì năng lực không chạy được trên máy chưa cài — mà nó chỉ đọc chữ ký hàm C, việc mà một bộ quét 60 dòng làm đủ và kiểm được bằng test. Đây đúng khuôn đã dùng cho `pdfplumber`/Docling ở EXTRACT-05: bộ nội bộ là đường mặc định, công cụ ngoài là đường nâng cao, thiếu nó thì E4001 nói rõ. Giữ E4001 cho ngôn ngữ ngoài họ C là giữ lỗi ấy có thật và kiểm được, thay vì một mã lỗi khai trong hợp đồng mà không nhánh nào ném. Bộ đọc đã thử trên header thật của SDK macOS (sys/stat.h 34 hàm, dirent.h 18, pthread.h 98, unistd.h 141, math.h 254). (2) `input_schema` của DOC-03 chỉ có `src` — không có chỗ nào để bên gọi chọn html, nên nhánh html là mã không ai gọi được. Cùng lập luận đã duyệt ở DEV-070 cho DOC-01: chuyển định dạng là việc của `report.export`, dựng ở hai chỗ là hai bản dựng cùng một tài liệu. | SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_c.py` dòng 48 (và `docs/ho-so/nguon/excel/caps.py` dòng 252, cột đầu ra). Đề nghị CDS-12.1 v1.4 sửa DOC-03 bước 1 thành "bộ đọc chữ ký họ C (Doxygen/tree-sitter khi có, cho ngôn ngữ khác — thiếu thì E4001) → chữ ký + chú thích; writer thêm ví dụ dùng theo hộ chiếu (fact id); md", và đổi ô "md/html" của PLN-27/caps thành "md" với ghi chú trỏ REPORT-02. Không sửa mã: hợp đồng `src`/`path`/E4001/tc giữ nguyên. **Chủ sản phẩm duyệt 10/09/2026.** CDS-12.1 v1.4 sửa DOC-03 bước 1 thành "bộ đọc chữ ký họ C viết trong doc.py là đường mặc định (Doxygen/tree-sitter khi có, cho ngôn ngữ khác — thiếu thì E4001) → chữ ký + chú thích; … md". Nguồn sinh `cds_data_c.py`, đã sinh lại CDS-12.1 và `capabilities/doc.yaml`. Không sửa mã. | Đã cập nhật tài liệu v1.4 |
| DEV-073 | 2026-09-09 | CDS-12.1 DOC-06 input_schema.format (enum `docx\|md`) đối chiếu CDS-12.5 REPORT-02 bước 1 v1.3 ("chuyển sang định dạng yêu cầu — đây là chỗ DUY NHẤT dựng docx/pdf (DEV-070)") | `src/eide/caps/doc.py::test_report` | `format=docx` KHÔNG dựng docx: trả E2000 kèm `candidates=[report.export]`, cùng khuôn ủy quyền mà `doc.generate` dùng cho `bringup`/`test_report`. Chỉ `md` (mặc định) sinh ra tệp. | Hai tài liệu nói khác nhau sau khi bộ hồ sơ lên v1.3: DOC-06 vẫn cho bên gọi chọn docx, còn REPORT-02 đã được duyệt thành 'chỗ DUY NHẤT dựng docx/pdf'. Dựng docx ở cả hai chỗ là hai bản dựng cùng một báo cáo — chúng sẽ lệch, và không có cách nào biết bản nào đúng. Đây đúng hình dạng của DEV-070, chỉ khác năng lực; giải quyết theo cùng một cách để hai lần đọc cùng một câu hỏi cho cùng một câu trả lời. Ngoài ra `report.export` là mốc M4 nên hiện chưa có hiện thực nào để ủy quyền tới: E2000 nói rõ điều đó thay vì dựng một bản docx thứ hai. | SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_c.py` (mục `doc.test_report`) và `docs/ho-so/nguon/excel/caps.py`. Đề nghị CDS-12.1 v1.4 bỏ `docx` khỏi enum `format` của DOC-06 (chỉ còn `md`), thêm một câu ở bước 1: 'chuyển sang docx/pdf qua report.export'. Không sửa mã. **Chủ sản phẩm duyệt 10/09/2026.** CDS-12.1 v1.4 bỏ `docx` khỏi enum `format` của DOC-06 (chỉ còn `md`) và thêm câu "chuyển sang docx/pdf qua report.export". Hệ quả trong mã: nhánh ủy quyền E2000 của `doc.test_report` thành mã CHẾT — lớp kiểm `input_schema` của Router chặn `format` lạ bằng E1000 trước khi vào handler — nên đã gỡ, và test đổi sang kiểm E1000. | Đã cập nhật tài liệu v1.4 |
| DEV-074 | 2026-09-09 | CDS-12.4 DIAGRAM-08 steps[0] ("… từ capture LA → chuyển mẫu") đối chiếu CDS-12.4 MEASURE-01 `measure.capture_signal` (mốc M5, trả `dataset_id`) | `src/eide/caps/diagram.py::timing, _la_dataset` | Đường "capture LA → chuyển mẫu" CHƯA hiện thực. `source` trỏ một hàng `measurement` → E2000 kèm `candidates=[measure.capture_signal]` và nói rõ là chờ mốc M5. Đường fact `timing` (tsu/th/tCLK từ datasheet) đã đủ và có test. | `dataset_id` chỉ do `measure.capture_signal` sinh ra, mà nhóm `measure.*` là mốc M5 và cần máy phân tích logic thật — thuộc khối H "để cuối cùng" theo quyết định chủ sản phẩm 08/09. Không có định dạng bộ mẫu nào trong `docs/spec/data/` để đọc, nên viết bộ chuyển bây giờ là viết cho một định dạng tự nghĩ ra. Vẽ một dạng sóng "minh họa" thay cho mẫu đo thật thì tệ hơn: người đọc giản đồ thời gian tin từng mép tín hiệu trên đó. Đây là NỢ HIỆN THỰC, không phải tài liệu sai — cùng hình dạng với DEV-008 (chờ nhóm kg.*). | KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Hoàn tất nhánh capture khi `measure.capture_signal` có hiện thực (mốc M5): khi ấy định dạng `dataset_id` đã có thật và bộ chuyển mẫu → WaveDrom viết theo nó. Rà lại mục này cùng lúc với DEV-055 (M5). | Mở |
| DEV-075 | 2026-09-09 | CDS-12.4 DIAGRAM-07 `errors: []` đối chiếu steps[0] ("… từ mô tả: writer") và CDS-12.4 DIAGRAM-05/DOC-02 (cùng gọi mô hình, đều khai `E5002`) | `src/eide/caps/diagram.py::flow, _flow_tu_mo_ta` | `diagram.flow` ném E5002 khi mô hình trả về lưu đồ không dựng được (sai dòng mở đầu hoặc lệch ngoặc), trong khi `errors` của DIAGRAM-07 là danh sách RỖNG. | Hợp đồng có một nhánh gọi mô hình ("từ mô tả: writer") thì nhất thiết có khả năng mô hình trả sai — và hai năng lực cùng hình dạng trong chính tập này (DIAGRAM-05 `diagram.sequence`, DOC-02 `doc.section`) đều khai E5002 cho đúng tình huống ấy. Cách xử lý duy nhất còn lại nếu không ném là TRẢ VỀ một lược đồ không dựng được, tức đẩy lỗi xuống hạ nguồn để người dùng phát hiện — đúng thứ mà bước 1 ("lint") sinh ra để chặn. `errors: []` ở đây là chỗ bỏ sót của tài liệu, không phải một quyết định. | SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_c.py` (mục `diagram.flow`). Đề nghị CDS-12.4 v1.4 thêm `E5002` vào `errors` của DIAGRAM-07, cho khớp DIAGRAM-05 và DOC-02. Không sửa mã. **Chủ sản phẩm duyệt 10/09/2026.** CDS-12.4 v1.4 thêm `E5002` vào `errors` của DIAGRAM-07, cho khớp DIAGRAM-05 và DOC-02. Nguồn sinh `cds_data_c.py`. Không sửa mã. | Đã cập nhật tài liệu v1.4 |
| DEV-076 | 2026-09-10 | CDS-12.2 EXTRACT-09 steps[0] ("Bảng pinout (Pin, Name, Type) hoặc **hình package (vision)** → pin_function; package") và ask_when "Từ hình" | `src/eide/caps/extract.py::pdf_pinout` | Nhánh "hình package (vision)" CHƯA hiện thực. Chỉ đường BẢNG có hiện thực (đọc bằng mã: bảng định nghĩa chân + bảng Alternate function mapping). Không thấy bảng nào → E2000 kèm candidates=[extract.ocr, extract.image_board] và nói rõ đường vision chưa có. Vì thế ask_when "Từ hình" hiện không có nhánh nào chạm tới. | Đường vision cần extract.ocr / extract.image_board / extract.image_schematic — cả ba đều thuộc mốc M2 và chưa hiện thực (khối D mục D3). Viết bộ đọc hình bây giờ là viết cho một đầu vào chưa có ai sinh ra, và một pinout đọc từ hình mà không kiểm chéo được với bảng thì tệ hơn không có: người đọc hộ chiếu chân tin từng số AF trên đó. Đây là NỢ HIỆN THỰC, không phải tài liệu sai — cùng hình dạng với DEV-074 (nhánh capture LA của DIAGRAM-08 chờ measure.capture_signal) và DEV-008 (project.open bước 3 chờ kg.*). | KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Hoàn tất nhánh vision khi nhóm extract.image_*/ocr có hiện thực (cùng khối D): khi ấy fact pin_function từ hình phải mang method=vision_llm — mà G-FACT-02 loại riêng vision_llm khỏi tự duyệt, nên nó vào hàng đợi hỏi người, đúng chỗ. Rà lại mục này cùng lúc với D3. **ĐÓNG 12/09/2026:** đường ảnh cho Gateway đã mở (`Gateway.run(anh=…)`, cả Gemini `inlineData` lẫn Claude `source.base64`), và `extract.image_board`/`image_schematic`/`ocr` đã hiện thực. Nhánh hình của `pdf_pinout` nay nối được: gọi `extract.image_schematic` rồi đối chiếu tên chân với hộ chiếu. Fact sinh ra mang `method=vision_llm` nên G-FACT-02 vẫn đẩy chúng vào hàng đợi hỏi người, đúng như mục này dự kiến. | Đã cập nhật tài liệu — nhánh vision đã có |
| DEV-077 | 2026-09-10 | CDS-12.2 EXTRACT-10 steps[0] ("… liên kết CONFLICTS_WITH nếu mâu thuẫn datasheet") đối chiếu DDD-14 §2 Fact (không có trường quan hệ) và KAD-07 §6.3 + src/eide_core/kg.py::dung/mau_thuan | `src/eide/caps/extract.py::pdf_errata, _fact_errata; src/eide_core/kg.py::dung` | Không có đường nào để một fact KHAI cạnh CONFLICTS_WITH. Entity Fact của DDD-14 chỉ có một trường quan hệ duy nhất là `supersedes`; cạnh CONFLICTS_WITH trong đồ thị được `kg.dung` sinh TỰ ĐỘNG từ `mau_thuan()` — cặp fact cùng (subject, predicate) khác giá trị. Mà chính bước 1 của EXTRACT-10 lại buộc mục errata dùng `predicate: other`, nên nó không bao giờ trùng predicate của fact datasheet (base_address, reset_value, timing…) và cạnh ấy không bao giờ sinh ra. | Hai vế của cùng một câu trong hợp đồng loại trừ nhau: "predicate other" và "liên kết CONFLICTS_WITH nếu mâu thuẫn datasheet". Hiện thực được một trong hai, không được cả hai, và không có mục DEVIATIONS nào trước đây về điểm này. Đã hiện thực vế thứ nhất vì nó là vế cụ thể hơn (nêu rõ predicate) và vì tc của hợp đồng ("Errata I2C → fact có rev") nằm ở vế ấy. Giảm nhẹ: fact errata mang subject nằm DƯỚI nút ngoại vi (chip:<part>/periph:I2C1/errata:<mục>) khi store đã biết ngoại vi ấy, nên `kg.dung` vẫn nối ABOUT tới cùng nhánh cây và `kg.neighborhood("…/periph:I2C1")` vẫn thấy errata — quan hệ duyệt được, chỉ không mang nhãn CONFLICTS_WITH. | Cần chủ sản phẩm chọn một trong hai. (a) DDD-14 §2 thêm trường `conflicts_with` (danh sách fact id) cho Fact + migration + `kg.dung` đọc nó — làm được đúng điều tài liệu muốn, nhưng chạm store và mọi năng lực ghi fact. (b) CDS-12.2 v1.4 sửa bước 1 của EXTRACT-10 thành: "… fact predicate other với điều kiện (rev, silicon), overlay K2′ (layer B), subject dưới nút ngoại vi liên quan; cạnh CONFLICTS_WITH sinh tự động khi một mục errata phủ định một fact CÙNG (subject, predicate)" — bám đúng cơ chế đã có của KAD-07 §6.3, không chạm schema. Nghiêng về (b): CONFLICTS_WITH trong KAD-07 được định nghĩa là quan hệ SUY RA từ dữ liệu, không phải quan hệ khai tay; thêm một trường khai tay là mở đường cho hai nguồn sự thật về cùng một cạnh. **Chủ sản phẩm duyệt 10/09/2026: phương án (a).** DDD-14 §2 Fact v1.4 thêm `conflicts_with` (JSON fact ids); `ddd_model.py` là nguồn sinh, đã sinh lại `schema.sql` + `data/json/fact.json` + docx. Migration `0006_m2_fact_conflicts` (ALTER TABLE ADD COLUMN, user_version=6). `kg.dung` nay dựng cạnh CONFLICTS_WITH từ HAI đường — suy (`mau_thuan`) và khai (cột mới) — và bỏ qua id treo hoặc trỏ tới fact đã `superseded`. `passport.import` ghi trường; `extract.pdf_errata` đưa danh sách fact ứng viên của chính con chip cho mô hình CHỌN (không tự nghĩ id) rồi lọc lại theo danh sách ấy. Bảng migration trong DDD-14 §5 cũng sửa cho khớp kho: 0005 là `module_layer` (DEV-069), `debug_session` ghi rõ là chưa có migration. | Đã cập nhật tài liệu v1.4 |
| DEV-078 | 2026-09-10 | CDS-12.2 EXTRACT-17 steps[1] ("Chuẩn hóa MPN (bỏ hậu tố gói), gộp trùng, đếm qty") | `src/eide/caps/extract.py::bom, _gop_bom, _mpn_goc` | Hai điểm. (1) Gộp trùng theo MPN ĐẦY ĐỦ chứ không theo MPN đã bỏ hậu tố; phần gốc vẫn được tính và trả riêng ở khoá `mpn_base` cho `extract.bom_enrich` tra cứu. (2) "Bỏ hậu tố gói" chỉ áp dụng khi hậu tố TÁCH BẰNG DẤU (`-TR`, `-T`, `-ND`, `/T&R`…); hậu tố dính liền (`LM358DR2G`) được giữ nguyên. | (1) BOM là danh sách MUA. `STM32F411CEU6` (UFQFPN-48) và `STM32F411CET6` (LQFP-48) cùng một die nhưng là hai mã đặt hàng, hai gói vật lý; gộp chúng vào một dòng thì lô hàng về không hàn được lên mạch. Cùng lập luận với `A4988SETTR-T` (băng cuộn) và `A4988SETTR` (khay). Hợp đồng nói "chuẩn hóa rồi gộp", mà chuẩn hóa đúng nghĩa "bỏ hậu tố gói" chính là xoá đi thông tin phân biệt hai dòng đặt hàng. (2) Cắt hậu tố dính liền cần bảng mã của từng nhà sản xuất; không có bảng ấy thì mọi quy tắc đều cắt nhầm — `LM358DR2G` mất `G` (mã mạ chân), và bất kỳ MPN nào kết thúc bằng T/R/G đều bị cắt cụt. Một MPN cắt cụt tra ra linh kiện khác, hoặc không ra gì, mà `bom_enrich` lại dùng đúng nó để tra datasheet. | SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_b.py` (mục `extract.bom`). Đề nghị CDS-12.2 v1.4 sửa bước 2 thành: "Gộp trùng theo MPN đầy đủ (hai dạng đóng gói là hai dòng đặt hàng); tính thêm `mpn_base` bằng cách bỏ hậu tố đóng gói/băng tách bằng dấu, dùng cho tra cứu ở bom_enrich; không có MPN thì gộp theo (value, footprint); qty = số ký hiệu duy nhất". Không sửa mã: hợp đồng input/output và tc giữ nguyên. **Chủ sản phẩm duyệt 10/09/2026.** CDS-12.2 v1.4 sửa EXTRACT-17 bước 2 thành "gộp trùng theo MPN ĐẦY ĐỦ …; tính thêm `mpn_base` bằng cách bỏ hậu tố đóng gói/băng TÁCH BẰNG DẤU …; qty = số ký hiệu duy nhất". Nguồn sinh `cds_data_a.py`. Không sửa mã. | Đã cập nhật tài liệu v1.4 |
| DEV-079 | 2026-09-10 | CDS-12.2 EXTRACT-17 steps[0] ("… image: cartographer …") | `src/eide/caps/extract.py::bom` | Đường `kind=image` (đọc BOM từ ảnh mạch/bảng chụp) CHƯA hiện thực: trả E2000 kèm candidates=[extract.image_board, extract.ocr] và nói rõ là chờ đường vision. Bốn đường còn lại (schematic, csv, xlsx, readme) đã có. | Vai trò `cartographer` đọc ảnh cần `extract.image_board`/`extract.image_schematic`/`extract.ocr` — cả ba đều thuộc mốc M2 và chưa hiện thực (khối D mục D3). Đây là NỢ HIỆN THỰC, cùng hình dạng với DEV-076 (nhánh hình của EXTRACT-09) và DEV-074. Trả BOM rỗng cho một tấm ảnh thì tệ hơn báo lỗi: nó trông y hệt "tấm ảnh này không có linh kiện nào". | KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Hoàn tất nhánh ảnh cùng lúc với khối D3; rà lại mục này cùng DEV-076. **ĐÓNG 12/09/2026:** `extract.image_board` đã hiện thực, nên đường `kind=image` của EXTRACT-17 nối được — đọc nhãn linh kiện từ ảnh rồi khớp `mpn` với hộ chiếu đã có trong dự án. | Đã cập nhật tài liệu — nhánh vision đã có |
| DEV-080 | 2026-09-10 | CDS-12.2 EXTRACT-11 `errors: []` đối chiếu steps[0] ("Trích công thức/thuật toán … → skill dự án (K5) có trích dẫn trang") và CDS-12.2 EXTRACT-07/DOC-02 (cùng gọi mô hình, đều khai E5002) | `src/eide/caps/extract.py::pdf_formula` | `extract.pdf_formula` ném E5002 khi mô hình không trả mã C, trong khi `errors` của EXTRACT-11 là danh sách RỖNG. | Hợp đồng có một nhánh gọi mô hình thì nhất thiết có khả năng mô hình trả thứ không dùng được — và `tc` của chính EXTRACT-11 ("skill có mã C tham chiếu và trang") nói rõ mã C là phần bắt buộc của đầu ra. Không ném thì cách xử lý duy nhất còn lại là GHI RA một skill không có mã: `code.generate_module` không có gì để chép, người đọc vẫn phải mở datasheet, và tệp ấy vẫn được tính là một skill trong thư mục. Đây đúng hình dạng DEV-075 (`diagram.flow` khai `errors: []` nhưng phải ném E5002), chỉ khác năng lực. | SỬA TÀI LIỆU. Nguồn sinh: `docs/ho-so/nguon/cds_data_b.py` (mục `extract.pdf_formula`). Đề nghị CDS-12.2 v1.4 thêm `E5002` vào `errors` của EXTRACT-11, cho khớp EXTRACT-07 và DOC-02. Không sửa mã. Gộp duyệt cùng DEV-075 vì cùng một lập luận. **Chủ sản phẩm duyệt 10/09/2026.** CDS-12.2 v1.4 thêm `E5002` vào `errors` của EXTRACT-11, cùng lập luận DEV-075. Nguồn sinh `cds_data_a.py`. Không sửa mã. | Đã cập nhật tài liệu v1.4 |
| DEV-081 | 2026-09-10 | CDS-12.3 PROJECT-04 bước 3 ("Ghi ledger project.clone với nguồn") đối chiếu API-15 §7 `ledger_events.json` (25 kiểu sự kiện) | `src/eide/caps/project.py::clone, archive, rollback` | Không có kiểu sự kiện nào cho ba năng lực này: 25 kiểu của API-15 §7 không có `project.clone`, `project.archive` hay `project.rollback`. `Ledger.append` từ chối kiểu ngoài danh sách (E6001), nên hiện thực mượn kiểu `report` và ghi loại thật vào `data.kind`: `{kind: "project.clone", src, dst, keep, n_facts}`. | Cùng hình dạng DEV-013 (leo thang không có kiểu, tạm mượn `question`) và DEV-031 (ký danh sách trắng, tạm mượn `gate.human`) — cả hai sau đó được duyệt thêm kiểu mới. Không ghi gì thì mất luôn đường truy nguyên "bản sao này từ đâu ra", mà đó chính là điều bước 3 của hợp đồng sinh ra để giữ: nhìn một dự án ba tháng sau, `cloned_from` trong constraints.yaml nói tên, còn ledger nói THỜI ĐIỂM và giữ nguyên chuỗi băm. Ba năng lực này đều đổi trạng thái của một dự án ở mức vật thể (sao chép, đóng gói, quay lui) nên chúng thuộc cùng một họ sự kiện. | Đề nghị API-15 §7 v1.7 thêm một kiểu `project.state {op: clone\|archive\|rollback, project, ref, detail}` — một kiểu cho cả họ thay vì ba kiểu riêng, vì chúng luôn được đọc cùng nhau khi dựng lịch sử một dự án. Nguồn sinh: `docs/ho-so/nguon/api.js` (literal `EVENTS`). Khi có kiểu ấy, `project.py` bỏ cách mượn `report`. Trong lúc chờ: `data.kind` giữ đúng tên thao tác nên không mất thông tin nào. **Chủ sản phẩm duyệt 10/09/2026.** API-15 §7 v1.7 thêm kiểu `project.state {op: clone|archive|rollback, project, ref, detail}` — MỘT kiểu cho cả họ, vì chúng luôn được đọc cùng nhau khi dựng lại lịch sử một dự án. Nguồn sinh `api.js` (literal `LED`), đã sinh lại API-15 và `api/ledger_events.json`. `project.py` bỏ cách mượn `report`. | Đã cập nhật tài liệu v1.7 |
| DEV-082 | 2026-09-11 | SIM-20 §2 ("bảng renode_models.yaml theo họ") và TGT-19 manifest ISA ("sim: {engine, fallback}") | `src/eide/caps/data/renode_models.yaml, src/eide/caps/sim.py::QEMU_THEO_ISA, QEMU_MACHINE` | SIM-20 §2 nhắc đích danh một tệp renode_models.yaml nhưng không bộ sinh nào phát ra nó, và docs/spec/ là thư mục SINH RA nên không đặt tay vào đó được — bảng nằm ở src/eide/caps/data/. Manifest ISA khai sim.engine là tên HỌ engine (qemu), không phải tên chương trình; mà qemu không phải một chương trình nào cả, nó là họ qemu-system-<kiến trúc>, và mỗi chương trình ấy chỉ chạy những máy ảo đã biên dịch sẵn vào nó. Hai bảng bù chỗ ấy nằm trong mã. | Không có tên chương trình thì không tìm được engine; không có bảng máy ảo thì sim.build_platform sinh ra một nền tảng chạy lên là báo machine không tồn tại. Nguy hiểm hơn: gán một máy ảo GẦN GIỐNG (STM32F411 → netduinoplus2, vốn là STM32F405) thì firmware chạy được trên một con chip khác chip nó được dịch cho, rồi báo ĐẠT — nên bảng phải khớp chính xác và thiếu mục là engine ấy coi như không dùng được. | Đề xuất TGT-19 v1.x thêm hai trường vào khối sim của manifest ISA: sim.exe (tên chương trình theo engine) và sim.machines (mẫu họ chip → máy ảo). Khi ấy hai bảng trong mã biến mất và ISA mới vào bằng dữ liệu đúng như NFR-08 hứa. renode_models.yaml thì đề xuất giữ ở src/ và ghi rõ vị trí vào SIM-20 §2, vì nó là bảng ánh xạ của HIỆN THỰC (mô hình nào Renode bản này có), không phải đặc tả. | Mở |
| DEV-083 | 2026-09-11 | CDS-12.4 SIM-05 output_schema ("ToolReport + captured{uart[], gpio[], vars{}}") và SIM-20 §5 (expect kind var/gpio, "đọc biến qua GDB stub/monitor") | `src/eide/caps/sim.py::KENH_QUAN_SAT, _cham_expect` | Kênh quan sát var (biến firmware qua monitor/GDB stub) và gpio CHƯA nối. Chỉ uart có hiện thực, qua stdout của engine. Một dòng expect kind=var/gpio trả status=unverified kèm lý do, và một kịch bản có dù một dòng unverified thì report.passed=false. captured.gpio và captured.vars vì thế luôn rỗng. | Đọc biến cần monitor của Renode hoặc GDB stub cộng arm-none-eabi-gdb; máy phát triển hiện không có Renode (không có công thức brew) lẫn gdb cho ARM. Viết một đường đọc biến không chạy thử được là viết cho một giao diện chưa ai chạm tới. Điều KHÔNG được làm là coi dòng ấy như đạt: sim_first dùng chính report.passed để cho phép nạp firmware lên board, nên một passed=true dựa trên dòng chưa ai kiểm là đúng thứ cơ chế ấy sinh ra để chặn. Đây là NỢ HIỆN THỰC, cùng hình dạng với DEV-074 và DEV-076. | KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Nối kênh var qua monitor Renode (writeFile/sysbus.ReadDoubleWord tại symbol đọc từ .map của code.build) và kênh gpio qua GPIO analyzer khi có Renode chạy được — cùng lúc với DEV-086. Khi ấy KENH_QUAN_SAT nới ra và các test unverified hiện có tự đổi sang passed/failed. | Mở |
| DEV-084 | 2026-09-11 | SIM-20 §4 ("Nối: A4988Mock.on_step → plant.actuator; plant.sensors → MPU6050Mock.source; đồng bộ dt với Renode emulation time") và §5 (record: [plant.theta]) | `src/eide/caps/sim.py::run, sweep` | Đồng mô phỏng plant ↔ engine chip CHƯA nối. sim.model_plant sinh ra mô hình chạy được và sim.sweep quét trên nó (đó là đường của ví dụ SIM-06 {kp: [1,10,1]}), nhưng trong một lượt sim.run thì plant không chạy song song với firmware: expect kind=plant trả unverified, và record plant.* không có dữ liệu. | Nối hai bên cần một kênh hai chiều với engine theo thời gian mô phỏng (Renode Python peripheral gọi ngược vào tiến trình cha, hoặc một ổ cắm đồng bộ mỗi tick). Không có engine nào chạy được trên máy này (DEV-086) thì viết kênh ấy là viết cho một đầu chưa tồn tại. Phần đáng giá của SIM-03 — mô hình đúng, tham số có nhãn tạm — thì kiểm được ngay và đã kiểm: TC-SM-03 đo trên chính mô-đun sinh ra. | KHÔNG sửa tài liệu. Nối sau DEV-086, khi có engine chạy thật: mock Python của Renode gọi plant.step(dt) mỗi lần firmware đọc cảm biến, và sim.run ghi plant.csv vào captured. Rà lại mục này cùng DEV-083. | Mở |
| DEV-085 | 2026-09-11 | SIM-20 §2 ("sim/run.resc: … uart CreateFileBackend @sim/uart.log") và §5 ("within_s", "captured (uart.log, …)") | `src/eide/caps/sim.py::_resc, _cham_expect` | run.resc sinh ra dùng showAnalyzer (UART ra console) chứ không CreateFileBackend ra sim/uart.log. Hệ quả: log không có mốc thời gian theo dòng, nên within_s chỉ kết luận được khi within_s >= duration_s của cả lượt chạy; nhỏ hơn thì dòng expect ấy trả unverified. | Engine chạy trong sandbox SEC-25 §2, nơi tiến trình chỉ ghi được vào thư mục làm việc TẠM của chính lần chạy ấy — một tệp dưới sim/ bị từ chối, và nếu không bị từ chối thì nó nằm ở thư mục tạm mà sim.run không biết tên (Sandbox không trả cwd). Console thì Sandbox đã bắt sẵn vào stdout_ref. Phép suy within_s >= duration_s là CHẶT: nếu cả lượt chạy chỉ dài duration_s thì mọi dòng đều xuất hiện trong hạn. | Hai đường, cần chọn: (a) thêm một tham số cho Sandbox.run trả về cwd tạm (hoặc nhận sẵn một thư mục ghi được do người gọi đặt), rồi dùng CreateFileBackend đúng như §2 — sửa lõi, ảnh hưởng mọi năng lực chạy công cụ ngoài; (b) đọc stdout theo dòng và tự đóng mốc thời gian ở tiến trình cha, tức Sandbox phải dùng ống dẫn thay vì ghi thẳng ra tệp. Nghiêng về (b) vì nó cho mốc thời gian cho MỌI công cụ, không riêng engine mô phỏng. Trong lúc chờ: within_s < duration_s nói thẳng là chưa kết luận được, không làm tròn thành ĐẠT. | Mở |
| DEV-086 | 2026-09-11 | docs/spec/isa/avr8.yaml ("sim: {engine: simavr}") và SIM-20 §1 bảng chế độ (simavr cho AVR8, qemu cho Xtensa/RISC-V); STP-05 TC-SM-06 ("Uno blink+UART trên simavr") | `docs/ho-so/nguon/tgt_sim.js (literal isa/avr8.yaml), src/eide/caps/sim.py::QEMU_MACHINE` | Không engine nào trong bộ hồ sơ chạy được trên máy phát triển này, nên không năng lực sim.* nào chạy được firmware thật ở đây. simavr KHÔNG có công thức brew (đã kiểm 11/09/2026: brew search simavr → không có gì); Renode cũng không có cask; qemu-system-arm thì CÓ nhưng không mang máy ảo nào cho họ STM32F4 (máy gần nhất là netduinoplus2 = STM32F405). Trong khi đó qemu-system-avr ĐÃ CÓ SẴN trên máy và mang đúng máy ảo arduino-uno = ATmega328P, khớp family_patterns ^ATmega của avr8.yaml — nhưng avr8.yaml không khai fallback nên EIDE không được phép dùng nó. | armv7e-m.yaml đã khai fallback: qemu; avr8.yaml thì không, và không có lý do nào trong TGT-19 hay SIM-20 giải thích sự khác nhau ấy. Hệ quả đo được: khối F1 (sáu năng lực sim.*) hiện thực xong và có 44 test, nhưng đường chạy engine THẬT chưa lần nào được thi hành — cùng hình dạng với mục I2 (bốn bộ dựng lược đồ ngoài Graphviz chưa nhánh nào chạy). Một đường mã chưa lần nào chạy thật là một đường mã chưa ai biết có đúng không, bất kể bao nhiêu test giả lập xanh. | CẦN CHỦ SẢN PHẨM DUYỆT (sửa docs/spec). Đề xuất thêm fallback: qemu vào khối sim của isa/avr8.yaml trong docs/ho-so/nguon/tgt_sim.js rồi sinh lại TGT-19 — một dòng, cùng khuôn với armv7e-m.yaml. Đổi lại: cả nhóm sim.* có một đường chạy engine thật kiểm được trên máy này (ATmega328P + arduino-uno), và TC-SM-06 chuyển từ simavr sang qemu cho nửa AVR. Mở được đường ấy cũng là điều kiện để đóng DEV-083 (kênh var/gpio) và DEV-084 (đồng mô phỏng plant). **CHỦ SẢN PHẨM DUYỆT 11/09/2026.** `tgt_sim.js` thêm `fallback: qemu` vào khối sim của avr8; TGT-19 lên **v1.2** (§2 cột Sim ghi đường lui), SIM-20 lên **v1.1** (§1 bảng chế độ ghi vai trò đường lui của qemu kèm điều kiện dùng được; §7 TC-SM-06 nhận cả hai đường). **Đường chạy engine THẬT đã được thi hành**: `tests/test_sim.py::test_duong_engine_THAT_chay_firmware_AVR_that` chạy `qemu-system-avr -M arduino-uno` với một ELF AVR dựng bằng tay (98 byte, không cần `avr-gcc` vốn không có trên máy này) và đọc được `captured.uart == ['E']` — ký tự firmware ghi vào UDR0, không phải chuỗi một shim echo ra. Nhánh `TU_DUNG["qemu"] = False` (hết giờ LÀ cách lượt chạy kết thúc) nhờ đó lần đầu được một engine thật chứng minh. | Đã cập nhật tài liệu TGT-19 v1.2 + SIM-20 v1.1 |
| DEV-087 | 2026-09-11 | CDS-12.2 SEARCH-05 bước 1 ("Điểm = tier dự kiến + **domain tin cậy (+2)** + …") đối chiếu POL-17 §3 (`trusted_sources` = danh sách CHO PHÉP TẢI) và TGT-19 §8 (bảng nguồn hãng `sources/vendors.yaml`) | `src/eide/caps/search.py::rank, _domain_tin_cay, _trusted` | `search.rank` lấy "domain tin cậy" từ `trusted_sources`, tức từ danh sách trắng TẢI VỀ. Hai thứ ấy trả lời hai câu khác nhau: `trusted_sources` trả lời *"có được tải tự động không"* (6 tên miền, cần chữ ký chủ sản phẩm), còn xếp hạng cần *"nguồn này đáng tin tới đâu"* — vốn là việc của bảng nguồn hãng TGT-19 §8, nơi có 12 hãng kèm tầng từng loại tệp. Hệ quả: **một hãng không nằm trong danh sách trắng tải về thì vĩnh viễn không bao giờ được +2**, kể cả trên trang datasheet của chính nó. | Lộ ra khi thêm `raw.githubusercontent.com` vào `trusted_sources` (WI-257, chủ sản phẩm duyệt 11/09/2026). Đo trên đúng fixture của tc SEARCH-04, truy vấn "A4988 datasheet": trước khi thêm — `allegromicro.com` 4,0 (silver 2 + khớp mã 2) trên `raw.githubusercontent.com` 3,0 (gold 3); sau khi thêm — mirror lên **5,0** và trang hãng tụt xuống hạng hai. Nhưng con số quan trọng hơn là con số CŨ: `allegromicro.com` chưa bao giờ nhận +2 vì Allegro không có trong danh sách trắng tải về, và nó thắng nhờ **khớp mã linh kiện**, không nhờ tầng. Docstring của `_tang_du_kien` và của chính bài test đều ghi "trang hãng thắng vì TẦNG khác nhau" — câu ấy sai từ trước: tầng đang NGƯỢC (mirror gold, trang hãng silver, vì TGT-19 §8 xếp SVD trên PDF). Bài `test_TC_trang_hang_xep_tren_forum` xanh suốt nhờ một biên một điểm đến từ lý do khác với lý do nó được viết ra — cùng họ với DEV-058 và với tình huống S33 của lỗi im lặng số 5. | Đề xuất: `search.rank` đọc "domain tin cậy" từ **bảng nguồn hãng** (`_la_trang_hang`, đã có sẵn và đã đọc từ spec) thay vì từ `trusted_sources`. Khi ấy `allegromicro.com` = 6,0 (silver 2 + domain 2 + khớp mã 2) trên mirror 5,0 (gold 3 + domain 2), tức tc SEARCH-04 đạt VÌ ĐÚNG LÝ DO nó nêu, và danh sách trắng tải về thôi kiêm một việc nó không được dựng lên để làm. Cần chủ sản phẩm duyệt vì CDS-12.2 SEARCH-05 bước 1 viết "domain tin cậy" mà không nói lấy từ đâu — sửa là làm RÕ chỗ thiếu, không phải trái điều đã viết. **CHỦ SẢN PHẨM DUYỆT 11/09/2026** (phương án đọc từ bảng nguồn hãng). Đã sửa: `rank` gọi `_la_trang_hang`; `_domain_tin_cay` và `_trusted` thành mã chết nên xóa — docstring của cái đầu còn nhận là nó quyết G-SRC, việc nó chưa bao giờ làm (G-SRC-01 do PolicyGate áp, khớp CHÍNH XÁC). Phép canh biên dấu chấm (`evil-st.com` không được khớp `st.com`) chuyển sang `_la_trang_hang`, nơi lỗ hổng ấy nay thật sự nằm: nó tặng đúng +2 mà trang hãng thật được hưởng. Đo lại trên fixture tc SEARCH-04: `allegromicro.com` 6,0 > mirror 5,0 > forum 3,0. Còn lại: đề xuất CDS-12.2 SEARCH-05 bước 1 ở v1.x nói rõ "domain tin cậy" lấy từ bảng nguồn hãng TGT-19 §8 — hôm nay câu ấy không sai, chỉ là không nói nguồn, và chính chỗ không nói ấy đã để hai danh sách bị trộn suốt bốn ngày. | Đã duyệt |
| DEV-088 | 2026-09-11 | SEC-25 §3 ("môi trường dựng lại từ đầu; `PATH` = `/usr/bin:/bin:/usr/sbin:/sbin`") và §2 ("thư mục làm việc tạm riêng mỗi lần chạy") đối chiếu TGT-19 `toolchain.build` (`cmake -S . -B build`, `artifact: build/*.elf`) | `src/eide_core/sandbox.py::Sandbox.run, _moi_truong, _kiem_cwd`; `src/eide/caps/env.py::chay_sandbox`; `src/eide/caps/code.py::build` | `Sandbox.run` nhận thêm hai tham số KHÔNG có trong SEC-25: `cwd` (thư mục làm việc do người gọi đặt, bắt buộc nằm trong `allowed_dirs`) và `them_path` (thêm thư mục công cụ vào `PATH`). Không có chúng thì **`code.build` không thể thành công trên bất kỳ máy nào** — đo 11/09/2026, ba tầng lỗi nối nhau: (1) `execvp() of 'cmake' failed` vì truyền tên trần trong khi `PATH` sandbox chỉ có bốn thư mục hệ thống; (2) `cmake` báo *"does not appear to contain CMakeLists.txt"* vì `-S .` trỏ vào thư mục tạm chứ không vào gốc dự án; (3) `cmake` báo *"unable to find a build program corresponding to Ninja"* vì **công cụ tự gọi công cụ** — giải `argv[0]` chữa được lệnh đầu, không chữa được `cmake → ninja` hay `make → gcc`. | Đây là chỗ tài liệu và hiện thực mâu thuẫn ở mức KHÔNG THỂ CÙNG ĐÚNG: SEC-25 §3 đòi `PATH` bốn thư mục, TGT-19 đòi chạy `cmake`/`ninja` mà trên macOS nằm ở `/opt/homebrew/bin`. Một trong hai phải nhường. Lỗi nằm im từ Sprint 2 vì **không test nào từng dựng THÀNH CÔNG**: `test_code.py` chỉ kiểm nhánh hỏng (thiếu công cụ → E4001, chưa ghim ISA → E2000), `nghiem_thu_sprint2.sh` không có bước biên dịch, `test_that.py` không có test gọi thật cho `code.build`. Cùng hình dạng với lỗi im lặng số 7 (ba hằng số thời gian chờ) và với chính DEV-086. | Hiện thực chọn phương án hẹp nhất: `cwd` chỉ nhận thư mục ĐÃ khai trong `allowed_dirs` (không cấp thêm quyền, chỉ đổi chỗ đứng — hồ sơ sandbox vốn đã cấp ghi cho thư mục làm việc); `them_path` nhận ĐÚNG thư mục của những công cụ manifest ISA khai và `env.check` đã tìm thấy, không mở cả `PATH` người dùng. Hồ sơ `.eide-sandbox.sb` vẫn nằm ở thư mục tạm, không rơi vào cây mã dự án. Đề xuất SEC-25 v1.x: §3 nói rõ `PATH` = bốn thư mục hệ thống **cộng thư mục của chuỗi công cụ đã khai trong manifest ISA**, và §2 nói rõ thư mục làm việc có thể là gốc dự án khi năng lực đã khai `allowed_dirs` chứa nó. Cũng là lời đáp một phần cho DEV-085 phương án (a). **CHỦ SẢN PHẨM DUYỆT 13/09/2026.** SEC-25 lên **v1.2**: §2 nay ghi rõ (a) thư mục làm việc mặc định là thư mục tạm, người gọi chỉ định được `cwd` khác nhưng BẮT BUỘC nằm trong `allowed_dirs`; (b) `PATH` gồm bốn thư mục hệ thống CỘNG thư mục của những công cụ mà manifest ISA khai và `env.check` đã tìm thấy — cho phép chạy chuỗi công cụ ĐÃ KHAI, khác hẳn cho phép chạy bất cứ thứ gì người dùng từng cài. Tài liệu nay mô tả đúng thứ mã làm, và lý do vì sao nó buộc phải thế. | Đã cập nhật tài liệu SEC-25 v1.2 |
| DEV-089 | 2026-09-11 | POL-17 §3 `trusted_packages` (danh sách ĐÃ KÝ) đối chiếu TGT-19 §3 ("danh sách mặc định: gcc-arm-none-eabi, avr-gcc, avrdude, … cmake, ninja, … cppcheck, clang-tidy, … docling" — 24 gói) và TGT-19 §1 `toolchain.compiler.name` của `isa/armv7e-m.yaml` | `docs/spec/policy/defaults.yaml`; `src/eide/caps/env.py::install` | Hai sai khác chồng lên nhau. (a) `defaults.yaml` chỉ có **12 trong 24** gói mà TGT-19 §3 kể là danh sách mặc định — thiếu `avrdude`, `cmake`, `ninja`, `cppcheck`, `clang-tidy`, `qemu`, `pymcuprog`, `picotool`, `riscv-none-elf-gcc`, `esp-idf`, `sigrok-cli`, `docling`. (b) Gói trình dịch ARM ghi là **`gcc-arm-none-eabi`** — tên của **apt** — trong khi Homebrew và `toolchain.compiler.name` đều dùng **`arm-none-eabi-gcc`** (ngược thứ tự từ). Đo 11/09/2026: **7 trên 8** công cụ mà các manifest ISA khai đều vắng khỏi danh sách trắng; chỉ `avr-gcc` khớp. | Hệ quả: `env.install` rơi vào **ASK (G-OPS-05)** ở gần như mọi công cụ của chuỗi dựng, kể cả những gói mà chính tài liệu ghi là tin cậy — và một cổng hỏi quá nhiều là một cổng người ta bấm qua cho xong, tức mất tác dụng ở cả những lần hỏi đúng. Đúng hình dạng WI-257 ở nhóm nguồn. Không ai thấy vì `env.install` chưa lần nào được gọi thật cho chuỗi công cụ ARM — cùng lý do với DEV-088, và cùng họ với lỗi im lặng số 11. Nguyên nhân gốc của (b): danh sách trắng gói chỉ có MỘT, dùng chung cho brew/apt/winget, nên một tên apt lọt vào đó làm nền tảng thứ tự 1 trượt. | **Chủ sản phẩm đã ký lại 12/09/2026** (băm `5c1f3da127124885…`): `trusted_packages` nay đủ 24 gói của TGT-19 §3, cộng `arm-none-eabi-gcc` và `arm-none-eabi-binutils`. Hai bài test mới giữ cả hai chiều: `test_moi_cong_cu_ISA_khai_deu_trong_danh_sach_trang` (mọi công cụ manifest khai phải có mặt) và `test_danh_sach_goi_khong_tut_khoi_muc_TGT19_da_ghi` (đọc thẳng câu văn TGT-19 §3 trong nguồn sinh, nên đỏ khi hai bên trôi theo bất kỳ chiều nào). **CÒN MỞ — sai tầng, chưa sửa:** `env.install` dùng `tool` làm luôn tên GÓI, nhưng `arm-none-eabi-size`/`arm-none-eabi-objcopy`/`avr-size` là **chương trình** đến từ gói `arm-none-eabi-binutils`/`avr-gcc`. `env.install arm-none-eabi-size` vì thế vẫn hỏng dù danh sách trắng có đúng. Đề xuất TGT-19 v1.x thêm trường `package` cho từng mục `toolchain.tools[]` (tên gói theo từng OS), để `env.install` thôi đoán rằng tên chương trình và tên gói là một. **CHỦ SẢN PHẨM DUYỆT 13/09/2026 — phần còn Mở đã đóng.** TGT-19 lên **v1.3**: `toolchain.compiler` và từng mục `toolchain.tools[]` nhận trường **`package`** (tên gói theo từng OS), khai khi nó KHÁC tên chương trình. `env.install` nay tra `_ten_goi()` từ spec — không chép bảng vào Python, nên thêm một ISA là thêm một tệp YAML. Đo được: `arm-none-eabi-size` → `arm-none-eabi-binutils` (brew) / `binutils-arm-none-eabi` (apt); `avr-size` → `avr-gcc` / `binutils-avr`. Danh sách trắng cũng đối chiếu bằng tên GÓI, vì POL-17 §3 kể tên gói. | Đã cập nhật tài liệu TGT-19 v1.3 |
| DEV-090 | 2026-09-12 | CDS-12.4 DIAGRAM-14 bước 1 ("to_code/from_code: sinh patch (qua code.modify / cập nhật src)"; "tree-sitter") | `src/eide/caps/diagram.py::sync` | Hai hướng GHI (`to_code`, `from_code`) trả `diff` thật kèm `applied: false` và lý do, KHÔNG sinh patch. Hướng `check` — mặc định — hiện thực đầy đủ: so trạng thái của `stateDiagram-v2` với nhãn `case ST_…` cùng phép gán `state = ST_…` trong mã, đánh dấu `diagram.stale`, và E3000 khi lệch quá 34%. | Sinh patch cho mã C cần một cây cú pháp thật — hợp đồng nêu đích danh tree-sitter, và `pyproject.toml` chưa có phụ thuộc ấy. Viết một bộ sinh patch bằng regex cho một ngôn ngữ có tiền xử lý là cách chắc chắn làm hỏng mã người khác: `#ifdef` quanh một nhánh `case`, một macro sinh ra nhãn trạng thái, hay một `switch` lồng đều đủ để phép thay thế bằng biểu thức chính quy cắt nhầm chỗ — và triệu chứng sẽ là firmware không biên dịch được ở một dòng không liên quan. Đây là NỢ HIỆN THỰC, cùng hình dạng với DEV-074 và DEV-076: phần đo được thì đã đo, phần cần một công cụ chưa có thì nói thẳng là chưa có. Điều KHÔNG được làm là sinh patch "gần đúng" rồi để `code.modify` và cổng G3 gánh — G3 xét diff chứ không xét việc diff ấy được dựng bằng gì. | KHÔNG sửa tài liệu, KHÔNG sửa hợp đồng. Thêm `tree-sitter` + `tree-sitter-c` vào `pyproject.toml` (kèm lý do trong commit, theo CLAUDE.md §Chuẩn mã) rồi nối `to_code`: từ `diff.in_diagram_only` sinh nhánh `case` mới đặt đúng chỗ trong `switch`, từ `in_code_only` thêm chuyển vào lược đồ. `from_code` rẻ hơn nhiều vì đích là Mermaid chứ không phải C — có thể làm trước, độc lập với tree-sitter. Rà lại cùng lúc với B4. **HƯỚNG `from_code` ĐÃ XONG 12/09/2026.** Nó rẻ hơn hẳn vì đích là Mermaid — ngôn ngữ ta sinh ra được trọn vẹn bằng mã — chứ không phải C với tiền xử lý và macro. Hai bất biến: **giữ phần người vẽ** (nhãn trên cạnh, thứ tự đọc) thay vì sinh lại từ đầu, vì sinh lại là ném đi phần người làm để lấy phần máy đọc được; và **không tự xoá** trạng thái hình có mà mã không có — nó có thể là một nhánh CHƯA viết chứ không phải một nhánh đã bỏ, và `diff.in_diagram_only` đã nêu tên chúng cho người quyết. CÒN NỢ: `to_code` (sinh patch cho mã C) vẫn cần tree-sitter. **CHỦ SẢN PHẨM DUYỆT 13/09/2026 — ĐÓNG TRỌN.** `tree-sitter` + `tree-sitter-c` vào `pyproject.toml` kèm lý do (CLAUDE.md §Chuẩn mã), import TRỄ như `pdfplumber`. `to_code` sinh patch bằng CÂY CÚ PHÁP: tìm `switch` có điều kiện là một biến trạng thái (`state`/`st`/`fsm`/`mode`), chèn nhánh `case` mới **TRƯỚC `default:`** — đặt sau `default` là viết một nhánh không bao giờ chạy tới, mà trình dịch không báo và người đọc thì tin là nó có tác dụng. Thân nhánh chỉ `break;` kèm `TODO`: sinh thân thật là đoán hành vi của một trạng thái mà lược đồ chỉ mới đặt tên. `applied: false` — patch đi qua `code.modify` và cổng G3 như mọi thay đổi mã khác, không qua đường riêng. Bốn test, trong đó một bài phân tích lại mã ĐÃ VÁ bằng chính tree-sitter để chắc nó còn là C hợp lệ. Một chi tiết ghi lại: tree-sitter-c gói điều kiện `switch` trong `parenthesized_expression`, không phải `condition_clause` — bản đầu tìm sai tên nút và lặng lẽ trả `None`. | Đã cập nhật tài liệu — hiện thực đủ |
| DEV-091 | 2026-09-12 | PKG-22 (registry là nơi "git push" gói lên) và CDS-12.5 REGISTRY-04 bước 1 ("git push registry") | `src/eide/caps/registry.py::_kho, publish, pull, search, pack` | Registry hiện thực thành một **THƯ MỤC** — mặc định `<user_data>/registry`, đổi bằng biến môi trường `EIDE_REGISTRY` — chứ không phải một kho git từ xa. `publish` chép gói vào thư mục ấy và cập nhật `index.json`; `search`/`pull` đọc từ đó. | Registry từ xa là **hạ tầng ngoài phạm vi đề án**: nó cần một máy chủ, một chính sách tài khoản, và một quy trình thu hồi gói. Chờ nó thì bốn năng lực M4 nằm không, và phần đáng kiểm nhất của chúng — chữ ký, license, không rò K3/K6 — không bao giờ được kiểm. Điều làm lựa chọn này KHÔNG phải một bản giả: định dạng `.hkp` là thật (cùng `manifest.json` + `manifest.sig` mà `env.install_pack` đã đọc từ Sprint 2), chữ ký là băm nội dung thật, và bốn phép kiểm của hợp đồng đều chạy. Cái duy nhất cục bộ là VẬN CHUYỂN. | KHÔNG sửa tài liệu. Thay `_kho()` bằng một lớp vận chuyển git/HTTP khi có registry thật — bốn năng lực còn lại không đổi một dòng, vì chúng chỉ nói chuyện với `_kho()`. Đề xuất PKG-22 v1.x ghi rõ registry có HAI dạng vận chuyển (thư mục cục bộ và kho từ xa) thay vì giả định một; dạng cục bộ cũng là thứ một nhóm nhỏ thật sự dùng — một thư mục chia sẻ trên NAS là registry đủ dùng cho một phòng thí nghiệm. | Mở |
| DEV-092 | 2026-09-13 | API-15 §3 bảng mã lỗi (`E1003 UNAUTHORIZED`, `E6002 CONFLICT`) đối chiếu `errors[]` của cả 238 hợp đồng trong CDS-12 | `docs/spec/api/errors.json`, `docs/spec/cds.json` | Hai mã lỗi có trong bảng API-15 §3 mà **KHÔNG hợp đồng năng lực nào khai** trong `errors[]`. Đo 13/09/2026 bằng cách quét cả 238 hợp đồng: `E4003` được 4 năng lực khai (đều cần board, nên chưa ném là đúng lịch); `E1003` và `E6002` thì không ai. | Không phải nợ hiện thực mà là mã lỗi KHÔNG CÓ CHỦ — không năng lực nào được phép ném chúng, vì `errors[]` của hợp đồng là danh sách đóng. Ép một năng lực ném E6002 sẽ làm nó trái chính hợp đồng của mình: `passport.query`, `kg.conflicts` và `req.ground_hw` đều khai `errors: []`, tức chúng KHÔNG ném gì — và đó là thiết kế đúng, vì một câu truy vấn tri thức không nên hỏng chỉ vì store có một xung đột chưa ai giải. `E1003` ("Token REST sai") thì chưa có bề mặt nào để ném: daemon là JSON-RPC qua stdio, không có token. | Hai hướng, cần chủ sản phẩm chọn. (a) **Gán chủ**: thêm `E6002` vào `errors[]` của `kg.resolve_conflict` (nơi duy nhất một xung đột thật sự chặn việc), và giữ `E1003` là mã DÀNH SẴN cho bề mặt REST tương lai — ghi rõ điều đó vào API-15 §3 để nó thôi trông như một mã bị bỏ quên. (b) **Bỏ khỏi bảng** nếu thấy chúng không thuộc về đâu cả. Nghiêng về (a): một mã dành sẵn có ghi chú thì đọc được, còn một mã không ai khai thì mỗi lần rà lại phải điều tra lại từ đầu — lần này là lần thứ hai. | Mở |
| DEV-093 | 2026-09-13 | UXD-13 §2 bảng màn hình (màn 20 "Models (Mô hình & chi phí)", năng lực `policy`, `gateway`) đối chiếu API-15 §1 (57 phương thức JSON-RPC) và §5 (26 kiểu sự kiện sổ cái) | `apps/geditor/Sources/EIDEKit/EideSystemViews.swift::ModelsView`, `src/eide/daemon/rpc.py::SU_KIEN` | Màn 20 hiện ĐƯỢC ÍT HƠN tài liệu mô tả: chỉ `cost_today` (từ `project.status`) và mức tự chủ (từ `autonomy.get`). Bảng vai trò → mô hình, chi phí theo từng lượt gọi, và độ trễ mỗi vai trò thì không hiện được. | **API-15 không có phương thức nào cho màn này.** Quét cả 57 phương thức: không cái nào trả cấu hình mô hình hay chi tiết chi phí. Dữ liệu ấy TỒN TẠI và đầy đủ — `model.call` trong sổ cái mang `role, model_id, request_hash, prompt_hash, tokens_in, tokens_out, cache_read_tokens, latency_ms, cost_usd, stop_reason` — nhưng nó không nằm trong 16 phương thức `event.*` của API-15 §1, nên `SU_KIEN` của daemon cũng không map nó, và panel không nhận được gì theo thời gian thực. Khoảng trống này chỉ lộ ra khi có người ngồi dựng đúng màn hình ấy: bảng §2 liệt kê màn, bảng §1 liệt kê phương thức, và không ai đối chiếu hai bảng với nhau. Đây cũng là lý do dựng cả 23 màn đáng làm dù 29 năng lực còn chờ board — viết giao diện là phép kiểm chéo rẻ nhất giữa hai tài liệu. | Không lấp bằng cách vẽ một bảng mô hình từ dữ liệu không có: `ModelsView` hiện đúng thứ lấy được và NÓI RA phần thiếu kèm đường xem thật (`eide ledger --kind model.call`). Đề xuất API-15 v1.x thêm `models.list` (vai trò → mô hình → giá, đọc từ `models.yaml`) và `cost.today` (tổng hợp `model.call` theo vai trò), hoặc `event.model.call` nếu muốn panel theo dõi chi phí trực tiếp — nghiêng về hai phương thức đọc thay vì một sự kiện, vì một lượt gọi mô hình có thể xảy ra hàng chục lần một phút và panel không cần từng cái, nó cần TỔNG. Chờ chủ sản phẩm quyết. **ĐÓNG 15/09/2026.** API-15 nay có `event.model.call` (DEV-107) và `view.timeline` (VIEW-12, `ref: memory.ledger`) trả bản ghi sổ cái thô — đủ để màn gom `model.call` theo vai trò mà không cần thêm phương thức RPC nào. `ModelsView` đổi nguồn: đọc SỔ CÁI thay vì `models.yaml` + `project.status`. Khác biệt là khác biệt về nghĩa, không về dữ liệu: cấu hình nói tác tử ĐƯỢC PHÉP dùng mô hình nào, sổ cái nói nó ĐÃ dùng gì — và người trả tiền hỏi câu thứ hai. Không hiện nội dung prompt (`TRUONG_KHONG_DAY` lược từ daemon). Sắp theo chi phí giảm dần vì người mở màn này muốn biết tiền đi đâu. | Đã duyệt |
| DEV-094 | 2026-09-13 | UXD-13 §2 bảng màn hình (cột `nang_luc` của màn 20 Models, 22 FlowMap, 23 Trạng thái/khung) và U1 ("mọi màn hình khác mở được từ lệnh hoặc từ kết quả trong chat") | `src/eide_core/registry.py::_ui_tu_spec`, `apps/geditor/Sources/EIDEKit/EidePanel.swift::_moTheoTenMan` | Panel mở màn được bằng **tên màn** (`/FlowMap`, `/Models`) chứ không chỉ bằng id năng lực (`/passport.query`). Ba màn ấy cũng vào menu "/" dưới mô tả "màn hình (không phải năng lực)". | **Ba màn không có năng lực nào trỏ tới, nên không lệnh `/ns.name` nào với tới được.** Đo 13/09/2026 bằng cách chạy `_ui_tu_spec()` trên cả 238 năng lực: `FlowMap` khai `nang_luc: []` — cố ý, vì nó hiện trạng thái của chính dòng công việc chứ không hiện kết quả của một năng lực; `Models` khai `policy` và `gateway`, hai chuỗi không khớp quy ước nào của bảng (phải là `ns.*` hoặc `ns.name` đầy đủ) nên khớp 0 năng lực, và `gateway` thì không phải namespace nào trong cds.json; `Trạng thái/khung` khai `policy.set_autonomy` nhưng màn 1 đã nhận năng lực ấy trước qua mẫu `policy.*`, và `_ui_tu_spec` lấy màn ĐẦU TIÊN (DEV-046). Hệ quả: `ModelsView` và `FlowMapView` vừa dựng xong đã là mã chết — có khung nhìn, có test, không cách nào mở ra. Loại lỗi này không bao giờ lộ ở tầng đơn vị: mỗi mảnh đều đúng, chỗ hỏng nằm ở khoảng giữa. | U1 nói "mở được từ lệnh", không nói phải qua một năng lực — nên lối mở theo tên màn KHÔNG trái tài liệu, nó lấp một chỗ tài liệu không nói tới. Đề xuất UXD-13 v1.x sửa cột `nang_luc`: `Models` → `policy.*` (và bỏ `gateway`, hoặc ghi rõ đó là tên tầng chứ không phải namespace năng lực), `Trạng thái/khung` → thêm `policy.emergency_stop` (bảng đang ghi `emergency_stop` thiếu tiền tố ns, nên nó cũng không khớp gì). `FlowMap` thì giữ rỗng — nó đúng là màn không thuộc năng lực nào, và điều cần sửa là quy ước, không phải bảng. Nếu cả ba được sửa, lối mở theo tên màn thành dư thừa cho hai màn nhưng vẫn cần cho FlowMap — và `EideMoManTests` sẽ đỏ để nhắc rà lại. Chờ chủ sản phẩm quyết. | Mở |
| DEV-095 | 2026-09-13 | CDS-12 `output_schema` của cả 238 hợp đồng; API-15 §1 `caps.describe` | `apps/geditor/Tests/EIDEKitTests/EideE2ETests.swift::EideKhungNhinDocSchemaTests` | Bài test đối chiếu khung nhìn với hợp đồng phải ĐỌC CẢ `description` của schema, không chỉ `properties` — vì phần lớn cấu trúc kết quả không nằm trong `properties`. | **181 chỗ trong `output_schema` là object tự do** (`{"type": "object"}` không kèm `properties`), đo 13/09/2026 bằng cách quét đệ quy cả 238 hợp đồng. Trong đó 46 chỗ đặt cấu trúc thật vào `description` dạng danh sách tên (`project.status.report` → "features, gates_open, undo_items, cost_today, autonomy, target"; `debug.log_stats.stats` → "top_patterns[], time_span, gaps[], levels{}"), và **135 chỗ không nói gì cả**. Hệ quả đo được: `jsonschema` chấp nhận bất cứ object nào ở những chỗ ấy, nên phép kiểm `output_schema` của Router không phát hiện được một hiện thực trả sai tên trường — và đó đúng là hình dạng của hai lỗi đã xảy ra (`DocView` đọc `sections[]` không tồn tại, `SimView` đọc `report.expectations` trong khi tên thật là `metrics.expect`). Không phải lỗi của ai: CDS-12 sinh từ bảng Excel, nơi cột "đầu ra" là một ô văn bản. | KHÔNG sửa 238 hợp đồng bằng tay. Hai bước, theo thứ tự rẻ-trước: (a) bộ sinh `cds.js` đọc `description` dạng danh sách tên và sinh `properties` tối thiểu (`{type: object}` cho tên trần, `{type: array}` cho `ten[]`) — 46 chỗ được máy-kiểm ngay, không ai phải gõ gì; (b) 135 chỗ còn lại thì điền dần khi có người chạm tới hợp đồng ấy, ưu tiên những cái có khung nhìn đọc (đã liệt kê trong `EideKhungNhinDocSchemaTests.bang`). Tạm thời bài test tự tách tên từ `description`, và nó cố ý KHÔNG im lặng về phần không kiểm được: schema không mô tả gì thì khung nhìn được cho qua, vì một dương tính giả còn tệ hơn không kiểm — nó dạy người ta bỏ qua test. Chờ chủ sản phẩm quyết (a). | Mở |
| DEV-096 | 2026-09-14 | UXD-13 §6 bảng phím tắt (10 tổ hợp) và §4 RunProgress ("hủy nút") | `apps/geditor/Sources/EIDEKit/EidePanel.swift::performKeyEquivalent`, `EideViews.swift::hoanTacMucDau` | Panel chỉ nhận phím tắt khi first responder nằm TRONG cây của nó; ngoài panel, GEditor giữ nguyên mọi phím. `⌘Z` hoàn tác mục GẦN NHẤT thay vì "mục đã chọn". `⌘K` điền dấu `/` vào ô lệnh thay vì mở một bảng lệnh riêng. Chuỗi thường không huỷ được từ thẻ tiến độ — chỉ việc nặng (`job.cancel`) huỷ được. | **UXD-13 §6 viết cho một EIDE chiếm cả cửa sổ** (mockup 1440×900, có sidebar 23 màn). Thực tế EIDE là một panel trong GEditor, nơi hai phím đã có chủ: `⌘L` là "Đi tới dòng…" và `⌘Z` là "Hoàn tác" của trình soạn thảo (đo 14/09 trên `AppDelegate.swift`). Cướp chúng ở mức cửa sổ nghĩa là người đang sửa mã bấm `⌘Z` và thấy một mục hàng đợi bị hoàn tác thay vì dòng vừa gõ. `⌘K` thì trống, nhưng dựng một bảng lệnh thứ hai làm đúng việc menu `/` đã làm là thêm một chỗ để hai bên lệch nhau. `⌘Z` "mục đã chọn" cần khái niệm CHỌN mà `ReviewQueueView` chưa có; mục gần nhất là thứ người vừa thấy máy làm, tức thứ họ định rút lại. `job.cancel` chỉ nhận `job_id`, và API-15 không có phương thức huỷ một CHUỖI — dừng khẩn là đường duy nhất, nên thẻ tiến độ nói thẳng điều đó thay vì im lặng không làm gì. | Ba việc, hai cho tài liệu và một cho mã. (a) UXD-13 v1.x ghi rõ bảng §6 áp dụng **trong ngữ cảnh panel**, và nêu hai phím xung đột với chủ nhà; đó cũng là điều đúng cho bất kỳ IDE nào khác cắm EIDE vào sau này (GPI-23 §1). (b) Cân nhắc thêm `chuoi.cancel` vào API-15: một chuỗi 17 bước chỉ dừng được bằng dừng khẩn là một lựa chọn quá thô — dừng khẩn hạ luôn mức tự chủ về A0, tức người dùng trả giá cả phiên làm việc để huỷ một việc. (c) Thêm khái niệm chọn dòng cho `ReviewQueueView` rồi `⌘Z` mới đúng nghĩa §6. Chờ chủ sản phẩm quyết (b). | Mở |
| DEV-097 | 2026-09-14 | CDS-12.3 DISCOVER-10 `errors: [E3000]` và `output_schema` (`{devices}`, `additionalProperties: false`); DISCOVER-12 `steps` ("từ kết quả dò") | `src/eide/caps/discover.py::network, _doc_discovery` | (a) `discover.network` ném **E4001 TOOL_MISSING** — mã KHÔNG có trong `errors[]` của hợp đồng — khi máy không có `dns-sd` (macOS) hay `avahi-browse` (Linux). (b) `discover.auto_setup` đọc kết quả dò từ tệp `.eide/discovery/<id>.json` thay vì từ bảng `discovery` trong store. | (a) Hợp đồng không lường trước tình huống "chưa quét được". Ba lựa chọn, và hai cái đầu tệ hơn: trả `devices: []` là **nói dối bằng im lặng** (người đọc hiểu "đã quét, không thấy gì" trong khi chưa quét lần nào — đúng hình dạng lỗi im lặng số 21 vừa sửa ở tầng UI); thêm một trường `note` thì `additionalProperties: false` chặn, và Router sẽ ném E1004 — tức hiện thực trái chính output_schema của mình. E4001 là mã CÓ THẬT trong API-15 §3, tên nó (TOOL_MISSING) đúng nghĩa tình huống, và `discover.env_hw` cũng đã nói trước rằng những công cụ ấy thiếu. (b) DDD-14 có thực thể `Discovery` nhưng store CHƯA có bảng cho nó (migration 0001–0007 không có `discovery`); viết kết quả dò vào store cần một migration, và các năng lực sinh ra dữ liệu ấy (`chip_id`, `bus_scan`…) đều chưa hiện thực vì cần board — dựng bảng cho một luồng chưa có nguồn dữ liệu là dựng một chỗ trống. | (a) Thêm `E4001` vào `errors[]` của DISCOVER-10 trong `cds_data_c.py` rồi sinh lại — một dòng, và nó làm hợp đồng nói đúng thứ hiện thực làm. Cùng hình dạng với DEV-092 (mã lỗi không có chủ): mã tồn tại trong bảng API-15 mà không hợp đồng nào khai thì mỗi lần rà lại phải điều tra từ đầu. (b) Thêm migration `discovery` cùng lúc với `discover.chip_id` (mốc M2, cần board) — lúc ấy mới có dữ liệu thật để ghi, và `_doc_discovery` đổi một hàm. Chờ chủ sản phẩm quyết (a). | Mở |
| DEV-098 | 2026-09-14 | GPI-23 §1 (EIDE là plugin/client thuần của GEditor), DEVIATIONS DEV-004 (panel trong app); UXD-13 §1/§2 (khung cửa sổ, sidebar 23 màn, mockup 1440×900) | `apps/geditor/Sources/GEditorApp/MainWindowController.swift::dungKhungEide, chonManEide`, `AppDelegate.swift`, `apps/geditor/Sources/EIDEKit/EideWindow.swift` | **GEditor và EIDE gộp thành MỘT sản phẩm tên EIDE.** Cửa sổ chính nay có sidebar 21 màn của UXD-13 §2 bên trái; trình soạn thảo là **màn "Mã nguồn"** (số 11), không còn là chủ nhà. Tên hiển thị (menu Giới thiệu, `applicationName`, tiêu đề cửa sổ lúc mở) đổi thành EIDE. Module mã nguồn vẫn tên `GEditorCore`/`GEditorApp`. | **Quyết định của chủ sản phẩm 14/09/2026**, sau khi bật giao diện lên kiểm: *"Mình muốn code của GEditor và code của EIDE là một thôi. Tất cả tích hợp chung vào thành 01 giao diện đặt tên là EIDE."* Lý do đo được ngay trong lần bật ấy: người mở sản phẩm lên thấy **một trình soạn thảo văn bản**, còn EIDE là một mục trong menu Format — và mục ấy `enabled: false` khi chạy bản dựng phát triển (app không bundle thì AppKit vô hiệu hoá cả menu), nên không có đường nào tới 23 màn vừa dựng. Quan trọng hơn: mockup UXD-13 §2 VỐN vẽ đúng hình dạng mới — "Code (Mã nguồn)" là màn số 13 trong sidebar, tức tài liệu luôn coi trình soạn thảo là MỘT MÀN của EIDE. GPI-23 §1 nói ngược lại vì nó viết cho tình huống EIDE cắm vào một IDE có sẵn; khi hai thứ là một sản phẩm thì tình huống ấy không còn. | GPI-23 cần một bản v1.x tách hai vai: (a) *EIDE-trong-EIDE* — quan hệ hiện tại, trình soạn thảo là màn 11, panel `EidePanel` dùng chung cho cả hai lối vào; (b) *EIDE-là-plugin* — vẫn đúng và vẫn cần, cho ngày EIDE cắm vào VS Code hay CLion, và `EideClient` + kênh `event.*` đã dựng sẵn cho đúng ngày ấy. DEV-004 giữ nguyên giá trị: panel cạnh mã vẫn còn, chỉ thôi là lối vào DUY NHẤT. Việc chưa làm: đổi `CFBundleName`/`CFBundleIdentifier` trong bản phát hành và trong `build-universal.sh` (bản dựng phát triển chạy binary trần nên chưa chạm tới), và rà lại chuỗi ký/notarize theo tên mới. | Mở |
| DEV-099 | 2026-09-14 | CDS-12.2 EXTRACT-04 header_c (desc: "Header C chính hãng → địa chỉ/bit (đối chiếu SVD)"); CDS-12.4 SIM-01 grounding "Hộ chiếu vàng"; thông điệp E2000 của `sim.build_platform` ("chạy `extract.svd` hoặc `registry.pull`") | `src/eide/caps/extract.py::_vung_nho_header, MAU_BIEN_VUNG, TU_BO_NHO` | EXTRACT-04 khai phạm vi "địa chỉ/bit"; hiện thực nay rút thêm fact `memory_size` từ cặp `<TÊN>_LOW` / `<TÊN>_HIGH` trong header hãng (quy ước ESP-IDF `soc/soc.h`), tier vàng, parser thuần. Mười vùng cho ESP32-C3, 20 fact. | Ba năng lực đọc `memory_size` — `sim.build_platform`, `arch.memory_budget`, `diagram.memory_map` — nhưng chỉ `extract.svd` và `extract.atdf` sinh được nó. SVD của Espressif KHÔNG khai vùng nhớ nào (khác ST): không `<peripheral>` giả tên FLASH/RAM, không `addressBlock` phủ SRAM. Đo 14/09/2026 trên dự án ESP32-C3 thật: 8.169 fact từ SVD, 15.834 nút đồ thị, firmware dịch xong — và `sim.build_platform` vẫn dừng ở E2000 vì thiếu đúng một con số. Đường datasheet thì có con số (tr.34: "384 KB of ROM", "400 KB of on-chip SRAM", "RTC FAST memory: 8 KB") nhưng mọi extractor PDF đều đi qua vai trò mô hình `librarian`, và môi trường này không có khoá mô hình. Header chính hãng có cùng ba con số ở dạng máy đọc được: IRAM_HIGH − IRAM_LOW = 0x64000 = 400 KB, RTC_IRAM 8 KB, IROM_MASK 384 KB — khớp datasheet từng con số, hai nguồn độc lập. | Đề nghị CDS-12.2 EXTRACT-04 v1.x mở `desc` thành "địa chỉ/bit/biên vùng nhớ", hoặc CDS-12 thêm một năng lực riêng cho đặc tính bộ nhớ. Ràng buộc an toàn đã cài trong mã và cần giữ nếu tài liệu đổi: tên vùng phải chứa RAM/ROM/FLASH (không có nó thì `RTC_CNTL_DIG_DBIAS_LOW` và `APB_CLK_FREQ_HIGH` thành vùng nhớ ma), phải có CẢ hai biên và HIGH > LOW (một biên không đủ dựng một vùng, và đoán cạnh còn lại là bịa), vùng chồng nhau KHÔNG gộp (IRAM 0x4037C000 và DRAM 0x3FC80000 là hai cửa sổ vào cùng khối SRAM; gộp lại là bỏ đi đúng thứ phân biệt "firmware nạp sai cửa sổ" với "lỗi trong mã"). Cũng nên xem lại thông điệp E2000 của SIM-01: nó chỉ kể `extract.svd` và `registry.pull`, nên người dùng chip Espressif được chỉ vào hai đường đều không dẫn tới đâu. | Mở |
| DEV-100 | 2026-09-14 | APD-08 §1 (duyệt mục chờ là quyết định của người) và §4.1 (thang quyết định năm tầng); UXD-13 U2 (hàng đợi hai danh sách); API-15 §2 `gate.decide` | `src/eide_core/policy.py::decide, _cong_tu_xu_nguoi` | Nhánh nhường cho `actor == "human"` nằm ở TẦNG 5 của `decide`, tức sau khi quy tắc theo cổng (tầng 3) và quy tắc chung (tầng 4) đã trả lời. Nhưng tầng 5 chỉ tới khi KHÔNG quy tắc nào khớp — mà một mục vào hàng đợi thì gần như luôn vì một quy tắc ASK ở tầng 3/4 vừa khớp. Hệ quả: `Router.quyet_dinh()` duyệt xong chạy lại với `actor="human"`, quy tắc cũ khớp lại, ASK lại. Hàng đợi U2 không đóng được mục nào. | Đo 14/09/2026 trên dự án thật: `search.fetch` một header ESP-IDF từ `raw.githubusercontent.com` rơi vào G-SRC-99; `eide queue approve` → "CHỜ NGƯỜI (G-SRC · G-SRC-99)" — cùng câu hỏi, run_id mới. Duyệt bao nhiêu lần cũng thế. Chính docstring của `Router.quyet_dinh` mô tả hành vi mà mã không có: "PolicyGate tầng 5 trả APPROVE cho người gọi trực tiếp, nên lời gọi không quay lại hàng đợi một lần nữa". Test `test_hang_doi.test_duyet_thi_chay_tiep_khong_hoi_lai` xanh suốt vì nó dùng `kg.resolve_conflict` — một T2 gắn cổng `*`, đúng ca DUY NHẤT rơi xuống tầng 5. Bài test chọn trúng ngoại lệ và chứng minh cho trường hợp không xảy ra trong đời thật. | Đã sửa, và sửa hẹp: ở tầng 3/4, quy tắc ASK gặp `actor == "human"` thì thành APPROVE và MƯỢN id quy tắc, để `decision_log` còn truy được người đã trả lời câu hỏi nào. REJECT thì không nhường — duyệt là trả lời một câu HỎI, còn một quy tắc REJECT không hỏi gì cả. Cổng nào đã tự viết điều kiện về `actor` trong POL-17 thì nhánh chung tránh ra (`_cong_tu_xu_nguoi`, đọc từ `when` chứ không liệt kê tên cổng): G-WL-01 nói người ký được duyệt KHI NIÊM KHỚP, nên S48 ("người ký nhưng băm không khớp niêm" → ASK) vẫn đúng. CÒN MỞ, cần chủ sản phẩm quyết: vòng lặp vẫn còn ở ngưỡng cứng tầng 2 (HARD-R4 và "lớp vượt mức tự chủ") — hai chỗ ấy ép ASK trước khi cổng được hỏi, nên người duyệt một năng lực R4 vẫn bị hỏi lại mãi. Chỉ chạm hai năng lực R4 (`target.erase_fuse`, `registry.publish` công khai) và cả hai cần phần cứng hoặc mạng thật, nên chưa gặp — nhưng nới ranh giới R4 là quyết định của người, không phải của tác tử. | Mở |
| DEV-101 | 2026-09-14 | TGT-19 §1 (`isa/<isa>.yaml` trường `sim.cmd`); SIM-20 §2 (engine theo ISA); CDS-12.4 SIM-05 | `src/eide/caps/sim.py::_argv, _argv_manifest` | `_argv` dựng dòng lệnh engine BẰNG TAY và chưa bao giờ đọc `sim.cmd` của manifest ISA. Nay manifest nói trước: có `sim.cmd` thì nó LÀ dòng lệnh (chỉ argv[0] được thay bằng đường tuyệt đối `_tim_exe` dò được, vì PLATFORM.md không cho hard-code đường dẫn). | Với rv32imac chỗ khác nhau là `-bios none`. Thiếu nó, máy `virt` của qemu nạp OpenSBI mặc định ở 0x80000000 — đúng địa chỉ linker script đặt firmware — và lượt chạy chết với "Some ROM regions are overlapping", một lỗi trông như lỗi của người viết firmware. Đo 14/09/2026 trên dự án ESP32-C3: nền tảng dựng xong (10 vùng nhớ, 37 ngoại vi chưa mô hình), firmware dịch xong, và `sim.run` vẫn không chạy nổi. Nằm im vì armv7e-m và avr8 KHÔNG khai `sim.cmd`, nên với hai ISA ấy đường dựng tay và đường manifest trùng nhau — manifest thứ ba là manifest đầu tiên khai nó, và cũng là lần đầu hai đường tách ra. | Đã sửa. Không đổi tài liệu: TGT-19 vốn khai trường `sim.cmd` và mã mới là bên không đọc. Đề nghị TGT-19 v1.x nói rõ hai điều để chỗ này không trôi lại: (a) `sim.cmd` là dòng lệnh ĐẦY ĐỦ, không phải gợi ý; (b) placeholder được đỡ hiện nay là `{artifact}` và `{duration}` — manifest viết placeholder khác sẽ đi thẳng vào argv dưới dạng chữ, cùng bẫy với `{march}` trong `build.cmd` đã gặp 13/09. | Mở |
| DEV-102 | 2026-09-14 | SEC-25 §2 (sandbox có thư mục làm việc tạm riêng mỗi lần chạy); CDS-12.4 SIM-05 (ví dụ `artifact: build/fw.elf` — đường dẫn TƯƠNG ĐỐI) | `src/eide/cli.py::_router` | CLI không tuyệt đối hoá `--project`, nên `eide caps invoke -p . …` cho `project_dir = Path(".")` và mọi đường dẫn năng lực dựng từ đó cũng tương đối theo. | Phần lớn năng lực không sao vì chúng chạy trong chính tiến trình này, nơi thư mục hiện hành đúng là dự án. Hỏng ở đúng chỗ giao việc RA NGOÀI: sandbox SEC-25 §2 đổi thư mục làm việc, nên `qemu … -kernel ./build/fw.elf` báo "No such file or directory" cho một tệp có thật — và thông điệp ấy đổ lỗi cho tệp chứ không cho đường dẫn. Đo 14/09/2026 bằng `sim.run` với đúng ví dụ đường dẫn mà hợp đồng SIM-05 viết. `-p .` là cách gõ tự nhiên nhất từ trong thư mục dự án, nên đây là đường người dùng thật sẽ đi. | Đã sửa ở lối vào CLI (`expanduser().resolve()`), một chỗ cho mọi năng lực. Kèm theo: `sim.run` nay trả `log_ref` qua `_tuong_doi` — trước đây nó tương đối một cách TÌNH CỜ, chỉ vì root tương đối, nên tuyệt đối hoá root làm báo cáo mang theo cây thư mục của người chạy. Không đổi tài liệu: SEC-25 và CDS-12.4 đều đúng, mã là bên sai. | Mở |
| DEV-103 | 2026-09-14 | POL-17 §3 `trusted_sources` (danh sách CHO PHÉP TẢI, khớp CHÍNH XÁC chuỗi tên miền) đối chiếu TGT-19 §8 bảng nguồn hãng (`docs/spec/sources/vendors.yaml`, trường `urls[].template`) | `docs/spec/policy/defaults.yaml::trusted_sources; src/eide_core/policy.py (quy tắc G-SRC-01)` | Danh sách trắng chứa tên miền TRẦN (`st.com`, `microchip.com`, `nordicsemi.com`, `espressif.com`, `bosch-sensortec.com`) trong khi mọi mẫu URL của bảng nguồn hãng đều dùng SUBDOMAIN thật (`www.st.com`, `packs.download.microchip.com`, `ww1.microchip.com`, `datasheets.raspberrypi.com`, `www.bosch-sensortec.com`, `invensense.tdk.com`, `www.allegromicro.com`). | Đo 14/09/2026 bằng cách đối chiếu hai tệp: **4 trên 12 mẫu URL hãng tải tự động được, và cả bốn đều là `raw.githubusercontent.com`** — không một trang hãng THẬT nào qua được cổng. `microchip.com` và `nordicsemi.com` là mục CHẾT: không mẫu URL nào dùng tên miền trần ấy, nên chữ ký của chủ sản phẩm trên hai dòng đó chưa bao giờ cho phép tải được gì. Lộ ra khi làm luồng AVR: `search.vendor` đề xuất đúng hai nguồn Microchip chính thức, và cả hai rơi vào ASK G-SRC-99 "nguồn ngoài danh sách tin cậy" — một cổng hỏi người về đúng thứ nó được dựng lên để cho qua, y hệt hình dạng WI-257 và DEV-087. Không ai thấy sớm hơn vì hai luồng đã chạy trước (STM32, ESP32-C3) đều đi qua `raw.githubusercontent.com`, tức đúng bốn mục xanh. | KHÔNG tự sửa: nới phép khớp của danh sách trắng là nới quyền, và S47 nói thẳng tác tử không được tự thêm nguồn tin cậy. Hai phương án cho chủ sản phẩm. (a) Khớp theo tên miền đăng ký với CANH BIÊN DẤU CHẤM: `d == t or d.endswith("." + t)` — `www.st.com` thuộc `st.com`, còn `evil-st.com` thì không, đúng phép canh biên mà `_la_trang_hang` đã dùng từ DEV-087. Một dòng trong `policy.py`, không phải ký lại danh sách, và nó làm chữ ký hiện có có hiệu lực đúng như người ký tưởng. (b) Thêm đủ bảy subdomain vào `trusted_sources` rồi `eide policy sign` lại — hẹp hơn nhưng phải ký lại mỗi lần TGT-19 §8 thêm một hãng, và bảng nguồn hãng thì còn dài ra. Nghiêng về (a). Dù chọn đường nào, đề nghị thêm một test đối chiếu HAI CHIỀU giữa `vendors.yaml` và `trusted_sources` — hôm nay không test nào so hai tệp ấy với nhau, và đó là lý do lệch này sống được qua ba lần ký. | Mở |
| DEV-104 | 2026-09-14 | TGT-19 §1 `toolchain.build.cmd` (placeholder `{mcu}`, `{f_cpu}`); CDS-12.1 CODE-05 | `src/eide/caps/code.py::_giai_placeholder, build` | `code.build` chạy `toolchain.build.cmd` NGUYÊN VĂN, không giải placeholder. Nay giải `{isa}`, `{mcu}`, `{chip}`, `{board}`, `{f_cpu}` từ mục `target` của `.eide/constraints.yaml`; placeholder nào không có giá trị thì E2000 nói rõ thiếu gì và đặt ở đâu. | Manifest `avr8` khai `make -C . MCU={mcu} F_CPU={f_cpu}`, nên trước khi sửa, `make` nhận một biến `MCU` mang đúng năm ký tự `{mcu}` và `avr-gcc -mmcu={mcu}` hỏng bằng một thông báo của trình dịch. TGT-19 dùng placeholder xuyên suốt — `flash.cmd` có `{programmer} {part} {port} {hex}`, `id_read.cmd` có `{port}`, `sim.cmd` có `{artifact}` — và mọi chỗ dùng chúng đều tự giải; `build.cmd` là chỗ DUY NHẤT không. Nằm im vì `armv7e-m` và `rv32imac` viết lệnh cmake không placeholder, và vì chưa ai dựng AVR bao giờ. Cùng bẫy đã gặp 13/09 với `{march}` trong `build.cmd` của rv32imac — lần ấy chữa bằng cách bỏ placeholder khỏi manifest, tức chữa triệu chứng. | Đã sửa, và sửa theo hướng KHÔNG ĐOÁN: Arduino Uno chạy 16 MHz và ai cũng biết, nhưng không fact nào trong dự án nói thế, nên thiếu `f_cpu` là E2000 chứ không phải điền 16000000. Điền theo trí nhớ là gieo một hằng số không nguồn vào tận dòng lệnh dựng, nơi không cổng nào của EIDE còn nhìn thấy nó — và sai `F_CPU` thì UART ra ký tự rác trong khi mọi thứ khác trông vẫn đúng. Đề nghị TGT-19 v1.x liệt kê tập placeholder được đỡ và nguồn giá trị của từng cái (`target.*` trong constraints, hoặc hộ chiếu board), để manifest không viết một placeholder mà không ai giải. Còn mở: `{f_cpu}` hôm nay phải khai tay trong `constraints.yaml`; chỗ đúng của nó là **hộ chiếu board** (`arduino-uno` = thạch anh 16 MHz), mà `board.*` chưa có đường nhập hộ chiếu board cho kit phổ thông. | Mở |
| DEV-105 | 2026-09-14 | TGT-19 §2 bảng chuỗi công cụ theo ISA (hàng avr8: "avr-gcc 12, avrdude 7"); `docs/spec/isa/avr8.yaml` `toolchain.compiler.min` | `docs/ho-so/nguon/tgt_sim.js; docs/spec/isa/avr8.yaml` | Hạ `avr-gcc.min` từ **12.0** xuống **7.3**, và bảng §2 nay ghi "avr-gcc ≥7.3 (ATmega) / ≥8 cho ATtiny UPDI". | Ngưỡng 12.0 trên macOS chỉ đạt được qua tap `osx-cross/avr`, mà Homebrew bản mới từ chối nạp tap bên thứ ba cho tới khi người dùng chạy `brew trust osx-cross/avr` — cấp cho tap quyền chạy mã tuỳ ý lúc cài. Đó là quyết định bảo mật về máy của chủ sản phẩm, không phải việc tác tử tự nhận. Trong khi đó chuỗi công cụ CHÍNH THỨC của Arduino (`arduino-cli core install arduino:avr`, homebrew-core, không cần trust) mang avr-gcc **7.3.0** và avrdude **8.0** — và nó dựng được ATmega328P: đo 14/09/2026, `code.build` passed, `fw.elf` 650 B flash / 108 B RAM, chạy trong `qemu-system-avr -M arduino-uno` với 6/6 dòng expect đạt. Ngưỡng 12.0 vì thế loại đúng chuỗi công cụ mà người dùng Arduino Uno thật sự có, để đổi lấy một tính chất không dùng tới. | Ngưỡng nên theo CHIP chứ không theo ISA. TGT-19 §2 xếp ATmega328P và ATtiny1616 chung một hàng avr8, nhưng chúng khác nhau đúng ở điểm này: ATtiny1616 là lõi AVRxt nạp qua UPDI, cần avr-gcc ≥8 kèm device pack, còn ATmega328P thì gcc 7.3 dựng tốt. Ghi một ngưỡng duy nhất cho cả ISA buộc phải chọn con số của chip khó nhất, và thế là loại mọi máy chỉ làm chip dễ. Đề nghị TGT-19 v1.x cho `toolchain.compiler.min` nhận dạng bảng theo `family_patterns` (như `flash.cmd` đã nhận bảng theo adapter). Trong lúc chờ, `7.3` là ngưỡng đúng cho hàng ATmega và chú thích §2 nói rõ ATtiny UPDI cần ≥8. | Mở |
| DEV-106 | 2026-09-14 | CDS-12.3 ENV-02 ("env.check chạy `check` của từng tool trong sandbox, so `min` theo semver" — TGT-19 §3); `docs/spec/isa/*.yaml` trường `toolchain.compiler.check` | `src/eide_core/tools.py::version_of, CO_PHIEN_BAN; src/eide/caps/env.py::_ver_ok` | `version_of` chọn dòng phiên bản theo NỘI DUNG (dòng đầu tiên có số dạng `\\d+\\.\\d+` sau khi bỏ các từ là đường dẫn), thử lần lượt `--version`, `-v`, `version` — thay vì trả thẳng dòng đầu của stdout/stderr. `_ver_ok` cũng bỏ từ chứa `/` trước khi tìm số. | Đo trên avrdude 8 (Arduino core) 14/09/2026: `avrdude --version` trả mã thoát **0** nhưng in `"/Users/…/avrdude/8.0.0-arduino1/bin/avrdude: illegal option -- -"`, còn `avrdude -v` trả mã **1** nhưng in đúng `"Avrdude version 8.0-arduino.1"`. Hai hệ quả chồng nhau. (a) `version_of` lấy dòng đầu nên `env.check` ghi `version` là chính thông báo lỗi ấy. (b) `_ver_ok` tìm `(\\d+)\\.(\\d+)` trên CẢ chuỗi nên nó thấy `8.0.0` — **trong tên thư mục cài đặt nằm trong thông báo lỗi** — và kết luận `ok: true`. Lần này con số tình cờ đúng vì Arduino đặt tên thư mục theo phiên bản; lần sau ai cài một avrdude 6.3 vào thư mục tên `8.0.0` thì EIDE vẫn khẳng định đạt. Đây là ENV-02 nói "so `min` theo semver" trong khi thứ được so không phải semver của công cụ. Cũng cho thấy mã thoát không dùng làm tín hiệu được: lọc theo nó thì nhận chuỗi rác và bỏ chuỗi thật, đúng ngược. | Đã sửa hai tầng (nhận dạng nội dung ở `version_of`, lọc đường dẫn ở `_ver_ok`). `-V` cố ý KHÔNG có trong danh sách cờ dù nó là quy ước BSD: với avrdude, `-V` nghĩa là **bỏ qua verify sau khi ghi**, và thử một cờ đoán chừng trên công cụ nạp firmware là đúng loại việc không được làm — có test giữ điều này. CÒN MỞ: manifest ISA đã khai `toolchain.compiler.check` (`"avr-gcc --version"`) mà `env.check` không đọc, nó tự đoán cờ. Lại đúng hình dạng "một bên khai, bên kia không đọc" của lỗi im lặng 22–25. Đề nghị: `env.check` dùng `check` khi manifest có, và chỉ dò cờ khi không có; đồng thời TGT-19 cho `tools[]` khai `check` riêng, vì hôm nay chỉ `compiler` có trường ấy — mà avrdude mới là công cụ cần nó nhất. | Mở |
| DEV-107 | 2026-09-14 | API-15 §1 (bảng 16 phương thức `event.*`); UXD-13 U2 (hàng đợi, thanh tự chủ luôn hiện); APD-08 §2 (mức A0–A4) §5 (leo thang) | `docs/ho-so/nguon/api.js (literal EV); src/eide/daemon/rpc.py::SU_KIEN; apps/geditor/Sources/EIDEKit/EideNhatKyView.swift, EideDuAn.swift, EideViews.swift::AutonomyBar` | API-15 §1 thêm NĂM sự kiện: `event.gate.decided`, `event.model.call`, `event.tool.report`, `event.project.changed`, `event.chat.intent` (16 → 21). Giao diện thêm màn **Nhật ký hoạt động** (sidebar 21 → 22 màn), đường **mở dự án EIDE**, ô **đổi mức tự chủ** và **băng leo thang** trên `AutonomyBar`. | Chủ sản phẩm duyệt 14/09/2026 sau bản phân tích `docs/GIAM-SAT-UI.md`: *"toàn bộ các tính năng khi hoạt động cần được show lên giao diện để người dùng có thể giám sát và tham gia vào quá trình nếu muốn"*. Đo được trước khi làm: **13 trong 26 kiểu sự kiện sổ cái không có đường nào lên giao diện**, và 16 sự kiện API-15 hiện có chỉ kể những thứ ĐÒI người làm gì đó. Ba cái thiếu đau nhất: `model.call` (tác tử tiêu bao nhiêu token), `tool.report` (build/sim chạy ra sao), `policy.escalate` (tác tử đang dừng chờ người). Riêng `gate.decision` còn bị lọc bỏ khi quyết định KHÁC ASK — nên người không thấy tác tử tự duyệt những gì, mà ở mức tự chủ cao thì đó chính là thứ cần nhìn nhất. Về giao diện: không màn nào trong 23 màn UXD-13 trả lời được câu "tác tử vừa làm gì" — cả 23 đều là màn chuyên đề; và `AutonomyBar` hiện mức nhưng không đổi được, nên núm điều khiển chính của APD-08 chỉ dùng được bằng cách sửa `.eide/autonomy.yaml` rồi khởi động lại daemon. | Đề nghị API-15 v1.x ghi năm sự kiện mới vào bảng §1 (đã thêm qua literal `EV` của `api.js`, không sửa tay openrpc.json) và nói rõ nguyên tắc phân đôi: 16 sự kiện gốc là **việc cần người**, năm cái mới là **việc tác tử tự làm xong**. UXD-13 §2 cần thêm màn "Nhật ký hoạt động" vào bảng 23 màn (nay 24 với cả `ReviewQueue`), và §4 mô tả `AutonomyBar` có ô chọn mức. Hai ràng buộc phải giữ nếu tài liệu đổi: (a) `event.model.call` KHÔNG mang `prompt`/`context`/`messages` và `context.bundle` không lên UI — sổ cái đã che khoá API, nhưng che khoá khác với không gửi mã nguồn người dùng ra một cửa sổ có thể đang chia sẻ màn hình; (b) `nhatKy.them` gọi TRƯỚC `switch` trong `nhanSuKien`, vì để nó trong một `case` thì mỗi sự kiện thêm về sau là một chỗ có thể quên — có test đọc mã nguồn canh đúng vị trí ấy. | Mở |
| DEV-108 | 2026-09-14 | POL-17 §5 (`.eide/autonomy.yaml` là chính sách CỦA DỰ ÁN) §3 (niêm `policy.sig` theo dự án); APD-08 §2; UXD-13 U2 | `src/eide/daemon/rpc.py::Daemon.__init__, _autonomy_du_an; apps/geditor/Sources/EIDEKit/EidePanel.swift::_moTheoTenMan` | Hai chỗ giao diện lệch khỏi CLI, phát hiện khi bật bản dựng thật lên kiểm. (a) `Daemon` dựng `PolicyGate()` TRẦN — không đọc `.eide/autonomy.yaml` lẫn `.eide/policy.sig` của dự án, trong khi `cli._router` đọc cả hai từ đầu. (b) Mở màn từ sidebar (`_moTheoTenMan`) không hỏi `napMacDinh`, chỉ xử lý hai màn nạp bằng phương thức daemon. | (a) **Giao diện chạy với một chính sách khác CLI trên cùng một dự án.** Đo 14/09/2026 trên dự án AVR: tệp ghi `autonomy: A2`, `eide caps invoke` áp A2, còn cửa sổ EIDE hiện mức "—" và quyết định theo mặc định toàn cục. Hai nguồn sự thật cho cùng một câu hỏi, và cái người dùng NHÌN THẤY là cái sai. Niêm cũng thế: `eide policy sign -p <dự án>` ghi ra `.eide/policy.sig`, daemon đọc `defaults.sig` toàn cục — nên chữ ký của chủ sản phẩm trên dự án ấy không có hiệu lực và bốn danh sách trắng bị bỏ (POL-17 §3). (b) **Mở màn nào từ sidebar cũng ra "chưa có nguồn dữ liệu"** trừ FlowMap và Models — kể cả Tổng quan, Hộ chiếu, Môi trường, những màn đã có sẵn năng lực trong `napMacDinh`. Gốc rễ: `napMacDinh` thêm 13/09 khi sửa lỗi im lặng số 21, nhưng chỉ chạy ở đường `_napLaiManDangMo` — tức chỉ khi một sự kiện `knowledge.changed` tới. Hai đường vào cùng một màn, chỉ một đường nạp dữ liệu, và đường người dùng thật sự đi là đường không nạp. | Đã sửa cả hai. (a) `Daemon.__init__` nhận `config=_autonomy_du_an(project)` và `sig_path=.eide/policy.sig`; `autonomy.yaml` hỏng thì rơi về mặc định chứ không làm daemon chết — mất cả giao diện vì một dòng YAML, và người dùng không có cách nào biết vì sao vì daemon chết trước khi kịp nói gì. Có test đối chiếu `_autonomy_du_an` với `cli._autonomy_cua_du_an` để hai bản sao không trôi khỏi nhau. (b) `_moTheoTenMan` hỏi `napMacDinh` trước, `_napManKhongNangLuc` sau; test đọc mã nguồn canh đúng THỨ TỰ ấy, và một test thứ hai duyệt cả 22 màn sidebar đòi mỗi màn hoặc có đường nạp hoặc được nhắc tới kèm lý do. Không đổi tài liệu: POL-17 và UXD-13 đều đúng, mã là bên sai. | Mở |
| DEV-109 | 2026-09-15 | UXD-13 §2 (bảng 23 màn); CDS-12.6 CHAT-04/05; CDS-12.4 KG-02/KG-06 | `apps/geditor/Sources/EIDEKit/EideLamRoView.swift, EideXungDotView.swift; EidePanel.swift::nangLucChinh; EideDieuHuong.swift::NHOM` | Thêm HAI màn ngoài bảng 23 màn của UXD-13 §2: **Làm rõ yêu cầu** (UC-B2/B3 — `chat.restate` + `chat.clarify`) và **Xung đột tri thức** (UC-C9 — `kg.conflicts` + `kg.resolve_conflict`). Điều hướng nay 23 màn chuyên đề. | Chủ sản phẩm duyệt 15/09/2026 theo `docs/USECASE.md` §4 ưu tiên 1. Hai khoảng trống đo được: (a) cách-tác-tử-hiểu chỉ hiện bằng một thẻ TRÔI trong dòng chat — thẻ mất khi có thêm vài lượt, và `chat.clarify` trả câu hỏi GỘP có phương án + mặc định mà một bong bóng chat không đặt được bốn lựa chọn. Đây lại là chỗ RẺ NHẤT để sửa một hiểu lầm: sửa ở đây tốn một câu, để tác tử chạy xong mới phát hiện thì tốn cả một lượt sinh mã, một lượt dựng và niềm tin. (b) `kg.conflicts` KHÔNG gắn màn nào (`EidePanel.noUI`) — xung đột được đếm trong bản đồ tri thức rồi thôi; người dùng biết có 3 xung đột và không có đường nào xem chúng là gì. Mà `kg.resolve_conflict` là T2 với `ask_when: "khi người chọn"`, tức hợp đồng nói thẳng việc này thuộc về người: một xung đột fact nghĩa là hai nguồn hãng nói hai điều khác nhau về cùng một con chip, và chọn sai bên thì mã, mô phỏng, ngân sách bộ nhớ đều sai theo, im lặng. | Đề nghị UXD-13 v1.x thêm hai màn vào bảng §2 (23 → 25 dòng, kể cả ReviewQueue). Ba ràng buộc thiết kế nên ghi vào tài liệu vì chúng là quyết định chứ không phải chi tiết: (1) nút "Đúng — làm đi" KHÔNG nhận phím Enter — đây là xác nhận một việc tác tử sắp làm thật, để Enter bấm hộ là biến một cú gõ vô ý thành một lượt chạy; (2) hai bên xung đột đặt CÙNG HÀNG, mỗi bên kèm nguồn và tầng hiện ngay cạnh giá trị, vì xếp dọc thì người đọc phải nhớ bên trên nói gì trong lúc đọc bên dưới; (3) lựa chọn thứ ba `both_conditional` phải có mặt và phải ĐÒI một điều kiện — hai giá trị đều đúng cho hai revision silicon khác nhau là trường hợp rất thật (ESP32-C3 v0.4 và v1.1 khác nhau ~10 KB SRAM khả dụng), và không có điều kiện thì "cả hai" là vô nghĩa. | Mở |
| DEV-110 | 2026-09-15 | MEM-11 §2 (M2 gồm "quyền theo phiên (R4)" và "target đang cắm"); API-15 §2 | `src/eide_core/memory.py::SessionMemory; src/eide/daemon/rpc.py::session_state; docs/ho-so/nguon/api.js` | Thêm phương thức `session.state` trả M2 của phiên đang mở. Payload có `permits[]` và `board` theo đúng MEM-11 §2, nhưng **hai trường ấy luôn RỖNG**: `SessionMemory` chỉ lưu `session_id, project, opened_at, autonomy_effective, stopped, turns, undo_items, closed_at, summary` — không có quyền R4 và không có board. Kèm khoá `thieu[]` liệt kê chính hai tên ấy, và màn Tổng quan hiện câu giải thích. | USECASE §4 mục 7, chủ sản phẩm duyệt 15/09/2026. Trước đó giao diện không hiện gì về M2 — người dùng không có cách nào biết phiên này đã cấp những quyền gì, mà quyền R4 theo phiên là thứ nguy hiểm nhất còn hiệu lực: nó cho tác tử làm một việc KHÔNG hoàn tác được mà không hỏi lại. Ba cách xử lý khoảng trống, và hai cách đầu tệ hơn: (a) bỏ hẳn hai khoá — giao diện không biết chúng tồn tại và thiếu sót biến mất khỏi tầm nhìn, đúng khuôn DEV-093 nơi một khoảng trống nằm im vì không ai hỏi tới; (b) bịa dữ liệu, ví dụ đọc `discover.ports` rồi gọi đó là "board đang cắm" — board DÒ ĐƯỢC khác board đã GẮN vào phiên, và trộn hai thứ là nói sai về quyền đang có hiệu lực; (c) trả rỗng kèm khoá `thieu`, giao diện hiện đúng thứ có thật và nói ra phần chưa có. | Đề nghị MEM-11 v1.x giữ nguyên định nghĩa M2 (nó đúng) và ghi rõ hiện thực còn nợ hai trường. Hai trường ấy cần: (1) `permits[]` — `policy.permit` (POLICY-02, T2) cấp quyền R4 theo phiên, nhưng chưa ai ghi kết quả vào `SessionMemory`; thêm một lời gọi ở `Router.quyet_dinh` khi quyết định là cấp quyền. (2) `board` — `target.detect` gắn board vào phiên, nhưng nhóm `target.*` cần phần cứng nên chưa hiện thực; làm cùng lúc với `discover.chip_id` (mốc M2). Khi hai trường có dữ liệu, `session.state` và màn Tổng quan không phải đổi một dòng nào — chúng đã đọc sẵn. | Mở |
| DEV-111 | 2026-09-15 | API-15 §1 (JSON-RPC 2.0 qua stdio — không nói gì về nhiều lời gọi chồng nhau); `docs/spec/api/openrpc.json` | `apps/geditor/Sources/EIDEKit/EideClient.swift::vaoHang, roiHang, goi` | `EideClient` nay TUẦN TỰ HOÁ mọi lời gọi bằng một mutex bất đồng bộ (`CheckedContinuation`): lời gọi thứ hai chờ lời gọi thứ nhất đọc xong phản hồi rồi mới ghi vào stdin. Một lời gọi một lúc, dù JSON-RPC 2.0 cho phép chồng. | Trước bản sửa, hai lời gọi cùng lúc làm TREO client vĩnh viễn (lỗi im lặng số 35). Gốc rễ là một hiểu sai về `actor` trong Swift: actor tuần tự hoá phần mã GIỮA các điểm treo, không phải cả hàm — tới `await` là nhả quyền, nên hai lời gọi cùng ghi stdin rồi cùng đọc stdout, mỗi bên nhận câu trả lời của bên kia và cả hai chờ một phản hồi không bao giờ tới. Trong giao diện: một màn đứng im mãi mãi, không lỗi, không xoay. Lối đi ĐÚNG theo JSON-RPC là khớp phản hồi theo `id` và cho phép chồng — nhưng nó cần một vòng đọc nền và một bảng `id → continuation`, tức một tầng đồng thời nữa trong đúng chỗ vừa sinh ra lỗi này. Tuần tự hoá đổi lấy: mất song song (một lời gọi chậm chặn các lời gọi sau) để lấy một bất biến kiểm được bằng mắt. Với tải thật của giao diện — người bấm, một màn nạp một năng lực — hàng chờ hầu như luôn rỗng. | Đề nghị API-15 v1.x NÓI RA chuyện này, vì hôm nay nó im lặng và sự im lặng ấy là chỗ lỗi sinh ra: ghi rõ daemon có chấp nhận nhiều lời gọi chồng nhau trên một ống stdio hay không, và nếu có thì phản hồi được phép trả ra KHÔNG theo thứ tự hay không. Hai câu ấy quyết định client phải viết kiểu gì. Nếu sau này cần song song thật (ví dụ tải nhiều màn cùng lúc khi mở dự án lớn), đổi mutex thành bảng `id → continuation` cộng một vòng đọc nền — và khi ấy phải có test bắn ≥3 lời gọi chồng nhau kèm hạn giờ, như test đang có cho 2 lời gọi. | Mở |
