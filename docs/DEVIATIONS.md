# Nhật ký sai khác giữa mã và tài liệu (DEVIATIONS)

Quy tắc: mọi chỗ mã **buộc phải khác** bộ hồ sơ v1.2 đều ghi ở đây **trước khi commit**. Không sửa tài liệu trực tiếp trong docx; tài liệu được sinh lại từ `ho-so/nguon/` sau khi chủ sản phẩm duyệt. Thêm mục bằng `/sai-khac` hoặc `python scripts/new_deviation.py`.

Trạng thái: `Mở` → `Đã duyệt` (chủ sản phẩm đồng ý sửa tài liệu) | `Bác` (sửa mã cho khớp tài liệu) → `Đã cập nhật tài liệu vX.Y`.

| Mã | Ngày | Tài liệu / mục | Mã nguồn | Sai khác | Lý do | Đề xuất | Trạng thái |
|---|---|---|---|---|---|---|---|
| DEV-001 | 2026-09-05 | POL-17 §2 rules.yaml (`thresholds.*`, `trusted_sources`, `allowed_licenses`) | `src/eide_core/policy.py`, `docs/spec/policy/defaults.yaml` | rules.yaml tham chiếu ngưỡng nhưng giá trị mặc định chỉ có trong schema autonomy.json (POL-17 §5) và ví dụ SDD-04 §6; `allowed_licenses` không có giá trị mặc định ở đâu | Cần tệp mặc định để PolicyGate chạy được khi dự án chưa có `.eide/autonomy.yaml` | Thêm `spec/policy/defaults.yaml` (ngưỡng theo schema; `allowed_licenses` = MIT, BSD-2/3, Apache-2.0, CC-BY-4.0, vendor-doc) và ghi vào POL-17 §5 ở v1.3 | Mở |
| DEV-002 | 2026-09-05 | API-15 §3 errors.json | `src/eide_core/registry.py::validate_output` | Không có mã lỗi cho trường hợp *kết quả năng lực* không khớp `output_schema` (E5002 chỉ dành cho đầu ra mô hình) | Registry kiểm output mọi lời gọi để bắt lỗi hiện thực sớm (STP-05) | Tạm dùng E6001 SCHEMA_VIOLATION; đề xuất thêm E1004 OUTPUT_SCHEMA ở API-15 v1.3 | Mở |
| DEV-016 | 2026-09-06 | API-15 §7 (bộ lọc bí mật trước khi ghi ledger) | `src/eide_core/ledger.py::che_bi_mat` | Tài liệu nêu bộ lọc nhưng SPRINT-01 xếp nó vào Sprint 2 (ghi chú WI-008); nay đã hiện thực sớm | WI-013 làm khóa API đi qua tiến trình lần đầu. Ledger là append-only và chống sửa: một khóa lọt vào thì không gỡ ra được nữa mà không phá chuỗi hash — và vẫn phải đổi khóa. Chờ tới Sprint 2 là chờ quá một sự kiện không hoàn tác được | Đã che ở tầng `Ledger.append`, TRƯỚC khi băm, nên chuỗi hash vẫn liền. Giữ 8 ký tự đầu để còn truy được là khóa nào. Không sửa tài liệu — đây là làm sớm, không phải làm khác | Bác |
| DEV-015 | 2026-09-06 | SDD-04 §6 `models.yaml`; DDD-14 §6 (models.yaml "từ M0"); PRS-16 §2 | `docs/spec/models.yaml` | `models.yaml` được cả ba tài liệu nhắc tới nhưng không có trong `docs/spec/`; và SDD-04 §6 dùng BÍ DANH ("gemini-flash", "claude-opus") chứ không phải mã model thật, lại không nói giá lấy ở đâu | Không có tệp thì Gateway không chọn được mô hình; không có ánh xạ bí danh thì không gọi được API nào; không có giá thì `daily_budget_usd` không cưỡng chế được và `model.call.cost_usd` luôn bằng 0 | Thêm `docs/spec/models.yaml` — phần `roles` và `policy` chép ĐÚNG SDD-04 §6; bổ sung `aliases` (bí danh → mã model thật, đã đối chiếu API 06/09/2026) và `pricing` (USD/1M token). Cùng khuôn mẫu với `defaults.yaml` của DEV-001. Đề xuất SDD-04 v1.3 ghi rõ hai phần bổ sung này | Mở |
| DEV-014 | 2026-09-06 | POL-17 §5-6; DEP-26 §3 (một daemon mỗi người dùng, nhiều dự án); API-15 §7 | `src/eide/cli.py::_router`, `src/eide_core/undo.py` | Mục hoàn tác của `project.create` nằm ở ledger NGƯỜI DÙNG chứ không ở ledger dự án, vì lúc gọi thì dự án chưa tồn tại. Tài liệu không nói cửa sổ hoàn tác có phạm vi nào | Hai ledger là hệ quả của mô hình DEP-26 §3, không phải lỗi; nhưng `policy.undo_window` gọi trong dự án sẽ KHÔNG thấy mục tạo dự án, còn gọi ngoài dự án thì thấy | Giữ nguyên phân chia (sự kiện liên-dự-án ở ledger người dùng, sự kiện dự án ở ledger dự án) và ghi rõ vào POL-17 §5 ở v1.3. **Màn hình ReviewQueue (UXD-13) phải gộp hai nguồn** — nếu chỉ đọc ledger dự án thì người dùng không bao giờ thấy nút hoàn tác cho việc tạo dự án | Mở |
| DEV-013 | 2026-09-06 | API-15 §5 `ledger_events.json`; CDS-12.5 POLICY-04 bước "ghi ledger" | `src/eide/caps/policy.py::escalate` | Không có kiểu sự kiện nào cho leo thang: danh sách 15 kiểu không có `escalate` hay `policy.escalate`, mà POL-17 §6 lại có hẳn một máy trạng thái leo thang | Lớp `Ledger` từ chối kiểu không có trong `ledger_events.json` (E6001), nên phải chọn một kiểu đã có | Tạm dùng `question` với `{question_id, reason, channels, escalated: true}` — leo thang chính là đưa một câu hỏi tới người qua kênh phù hợp. Đề xuất API-15 v1.3 thêm kiểu `policy.escalate {ref, reason, channels, level}` để phân biệt câu hỏi thường với câu hỏi đã leo thang | Mở |
| DEV-012 | 2026-09-06 | APD-08 §4.1 tầng 2 vs POL-17 quy tắc `G-OPS-02`, `G5-02`; situations S31, S39 | `src/eide_core/policy.py::decide` | Hai quy tắc có lý do ghi "(R4)" không bao giờ thắng: tầng ngưỡng cứng bắt mọi hành động R4 và trả `HARD-R4` trước khi xét quy tắc cổng. Fixture ghi `ASK G-OPS-02` / `ASK G5-02`, máy trả `ASK HARD-R4` | Quyết định GIỐNG NHAU (đều ASK) nên không phải lỗi an toàn; chỉ là quy tắc chết và nhãn trong nhật ký khác điều tài liệu hứa. Người đọc `decision_log` sẽ không thấy G-OPS-02 bao giờ | Chủ sản phẩm duyệt 06/09/2026. ĐÃ SỬA theo phương án (b): tầng 2 tra quy tắc cổng trước và mượn `rule_id` + lý do, nhưng CHỈ từ dải chặn (priority ≤ 5) — quy tắc ưu tiên lớn hơn là lời khuyên chung, dùng nó làm lý do cho hành động R4 là nói nhỏ đi mức nghiêm trọng. Ngưỡng cứng chỉ siết, không nới: quy tắc APPROVE gặp ngưỡng cứng vẫn ra ASK. Còn lại: APD-08 §4.1 và POL-17 v1.3 ghi rõ tầng 2 mượn lý do từ dải chặn | Đã duyệt |
| DEV-011 | 2026-09-06 | POL-17 §2 `rules.yaml` `G-SRC-01` priority 10 vs `G-SRC-06` priority 25; situations.jsonl S06 | `docs/spec/policy/rules.yaml` (chưa sửa), `tests/test_policy_situations.py::LECH` | **Lệch quyết định, không phải lệch nhãn.** S06 ("A3; bosch-sensortec.com; match_score 0,4") được fixture ghi là `ASK G-SRC-06`, nhưng PolicyGate trả `APPROVE G-SRC-01`: quy tắc "nguồn hãng tin cậy" (priority 10) thắng trước quy tắc "tài liệu có thể không đúng linh kiện" (priority 25), nên `match_score` không bao giờ được xét khi domain đã tin cậy | Hậu quả: một datasheet của biến thể chip KHÁC, tải từ đúng trang hãng, được tự động nhận vào mà không hỏi ai. Mọi fact trích từ nó sau đó mang nhãn "nguồn tin cậy" — đúng kiểu nhiễm tri thức mà G-SRC sinh ra để chặn | Sửa `rules.yaml`: hạ `G-SRC-06` xuống priority 5 (ngang `G-SRC-03` hash lệch và `G-SRC-07` nhạy cảm — cùng loại "chặn trước khi tin"), HOẶC thêm `source.match_score >= thresholds.source_match_min` vào điều kiện `G-SRC-01`. Chủ sản phẩm duyệt 06/09/2026 (uỷ quyền quyết định). ĐÃ SỬA phương án hạ priority: `pol.js` dòng G-SRC-06 25→5, sinh lại `docs/spec/policy/rules.yaml` bằng `node scripts/gen_policy_spec.js`. S06 nay ra đúng `ASK G-SRC-06`. Còn lại: sinh lại docx POL-17 ở v1.3 | Đã duyệt |
| DEV-010 | 2026-09-06 | DDD-14 §2 thực thể Preference (bảng `preference`) vs §6 `preferences.yaml`; CDS-12.3 PROJECT-09 | `src/eide/caps/project.py::preferences` | Tài liệu có HAI nơi lưu cùng một thứ với cùng hình dạng ({value, scope, learned_from, ttl_days}) — bảng `preference` (§2, khóa chính (key, scope)) và tệp `preferences.yaml` (§6) — nhưng không nói nơi nào là chuẩn cho phạm vi nào | Phải chọn một cách nhất quán, và phép thử là ngữ nghĩa của `scope`: bảng `preference` nằm trong store của TỪNG dự án, nên tùy chọn phạm vi `user` ghi ở dự án A sẽ không đọc được ở dự án B — trái đúng nghĩa "user" | `scope=project` → bảng `preference` trong store dự án; `scope=user` → `preferences.yaml` trong thư mục cấu hình người dùng (platformdirs). Đề xuất DDD-14 v1.3 ghi rõ phân công này ở cả §2 và §6 | Mở |
| DEV-009 | 2026-09-06 | CDS-12.3 PROJECT-09 `input_schema.value` và trường "Ví dụ gọi" | `src/eide/caps/project.py::preferences`, `tests/test_project_preferences.py` | Hợp đồng khai `"value": {"type": "object"}` nhưng ví dụ ngay bên cạnh là `{"op":"set","key":"probe","value":"stlink","scope":"user"}` — giá trị là chuỗi. Ví dụ của chính hợp đồng KHÔNG qua nổi input_schema của nó: Router chặn bằng E1000 trước khi vào handler | Router kiểm input theo `input_schema` (API-15 E1000) nên một trong hai vế phải sai; không thể hiện thực cả hai | Bám `input_schema` (object) vì nó khớp hình dạng Preference của DDD-14 {value, learned_from, ttl_days}; ví dụ dùng dạng `{"value":{"value":"stlink"}}`. Đề xuất v1.3 sửa **ví dụ** trong CDS-12.3 cho khớp schema (không nới schema thành scalar, vì learned_from của D8 cần chỗ đứng). Không sửa `cds.json` — tệp sinh | Mở |
| DEV-008 | 2026-09-06 | CDS-12.3 PROJECT-02 bước 3 và 4 | `src/eide/caps/project.py::open_project` | Bước 3 "tái dựng KG từ cache hoặc từ store" chưa hiện thực. **Bước 4 đã xong 06/09/2026** cùng WI-007: project.open mở SessionMemory mới, đóng phiên trước và trả `previous_session` | Nhóm `kg.*` chưa có ở Sprint 1 (mốc M1 theo PLN-27); phần còn lại của hợp đồng đứng độc lập và `tc` kiểm được ngay | Hoàn tất bước 3 khi nhóm `kg.*` có; (`summary.undo_open` đã có nguồn thật từ 06/09/2026 — POLICY-03 UndoService.) Không sửa tài liệu — đây là nợ hiện thực, không phải tài liệu sai | Mở |
| DEV-007 | 2026-09-06 | CDS-12.3 PROJECT-02 bước 2 ("Kiểm hash store vs ledger.last_hash"); API-15 §5 `store.write` | `src/eide_core/store.py` (`content_digest`, `write_seal`, `verify_seal`) | Tài liệu nêu phép kiểm nhưng không định nghĩa "hash store" được tính thế nào, lưu ở đâu, và ghi vào ledger lúc nào — `store.write` chỉ có {batch_id, n_facts, n_conflicts, actor, reason}, không có trường hash | Không có cơ chế thì E6000 không hiện thực được, mà `tc` của PROJECT-02 đòi đúng nhánh đó | Thêm niêm phong `store.sqlite.seal.json` {content_hash, ledger_last_hash, user_version, at}, ghi sau mỗi lần ghi qua cổng (hiện tại: `migrate`). `content_hash` băm NỘI DUNG LOGIC (bảng theo tên, dòng đã sắp) chứ không băm byte tệp — WAL, checkpoint và VACUUM đổi tệp mà không đổi dữ liệu, băm byte sẽ báo động giả mỗi lần mở. Đề xuất bổ sung trường `hash` vào `store.write` và mô tả niêm phong vào DDD-14 §5 ở v1.3 | Mở |
| DEV-006 | 2026-09-06 | DDD-14 §5 bảng lịch migration (đối chiếu §2.25 và MEM-11 §2) | `src/eide_core/migrations/session_db.sql`, `0002_m1_policy_runtime.sql` | Bảng lịch §5 có một dòng cho cơ sở dữ liệu RIÊNG `index.sqlite` (0003) nhưng không có dòng tương đương cho `session.sqlite`, nên đọc §5 một mình sẽ tưởng bảng `session` bị bỏ sót | **Ghi nhận đầu (06/09/2026) SAI và đã sửa cùng ngày.** Ban đầu tôi đặt `session` vào migration 0002 của store.sqlite vì tưởng §5 bỏ quên. Thật ra DDD-14 §2.25 ghi rõ "Phiên làm việc (session.sqlite, không commit)" và MEM-11 §2 xếp M2 vào `session.sqlite` — nó thuộc cơ sở dữ liệu khác, đúng như `rag_chunk` thuộc index.sqlite. Đã gỡ `session` khỏi 0002 và dựng `session_db.sql` riêng. Sửa được vì 0002 chưa phát hành (v0.1.0), không có store thật nào cần di trú | Bổ sung vào DDD-14 §5 một dòng cho `session.sqlite` giống dòng 0003 của `index.sqlite`, để đọc §5 một mình là đủ biết bảng nào ở đâu | Mở |
| DEV-005 | 2026-09-06 | PLATFORM.md §ma trận (`macos-13` Intel) | `.github/workflows/ci.yml` | Runner `macos-13` (Intel) của GitHub Actions đã ngừng phục vụ; không còn runner Intel miễn phí để chạy ma trận như PLATFORM.md mô tả | Vẫn phải chứng minh sản phẩm chạy trên Mac Intel (nền tảng thứ tự 1) | Phủ Intel bằng hai đường: (a) `arch -x86_64` trên runner arm64 và trên máy chủ sản phẩm — đã có Rosetta + Python universal2 ở `/usr/local`; (b) với phần Swift, cross-compile x86_64 rồi kiểm bằng `lipo` như `apps/geditor/scripts/build-universal.sh` đã làm. Sửa PLATFORM.md ở v1.3 | Mở |
| DEV-004 | 2026-09-06 | SEC-25 §quản lý bí mật; GPI-23 §1 (GEditor là phụ thuộc ngoài) | `apps/geditor/**`, `.gitignore`, `apps/geditor/scripts/check_no_secrets.py` | GEditor được **chuyển hẳn** vào kho EIDE (`apps/geditor/`) thành một phần sản phẩm, không phải phụ thuộc ngoài như GPI-23 giả định. Kèm theo: khóa riêng ký cập nhật Sparkle (`secrets/sparkle_eddsa_private.txt`) có trong kho Geditor gốc đã **không** được mang sang, và ngoại lệ tương ứng trong `check_no_secrets.py` đã bị gỡ | Chủ sản phẩm quyết định 06/09/2026: hai kho đi hai hướng phát triển khác nhau nên fork thay vì tham chiếu. Khóa riêng không được nhân thêm nơi lưu (SEC-25) | Sửa GPI-23 §1 ở v1.3: GEditor là thành phần trong kho, bỏ phần "khung plugin bên thứ ba". Với kho Geditor gốc: cân nhắc đổi khóa Sparkle và viết lại lịch sử — quyết định của chủ sản phẩm, ngoài phạm vi EIDE | Mở |
| DEV-003 | 2026-09-05 | PLN-27 WI-001; SAD-03 ADR (kế thừa hkw-core) | `toàn kho` | Kho eide viết mới hoàn toàn, không đổi tên/kế thừa mã hkw-core M0; lõi tách package eide_core/ ngay từ đầu; CLI dùng argparse thay vì typer/rich | Quyết định của chủ sản phẩm 05/09/2026: làm mới hoàn toàn; giảm phụ thuộc để chạy trên 3 nền tảng | Sửa PLN-27 WI-001 thành 'khởi tạo kho eide'; ghi ADR-16 trong SAD-03 v1.3; DEP-26 bỏ bước migrate .hkw→.eide | Mở |
